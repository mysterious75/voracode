---
title: Servidores LSP
description: O voracode integra-se aos seus servidores LSP.
---

O voracode pode se integrar a servidores Language Server Protocol (LSP) para usar diagnÃ³sticos como feedback para o agente.

---

## Integrados

O voracode vem com vÃ¡rios servidores LSP integrados para linguagens populares:

| Servidor LSP       | ExtensÃµes                                                           | Requisitos                                                               |
| ------------------ | ------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| astro              | .astro                                                              | Instala automaticamente para projetos Astro                              |
| bash               | .sh, .bash, .zsh, .ksh                                              | Instala automaticamente bash-language-server                             |
| clangd             | .c, .cpp, .cc, .cxx, .c++, .h, .hpp, .hh, .hxx, .h++                | Instala automaticamente para projetos C/C++                              |
| csharp             | .cs                                                                 | `.NET SDK` instalado                                                     |
| clojure-lsp        | .clj, .cljs, .cljc, .edn                                            | Comando `clojure-lsp` disponÃ­vel                                         |
| dart               | .dart                                                               | Comando `dart` disponÃ­vel                                                |
| deno               | .ts, .tsx, .js, .jsx, .mjs                                          | Comando `deno` disponÃ­vel (detecta automaticamente deno.json/deno.jsonc) |
| elixir-ls          | .ex, .exs                                                           | Comando `elixir` disponÃ­vel                                              |
| eslint             | .ts, .tsx, .js, .jsx, .mjs, .cjs, .mts, .cts, .vue                  | DependÃªncia `eslint` no projeto                                          |
| fsharp             | .fs, .fsi, .fsx, .fsscript                                          | `.NET SDK` instalado                                                     |
| gleam              | .gleam                                                              | Comando `gleam` disponÃ­vel                                               |
| gopls              | .go                                                                 | Comando `go` disponÃ­vel                                                  |
| hls                | .hs, .lhs                                                           | Comando `haskell-language-server-wrapper` disponÃ­vel                     |
| jdtls              | .java                                                               | `Java SDK (version 21+)` instalado                                       |
| julials            | .jl                                                                 | `julia` e `LanguageServer.jl` instalados                                 |
| kotlin-ls          | .kt, .kts                                                           | Instala automaticamente para projetos Kotlin                             |
| lua-ls             | .lua                                                                | Instala automaticamente para projetos Lua                                |
| nixd               | .nix                                                                | Comando `nixd` disponÃ­vel                                                |
| ocaml-lsp          | .ml, .mli                                                           | Comando `ocamllsp` disponÃ­vel                                            |
| oxlint             | .ts, .tsx, .js, .jsx, .mjs, .cjs, .mts, .cts, .vue, .astro, .svelte | DependÃªncia `oxlint` no projeto                                          |
| php intelephense   | .php                                                                | Instala automaticamente para projetos PHP                                |
| prisma             | .prisma                                                             | Comando `prisma` disponÃ­vel                                              |
| pyright            | .py, .pyi                                                           | DependÃªncia `pyright` instalada                                          |
| ruby-lsp (rubocop) | .rb, .rake, .gemspec, .ru                                           | Comandos `ruby` e `gem` disponÃ­veis                                      |
| rust               | .rs                                                                 | Comando `rust-analyzer` disponÃ­vel                                       |
| sourcekit-lsp      | .swift, .objc, .objcpp                                              | `swift` instalado (`xcode` no macOS)                                     |
| svelte             | .svelte                                                             | Instala automaticamente para projetos Svelte                             |
| terraform          | .tf, .tfvars                                                        | Instala automaticamente a partir de lanÃ§amentos do GitHub                |
| tinymist           | .typ, .typc                                                         | Instala automaticamente a partir de lanÃ§amentos do GitHub                |
| typescript         | .ts, .tsx, .js, .jsx, .mjs, .cjs, .mts, .cts                        | DependÃªncia `typescript` no projeto                                      |
| vue                | .vue                                                                | Instala automaticamente para projetos Vue                                |
| yaml-ls            | .yaml, .yml                                                         | Instala automaticamente o servidor yaml-language-server da Red Hat       |
| zls                | .zig, .zon                                                          | Comando `zig` disponÃ­vel                                                 |

O LSP fica desabilitado por padrÃ£o. Quando habilitado, os servidores iniciam quando uma das extensÃµes de arquivo acima Ã© detectada e os requisitos sÃ£o atendidos.

:::note
VocÃª pode desabilitar os downloads automÃ¡ticos do servidor LSP definindo a variÃ¡vel de ambiente `VORACODE_DISABLE_LSP_DOWNLOAD` como `true`.
:::

---

## Como Funciona

Quando o LSP estÃ¡ habilitado e o voracode abre um arquivo, ele:

1. Verifica a extensÃ£o do arquivo em relaÃ§Ã£o a todos os servidores LSP habilitados.
2. Inicia o servidor LSP apropriado se nÃ£o estiver em execuÃ§Ã£o.

---

## Boas prÃ¡ticas

O LSP pode ajudar o agente a encontrar e corrigir problemas fornecendo diagnÃ³sticos dos servidores de linguagem. Isso Ã© Ãºtil em alguns projetos, mas nem sempre Ã© um ganho lÃ­quido.

Servidores de linguagem podem ficar fora de sincronia, usar bastante memÃ³ria, variar por versÃ£o ou projeto e desacelerar fluxos de trabalho do agente. Em muitos projetos, Ã© melhor fazer o agente executar lint, typecheck ou outras ferramentas CLI de diagnÃ³stico diretamente, para que os erros voltem ao ciclo do agente sem essas compensaÃ§Ãµes. Documente esses comandos em arquivos de instruÃ§Ãµes como `AGENTS.md` ou skills, para que o agente saiba o que executar. Habilite o LSP quando seu projeto se beneficiar de feedback adicional do servidor de linguagem.

---

## ConfiguraÃ§Ã£o

VocÃª pode habilitar e personalizar os servidores LSP atravÃ©s da seÃ§Ã£o `lsp` na sua configuraÃ§Ã£o do voracode.

Para habilitar todos os servidores LSP integrados, defina `lsp` como `true`.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "lsp": true
}
```

Use um objeto para manter os servidores integrados habilitados enquanto configura substituiÃ§Ãµes ou servidores personalizados.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "lsp": {}
}
```

Cada servidor LSP suporta o seguinte:

| Propriedade      | Tipo     | DescriÃ§Ã£o                                                     |
| ---------------- | -------- | ------------------------------------------------------------- |
| `disabled`       | boolean  | Defina como `true` para desabilitar o servidor LSP            |
| `command`        | string[] | O comando para iniciar o servidor LSP                         |
| `extensions`     | string[] | ExtensÃµes de arquivo que este servidor LSP deve manipular     |
| `env`            | object   | VariÃ¡veis de ambiente a serem definidas ao iniciar o servidor |
| `initialization` | object   | OpÃ§Ãµes de inicializaÃ§Ã£o a serem enviadas ao servidor LSP      |

Vamos ver alguns exemplos.

---

### VariÃ¡veis de ambiente

Use a propriedade `env` para definir variÃ¡veis de ambiente ao iniciar o servidor LSP:

```json title="voracode.json" {5-7}
{
  "$schema": "https://voracode.ai/config.json",
  "lsp": {
    "rust": {
      "env": {
        "RUST_LOG": "debug"
      }
    }
  }
}
```

---

### OpÃ§Ãµes de inicializaÃ§Ã£o

Use a propriedade `initialization` para passar opÃ§Ãµes de inicializaÃ§Ã£o ao servidor LSP. Estas sÃ£o configuraÃ§Ãµes especÃ­ficas do servidor enviadas durante a solicitaÃ§Ã£o `initialize` do LSP:

```json title="voracode.json" {5-9}
{
  "$schema": "https://voracode.ai/config.json",
  "lsp": {
    "typescript": {
      "initialization": {
        "preferences": {
          "importModuleSpecifierPreference": "relative"
        }
      }
    }
  }
}
```

:::note
As opÃ§Ãµes de inicializaÃ§Ã£o variam de acordo com o servidor LSP. Verifique a documentaÃ§Ã£o do seu servidor LSP para opÃ§Ãµes disponÃ­veis.
:::

---

### Desabilitando servidores LSP

Se `lsp` for omitido, todos os servidores LSP ficam desabilitados. Para desabilitar todos os servidores LSP depois que outra configuraÃ§Ã£o os habilitou, defina `lsp` como `false`:

```json title="voracode.json" {3}
{
  "$schema": "https://voracode.ai/config.json",
  "lsp": false
}
```

Para desabilitar um servidor LSP **especÃ­fico**, defina `disabled` como `true`:

```json title="voracode.json" {5}
{
  "$schema": "https://voracode.ai/config.json",
  "lsp": {
    "typescript": {
      "disabled": true
    }
  }
}
```

---

### Servidores LSP personalizados

VocÃª pode adicionar servidores LSP personalizados especificando o comando e as extensÃµes de arquivo:

```json title="voracode.json" {4-7}
{
  "$schema": "https://voracode.ai/config.json",
  "lsp": {
    "custom-lsp": {
      "command": ["custom-lsp-server", "--stdio"],
      "extensions": [".custom"]
    }
  }
}
```

---

## InformaÃ§Ãµes Adicionais

### PHP Intelephense

PHP Intelephense oferece recursos premium atravÃ©s de uma chave de licenÃ§a. VocÃª pode fornecer uma chave de licenÃ§a colocando (apenas) a chave em um arquivo de texto em:

- No macOS/Linux: `$HOME/intelephense/license.txt`
- No Windows: `%USERPROFILE%/intelephense/license.txt`

O arquivo deve conter apenas a chave de licenÃ§a sem conteÃºdo adicional.
