---
title: GitHub
description: Use o voracode em problemas e pull-requests do GitHub.
---

O voracode integra-se ao seu fluxo de trabalho do GitHub. Mencione `/voracode` ou `/oc` em seu comentÃ¡rio, e o voracode executarÃ¡ tarefas dentro do seu runner do GitHub Actions.

---

## Recursos

- **Triagem de problemas**: PeÃ§a ao voracode para analisar um problema e explicÃ¡-lo para vocÃª.
- **Corrigir e implementar**: PeÃ§a ao voracode para corrigir um problema ou implementar um recurso. E ele trabalharÃ¡ em um novo branch e enviarÃ¡ um PR com todas as alteraÃ§Ãµes.
- **Seguro**: O voracode Ã© executado dentro dos runners do seu GitHub.

---

## InstalaÃ§Ã£o

Execute o seguinte comando em um projeto que estÃ¡ em um repositÃ³rio do GitHub:

```bash
voracode github install
```

Isso o guiarÃ¡ pela instalaÃ§Ã£o do aplicativo GitHub, criaÃ§Ã£o do fluxo de trabalho e configuraÃ§Ã£o de segredos.

---

### ConfiguraÃ§Ã£o Manual

Ou vocÃª pode configurÃ¡-lo manualmente.

1. **Instale o aplicativo GitHub**

   Acesse [**github.com/apps/voracode-agent**](https://github.com/apps/voracode-agent). Certifique-se de que estÃ¡ instalado no repositÃ³rio de destino.

2. **Adicione o fluxo de trabalho**

   Adicione o seguinte arquivo de fluxo de trabalho em `.github/workflows/voracode.yml` no seu repositÃ³rio. Certifique-se de definir o `model` apropriado e as chaves de API necessÃ¡rias em `env`.

   ```yml title=".github/workflows/voracode.yml" {24,26}
   name: voracode

   on:
     issue_comment:
       types: [created]
     pull_request_review_comment:
       types: [created]

   jobs:
     voracode:
       if: |
         contains(github.event.comment.body, '/oc') ||
         contains(github.event.comment.body, '/voracode')
       runs-on: ubuntu-latest
       permissions:
         id-token: write
       steps:
          - name: Checkout repository
            uses: actions/checkout@v6
            with:
              fetch-depth: 1
              persist-credentials: false

          - name: Run Voracode
           uses: mysterious75/voracode/github@latest
           env:
             ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
           with:
             model: anthropic/claude-sonnet-4-20250514
             # share: true
             # github_token: xxxx
   ```

3. **Armazene as chaves de API em segredos**

   Nas **configuraÃ§Ãµes** da sua organizaÃ§Ã£o ou projeto, expanda **Segredos e variÃ¡veis** Ã  esquerda e selecione **AÃ§Ãµes**. E adicione as chaves de API necessÃ¡rias.

---

## ConfiguraÃ§Ã£o

- `model`: O modelo a ser usado com o voracode. Tem o formato de `provider/model`. Isso Ã© **obrigatÃ³rio**.
- `agent`: O agente a ser usado. Deve ser um agente primÃ¡rio. Retorna ao `default_agent` da configuraÃ§Ã£o ou `"build"` se nÃ£o encontrado.
- `share`: Se deve compartilhar a sessÃ£o do voracode. O padrÃ£o Ã© **true** para repositÃ³rios pÃºblicos.
- `prompt`: Prompt personalizado opcional para substituir o comportamento padrÃ£o. Use isso para personalizar como o voracode processa solicitaÃ§Ãµes.
- `token`: Token de acesso do GitHub opcional para realizar operaÃ§Ãµes como criar comentÃ¡rios, confirmar alteraÃ§Ãµes e abrir pull requests. Por padrÃ£o, o voracode usa o token de acesso da instalaÃ§Ã£o do aplicativo GitHub voracode, entÃ£o commits, comentÃ¡rios e pull requests aparecem como se fossem da aplicaÃ§Ã£o.

  Alternativamente, vocÃª pode usar o [`GITHUB_TOKEN`](https://docs.github.com/en/actions/tutorials/authenticate-with-github_token) embutido do runner do GitHub Action sem instalar o aplicativo GitHub voracode. Apenas certifique-se de conceder as permissÃµes necessÃ¡rias em seu fluxo de trabalho:

  ```yaml
  permissions:
    id-token: write
    contents: write
    pull-requests: write
    issues: write
  ```

  VocÃª tambÃ©m pode usar um [token de acesso pessoal](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/managing-your-personal-access-tokens)(PAT) se preferir.

---

## Eventos Suportados

O voracode pode ser acionado pelos seguintes eventos do GitHub:

| Tipo de Evento                | Acionado Por                                        | Detalhes                                                                                                                               |
| ----------------------------- | --------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| `issue_comment`               | ComentÃ¡rio em um problema ou PR                     | Mencione `/voracode` ou `/oc` em seu comentÃ¡rio. O voracode lÃª o contexto e pode criar branches, abrir PRs ou responder.               |
| `pull_request_review_comment` | ComentÃ¡rio em linhas de cÃ³digo especÃ­ficas em um PR | Mencione `/voracode` ou `/oc` enquanto revisa o cÃ³digo. O voracode recebe o caminho do arquivo, nÃºmeros das linhas e contexto do diff. |
| `issues`                      | Problema aberto ou editado                          | Aciona automaticamente o voracode quando problemas sÃ£o criados ou modificados. Requer entrada de `prompt`.                             |
| `pull_request`                | PR aberto ou atualizado                             | Aciona automaticamente o voracode quando PRs sÃ£o abertos, sincronizados ou reabertos. Ãštil para revisÃµes automatizadas.                |
| `schedule`                    | Cron baseado em agendamento                         | Execute o voracode em um cronograma. Requer entrada de `prompt`. A saÃ­da vai para logs e PRs (sem problema para comentar).             |
| `workflow_dispatch`           | Acionamento manual pela interface do GitHub         | Acione o voracode sob demanda atravÃ©s da aba AÃ§Ãµes. Requer entrada de `prompt`. A saÃ­da vai para logs e PRs.                           |

### Exemplo de Agendamento

Execute o voracode em um cronograma para realizar tarefas automatizadas:

```yaml title=".github/workflows/voracode-scheduled.yml"
name: Scheduled Voracode Task

on:
  schedule:
    - cron: "0 9 * * 1" # Every Monday at 9am UTC

jobs:
  voracode:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
      pull-requests: write
      issues: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          persist-credentials: false

       - name: Run Voracode
         uses: mysterious75/voracode/github@latest
         env:
           ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
         with:
           model: anthropic/claude-sonnet-4-20250514
           prompt: |
             Revise a base de cÃ³digo para quaisquer comentÃ¡rios TODO e crie um resumo.
             Se encontrar problemas que valham a pena resolver, abra um issue para rastreÃ¡-los.
```

Para eventos agendados, a entrada `prompt` Ã© **obrigatÃ³ria** uma vez que nÃ£o hÃ¡ comentÃ¡rio para extrair instruÃ§Ãµes. Fluxos de trabalho agendados sÃ£o executados sem um contexto de usuÃ¡rio para verificaÃ§Ã£o de permissÃµes, entÃ£o o fluxo de trabalho deve conceder `contents: write` e `pull-requests: write` se vocÃª espera que o voracode crie branches ou PRs.

---

### Exemplo de Pull Request

Revise automaticamente PRs quando forem abertos ou atualizados:

```yaml title=".github/workflows/voracode-review.yml"
name: voracode-review

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      pull-requests: read
      issues: read
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
       - uses: mysterious75/voracode/github@latest
         env:
           ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
         with:
           model: anthropic/claude-sonnet-4-20250514
           use_github_token: true
           prompt: |
             Revise este pull request:
             - Verifique problemas de qualidade de cÃ³digo
             - Procure por potenciais bugs
             - Sugira melhorias
```

Para eventos de `pull_request`, se nenhum `prompt` for fornecido, o voracode padrÃ£o serÃ¡ revisar o pull request.

---

### Exemplo de Triagem de Problemas

Triagem automÃ¡tica de novos problemas. Este exemplo filtra contas com mais de 30 dias para reduzir spam:

```yaml title=".github/workflows/voracode-triage.yml"
name: Issue Triage

on:
  issues:
    types: [opened]

jobs:
  triage:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
      pull-requests: write
      issues: write
    steps:
      - name: Check account age
        id: check
        uses: actions/github-script@v7
        with:
          script: |
            const user = await github.rest.users.getByUsername({
              username: context.payload.issue.user.login
            });
            const created = new Date(user.data.created_at);
            const days = (Date.now() - created) / (1000 * 60 * 60 * 24);
            return days >= 30;
          result-encoding: string

      - uses: actions/checkout@v6
        if: steps.check.outputs.result == 'true'
        with:
          persist-credentials: false

       - uses: mysterious75/voracode/github@latest
         if: steps.check.outputs.result == 'true'
         env:
           ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
         with:
           model: anthropic/claude-sonnet-4-20250514
           prompt: |
             Revise este issue. Se houver uma correÃ§Ã£o clara ou docs relevantes:
             - ForneÃ§a links de documentaÃ§Ã£o
             - Adicione orientaÃ§Ã£o sobre tratamento de erros para exemplos de cÃ³digo
             Caso contrÃ¡rio, nÃ£o comente.
```

Para eventos de `issues`, a entrada `prompt` Ã© **obrigatÃ³ria** uma vez que nÃ£o hÃ¡ comentÃ¡rio para extrair instruÃ§Ãµes.

---

## Prompts Personalizados

Substitua o prompt padrÃ£o para personalizar o comportamento do voracode para seu fluxo de trabalho.

```yaml title=".github/workflows/voracode.yml"
- uses: mysterious75/voracode/github@latest
  with:
    model: anthropic/claude-sonnet-4-5
    prompt: |
      Revise este pull request:
      - Verifique problemas de qualidade de cÃ³digo
      - Procure por potenciais bugs
      - Sugira melhorias
```

Isso Ã© Ãºtil para impor critÃ©rios de revisÃ£o especÃ­ficos, padrÃµes de codificaÃ§Ã£o ou Ã¡reas de foco relevantes para seu projeto.

---

## Exemplos

Aqui estÃ£o alguns exemplos de como vocÃª pode usar o voracode no GitHub.

- **Explicar um problema**

  Adicione este comentÃ¡rio em um problema do GitHub.

  ```
  /voracode explain this issue
  ```

  O voracode lerÃ¡ toda a conversa, incluindo todos os comentÃ¡rios, e responderÃ¡ com uma explicaÃ§Ã£o clara.

- **Corrigir um problema**

  Em um problema do GitHub, diga:

  ```
  /voracode fix this
  ```

  E o voracode criarÃ¡ um novo branch, implementarÃ¡ as alteraÃ§Ãµes e abrirÃ¡ um PR com as mudanÃ§as.

- **Revisar PRs e fazer alteraÃ§Ãµes**

  Deixe o seguinte comentÃ¡rio em um PR do GitHub.

  ```
  Delete the attachment from S3 when the note is removed /oc
  ```

  O voracode implementarÃ¡ a alteraÃ§Ã£o solicitada e a confirmarÃ¡ no mesmo PR.

- **Revisar linhas de cÃ³digo especÃ­ficas**

  Deixe um comentÃ¡rio diretamente nas linhas de cÃ³digo na aba "Files" do PR. O voracode detecta automaticamente o arquivo, os nÃºmeros das linhas e o contexto do diff para fornecer respostas precisas.

  ```
  [ComentÃ¡rio em linhas especÃ­ficas na aba Files]
  /oc add error handling here
  ```

  Ao comentar sobre linhas especÃ­ficas, o voracode recebe:
  - O arquivo exato sendo revisado
  - As linhas especÃ­ficas de cÃ³digo
  - O contexto do diff ao redor
  - InformaÃ§Ãµes sobre nÃºmeros de linha

  Isso permite solicitaÃ§Ãµes mais direcionadas sem precisar especificar caminhos de arquivos ou nÃºmeros de linhas manualmente.
