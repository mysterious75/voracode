---
title: Komande
description: Kreirajte prilagoÄ‘ene komande za zadatke koji se ponavljaju.
---

PrilagoÄ‘ene komande vam omoguÄ‡avaju da odredite prompt koji Å¾elite da pokrenete kada se ta naredba izvrÅ¡i u TUI-ju.

```bash frame="none"
/my-command
```

PrilagoÄ‘ene komande su dodatak ugraÄ‘enim komandama kao Å¡to su `/init`, `/undo`, `/redo`, `/share`, `/help`. [Saznajte viÅ¡e](/docs/tui#commands).

---

## Kreiranje datoteka naredbi

Kreirajte markdown fajlove u direktorijumu `commands/` da definiÅ¡ete prilagoÄ‘ene komande.
Kreiraj `.voracode/commands/test.md`:

```md title=".voracode/commands/test.md"
---
description: Run tests with coverage
agent: build
model: anthropic/claude-3-5-sonnet-20241022
---

Run the full test suite with coverage report and show any failures.
Focus on the failing tests and suggest fixes.
```

Frontmatter definira svojstva komande. SadrÅ¾aj postaje predloÅ¾ak.
Koristite komandu tako Å¡to Ä‡ete upisati `/` nakon Äega slijedi naziv komande.

```bash frame="none"
"/test"
```

---

## Konfiguracija

MoÅ¾ete dodati prilagoÄ‘ene komande kroz Voracode konfiguraciju ili kreiranjem markdown datoteka u direktoriju `commands/`.

---

### JSON

Koristite opciju `command` u svom Voracode [config](/docs/config):

```json title="voracode.jsonc" {4-12}
{
  "$schema": "https://voracode.ai/config.json",
  "command": {
    // This becomes the name of the command
    "test": {
      // This is the prompt that will be sent to the LLM
      "template": "Run the full test suite with coverage report and show any failures.\nFocus on the failing tests and suggest fixes.",
      // This is shown as the description in the TUI
      "description": "Run tests with coverage",
      "agent": "build",
      "model": "anthropic/claude-3-5-sonnet-20241022"
    }
  }
}
```

Sada moÅ¾ete pokrenuti ovu naredbu u TUI:

```bash frame="none"
/test
```

---

### Markdown

TakoÄ‘er moÅ¾ete definirati komande koristeÄ‡i markdown datoteke. Stavite ih u:

- Globalno: `~/.config/voracode/commands/`
- Po projektu: `.voracode/commands/`

```markdown title="~/.config/voracode/commands/test.md"
---
description: Run tests with coverage
agent: build
model: anthropic/claude-3-5-sonnet-20241022
---

Run the full test suite with coverage report and show any failures.
Focus on the failing tests and suggest fixes.
```

Ime markdown datoteke postaje ime naredbe. Na primjer, `test.md` vam omoguÄ‡ava da pokrenete:

```bash frame="none"
/test
```

---

## Konfiguracija upita

Promptovi za prilagoÄ‘ene komande podrÅ¾avaju nekoliko posebnih Äuvara mjesta i sintakse.

---

### Argumenti

Proslijedite argumente naredbama koristeÄ‡i Äuvar mjesta `$ARGUMENTS`.

```md title=".voracode/commands/component.md"
---
description: Create a new component
---

Create a new React component named $ARGUMENTS with TypeScript support.
Include proper typing and basic structure.
```

Pokrenite naredbu s argumentima:

```bash frame="none"
/component Button
```

I `$ARGUMENTS` Ä‡e biti zamijenjen sa `Button`.
TakoÄ‘er moÅ¾ete pristupiti pojedinaÄnim argumentima koristeÄ‡i pozicione parametre:

- `$1` - Prvi argument
- `$2` - Drugi argument
- `$3` - TreÄ‡i argument
- I tako dalje...

Na primjer:

```md title=".voracode/commands/create-file.md"
---
description: Create a new file with content
---

Create a file named $1 in the directory $2
with the following content: $3
```

Pokrenite naredbu:

```bash frame="none"
/create-file config.json src "{ \"key\": \"value\" }"
```

Ovo zamjenjuje:

- `$1` do `config.json`
- `$2` do `src`
- `$3` do `{ "key": "value" }`

---

### Shell izlaz

Koristite _!`command`_ da ubacite izlaz [bash command](/docs/tui#bash-commands) u svoj prompt.
Na primjer, da kreirate prilagoÄ‘enu naredbu koja analizira pokrivenost testom:

```md title=".voracode/commands/analyze-coverage.md"
---
description: Analyze test coverage
---

Here are the current test results:
!`npm test`

Based on these results, suggest improvements to increase coverage.
```

Ili da vidite nedavne promjene:

```md title=".voracode/commands/review-changes.md"
---
description: Review recent changes
---

Recent git commits:
!`git log --oneline -10`

Review these changes and suggest any improvements.
```

Naredbe se pokreÄ‡u u korijenskom direktoriju vaÅ¡eg projekta i njihov izlaz postaje dio prompta.

---

### Reference datoteka

UkljuÄite datoteke u svoju naredbu koristeÄ‡i `@` nakon Äega slijedi naziv datoteke.

```md title=".voracode/commands/review-component.md"
---
description: Review component
---

Review the component in @src/components/Button.tsx.
Check for performance issues and suggest improvements.
```

SadrÅ¾aj datoteke se automatski ukljuÄuje u prompt.

---

## Opcije

Pogledajmo detaljno opcije konfiguracije.

---

### Å ablon

Opcija `template` definira prompt koji Ä‡e biti poslan LLM-u kada se naredba izvrÅ¡i.

```json title="voracode.json"
{
  "command": {
    "test": {
      "template": "Run the full test suite with coverage report and show any failures.\nFocus on the failing tests and suggest fixes."
    }
  }
}
```

Ovo je **obavezna** opcija konfiguracije.

---

### Opis

Koristite opciju `description` da pruÅ¾ite kratak opis onoga Å¡to naredba radi.

```json title="voracode.json"
{
  "command": {
    "test": {
      "description": "Run tests with coverage"
    }
  }
}
```

Ovo se prikazuje kao opis u TUI-u kada unesete naredbu.

---

### Agent

Koristite `agent` konfiguraciju da opciono odredite koji [agent](/docs/agents) treba da izvrÅ¡i ovu naredbu.
Ako je ovo [subagent](/docs/agents/#subagents) naredba Ä‡e po defaultu pokrenuti pozivanje subagenta.
Da onemoguÄ‡ite ovo ponaÅ¡anje, postavite `subtask` na `false`.

```json title="voracode.json"
{
  "command": {
    "review": {
      "agent": "plan"
    }
  }
}
```

Ovo je **opciona** opcija konfiguracije. Ako nije navedeno, podrazumevano je vaÅ¡ trenutni agent.

---

### Podzadatak

Koristite `subtask` boolean da prisilite naredbu da pokrene [subagent](/docs/agents/#subagents) pozivanje.
Ovo je korisno ako Å¾elite da naredba ne zagaÄ‘uje vaÅ¡ primarni kontekst i da Ä‡e **primorati** agenta da djeluje kao subagent,
Äak i ako je `mode` postavljeno na `primary` u konfiguraciji [agent](/docs/agents).

```json title="voracode.json"
{
  "command": {
    "analyze": {
      "subtask": true
    }
  }
}
```

Ovo je **opciona** opcija konfiguracije.

---

### Model

Koristite `model` konfiguraciju da nadjaÄate zadani model za ovu naredbu.

```json title="voracode.json"
{
  "command": {
    "analyze": {
      "model": "anthropic/claude-3-5-sonnet-20241022"
    }
  }
}
```

Ovo je **opciona** opcija konfiguracije.

---

## UgraÄ‘ene naredbe

Voracode ukljuÄuje nekoliko ugraÄ‘enih naredbi kao Å¡to su `/init`, `/undo`, `/redo`, `/share`, `/help`; [saznaj viÅ¡e](/docs/tui#commands).
:::note
PrilagoÄ‘ene komande mogu nadjaÄati ugraÄ‘ene komande.
:::
Ako definirate prilagoÄ‘enu naredbu s istim imenom, ona Ä‡e nadjaÄati ugraÄ‘enu naredbu.
