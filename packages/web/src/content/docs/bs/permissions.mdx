---
title: Dozvole
description: Kontrolirajte koje radnje zahtijevaju odobrenje za pokretanje.
---

Voracode koristi `permission` konfiguraciju da odluÄi da li Ä‡e se odreÄ‘ena radnja pokrenuti automatski, zatraÅ¾iti od vas ili biti blokirana.
Od `v1.1.1`, naslijeÄ‘ena `tools` logiÄka konfiguracija je zastarjela i spojena je u `permission`. Stara `tools` konfiguracija je joÅ¡ uvijek podrÅ¾ana za kompatibilnost unatrag.

---

## Akcije

Svako pravilo dozvole rjeÅ¡ava jedno od:

- `"allow"` â€” pokrenuti bez odobrenja
- `"ask"` â€” upit za odobrenje
- `"deny"` â€” blokiraj akciju

---

## Konfiguracija

Dozvole moÅ¾ete postaviti globalno (sa `*`) i nadjaÄati odreÄ‘ene alate.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "*": "ask",
    "bash": "allow",
    "edit": "deny"
  }
}
```

TakoÄ‘er moÅ¾ete postaviti sve dozvole odjednom:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": "allow"
}
```

---

## Granularna pravila (sintaksa objekta)

Za veÄ‡inu dozvola, moÅ¾ete koristiti objekt za primjenu razliÄitih radnji na osnovu unosa alata.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "bash": {
      "*": "ask",
      "git *": "allow",
      "npm *": "allow",
      "rm *": "deny",
      "grep *": "allow"
    },
    "edit": {
      "*": "deny",
      "packages/web/src/content/docs/*.mdx": "allow"
    }
  }
}
```

Pravila se procjenjuju na osnovu podudaranja uzorka, pri Äemu **pobjeÄ‘uje **poslednje odgovarajuÄ‡e pravilo\*\_. UobiÄajeni obrazac je da se prvo pravilo `"*"` stavi sveobuhvatno, a poslije njega konkretnija pravila.

### Zamjenski znakovi

Uzorci dozvola koriste jednostavno podudaranje zamjenskih znakova:

- `*` odgovara nula ili viÅ¡e bilo kojeg znaka
- `?` odgovara taÄno jednom znaku
- Svi ostali likovi se bukvalno podudaraju

### ProÅ¡irenje kuÄ‡nog direktorija

MoÅ¾ete koristiti `~` ili `$HOME` na poÄetku obrasca da referencirate svoj poÄetni direktorij. Ovo je posebno korisno za [`external_directory`](#external-directories) pravila.

- `~/projects/*` -> `/Users/username/projects/*`
- `$HOME/projects/*` -> `/Users/username/projects/*`
- `~` -> `/Users/username`

### Vanjski direktoriji

Koristite `external_directory` da dozvolite pozive alata koji dodiruju putanje izvan radnog direktorija gdje je Voracode pokrenut. Ovo se odnosi na bilo koji alat koji uzima putanju kao ulaz (na primjer `read`, `edit`, `glob`, `grep` i mnoge `bash` komande).
ProÅ¡irenje kuÄ‡e (poput `~/...`) utiÄe samo na naÄin na koji je obrazac napisan. Ne Äini vanjsku stazu dijelom trenutnog radnog prostora, tako da staze izvan radnog direktorija i dalje moraju biti dozvoljene preko `external_directory`.
Na primjer, ovo omoguÄ‡ava pristup svemu pod `~/projects/personal/`:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "external_directory": {
      "~/projects/personal/**": "allow"
    }
  }
}
```

Svaki direktorij koji je ovdje dozvoljen nasljeÄ‘uje iste zadane postavke kao trenutni radni prostor. PoÅ¡to je [`read` zadano na `allow`](#defaults), Äitanje je takoÄ‘er dozvoljeno za unose pod `external_directory` osim ako se ne poniÅ¡ti. Dodajte eksplicitna pravila kada bi alat trebao biti ograniÄen na ovim stazama, kao Å¡to je blokiranje ureÄ‘ivanja uz zadrÅ¾avanje Äitanja:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "external_directory": {
      "~/projects/personal/**": "allow"
    },
    "edit": {
      "~/projects/personal/**": "deny"
    }
  }
}
```

DrÅ¾ite ovu listu fokusiranom na pouzdane putanje, a dodatna allow/deny pravila dodajte po potrebi za druge alate (npr. `bash`).

## Dostupne dozvole

Dozvole Voracode su oznaÄene imenom alata, plus nekoliko sigurnosnih mjera:

- `read` â€” Äitanje datoteke (odgovara putanji datoteke)
- `edit` â€” sve izmjene fajlova (pokriva `edit`, `write`, `patch`)
- `glob` â€” globbiranje fajla (odgovara glob uzorku)
- `grep` â€” pretraga sadrÅ¾aja (podudara se sa regularnim izrazom)
- `bash` â€” izvoÄ‘enje komandi ljuske (podudara se s raÅ¡Älanjenim komandama kao Å¡to je `git status --porcelain`)
- `task` â€” pokretanje subagenta (odgovara tipu podagenta)
- `skill` â€” uÄitavanje vjeÅ¡tine (odgovara nazivu vjeÅ¡tine)
- `lsp` â€” pokretanje LSP upita (trenutno negranularno)
- `webfetch` â€” dohvaÄ‡anje URL-a (odgovara URL-u)
- `websearch` â€” pretraÅ¾ivanje weba (odgovara upitu)
- `external_directory` â€” pokreÄ‡e se kada alat dodirne staze izvan radnog direktorija projekta
- `doom_loop` â€” aktivira se kada se isti poziv alata ponovi 3 puta sa identiÄnim unosom

---

## Zadane postavke

Ako niÅ¡ta ne navedete, Voracode poÄinje od dozvoljenih zadanih vrijednosti:

- VeÄ‡ina dozvola je zadana na `"allow"`.
- `doom_loop` i `external_directory` zadano na `"ask"`.
- `read` je `"allow"`, ali `.env` fajlovi su po defaultu odbijeni:

```json title="voracode.json"
{
  "permission": {
    "read": {
      "*": "allow",
      "*.env": "deny",
      "*.env.*": "deny",
      "*.env.example": "allow"
    }
  }
}
```

---

## Å ta radi â€œAskâ€

Kada Voracode zatraÅ¾i odobrenje, korisniÄko suÄelje nudi tri ishoda:

- `once` â€” odobri samo ovaj zahtjev
- `always` â€” odobri buduÄ‡e zahtjeve koji odgovaraju predloÅ¾enim obrascima (za ostatak trenutne Voracode sesije)
- `reject` â€” odbiti zahtjev
  Skup obrazaca koje bi `always` odobrio pruÅ¾a alat (na primjer, bash odobrenja obiÄno stavljaju na bijelu listu sigurni prefiks komande kao Å¡to je `git status*`).

---

## Agenti

MoÅ¾ete nadjaÄati dozvole po agentu. Dozvole agenta su spojene sa globalnom konfiguracijom, a pravila agenta imaju prednost. [Saznajte viÅ¡e](/docs/agents#permissions) o dozvolama agenta.
:::note
Pogledajte gornji odjeljak [Granularna pravila (sintaksa objekata)](#granular-rules-object-syntax) za detaljnije primjere podudaranja uzoraka.
:::

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "bash": {
      "*": "ask",
      "git *": "allow",
      "git commit *": "deny",
      "git push *": "deny",
      "grep *": "allow"
    }
  },
  "agent": {
    "build": {
      "permission": {
        "bash": {
          "*": "ask",
          "git *": "allow",
          "git commit *": "ask",
          "git push *": "deny",
          "grep *": "allow"
        }
      }
    }
  }
}
```

TakoÄ‘er moÅ¾ete konfigurirati dozvole agenta u Markdownu:

```markdown title="~/.config/voracode/agents/review.md"
---
description: Code review without edits
mode: subagent
permission:
  edit: deny
  bash: ask
  webfetch: deny
---

Only analyze code and suggest changes.
```

:::tip
Koristite podudaranje uzoraka za naredbe s argumentima. `"grep *"` dozvoljava `grep pattern file.txt`, dok bi ga samo `"grep"` blokirao. Naredbe poput `git status` rade za zadano ponaÅ¡anje, ali zahtijevaju eksplicitnu dozvolu (kao `"git status *"`) kada se prosljeÄ‘uju argumenti.
:::
