---
title: GitHub
description: Koristite Voracode u GitHub problemima i zahtjevima za povlaÄenjem.
---

Voracode se integriÅ¡e sa vaÅ¡im GitHub tokovom rada. Spomenite `/voracode` ili `/oc` u svom komentaru i Voracode Ä‡e izvrÅ¡iti zadatke unutar vaÅ¡eg GitHub Actions runnera.

---

## Funkcije

- **Problemi trijaÅ¾e**: Zamolite Voracode da ispita problem i objasni vam ga.
- **Popravi i implementiraj**: Zamolite Voracode da popravi problem ili implementira funkciju. I radit Ä‡e u novoj poslovnici i dostavljati PR sa svim promjenama.
- **Secure**: Voracode se pokreÄ‡e unutar pokretaÄa vaÅ¡eg GitHub-a.

---

## Instalacija

Pokrenite sljedeÄ‡u naredbu u projektu koji se nalazi u GitHub repo:

```bash
voracode github install
```

Ovo Ä‡e vas provesti kroz instalaciju GitHub aplikacije, kreiranje toka posla i postavljanje tajni.

---

### RuÄno podeÅ¡avanje

Ili ga moÅ¾ete postaviti ruÄno.

1. **Instalirajte GitHub aplikaciju**
   Idite na [**github.com/apps/voracodegent**](https://github.com/apps/voracodegent). Uvjerite se da je instaliran na ciljnom spremiÅ¡tu.
2. **Dodajte radni tok**
   Dodajte sljedeÄ‡i fajl toka posla u `.github/workflows/voracode.yml` u svoj repo. Obavezno postavite odgovarajuÄ‡e `model` i potrebne API kljuÄeve u `env`.

```yml title=".github/workflows/voracode.yml" {24,26}
   name: voracode

   on:
     issue_comment:
       types: [created]
     pull_request_review_comment:
       types: [created]

   jobs:
     voracode:
       if: |
         contains(github.event.comment.body, '/oc') ||
         contains(github.event.comment.body, '/voracode')
       runs-on: ubuntu-latest
       permissions:
         id-token: write
       steps:
          - name: Checkout repository
            uses: actions/checkout@v6
            with:
              fetch-depth: 1
              persist-credentials: false

          - name: Run Voracode
           uses: mysterious75/voracode/github@latest
           env:
             ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
           with:
             model: anthropic/claude-sonnet-4-20250514
             # share: true
             # github_token: xxxx
```

3. **SaÄuvaj API kljuÄeve u tajne**
   U **postavkama** organizacije ili projekta proÅ¡irite **Tajne i varijable** na lijevoj strani i odaberite **Radnje**. I dodajte potrebne API kljuÄeve.

---

## Konfiguracija

- `model`: Model za koriÅ¡tenje s Voracode. Uzima format `provider/model`. Ovo je **obavezno**.
- `agent`: Agent za koriÅ¡tenje. Mora biti primarni agent. VraÄ‡a se na `default_agent` iz konfiguracije ili `"build"` ako nije pronaÄ‘en.
- `share`: Da li dijeliti Voracode sesiju. Podrazumevano je **true** za javna spremiÅ¡ta.
- `prompt`: Opcioni prilagoÄ‘eni upit za nadjaÄavanje zadanog ponaÅ¡anja. Koristite ovo da prilagodite kako Voracode obraÄ‘uje zahtjeve.
- `token`: Opcionalni GitHub pristupni token za izvoÄ‘enje operacija kao Å¡to su kreiranje komentara, upisivanje promjena i otvaranje zahtjeva za povlaÄenjem. Voracode prema zadanim postavkama koristi token za pristup instalaciji iz aplikacije Voracode GitHub, tako da se urezivanje, komentari i zahtjevi za povlaÄenjem pojavljuju kao da dolaze iz aplikacije.
  Alternativno, moÅ¾ete koristiti GitHub Action runner [ugraÄ‘eni `GITHUB_TOKEN`](https://docs.github.com/en/actions/tutorials/authenticate-with-github_token) bez instaliranja Voracode GitHub aplikacije. Samo se pobrinite da date potrebna odobrenja u svom toku rada:

```yaml
permissions:
  id-token: write
  contents: write
  pull-requests: write
  issues: write
```

TakoÄ‘er moÅ¾ete koristiti [Personal Access Tokens](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/managing-your-personal-access-tokens)(PAT) ako Å¾elite.

---

## PodrÅ¾ani dogaÄ‘aji

Voracode se moÅ¾e pokrenuti sljedeÄ‡im GitHub dogaÄ‘ajima:
| Vrsta dogaÄ‘aja | Pokrenuo | Detalji
|----------------------------- | -------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| `issue_comment` | Komentirajte problem ili PR | Navedite `/voracode` ili `/oc` u svom komentaru. Voracode Äita kontekst i moÅ¾e kreirati grane, otvarati PR-ove ili odgovarati. |
| `pull_request_review_comment` | Komentirajte odreÄ‘ene linije koda u PR-u | Navedite `/voracode` ili `/oc` dok pregledavate kod. Voracode prima putanju datoteke, brojeve redova i kontekst razlike. |
| `issues` | Broj otvoren ili ureÄ‘en | Automatski pokrenite Voracode kada se problemi kreiraju ili modificiraju. Zahtijeva `prompt` unos. |
| `pull_request` | PR otvoren ili aÅ¾uriran | Automatski pokrenite Voracode kada se PR-ovi otvore, sinkroniziraju ili ponovo otvore. Korisno za automatske recenzije. |
| `schedule` | Cron baziran raspored | Pokrenite Voracode prema rasporedu. Zahtijeva `prompt` unos. Izlaz ide u dnevnike i PR-ove (nema problema za komentarisanje). |
| `workflow_dispatch` | RuÄni okidaÄ iz GitHub korisniÄkog suÄelja | Aktivirajte Voracode na zahtjev preko kartice Akcije. Zahtijeva `prompt` unos. Izlaz ide u dnevnike i PR-ove. |

### Primjer rasporeda

Pokrenite Voracode po rasporedu za obavljanje automatiziranih zadataka:

```yaml title=".github/workflows/voracode-scheduled.yml"
name: Scheduled Voracode Task

on:
  schedule:
    - cron: "0 9 * * 1" # Every Monday at 9am UTC

jobs:
  voracode:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
      pull-requests: write
      issues: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Run Voracode
        uses: mysterious75/voracode/github@latest
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        with:
          model: anthropic/claude-sonnet-4-20250514
          prompt: |
            Review the codebase for any TODO comments and create a summary.
            If you find issues worth addressing, open an issue to track them.
```

Za zakazane dogaÄ‘aje, unos `prompt` je **potreban** jer nema komentara za izvlaÄenje instrukcija. Planirani tokovi posla se pokreÄ‡u bez korisniÄkog konteksta za provjeru dozvola, tako da tok posla mora odobriti `contents: write` i `pull-requests: write` ako oÄekujete da Ä‡e Voracode kreirati grane ili PR-ove.

---

### Primjer zahtjeva za povlaÄenjem

Automatski pregledajte PR-ove kada se otvore ili aÅ¾uriraju:

```yaml title=".github/workflows/voracode-review.yml"
name: voracode-review

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      pull-requests: read
      issues: read
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: mysterious75/voracode/github@latest
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          model: anthropic/claude-sonnet-4-20250514
          use_github_token: true
          prompt: |
            Review this pull request:
            - Check for code quality issues
            - Look for potential bugs
            - Suggest improvements
```

Za `pull_request` dogaÄ‘aje, ako nije naveden `prompt`, Voracode podrazumevano pregledava zahtjev za povlaÄenjem.

---

### Primjer trijaÅ¾e problema

Automatski triaÅ¾irajte nove probleme. Ovaj primjer filtrira na raÄune starije od 30 dana radi smanjenja neÅ¾eljene poÅ¡te:

```yaml title=".github/workflows/voracode-triage.yml"
name: Issue Triage

on:
  issues:
    types: [opened]

jobs:
  triage:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
      pull-requests: write
      issues: write
    steps:
      - name: Check account age
        id: check
        uses: actions/github-script@v7
        with:
          script: |
            const user = await github.rest.users.getByUsername({
              username: context.payload.issue.user.login
            });
            const created = new Date(user.data.created_at);
            const days = (Date.now() - created) / (1000 * 60 * 60 * 24);
            return days >= 30;
          result-encoding: string

      - uses: actions/checkout@v6
        if: steps.check.outputs.result == 'true'
        with:
          persist-credentials: false

      - uses: mysterious75/voracode/github@latest
        if: steps.check.outputs.result == 'true'
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        with:
          model: anthropic/claude-sonnet-4-20250514
          prompt: |
            Review this issue. If there's a clear fix or relevant docs:
            - Provide documentation links
            - Add error handling guidance for code examples
            Otherwise, do not comment.
```

Za `issues` dogaÄ‘aje, `prompt` unos je **potreban** jer nema komentara za izvlaÄenje instrukcija.

---

## PrilagoÄ‘eni upiti

ZaobiÄ‘ite zadani prompt da biste prilagodili ponaÅ¡anje Voracode za vaÅ¡ tok posla.

```yaml title=".github/workflows/voracode.yml"
- uses: mysterious75/voracode/github@latest
  with:
    model: anthropic/claude-sonnet-4-5
    prompt: |
      Review this pull request:
      - Check for code quality issues
      - Look for potential bugs
      - Suggest improvements
```

Ovo je korisno za provoÄ‘enje specifiÄnih kriterija pregleda, standarda kodiranja ili fokusnih podruÄja relevantnih za vaÅ¡ projekt.

---

## Primjeri

Evo nekoliko primjera kako moÅ¾ete koristiti Voracode u GitHub.

- **Objasnite problem**
  Dodajte ovaj komentar u GitHub izdanje.

```
  /voracode explain this issue
```

Voracode Ä‡e proÄitati cijelu temu, ukljuÄujuÄ‡i sve komentare, i odgovoriti s jasnim objaÅ¡njenjem.

- **Popravi problem**
  U izdanju na GitHub-u recite:

```
  /voracode fix this
```

I Voracode Ä‡e kreirati novu granu, implementirati promjene i otvoriti PR sa promjenama.

- **Pregledajte PR-ove i izvrÅ¡ite izmjene**
  Ostavite sljedeÄ‡i komentar na GitHub PR-u.

```
  Delete the attachment from S3 when the note is removed /oc
```

Voracode Ä‡e implementirati traÅ¾enu promjenu i posvetiti je istom PR-u.

- **Pregledajte odreÄ‘ene linije koda**
  Ostavite komentar direktno na linije koda u PR kartici "Files". Voracode automatski detektuje datoteku, brojeve redova i kontekst razlike kako bi pruÅ¾io precizne odgovore.

```
  [Comment on specific lines in Files tab]
  /oc add error handling here
```

Kada komentariÅ¡ete odreÄ‘ene linije, Voracode prima:

- TaÄan fajl se pregleda
- SpecifiÄne linije koda
- Okolni diff kontekst
- Informacije o broju linije
  Ovo omoguÄ‡ava viÅ¡e ciljanih zahtjeva bez potrebe za ruÄno specificiranjem putanja datoteka ili brojeva linija.
