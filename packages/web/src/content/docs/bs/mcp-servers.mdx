---
title: MCP serveri
description: Dodajte lokalne i udaljene MCP alate.
---

MoÅ¾ete dodati vanjske alate u Voracode koristeÄ‡i _Model Context Protocol_, ili MCP. Voracode podrÅ¾ava i lokalne i udaljene servere.
Jednom dodani, MCP alati su automatski dostupni LLM-u zajedno sa ugraÄ‘enim alatima.

---

#### Upozorenje

Kada koristite MCP server, on dodaje u kontekst. Ovo se moÅ¾e brzo zbrojiti ako imate puno alata. Stoga preporuÄujemo da pazite koje MCP servere koristite.
:::tip
MCP serveri dodaju vaÅ¡ kontekst, tako da Å¾elite da budete paÅ¾ljivi s tim koje Ä‡ete omoguÄ‡iti.
:::

OdreÄ‘eni MCP serveri, poput GitHub MCP servera, mogu dodati mnogo tokena i lako premaÅ¡iti limit konteksta.

## OmoguÄ‡avanje

MoÅ¾ete definirati MCP servere u vaÅ¡oj [Voracode Config](https://voracode.ai/docs/config/) pod `mcp`. Dodajte svaki MCP sa jedinstvenim imenom. MoÅ¾ete se pozvati na taj MCP po imenu kada traÅ¾ite LLM.

```jsonc title="voracode.jsonc" {6}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "name-of-mcp-server": {
      // ...
      "enabled": true,
    },
    "name-of-other-mcp-server": {
      // ...
    },
  },
}
```

TakoÄ‘er moÅ¾ete onemoguÄ‡iti server postavljanjem `enabled` na `false`. To je korisno kada ga Å¾elite privremeno iskljuÄiti bez uklanjanja iz konfiguracije.

### PoniÅ¡tavanje udaljenih zadanih postavki

Organizacije mogu obezbijediti zadane MCP servere preko svoje krajnje taÄke `.well-known/voracode`. Ovi serveri mogu biti onemoguÄ‡eni prema zadanim postavkama, omoguÄ‡avajuÄ‡i korisnicima da se odluÄe za one koji su im potrebni.
Da omoguÄ‡ite odreÄ‘eni server iz udaljene konfiguracije vaÅ¡e organizacije, dodajte ga u svoju lokalnu konfiguraciju sa `enabled: true`:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "jira": {
      "type": "remote",
      "url": "https://jira.example.com/mcp",
      "enabled": true
    }
  }
}
```

Vrijednosti lokalne konfiguracije nadjaÄavaju udaljene zadane postavke. Pogledajte [config precedence](/docs/config#precedence-order) za viÅ¡e detalja.

## Lokalno

Dodajte lokalne MCP servere koristeÄ‡i `type` u `"local"` unutar MCP objekta.

```jsonc title="voracode.jsonc" {15}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-local-mcp-server": {
      "type": "local",
      // Or ["bun", "x", "my-mcp-command"]
      "command": ["npx", "-y", "my-mcp-command"],
      "enabled": true,
      "environment": {
        "MY_ENV_VAR": "my_env_var_value",
      },
    },
  },
}
```

Naredba je naÄin na koji se pokreÄ‡e lokalni MCP server. TakoÄ‘er moÅ¾ete proslijediti listu varijabli okruÅ¾enja.
Na primjer, evo kako moÅ¾ete dodati testni [`@modelcontextprotocol/server-everything`](https://www.npmjs.com/package/@modelcontextprotocol/server-everything) MCP server.

```jsonc title="voracode.jsonc"
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "mcp_everything": {
      "type": "local",
      "command": ["npx", "-y", "@modelcontextprotocol/server-everything"],
    },
  },
}
```

I da ga koristim mogu dodati `use the mcp_everything tool` u svoje upite.

```txt "mcp_everything"
use the mcp_everything tool to add the number 3 and 4
```

---

#### Opcije

Ovdje su sve opcije za konfiguriranje lokalnog MCP servera.
| Opcija | Tip | Obavezno | Opis
|------------- | ------- | -------- | ----------------------------------------------------------------------------------- |
| `type` | String | Y | Tip veze sa MCP serverom, mora biti `"local"`. |
| `command` | Niz | Y | Naredba i argumenti za pokretanje MCP servera. |
| `environment` | Objekt | | Varijable okruÅ¾enja koje treba postaviti prilikom pokretanja servera. |
| `enabled` | Boolean | | OmoguÄ‡ite ili onemoguÄ‡ite MCP server pri pokretanju. |
| `timeout` | Broj | | Vremensko ograniÄenje u ms za dohvaÄ‡anje alata sa MCP servera. Podrazumevano je 5000 (5 sekundi). |

---

## Udaljeno

Dodajte udaljene MCP servere postavljanjem `type` na `"remote"`.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-remote-mcp": {
      "type": "remote",
      "url": "https://my-mcp-server.com",
      "enabled": true,
      "headers": {
        "Authorization": "Bearer MY_API_KEY"
      }
    }
  }
}
```

`url` je URL udaljenog MCP servera, a kroz opciju `headers` moÅ¾ete proslijediti listu zaglavlja.

#### Opcije

| Opcija    | Tip     | Obavezno | Opis                                                                                               |
| --------- | ------- | -------- | -------------------------------------------------------------------------------------------------- |
| `type`    | String  | Y        | Tip veze sa MCP serverom, mora biti `"remote"`.                                                    |
| `url`     | String  | Y        | URL udaljenog MCP servera.                                                                         |
| `enabled` | Boolean |          | OmoguÄ‡ite ili onemoguÄ‡ite MCP server pri pokretanju.                                               |
| `headers` | Objekt  |          | Zaglavlja za slanje uz zahtjev.                                                                    |
| `oauth`   | Objekt  |          | Konfiguracija OAuth provjere autentiÄnosti. Pogledajte odjeljak [OAuth](#oauth) ispod.             |
| `timeout` | Broj    |          | Vremensko ograniÄenje u ms za preuzimanje alata sa MCP servera. Podrazumevano je 5000 (5 sekundi). |

---

## OAuth

Voracode automatski rukuje OAuth autentifikacijom za udaljene MCP servere. Kada server zahtijeva autentifikaciju, Voracode Ä‡e:

1. Otkrijte 401 odgovor i pokrenite OAuth tok
2. Koristite **Dynamic Client Registration (RFC 7591)** ako podrÅ¾ava server
3. Sigurno Äuvajte tokene za buduÄ‡e zahtjeve

---

### Automatski

Za veÄ‡inu MCP servera sa omoguÄ‡enim OAuthom nije potrebna posebna konfiguracija. Samo konfigurirajte udaljeni server:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-oauth-server": {
      "type": "remote",
      "url": "https://mcp.example.com/mcp"
    }
  }
}
```

Ako server zahtijeva autentifikaciju, Voracode Ä‡e vas traÅ¾iti prijavu pri prvom koriÅ¡tenju. Ako se to ne desi, moÅ¾ete [ruÄno pokrenuti tok](#authenticating) naredbom `voracode mcp auth <server-name>`.

### Prethodno registrirano

Ako imate klijentske vjerodajnice od dobavljaÄa MCP servera, moÅ¾ete ih konfigurirati:

```json title="voracode.json" {7-11}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-oauth-server": {
      "type": "remote",
      "url": "https://mcp.example.com/mcp",
      "oauth": {
        "clientId": "{env:MY_MCP_CLIENT_ID}",
        "clientSecret": "{env:MY_MCP_CLIENT_SECRET}",
        "scope": "tools:read tools:execute"
      }
    }
  }
}
```

---

### Autentifikacija

MoÅ¾ete ruÄno pokrenuti autentifikaciju ili upravljati vjerodajnicama.
Autentifikacija sa odreÄ‘enim MCP serverom:

```bash
voracode mcp auth my-oauth-server
```

Navedite sve MCP servere i njihov status autentifikacije:

```bash
voracode mcp list
```

Uklonite pohranjene vjerodajnice:

```bash
voracode mcp logout my-oauth-server
```

Komanda `mcp auth` otvara pretraÅ¾ivaÄ za autorizaciju. Nakon odobrenja, Voracode sigurno Äuva tokene u `~/.local/share/voracode/mcp-auth.json`.

#### OnemoguÄ‡avanje OAuth-a

Ako Å¾elite onemoguÄ‡iti automatski OAuth za server (npr. za servere koji umjesto toga koriste API kljuÄeve), postavite `oauth` na `false`:

```json title="voracode.json" {7}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-api-key-server": {
      "type": "remote",
      "url": "https://mcp.example.com/mcp",
      "oauth": false,
      "headers": {
        "Authorization": "Bearer {env:MY_API_KEY}"
      }
    }
  }
}
```

---

#### OAuth opcije

| Opcija         | Tip             | Opis                                                                                          |
| -------------- | --------------- | --------------------------------------------------------------------------------------------- |
| `oauth`        | Objekt \| false | OAuth konfiguracijski objekt, ili `false` da onemoguÄ‡ite automatsko otkrivanje OAuth.         |
| `clientId`     | String          | ID OAuth klijenta. Ako nije navedeno, pokuÅ¡at Ä‡e se izvrÅ¡iti dinamiÄka registracija klijenta. |
| `clientSecret` | String          | Tajna OAuth klijenta, ako to zahtijeva autorizacijski server.                                 |
| `scope`        | String          | OAuth opseg zahtjeva za vrijeme autorizacije.                                                 |

#### Otklanjanje greÅ¡aka

Ako udaljeni MCP server ne uspije u autentifikaciji, moÅ¾ete dijagnosticirati probleme pomoÄ‡u:

```bash
# View auth status for all OAuth-capable servers
voracode mcp auth list

# Debug connection and OAuth flow for a specific server
voracode mcp debug my-oauth-server
```

Komanda `mcp debug` prikazuje trenutni auth status, testira HTTP povezanost i pokuÅ¡ava OAuth discovery flow.

## Upravljanje

VaÅ¡i MCP serveri su dostupni kao alati u Voracode, zajedno s ugraÄ‘enim alatima. MoÅ¾ete njima upravljati kroz Voracode konfiguraciju kao i bilo kojim drugim alatom.

### Globalno

To znaÄi da ih moÅ¾ete omoguÄ‡iti ili onemoguÄ‡iti globalno.

```json title="voracode.json" {14}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-mcp-foo": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command-foo"]
    },
    "my-mcp-bar": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command-bar"]
    }
  },
  "tools": {
    "my-mcp-foo": false
  }
}
```

TakoÄ‘er moÅ¾emo koristiti glob obrazac da onemoguÄ‡imo sve odgovarajuÄ‡e MCP-ove.

```json title="voracode.json" {14}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-mcp-foo": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command-foo"]
    },
    "my-mcp-bar": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command-bar"]
    }
  },
  "tools": {
    "my-mcp*": false
  }
}
```

Ovdje koristimo glob obrazac `my-mcp*` da onemoguÄ‡imo sve MCP servere.

### Po agentu

Ako imate veliki broj MCP servera, moÅ¾da Ä‡ete Å¾eleti da ih omoguÄ‡ite samo po agentu i da ih onemoguÄ‡ite globalno. Da biste to uÄinili:

1. OnemoguÄ‡ite ga kao alat globalno.
2. U vaÅ¡em [agent config](/docs/agents#tools), omoguÄ‡ite MCP server kao alat.

```json title="voracode.json" {11, 14-18}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-mcp": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command"],
      "enabled": true
    }
  },
  "tools": {
    "my-mcp*": false
  },
  "agent": {
    "my-agent": {
      "tools": {
        "my-mcp*": true
      }
    }
  }
}
```

---

#### Glob uzorci

Uzorak glob koristi jednostavne Å¡ablone globbiranja regularnih izraza:

- `*` odgovara nuli ili viÅ¡e bilo kojeg znaka (npr. `"my-mcp*"` odgovara `my-mcp_search`, `my-mcp_list`, itd.)
- `?` odgovara taÄno jednom znaku
- Svi ostali likovi se bukvalno podudaraju
  :::note
  MCP serverski alati se registruju sa imenom servera kao prefiksom, tako da onemoguÄ‡ite sve alate za server jednostavno koristite:

```
"mymcpservername_*": false
```

:::

---

## Primjeri

Ispod su primjeri uobiÄajenih MCP servera. MoÅ¾ete poslati PR ako Å¾elite dokumentovati druge servere.

### Sentry

Dodajte [Sentry MCP server](https://mcp.sentry.dev) za interakciju sa vaÅ¡im Sentry projektima i problemima.

```json title="voracode.json" {4-8}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "sentry": {
      "type": "remote",
      "url": "https://mcp.sentry.dev/mcp",
      "oauth": {}
    }
  }
}
```

Nakon dodavanja konfiguracije, autentifikujte se sa Sentry:

```bash
voracode mcp auth sentry
```

Ovo Ä‡e otvoriti prozor pretraÅ¾ivaÄa da zavrÅ¡ite OAuth tok i poveÅ¾ete Voracode sa vaÅ¡im Sentry nalogom.
Nakon provjere autentiÄnosti, moÅ¾ete koristiti Sentry alate u svojim upitima za upite o problemima, projektima i podacima o greÅ¡kama.

```txt "use sentry"
Show me the latest unresolved issues in my project. use sentry
```

---

### Context7

Dodajte [Context7 MCP server](https://github.com/upstash/context7) za pretraÅ¾ivanje dokumenata.

```json title="voracode.json" {4-7}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "context7": {
      "type": "remote",
      "url": "https://mcp.context7.com/mcp"
    }
  }
}
```

Ako ste se prijavili za besplatni raÄun, moÅ¾ete koristiti svoj API kljuÄ i dobiti viÅ¡a ograniÄenja stope.

```json title="voracode.json" {7-9}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "context7": {
      "type": "remote",
      "url": "https://mcp.context7.com/mcp",
      "headers": {
        "CONTEXT7_API_KEY": "{env:CONTEXT7_API_KEY}"
      }
    }
  }
}
```

Ovdje pretpostavljamo da imate postavljenu varijablu okruÅ¾enja `CONTEXT7_API_KEY`.
Dodajte `use context7` vaÅ¡im upitima za koriÅ¡tenje Context7 MCP servera.

```txt "use context7"
Configure a Cloudflare Worker script to cache JSON API responses for five minutes. use context7
```

Alternativno, moÅ¾ete dodati neÅ¡to poput ovoga na svoj [AGENTS.md](/docs/rules/).

```md title="AGENTS.md"
When you need to search docs, use `context7` tools.
```

---

### Grep by Vercel

Dodajte [Grep by Vercel](https://grep.app) MCP server za pretraÅ¾ivanje isjeÄaka koda na GitHub.

```json title="voracode.json" {4-7}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "gh_grep": {
      "type": "remote",
      "url": "https://mcp.grep.app"
    }
  }
}
```

PoÅ¡to smo naÅ¡ MCP server nazvali `gh_grep`, moÅ¾ete dodati `use the gh_grep tool` u svoje upite da natjerate agenta da ga koristi.

```txt "use the gh_grep tool"
What's the right way to set a custom domain in an SST Astro component? use the gh_grep tool
```

Alternativno, moÅ¾ete dodati neÅ¡to poput ovoga na svoj [AGENTS.md](/docs/rules/).

```md title="AGENTS.md"
If you are unsure how to do something, use `gh_grep` to search code examples from GitHub.
```
