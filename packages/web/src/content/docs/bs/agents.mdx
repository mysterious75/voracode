---
title: Agenti
description: Konfigurirajte i koristite specijalizirane agente.
---

Agenti su specijalizirani AI asistenti koji se mogu konfigurirati za specifiÄne zadatke i tokove posla. Oni vam omoguÄ‡avaju da kreirate fokusirane alate sa prilagoÄ‘enim upitima, modelima i pristupom alatima.

:::tip
Koristite agenta plana za analizu koda i pregled prijedloga bez ikakvih promjena koda.
:::

MoÅ¾ete se prebacivati â€‹â€‹izmeÄ‘u agenata tokom sesije ili ih pozvati spominjanjem `@`.

---

## Vrste

Postoje dvije vrste agenata u Voracode: primarni agenti i podagenti.

---

### Primarni agenti

Primarni agenti su glavni pomoÄ‡nici s kojima direktno komunicirate. MoÅ¾ete se kretati kroz njih pomoÄ‡u tipke **Tab** ili vaÅ¡eg konfigurisanog povezivanja tipki `switch_agent`. Ovi agenti vode vaÅ¡ glavni razgovor. Pristup alatima se konfiguriÅ¡e putem dozvola â€” na primjer, Build ima omoguÄ‡ene sve alate dok je Plan ograniÄen.

:::tip
MoÅ¾ete koristiti tipku **Tab** za prebacivanje izmeÄ‘u primarnih agenata tokom sesije.
:::

Voracode dolazi sa dva ugraÄ‘ena primarna agenta, **Build** i **Plan**. Pogledat Ä‡emo ih u nastavku.

---

### Subagenti

Subagenti su specijalizovani pomoÄ‡nici koje primarni agenti mogu pozvati za odreÄ‘ene zadatke. MoÅ¾ete ih i ruÄno pozvati **@ spominjanjem** u svojim porukama.

Voracode dolazi sa tri ugraÄ‘ena subagenta, **General**, **Explore** i **Scout**. Ovo Ä‡emo pogledati u nastavku.

---

## UgraÄ‘eni

Voracode dolazi sa dva ugraÄ‘ena primarna agenta i tri ugraÄ‘ena subagenta.

---

### Build agent

_ReÅ¾im_: `primary`

Build je **podrazumevani** primarni agent sa svim omoguÄ‡enim alatima. Ovo je standardni agent za razvojni rad gdje vam je potreban pun pristup operacijama datoteka i sistemskim komandama.

---

### Plan agent

_ReÅ¾im_: `primary`

OgraniÄeni agent dizajniran za planiranje i analizu. Koristimo sistem dozvola kako bismo vam pruÅ¾ili veÄ‡u kontrolu i sprijeÄili neÅ¾eljene promjene.
Prema zadanim postavkama, sve sljedeÄ‡e je postavljeno na `ask`:

- `file edits`: Sva upisivanja, zakrpe i ureÄ‘ivanja
- `bash`: Sve bash komande

Ovaj agent je koristan kada Å¾elite da LLM analizira kod, predloÅ¾i promjene ili kreira planove bez stvarnih modifikacija vaÅ¡e baze koda.

---

### General agent

_ReÅ¾im_: `subagent`

Agent opÄ‡e namjene za istraÅ¾ivanje sloÅ¾enih pitanja i izvrÅ¡avanje zadataka u viÅ¡e koraka. Ima potpuni pristup alatima (osim todo), tako da moÅ¾e mijenjati fajlove kada je to potrebno. Koristite ovo za paralelno pokretanje viÅ¡e jedinica rada.

---

### Explore agent

_ReÅ¾im_: `subagent`

Brzi agent samo za Äitanje za istraÅ¾ivanje kodnih baza. Nije moguÄ‡e mijenjati fajlove. Koristite ovo kada trebate brzo pronaÄ‡i datoteke po uzorku, pretraÅ¾iti kod za kljuÄne rijeÄi ili odgovoriti na pitanja o bazi kodova.

---

### Scout agent

_ReÅ¾im_: `subagent`

Agent samo za Äitanje za istraÅ¾ivanje eksterne dokumentacije i zavisnosti. Koristite ga kada trebate klonirati repozitorij zavisnosti u Voracode-ov upravljani cache, pregledati izvorni kod biblioteke ili uporediti lokalni kod sa upstream implementacijama bez mijenjanja vaÅ¡eg radnog prostora.

---

### Compaction agent

_ReÅ¾im_: `primary`

Skriveni sistemski agent koji saÅ¾ima dugi kontekst u manji saÅ¾etak. PokreÄ‡e se automatski kada je potrebno i ne moÅ¾e se odabrati u korisniÄkom interfejsu.

---

### Title agent

_ReÅ¾im_: `primary`

Skriveni sistemski agent koji generiÅ¡e kratke naslove sesija. PokreÄ‡e se automatski i ne moÅ¾e se odabrati u korisniÄkom interfejsu.

---

### Summary agent

_ReÅ¾im_: `primary`

Skriveni sistemski agent koji kreira saÅ¾etke sesije. PokreÄ‡e se automatski i ne moÅ¾e se odabrati u korisniÄkom interfejsu.

---

## KoriÅ¡tenje

1. Za primarne agente, koristite taster **Tab** za kretanje kroz njih tokom sesije. TakoÄ‘er moÅ¾ete koristiti svoju konfiguriranu vezu tipke `switch_agent`.

2. Subagenti se mogu pozvati:
   - **Automatski** od strane primarnih agenata za specijalizovane zadatke na osnovu njihovih opisa.
   - RuÄno **@ spominjanjem** subagenta u vaÅ¡oj poruci. Na primjer.

     ```txt frame="none"
     @general help me search for this function
     ```

3. **Navigacija izmeÄ‘u sesija**: Kada subagenti kreiraju vlastite podreÄ‘ene sesije, moÅ¾ete se kretati izmeÄ‘u roditeljske sesije i svih podreÄ‘enih sesija koristeÄ‡i:
   - **\<Leader>+Right** (ili vaÅ¡a konfigurirana `session_child_cycle` veza) za kretanje naprijed kroz roditelj â†’ dijete1 â†’ dijete2 â†’ ... â†’ roditelj
   - **\<Leader>+Left** (ili vaÅ¡e konfigurirano povezivanje tipki `session_child_cycle_reverse`) za kretanje unazad kroz roditelj â† dijete1 â† dijete2 â† ... â† roditelj

   Ovo vam omoguÄ‡ava neprimetno prebacivanje izmeÄ‘u glavnog razgovora i rada specijalizovanog podagenta.

---

## Konfiguracija

MoÅ¾ete prilagoditi ugraÄ‘ene agente ili kreirati vlastite kroz konfiguraciju. Agenti se mogu konfigurisati na dva naÄina:

---

### JSON

Konfigurirajte agente u svom konfiguracijskom fajlu `voracode.json`:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "agent": {
    "build": {
      "mode": "primary",
      "model": "anthropic/claude-sonnet-4-20250514",
      "prompt": "{file:./prompts/build.txt}",
      "tools": {
        "write": true,
        "edit": true,
        "bash": true
      }
    },
    "plan": {
      "mode": "primary",
      "model": "anthropic/claude-haiku-4-20250514",
      "tools": {
        "write": false,
        "edit": false,
        "bash": false
      }
    },
    "code-reviewer": {
      "description": "Reviews code for best practices and potential issues",
      "mode": "subagent",
      "model": "anthropic/claude-sonnet-4-20250514",
      "prompt": "You are a code reviewer. Focus on security, performance, and maintainability.",
      "tools": {
        "write": false,
        "edit": false
      }
    }
  }
}
```

---

### Markdown

TakoÄ‘er moÅ¾ete definirati agente koristeÄ‡i markdown datoteke. Stavite ih u:

- Globalno: `~/.config/voracode/agents/`
- Po projektu: `.voracode/agents/`

```markdown title="~/.config/voracode/agents/review.md"
---
description: Reviews code for quality and best practices
mode: subagent
model: anthropic/claude-sonnet-4-20250514
temperature: 0.1
tools:
  write: false
  edit: false
  bash: false
---

You are in code review mode. Focus on:

- Code quality and best practices
- Potential bugs and edge cases
- Performance implications
- Security considerations

Provide constructive feedback without making direct changes.
```

Ime markdown datoteke postaje ime agenta. Na primjer, `review.md` kreira `review` agenta.

---

## Opcije

Pogledajmo ove opcije konfiguracije detaljno.

---

### Opis

Koristite opciju `description` da pruÅ¾ite kratak opis onoga Å¡to agent radi i kada ga koristiti.

```json title="voracode.json"
{
  "agent": {
    "review": {
      "description": "Reviews code for best practices and potential issues"
    }
  }
}
```

Ovo je **obavezna** opcija konfiguracije.

---

### Temperatura

KontroliÅ¡ite sluÄajnost i kreativnost odgovora LLM-a pomoÄ‡u `temperature` konfiguracije.

NiÅ¾e vrijednosti Äine odgovore fokusiranijim i deterministiÄkim, dok viÅ¡e vrijednosti poveÄ‡avaju kreativnost i varijabilnost.

```json title="voracode.json"
{
  "agent": {
    "plan": {
      "temperature": 0.1
    },
    "creative": {
      "temperature": 0.8
    }
  }
}
```

Vrijednosti temperature se obiÄno kreÄ‡u od 0.0 do 1.0:

- **0.0-0.2**: Vrlo fokusirani i deterministiÄki odgovori, idealni za analizu i planiranje koda
- **0.3-0.5**: UravnoteÅ¾eni odgovori sa malo kreativnosti, dobro za opÅ¡te razvojne zadatke
- **0.6-1.0**: Kreativniji i raznovrsniji odgovori, korisni za razmiÅ¡ljanje i istraÅ¾ivanje

```json title="voracode.json"
{
  "agent": {
    "analyze": {
      "temperature": 0.1,
      "prompt": "{file:./prompts/analysis.txt}"
    },
    "build": {
      "temperature": 0.3
    },
    "brainstorm": {
      "temperature": 0.7,
      "prompt": "{file:./prompts/creative.txt}"
    }
  }
}
```

Ako temperatura nije navedena, Voracode koristi standardne postavke specifiÄne za model; obiÄno 0 za veÄ‡inu modela, 0.55 za Qwen modele.

---

### Maksimalan broj koraka

Kontrolirajte maksimalni broj iteracija agenta koje agent moÅ¾e izvesti prije nego Å¡to bude prisiljen da odgovori samo tekstom. Ovo omoguÄ‡ava korisnicima koji Å¾ele kontrolirati troÅ¡kove da postave ograniÄenje na akcije agenta.

Ako ovo nije postavljeno, agent Ä‡e nastaviti iterirati sve dok model ne odluÄi da se zaustavi ili korisnik ne prekine sesiju.

```json title="voracode.json"
{
  "agent": {
    "quick-thinker": {
      "description": "Fast reasoning with limited iterations",
      "prompt": "You are a quick thinker. Solve problems with minimal steps.",
      "steps": 5
    }
  }
}
```

Kada se dostigne ograniÄenje, agent prima poseban sistemski prompt koji ga upuÄ‡uje da odgovori sa rezimeom svog rada i preporuÄenim preostalim zadacima.

:::caution
NaslijeÄ‘eno polje `maxSteps` je zastarjelo. Umjesto toga koristite `steps`.
:::

---

### OnemoguÄ‡avanje

Postavite na `true` da onemoguÄ‡ite agenta.

```json title="voracode.json"
{
  "agent": {
    "review": {
      "disable": true
    }
  }
}
```

---

### Upit

Navedite prilagoÄ‘enu sistemsku prompt datoteku za ovog agenta sa `prompt` konfiguracijom. Datoteka s promptom treba da sadrÅ¾i upute specifiÄne za svrhu agenta.

```json title="voracode.json"
{
  "agent": {
    "review": {
      "prompt": "{file:./prompts/code-review.txt}"
    }
  }
}
```

Ova putanja je relativna u odnosu na mjesto gdje se nalazi konfiguracijski fajl. Dakle, ovo radi i za globalnu Voracode konfiguraciju i za konfiguraciju specifiÄne za projekat.

---

### Model

Koristite `model` konfiguraciju da nadjaÄate model za ovog agenta. Korisno za koriÅ¡tenje razliÄitih modela optimiziranih za razliÄite zadatke. Na primjer, brÅ¾i model za planiranje, sposobniji model za implementaciju.

:::tip
Ako ne navedete model, primarni agenti koriste [model globalno konfiguriran](/docs/config#models) dok Ä‡e podagenti koristiti model primarnog agenta koji je pozvao subagenta.
:::

```json title="voracode.json"
{
  "agent": {
    "plan": {
      "model": "anthropic/claude-haiku-4-20250514"
    }
  }
}
```

ID modela u vaÅ¡oj Voracode konfiguraciji koristi format `provider/model-id`. Na primjer, ako koristite [Voracode Zen](/docs/zen), koristili biste `voracode/gpt-5.1-codex` za GPT 5.1 Codex.

---

### Alati

Kontrolirajte koji su alati dostupni u ovom agentu koristeÄ‡i konfiguraciju `tools`. MoÅ¾ete omoguÄ‡iti ili onemoguÄ‡iti odreÄ‘ene alate tako Å¡to Ä‡ete ih postaviti na `true` ili `false`.

```json title="voracode.json" {3-6,9-12}
{
  "$schema": "https://voracode.ai/config.json",
  "tools": {
    "write": true,
    "bash": true
  },
  "agent": {
    "plan": {
      "tools": {
        "write": false,
        "bash": false
      }
    }
  }
}
```

:::note
Konfiguracija specifiÄna za agenta poniÅ¡tava globalnu konfiguraciju.
:::

TakoÄ‘er moÅ¾ete koristiti zamjenske znakove za kontrolu viÅ¡e alata odjednom. Na primjer, da onemoguÄ‡ite sve alate sa MCP servera:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "agent": {
    "readonly": {
      "tools": {
        "mymcp_*": false,
        "write": false,
        "edit": false
      }
    }
  }
}
```

[Saznajte viÅ¡e o alatima](/docs/tools).

---

### Dozvole

MoÅ¾ete konfigurirati dozvole za upravljanje radnjama koje agent moÅ¾e poduzeti. Trenutno se dozvole za alate `edit`, `bash` i `webfetch` mogu konfigurirati na:

- `"ask"` â€” ZatraÅ¾ite odobrenje prije pokretanja alata
- `"allow"` â€” Dozvoli sve operacije bez odobrenja
- `"deny"` â€” OnemoguÄ‡ite alat

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "edit": "deny"
  }
}
```

MoÅ¾ete nadjaÄati ove dozvole po agentu.

```json title="voracode.json" {3-5,8-10}
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "edit": "deny"
  },
  "agent": {
    "build": {
      "permission": {
        "edit": "ask"
      }
    }
  }
}
```

TakoÄ‘er moÅ¾ete postaviti dozvole u Markdown agentima.

```markdown title="~/.config/voracode/agents/review.md"
---
description: Code review without edits
mode: subagent
permission:
  edit: deny
  bash:
    "*": ask
    "git diff": allow
    "git log*": allow
    "grep *": allow
  webfetch: deny
---

Only analyze code and suggest changes.
```

MoÅ¾ete postaviti dozvole za odreÄ‘ene bash komande.

```json title="voracode.json" {7}
{
  "$schema": "https://voracode.ai/config.json",
  "agent": {
    "build": {
      "permission": {
        "bash": {
          "git push": "ask",
          "grep *": "allow"
        }
      }
    }
  }
}
```

Ovo moÅ¾e koristiti glob uzorak.

```json title="voracode.json" {7}
{
  "$schema": "https://voracode.ai/config.json",
  "agent": {
    "build": {
      "permission": {
        "bash": {
          "git *": "ask"
        }
      }
    }
  }
}
```

TakoÄ‘er moÅ¾ete koristiti zamjenski znak `*` za kontrolu dozvola za sve komande.
BuduÄ‡i da posljednje podudarno pravilo ima prednost, prvo postavite zamjenski znak `*`, a zatim navedena pravila.

```json title="voracode.json" {8}
{
  "$schema": "https://voracode.ai/config.json",
  "agent": {
    "build": {
      "permission": {
        "bash": {
          "*": "ask",
          "git status *": "allow"
        }
      }
    }
  }
}
```

[Saznajte viÅ¡e o dozvolama](/docs/permissions).

---

### NaÄin rada

Kontrolirajte naÄin rada agenta koristeÄ‡i konfiguraciju `mode`. Opcija `mode` se koristi da specificira kako se agent moÅ¾e koristiti.

```json title="voracode.json"
{
  "agent": {
    "review": {
      "mode": "subagent"
    }
  }
}
```

Opcija `mode` se moÅ¾e postaviti na `primary`, `subagent` ili `all`. Ako `mode` nije specificirano, podrazumevano je `all`.

---

### Skriveno

Sakrij podagenta iz `@` menija za automatsko dovrÅ¡avanje sa `hidden: true`. Korisno za interne podagente koje bi drugi agenti trebali programski pozvati samo preko Task alata.

```json title="voracode.json"
{
  "agent": {
    "internal-helper": {
      "mode": "subagent",
      "hidden": true
    }
  }
}
```

Ovo utiÄe samo na vidljivost korisnika u meniju za automatsko dovrÅ¡avanje. Skriveni agenti se i dalje mogu pozvati od strane modela putem alata Task ako dozvole to dozvoljavaju.

:::note
Odnosi se samo na `mode: subagent` agente.
:::

---

### Dozvole zadataka

Kontrolirajte koje podagente agent moÅ¾e pozvati preko Task alata sa `permission.task`. Koristi glob uzorke za fleksibilno uparivanje.

```json title="voracode.json"
{
  "agent": {
    "orchestrator": {
      "mode": "primary",
      "permission": {
        "task": {
          "*": "deny",
          "orchestrator-*": "allow",
          "code-reviewer": "ask"
        }
      }
    }
  }
}
```

Kada se postavi na `deny`, subagent se u potpunosti uklanja iz opisa alata za zadatak, tako da ga model neÄ‡e pokuÅ¡ati pozvati.

:::tip
Pravila se procjenjuju po redoslijedu i **posljednje odgovarajuÄ‡e pravilo pobjeÄ‘uje**. U gornjem primjeru, `orchestrator-planner` odgovara i `*` (deny) i `orchestrator-*` (allow), ali poÅ¡to `orchestrator-*` dolazi nakon `*`, rezultat je `allow`.
:::

:::tip
Korisnici uvijek mogu pozvati bilo kojeg subagenta direktno preko `@` menija za autodovrÅ¡avanje, Äak i ako bi dozvole za zadatak agenta to uskratile.
:::

---

### Boja

Prilagodite vizualni izgled agenta u korisniÄkom suÄelju s opcijom `color`. Ovo utiÄe na to kako se agent pojavljuje u interfejsu.

Koristite vaÅ¾eÄ‡u heksadecimalnu boju (npr. `#FF5733`) ili boju teme: `primary`, `secondary`, `accent`, `success`, `warning`, `error`, `info`.

```json title="voracode.json"
{
  "agent": {
    "creative": {
      "color": "#ff6b6b"
    },
    "code-reviewer": {
      "color": "accent"
    }
  }
}
```

---

### Top P

Kontrolirajte raznolikost odgovora s opcijom `top_p`. Alternativa temperaturi za kontrolu nasumice.

```json title="voracode.json"
{
  "agent": {
    "brainstorm": {
      "top_p": 0.9
    }
  }
}
```

Vrijednosti se kreÄ‡u od 0.0 do 1.0. NiÅ¾e vrijednosti su viÅ¡e fokusirane, viÅ¡e vrijednosti raznovrsnije.

---

### Dodatno

Sve druge opcije koje navedete u konfiguraciji agenta Ä‡e biti **direktno proslijeÄ‘ene** dobavljaÄu kao opcije modela. Ovo vam omoguÄ‡ava da koristite karakteristike i parametre specifiÄne za provajdera.

Na primjer, sa OpenAI-jevim modelima rezonovanja, moÅ¾ete kontrolisati napor rasuÄ‘ivanja:

```json title="voracode.json" {6,7}
{
  "agent": {
    "deep-thinker": {
      "description": "Agent that uses high reasoning effort for complex problems",
      "model": "openai/gpt-5",
      "reasoningEffort": "high",
      "textVerbosity": "low"
    }
  }
}
```

Ove dodatne opcije su specifiÄne za model i dobavljaÄa. U dokumentaciji vaÅ¡eg provajdera provjerite dostupne parametre.

:::tip
Pokrenite `voracode models` da vidite listu dostupnih modela.
:::

---

## Kreiranje agenata

MoÅ¾ete kreirati nove agente koristeÄ‡i sljedeÄ‡u naredbu:

```bash
voracode agent create
```

Ova interaktivna komanda Ä‡e:

1. Pitajte gdje da saÄuvate agenta; globalno ili specifiÄno za projekat.
2. Opis onoga Å¡to agent treba da uradi.
3. Generirajte odgovarajuÄ‡i sistemski prompt i identifikator.
4. OmoguÄ‡ite vam da odaberete kojim alatima agent moÅ¾e pristupiti.
5. KonaÄno, kreirajte markdown datoteku s konfiguracijom agenta.

---

## Primjeri upotrebe

Evo nekoliko uobiÄajenih sluÄajeva upotrebe razliÄitih agenata.

- **Build agent**: Potpuni razvojni rad sa svim omoguÄ‡enim alatima
- **Plan agent**: Analiza i planiranje bez unoÅ¡enja promjena
- **Review agent**: Code review sa pristupom samo za Äitanje plus alati za dokumentaciju
- **Debug agent**: Fokusiran na istragu sa omoguÄ‡enim bash i alatima za Äitanje
- **Docs agent**: Pisanje dokumentacije sa operacijama datoteka, ali bez sistemskih naredbi

---

## Primjeri

Evo nekoliko primjera agenata koji bi vam mogli biti korisni.

:::tip
Imate li agenta kojeg biste Å¾eljeli podijeliti? [PoÅ¡alji PR](https://github.com/mysterious75/voracode).
:::

---

### Agent za dokumentaciju

```markdown title="~/.config/voracode/agents/docs-writer.md"
---
description: Writes and maintains project documentation
mode: subagent
tools:
  bash: false
---

You are a technical writer. Create clear, comprehensive documentation.

Focus on:

- Clear explanations
- Proper structure
- Code examples
- User-friendly language
```

---

### Sigurnosni revizor

```markdown title="~/.config/voracode/agents/security-auditor.md"
---
description: Performs security audits and identifies vulnerabilities
mode: subagent
tools:
  write: false
  edit: false
---

You are a security expert. Focus on identifying potential security issues.

Look for:

- Input validation vulnerabilities
- Authentication and authorization flaws
- Data exposure risks
- Dependency vulnerabilities
- Configuration security issues
```
