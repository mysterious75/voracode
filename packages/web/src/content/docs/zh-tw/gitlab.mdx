---
title: GitLab
description: åœ¨ GitLab Issue å’Œåˆä½µè«‹æ±‚ä¸­ä½¿ç”¨ Voracodeã€‚
---

Voracode é€éŽ GitLab CI/CD ç®¡ç·šæˆ– GitLab Duo èˆ‡æ‚¨çš„ GitLab å·¥ä½œæµç¨‹æ•´åˆã€‚

åœ¨é€™å…©ç¨®æƒ…æ³ä¸‹ï¼ŒVoracode éƒ½å°‡åœ¨æ‚¨çš„ GitLab Runner ä¸ŠåŸ·è¡Œã€‚

---

## GitLab CI

Voracode å¯ä»¥åœ¨å¸¸è¦çš„ GitLab ç®¡ç·šä¸­åŸ·è¡Œã€‚æ‚¨å¯ä»¥å°‡å…¶ä½œç‚º [CI å…ƒä»¶](https://docs.gitlab.com/ee/ci/components/) æ•´åˆåˆ°ç®¡ç·šä¸­ã€‚

é€™è£¡æˆ‘å€‘ä½¿ç”¨çš„æ˜¯ç¤¾ç¾¤å»ºç«‹çš„ Voracode CI/CD å…ƒä»¶ â€” [nagyv/gitlab-voracode](https://gitlab.com/nagyv/gitlab-voracode)ã€‚

---

### åŠŸèƒ½ç‰¹æ€§

- **æŒ‰ä»»å‹™è‡ªè¨‚è¨­å®š**ï¼šä½¿ç”¨è‡ªè¨‚è¨­å®šç›®éŒ„ä¾†è¨­å®š Voracodeï¼Œä¾‹å¦‚ `./config/#custom-directory`ï¼Œä»¥ä¾¿ç‚ºæ¯æ¬¡ Voracode å‘¼å«å•Ÿç”¨æˆ–åœç”¨ç‰¹å®šåŠŸèƒ½ã€‚
- **æœ€å°åŒ–è¨­å®š**ï¼šCI å…ƒä»¶æœƒåœ¨èƒŒæ™¯å®Œæˆ Voracode çš„è¨­å®šï¼Œæ‚¨åªéœ€å»ºç«‹ Voracode è¨­å®šå’Œåˆå§‹æç¤ºè©žå³å¯ã€‚
- **éˆæ´»å¯è‡ªè¨‚**ï¼šCI å…ƒä»¶æ”¯æ´å¤šç¨®è¼¸å…¥åƒæ•¸ä¾†è‡ªè¨‚å…¶è¡Œç‚ºã€‚

---

### è¨­å®š

1. å°‡æ‚¨çš„ Voracode èº«åˆ†é©—è­‰ JSON ä½œç‚ºæª”æ¡ˆé¡žåž‹çš„ CI ç’°å¢ƒè®Šæ•¸å„²å­˜åœ¨ **Settings** > **CI/CD** > **Variables** ä¸‹ã€‚è«‹ç¢ºä¿å°‡å…¶æ¨™è¨˜ç‚ºã€ŒMasked and hiddenã€ã€‚
2. å°‡ä»¥ä¸‹å…§å®¹æ–°å¢žåˆ°æ‚¨çš„ `.gitlab-ci.yml` æª”æ¡ˆä¸­ã€‚

   ```yaml title=".gitlab-ci.yml"
   include:
     - component: $CI_SERVER_FQDN/nagyv/gitlab-voracode/voracode@2
       inputs:
         config_dir: ${CI_PROJECT_DIR}/voracode-config
         auth_json: $VORACODE_AUTH_JSON # The variable name for your Voracode authentication JSON
         command: optional-custom-command
         message: "Your prompt here"
   ```

æœ‰é—œæ›´å¤šè¼¸å…¥åƒæ•¸å’Œä½¿ç”¨æƒ…å¢ƒï¼Œè«‹[æŸ¥çœ‹è©²å…ƒä»¶çš„æ–‡ä»¶](https://gitlab.com/explore/catalog/nagyv/gitlab-voracode)ã€‚

---

## GitLab Duo

Voracode èˆ‡æ‚¨çš„ GitLab å·¥ä½œæµç¨‹æ•´åˆã€‚
åœ¨è©•è«–ä¸­æåŠ `@voracode`ï¼ŒVoracode å°‡åœ¨æ‚¨çš„ GitLab CI ç®¡ç·šä¸­åŸ·è¡Œä»»å‹™ã€‚

---

### åŠŸèƒ½ç‰¹æ€§

- **Issue åˆ†é¡ž**ï¼šè®“ Voracode èª¿æŸ¥æŸå€‹ Issue ä¸¦ç‚ºæ‚¨è§£é‡‹ã€‚
- **ä¿®å¾©èˆ‡å¯¦ä½œ**ï¼šè®“ Voracode ä¿®å¾© Issue æˆ–å¯¦ä½œæŸå€‹åŠŸèƒ½ã€‚å®ƒæœƒå»ºç«‹ä¸€å€‹æ–°åˆ†æ”¯ï¼Œä¸¦æäº¤åŒ…å«è®Šæ›´çš„åˆä½µè«‹æ±‚ã€‚
- **å®‰å…¨å¯é **ï¼šVoracode åœ¨æ‚¨çš„ GitLab Runner ä¸ŠåŸ·è¡Œã€‚

---

### è¨­å®š

Voracode åœ¨æ‚¨çš„ GitLab CI/CD ç®¡ç·šä¸­åŸ·è¡Œï¼Œä»¥ä¸‹æ˜¯è¨­å®šæ‰€éœ€çš„æ­¥é©Ÿï¼š

:::tip
è«‹æŸ¥çœ‹ [**GitLab æ–‡ä»¶**](https://docs.gitlab.com/user/duo_agent_platform/agent_assistant/) å–å¾—æœ€æ–°èªªæ˜Žã€‚
:::

1.  è¨­å®šæ‚¨çš„ GitLab ç’°å¢ƒ
2.  è¨­å®š CI/CD
3.  å–å¾— AI æ¨¡åž‹ä¾›æ‡‰å•†çš„ API é‡‘é‘°
4.  å»ºç«‹æœå‹™å¸³æˆ¶
5.  è¨­å®š CI/CD è®Šæ•¸
6.  å»ºç«‹æµç¨‹è¨­å®šæª”ï¼Œä»¥ä¸‹æ˜¯ä¸€å€‹ç¯„ä¾‹ï¼š

        <details>

    <summary>Flow configuration</summary>

    ```yaml
    image: node:22-slim
    commands:
      - echo "Installing voracode"
      - npm install --global voracode-ai
      - echo "Installing glab"
      - export GITLAB_TOKEN=$GITLAB_TOKEN_VORACODE
      - apt-get update --quiet && apt-get install --yes curl wget gpg git && rm --recursive --force /var/lib/apt/lists/*
      - curl --silent --show-error --location "https://raw.githubusercontent.com/upciti/wakemeops/main/assets/install_repository" | bash
      - apt-get install --yes glab
      - echo "Configuring glab"
      - echo $GITLAB_HOST
      - echo "Creating Voracode auth configuration"
      - mkdir --parents ~/.local/share/voracode
      - |
        cat > ~/.local/share/voracode/auth.json << EOF
        {
          "anthropic": {
            "type": "api",
            "key": "$ANTHROPIC_API_KEY"
          }
        }
        EOF
      - echo "Configuring git"
      - git config --global user.email "voracode@gitlab.com"
      - git config --global user.name "Voracode"
      - echo "Testing glab"
      - glab issue list
      - echo "Running Voracode"
      - |
        voracode run "
        You are an AI assistant helping with GitLab operations.

        Context: $AI_FLOW_CONTEXT
        Task: $AI_FLOW_INPUT
        Event: $AI_FLOW_EVENT

        Please execute the requested task using the available GitLab tools.
        Be thorough in your analysis and provide clear explanations.

        <important>
        Please use the glab CLI to access data from GitLab. The glab CLI has already been authenticated. You can run the corresponding commands.

        If you are asked to summarize an MR or issue or asked to provide more information then please post back a note to the MR/Issue so that the user can see it.
        You don't need to commit or push up changes, those will be done automatically based on the file changes you make.
        </important>
        "
      - git checkout --branch $CI_WORKLOAD_REF origin/$CI_WORKLOAD_REF
      - echo "Checking for git changes and pushing if any exist"
      - |
        if ! git diff --quiet || ! git diff --cached --quiet || [ --not --zero "$(git ls-files --others --exclude-standard)" ]; then
          echo "Git changes detected, adding and pushing..."
          git add .
          if git diff --cached --quiet; then
            echo "No staged changes to commit"
          else
            echo "Committing changes to branch: $CI_WORKLOAD_REF"
            git commit --message "Codex changes"
            echo "Pushing changes up to $CI_WORKLOAD_REF"
            git push https://gitlab-ci-token:$GITLAB_TOKEN@$GITLAB_HOST/gl-demo-ultimate-dev-ai-epic-17570/test-java-project.git $CI_WORKLOAD_REF
            echo "Changes successfully pushed"
          fi
        else
          echo "No git changes detected, skipping push"
        fi
    variables:
      - ANTHROPIC_API_KEY
      - GITLAB_TOKEN_VORACODE
      - GITLAB_HOST
    ```

        </details>

è©³ç´°èªªæ˜Žè«‹åƒè€ƒ [GitLab CLI agents æ–‡ä»¶](https://docs.gitlab.com/user/duo_agent_platform/agent_assistant/)ã€‚

---

### ç¯„ä¾‹

ä»¥ä¸‹æ˜¯åœ¨ GitLab ä¸­ä½¿ç”¨ Voracode çš„ä¸€äº›ç¯„ä¾‹ã€‚

:::tip
æ‚¨å¯ä»¥è¨­å®šä½¿ç”¨ä¸åŒæ–¼ `@voracode` çš„è§¸ç™¼è©žã€‚
:::

- **è§£é‡‹ Issue**

  åœ¨ GitLab Issue ä¸­æ–°å¢žä»¥ä¸‹è©•è«–ã€‚

  ```
  @voracode explain this issue
  ```

  Voracode æœƒé–±è®€è©² Issue ä¸¦å›žè¦†æ¸…æ™°çš„è§£é‡‹ã€‚

- **ä¿®å¾© Issue**

  åœ¨ GitLab Issue ä¸­è¼¸å…¥ï¼š

  ```
  @voracode fix this
  ```

  Voracode æœƒå»ºç«‹ä¸€å€‹æ–°åˆ†æ”¯ï¼Œå¯¦ä½œè®Šæ›´ï¼Œä¸¦æäº¤åŒ…å«è®Šæ›´çš„åˆä½µè«‹æ±‚ã€‚

- **å¯©æŸ¥åˆä½µè«‹æ±‚**

  åœ¨ GitLab åˆä½µè«‹æ±‚ä¸­ç•™ä¸‹ä»¥ä¸‹è©•è«–ã€‚

  ```
  @voracode review this merge request
  ```

  Voracode æœƒå¯©æŸ¥åˆä½µè«‹æ±‚ä¸¦æä¾›å›žé¥‹ã€‚
