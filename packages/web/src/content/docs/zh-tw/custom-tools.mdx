---
title: è‡ªè¨‚å·¥å…·
description: å»ºç«‹ LLM å¯åœ¨ Voracode ä¸­å‘¼å«çš„å·¥å…·ã€‚
---

è‡ªè¨‚å·¥å…·æ˜¯æ‚¨å»ºç«‹çš„å‡½å¼ï¼ŒLLM å¯ä»¥åœ¨å°è©±éŽç¨‹ä¸­å‘¼å«å®ƒå€‘ã€‚å®ƒå€‘èˆ‡ Voracode çš„[å…§å»ºå·¥å…·](/docs/tools)ï¼ˆå¦‚ `read`ã€`write` å’Œ `bash`ï¼‰å”åŒå·¥ä½œã€‚

---

## å»ºç«‹å·¥å…·

å·¥å…·ä»¥ **TypeScript** æˆ– **JavaScript** æª”æ¡ˆçš„å½¢å¼å®šç¾©ã€‚ä¸éŽï¼Œå·¥å…·å®šç¾©å¯ä»¥å‘¼å«**ä»»ä½•èªžè¨€**ç·¨å¯«çš„æŒ‡ä»¤ç¢¼â€”â€”TypeScript æˆ– JavaScript åƒ…ç”¨æ–¼å·¥å…·å®šç¾©æœ¬èº«ã€‚

---

### ä½ç½®

å·¥å…·å¯ä»¥åœ¨ä»¥ä¸‹ä½ç½®å®šç¾©ï¼š

- æœ¬åœ°å®šç¾©ï¼šå°‡å·¥å…·æª”æ¡ˆæ”¾åœ¨å°ˆæ¡ˆçš„ `.voracode/tools/` ç›®éŒ„ä¸­ã€‚
- å…¨åŸŸå®šç¾©ï¼šå°‡å·¥å…·æª”æ¡ˆæ”¾åœ¨ `~/.config/voracode/tools/` ä¸­ã€‚

---

### çµæ§‹

å»ºç«‹å·¥å…·æœ€ç°¡å–®çš„æ–¹å¼æ˜¯ä½¿ç”¨ `tool()` è¼”åŠ©å‡½å¼ï¼Œå®ƒæä¾›åž‹åˆ¥å®‰å…¨å’Œåƒæ•¸æ ¡é©—ã€‚

```ts title=".voracode/tools/database.ts" {1}
import { tool } from "@voracode-ai/plugin"

export default tool({
  description: "Query the project database",
  args: {
    query: tool.schema.string().describe("SQL query to execute"),
  },
  async execute(args) {
    // Your database logic here
    return `Executed query: ${args.query}`
  },
})
```

**æª”æ¡ˆåç¨±**å³ç‚º**å·¥å…·åç¨±**ã€‚ä¸Šé¢çš„ç¯„ä¾‹å»ºç«‹äº†ä¸€å€‹åç‚º `database` çš„å·¥å…·ã€‚

---

#### å–®æª”æ¡ˆå¤šå·¥å…·

æ‚¨ä¹Ÿå¯ä»¥å¾žå–®å€‹æª”æ¡ˆä¸­åŒ¯å‡ºå¤šå€‹å·¥å…·ã€‚æ¯å€‹åŒ¯å‡ºéƒ½æœƒæˆç‚º**ä¸€å€‹ç¨ç«‹çš„å·¥å…·**ï¼Œå‘½åæ ¼å¼ç‚º **`<filename>_<exportname>`**ï¼š

```ts title=".voracode/tools/math.ts"
import { tool } from "@voracode-ai/plugin"

export const add = tool({
  description: "Add two numbers",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args) {
    return args.a + args.b
  },
})

export const multiply = tool({
  description: "Multiply two numbers",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args) {
    return args.a * args.b
  },
})
```

é€™æœƒå»ºç«‹å…©å€‹å·¥å…·ï¼š`math_add` å’Œ `math_multiply`ã€‚

---

#### èˆ‡å…§å»ºå·¥å…·åç¨±è¡çª

è‡ªè¨‚å·¥å…·ä»¥å·¥å…·åç¨±ä½œç‚ºéµå€¼ã€‚å¦‚æžœè‡ªè¨‚å·¥å…·ä½¿ç”¨èˆ‡å…§å»ºå·¥å…·ç›¸åŒçš„åç¨±ï¼Œå‰‡è‡ªè¨‚å·¥å…·å…·æœ‰å„ªå…ˆæ¬Šã€‚

ä¾‹å¦‚ï¼Œæ­¤æª”æ¡ˆå°‡æ›¿æ›å…§å»ºçš„ `bash` å·¥å…·ï¼š

```ts title=".voracode/tools/bash.ts"
import { tool } from "@voracode-ai/plugin"

export default tool({
  description: "Restricted bash wrapper",
  args: {
    command: tool.schema.string(),
  },
  async execute(args) {
    return `blocked: ${args.command}`
  },
})
```

:::note
é™¤éžæ‚¨æœ‰æ„æ›¿æ›å…§å»ºå·¥å…·ï¼Œå¦å‰‡è«‹ä½¿ç”¨å”¯ä¸€çš„åç¨±ã€‚å¦‚æžœæ‚¨æƒ³åœç”¨å…§å»ºå·¥å…·ä½†ä¸è¦†å¯«å®ƒï¼Œè«‹ä½¿ç”¨[æ¬Šé™](/docs/permissions)ã€‚
:::

---

### åƒæ•¸

æ‚¨å¯ä»¥ä½¿ç”¨ `tool.schema`ï¼ˆå³ [Zod](https://zod.dev)ï¼‰ä¾†å®šç¾©åƒæ•¸åž‹åˆ¥ã€‚

```ts "tool.schema"
args: {
  query: tool.schema.string().describe("SQL query to execute")
}
```

æ‚¨ä¹Ÿå¯ä»¥ç›´æŽ¥åŒ¯å…¥ [Zod](https://zod.dev) ä¸¦å›žå‚³ä¸€å€‹æ™®é€šç‰©ä»¶ï¼š

```ts {6}
import { z } from "zod"

export default {
  description: "Tool description",
  args: {
    param: z.string().describe("Parameter description"),
  },
  async execute(args, context) {
    // Tool implementation
    return "result"
  },
}
```

---

### ä¸Šä¸‹æ–‡

å·¥å…·æœƒæŽ¥æ”¶ç•¶å‰å·¥ä½œéšŽæ®µçš„ä¸Šä¸‹æ–‡è³‡è¨Šï¼š

```ts title=".voracode/tools/project.ts" {8}
import { tool } from "@voracode-ai/plugin"

export default tool({
  description: "Get project information",
  args: {},
  async execute(args, context) {
    // Access context information
    const { agent, sessionID, messageID, directory, worktree } = context
    return `Agent: ${agent}, Session: ${sessionID}, Message: ${messageID}, Directory: ${directory}, Worktree: ${worktree}`
  },
})
```

ä½¿ç”¨ `context.directory` å–å¾—å·¥ä½œéšŽæ®µçš„å·¥ä½œç›®éŒ„ã€‚
ä½¿ç”¨ `context.worktree` å–å¾— git worktree æ ¹ç›®éŒ„ã€‚

---

## ç¯„ä¾‹

### ç”¨ Python ç·¨å¯«å·¥å…·

æ‚¨å¯ä»¥ä½¿ç”¨ä»»ä½•èªžè¨€ç·¨å¯«å·¥å…·ã€‚ä»¥ä¸‹ç¯„ä¾‹å±•ç¤ºäº†å¦‚ä½•ç”¨ Python å¯¦ä½œå…©æ•¸ç›¸åŠ ã€‚

é¦–å…ˆï¼Œå»ºç«‹ä¸€å€‹ Python æŒ‡ä»¤ç¢¼ä½œç‚ºå·¥å…·ï¼š

```python title=".voracode/tools/add.py"
import sys

a = int(sys.argv[1])
b = int(sys.argv[2])
print(a + b)
```

ç„¶å¾Œå»ºç«‹å‘¼å«è©²æŒ‡ä»¤ç¢¼çš„å·¥å…·å®šç¾©ï¼š

```ts title=".voracode/tools/python-add.ts" {10}
import { tool } from "@voracode-ai/plugin"
import path from "path"

export default tool({
  description: "Add two numbers using Python",
  args: {
    a: tool.schema.number().describe("First number"),
    b: tool.schema.number().describe("Second number"),
  },
  async execute(args, context) {
    const script = path.join(context.worktree, ".voracode/tools/add.py")
    const result = await Bun.$`python3 ${script} ${args.a} ${args.b}`.text()
    return result.trim()
  },
})
```

é€™è£¡æˆ‘å€‘ä½¿ç”¨ [`Bun.$`](https://bun.com/docs/runtime/shell) å·¥å…·å‡½å¼ä¾†åŸ·è¡Œ Python æŒ‡ä»¤ç¢¼ã€‚
