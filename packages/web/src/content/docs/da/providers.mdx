---
title: Udbydere
description: Brug en hvilken som helst LLM-udbyder i Voracode.
---

import config from "../../../../config.mjs"
export const console = config.console

Voracode bruger [AI SDK](https://ai-sdk.dev/) og [Models.dev](https://models.dev) til at understÃ¸tte **75+ LLM-udbydere** og understÃ¸tter kÃ¸rsel af lokale modeller.

For at tilfÃ¸je en udbyder skal du:

1. TilfÃ¸je API-nÃ¸glerne for udbyderen ved at bruge kommandoen `/connect`.
2. Konfigurere udbyderen i Voracode-konfigurationen.

---

### Legitimationsoplysninger

NÃ¥r du tilfÃ¸jer en udbyders API-nÃ¸gler med `/connect`-kommandoen, gemmes de i `~/.local/share/voracode/auth.json`.

---

### Konfiguration

Du kan tilpasse udbyderne gennem `provider`-delen i Voracode-konfigurationen.

---

#### Base URL

Du kan tilpasse base URL for enhver udbyder ved at angive muligheden `baseURL`. Dette er nyttigt, nÃ¥r du bruger proxy-tjenester eller tilpassede endpoints.

```json title="voracode.json" {6}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "anthropic": {
      "options": {
        "baseURL": "https://api.anthropic.com/v1"
      }
    }
  }
}
```

---

## Voracode Zen

Voracode Zen er en liste over modeller leveret af Voracode-teamet, som er blevet testet og verificeret til at fungere godt med Voracode. [LÃ¦s mere](/docs/zen).

:::tip
Hvis du er ny, anbefaler vi at starte med Voracode Zen.
:::

1. KÃ¸r kommandoen `/connect` i TUI, vÃ¦lg voracode og gÃ¥ til [voracode.ai/auth](https://voracode.ai/auth).

   ```txt
   /connect
   ```

2. Log ind, tilfÃ¸j dine faktureringsoplysninger og kopier API-nÃ¸glen.

3. IndsÃ¦t API-nÃ¸glen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r `/models` i TUI for at se listen over modeller, vi anbefaler.

   ```txt
   /models
   ```

Det fungerer som alle andre udbydere i Voracode og er helt valgfrit at bruge.

---

## Voracode Go

Voracode Go er en billig abonnementsplan, der giver pÃ¥lidelig adgang til populÃ¦re Ã¥bne kodningsmodeller leveret af Voracode-teamet, som er testet og verificeret til at fungere godt med Voracode.

1. KÃ¸r kommandoen `/connect` i TUI, vÃ¦lg `Voracode Go`, og gÃ¥ til [voracode.ai/auth](https://voracode.ai/zen).

   ```txt
   /connect
   ```

2. Log ind, tilfÃ¸j dine faktureringsoplysninger og kopier din API-nÃ¸gle.

3. IndsÃ¦t din API-nÃ¸gle.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r `/models` i TUI for at se listen over modeller, vi anbefaler.

   ```txt
   /models
   ```

Det fungerer som alle andre udbydere i Voracode og er helt valgfrit at bruge.

---

## Katalog

Lad os se pÃ¥ nogle af udbyderne i detaljer. Hvis du vil tilfÃ¸je en udbyder til listen, er du velkommen til at Ã¥bne en PR.

:::note
Ser du ikke en udbyder her? Indsend en PR.
:::

---

### 302.AI

1. GÃ¥ til [302.AI-konsollen](https://302.ai/), opret en konto og generer en API-nÃ¸gle.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **302.AI**.

   ```txt
   /connect
   ```

3. Indtast 302.AI API-nÃ¸glen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge en model.

   ```txt
   /models
   ```

---

### Amazon Bedrock

SÃ¥dan bruger du Amazon Bedrock med Voracode:

1. GÃ¥ til **modelkataloget** i Amazon Bedrock-konsollen og anmod om adgang til de modeller, du Ã¸nsker.

   :::tip
   Du skal have adgang til modellen du Ã¸nsker i Amazon Bedrock.
   :::

2. **Konfigurer godkendelse** ved at bruge en af fÃ¸lgende metoder:

   #### MiljÃ¸variabler (hurtigstart)

   Angiv en af disse miljÃ¸variabler, mens du kÃ¸rer voracode:

   ```bash
   # Option 1: Using AWS access keys
   AWS_ACCESS_KEY_ID=XXX AWS_SECRET_ACCESS_KEY=YYY voracode

   # Option 2: Using named AWS profile
   AWS_PROFILE=my-profile voracode

   # Option 3: Using Bedrock bearer token
   AWS_BEARER_TOKEN_BEDROCK=XXX voracode
   ```

   Eller tilfÃ¸j dem til din bash-profil:

   ```bash title="~/.bash_profile"
   export AWS_PROFILE=my-dev-profile
   export AWS_REGION=us-east-1
   ```

   #### Konfigurationsfil (anbefalet)

   For projektspecifik eller vedvarende konfiguration, brug `voracode.json`:

   ```json title="voracode.json"
   {
     "$schema": "https://voracode.ai/config.json",
     "provider": {
       "amazon-bedrock": {
         "options": {
           "region": "us-east-1",
           "profile": "my-aws-profile"
         }
       }
     }
   }
   ```

   **TilgÃ¦ngelige muligheder:**
   - `region` - AWS region (f.eks. `us-east-1`, `eu-west-1`)
   - `profile` - AWS navngivet profil fra `~/.aws/credentials`
   - `endpoint` - Brugerdefineret endpoint URL for VPC endpoints (alias for generisk `baseURL`-mulighed)

   :::tip
   Konfigurationsfil-muligheder har forrang over miljÃ¸variabler.
   :::

   #### Avanceret: VPC Endpoints

   Hvis du bruger VPC endpoints for Bedrock:

   ```json title="voracode.json"
   {
     "$schema": "https://voracode.ai/config.json",
     "provider": {
       "amazon-bedrock": {
         "options": {
           "region": "us-east-1",
           "profile": "production",
           "endpoint": "https://bedrock-runtime.us-east-1.vpce-xxxxx.amazonaws.com"
         }
       }
     }
   }
   ```

   :::note
   Muligheden `endpoint` er et alias for den generiske mulighed `baseURL`, der bruger AWS-specifik terminologi. Hvis bÃ¥de `endpoint` og `baseURL` er specificeret, har `endpoint` forrang.
   :::

   #### Godkendelsesmetoder
   - **`AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY`**: Opret en IAM-bruger og generer adgangsnÃ¸gler i AWS-konsollen
   - **`AWS_PROFILE`**: Brug navngivne profiler fra `~/.aws/credentials`. Konfigurer fÃ¸rst med `aws configure --profile my-profile` eller `aws sso login`
   - **`AWS_BEARER_TOKEN_BEDROCK`**: Generer langsigtede API-nÃ¸gler fra Amazon Bedrock-konsollen
   - **`AWS_WEB_IDENTITY_TOKEN_FILE` / `AWS_ROLE_ARN`**: For EKS IRSA (IAM roller for tjenestekonti) eller andre Kubernetes-miljÃ¸er med OIDC fÃ¸deration. Disse miljÃ¸variabler injiceres automatisk af Kubernetes, nÃ¥r du bruger tjenestekontokommentarer.

   #### Godkendelsesprioritet

   Amazon Bedrock bruger fÃ¸lgende godkendelsesprioritet:
   1. **Bearer Token** - `AWS_BEARER_TOKEN_BEDROCK` miljÃ¸variabel eller token fra kommandoen `/connect`
   2. **AWS legitimationskÃ¦de** - profil, adgangsnÃ¸gler, delte legitimationsoplysninger, IAM roller, webidentitetstokens (EKS IRSA), instansmetadata

   :::note
   NÃ¥r et bearer token er angivet (via `/connect` eller `AWS_BEARER_TOKEN_BEDROCK`), har det forrang over alle AWS godkendelsesmetoder inklusiv konfigurerede profiler.
   :::

3. KÃ¸r kommandoen `/models` for at vÃ¦lge den model, du Ã¸nsker.

   ```txt
   /models
   ```

:::note
For brugerdefinerede inferensprofiler, brug modellen og udbydernavnet i nÃ¸glen og sÃ¦t egenskaben `id` til arn. Dette sikrer korrekt caching:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "amazon-bedrock": {
      // ...
      "models": {
        "anthropic-claude-sonnet-4.5": {
          "id": "arn:aws:bedrock:us-east-1:xxx:application-inference-profile/yyy"
        }
      }
    }
  }
}
```

:::

---

### Anthropic

1. NÃ¥r du har registreret dig, kÃ¸r kommandoen `/connect` og vÃ¦lg Anthropic.

   ```txt
   /connect
   ```

2. Her kan du vÃ¦lge muligheden **Claude Pro/Max**, og det vil Ã¥bne din browser og bede dig om at godkende.

   ```txt
   â”Œ Select auth method
   â”‚
   â”‚ Claude Pro/Max
   â”‚ Create an API Key
   â”‚ Manually enter API Key
   â””
   ```

3. Nu skal alle de Anthropiske modeller vÃ¦re tilgÃ¦ngelige, nÃ¥r du bruger kommandoen `/models`.

   ```txt
   /models
   ```

:::info
At bruge dit Claude Pro/Max-abonnement i Voracode understÃ¸ttes ikke officielt af [Anthropic](https://anthropic.com).
:::

##### Brug API-nÃ¸gler

Du kan ogsÃ¥ vÃ¦lge **Create an API Key**, hvis du ikke har et Pro/Max-abonnement. Det Ã¥bner ogsÃ¥ din browser og beder dig logge pÃ¥ Anthropic og giver dig en kode, du kan indsÃ¦tte i din terminal.

Eller hvis du allerede har en API-nÃ¸gle, kan du vÃ¦lge **Manually enter API Key** og indsÃ¦tte den i terminalen.

---

### Atomic Chat

Du kan konfigurere voracode til at bruge lokale modeller via [Atomic Chat](https://atomic.chat) â€” en desktopapplikation, der kÃ¸rer lokale LLM'er bag en OpenAI-kompatibel API-server (standard-endpoint `http://127.0.0.1:1337/v1`).

```json title="voracode.json" "atomic-chat" {5, 6, 8, 10-14}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "atomic-chat": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Atomic Chat (local)",
      "options": {
        "baseURL": "http://127.0.0.1:1337/v1"
      },
      "models": {
        "<your-model-id>": {
          "name": "<your-model-name>"
        }
      }
    }
  }
}
```

I dette eksempel:

- `atomic-chat` er det brugerdefinerede udbyder-ID. Det kan vÃ¦re en vilkÃ¥rlig streng.
- `npm` specificerer pakken, der skal bruges for denne udbyder. Her bruges `@ai-sdk/openai-compatible` til enhver OpenAI-kompatibel API.
- `name` er det viste navn pÃ¥ udbyderen i grÃ¦nsefladen.
- `options.baseURL` er endpoint'et for den lokale server. Ã†ndr vÃ¦rt og port for at matche din Atomic Chat-opsÃ¦tning.
- `models` er en afbildning af model-ID'er til deres viste navne. Hvert ID skal matche det `id`, der returneres af `GET /v1/models` â€” kÃ¸r `curl http://127.0.0.1:1337/v1/models` for at liste ID'erne, der i Ã¸jeblikket er indlÃ¦st i Atomic Chat.

:::tip
Hvis vÃ¦rktÃ¸jskald ikke fungerer godt, sÃ¥ vÃ¦lg en indlÃ¦st model med god tool calling-understÃ¸ttelse (for eksempel en Qwen-Coder- eller DeepSeek-Coder-variant).
:::

---

### Azure OpenAI

:::note
Hvis du stÃ¸der pÃ¥ "Beklager, men jeg kan ikke hjÃ¦lpe med den anmodning"-fejl, kan du prÃ¸ve at Ã¦ndre indholdsfilteret fra **DefaultV2** til **Default** i Azure-ressourcen.
:::

1. GÃ¥ til [Azure-portalen](https://portal.azure.com/) og opret en **Azure OpenAI**-ressource. Du skal bruge:
   - **Ressourcenavn**: Dette bliver en del af API-endpointet (`https://RESOURCE_NAME.openai.azure.com/`)
   - **API-nÃ¸gle**: Enten `KEY 1` eller `KEY 2` fra din ressource

2. GÃ¥ til [Azure AI Foundry](https://ai.azure.com/) og distribuer en model.

   :::note
   Distributionsnavnet skal matche modelnavnet for at voracode skal fungere korrekt.
   :::

3. KÃ¸r kommandoen `/connect` og sÃ¸g efter **Azure**.

   ```txt
   /connect
   ```

4. Indtast API-nÃ¸glen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

5. Angiv dit ressourcenavn som en miljÃ¸variabel:

   ```bash
   AZURE_RESOURCE_NAME=XXX voracode
   ```

   Eller tilfÃ¸j den til din bash-profil:

   ```bash title="~/.bash_profile"
   export AZURE_RESOURCE_NAME=XXX
   ```

6. KÃ¸r kommandoen `/models` for at vÃ¦lge den distribuerede model.

   ```txt
   /models
   ```

---

### Azure Cognitive Services

1. GÃ¥ til [Azure-portalen](https://portal.azure.com/) og opret en **Azure OpenAI**-ressource. Du skal bruge:
   - **Ressourcenavn**: Dette bliver en del af API-endpointet (`https://AZURE_COGNITIVE_SERVICES_RESOURCE_NAME.cognitiveservices.azure.com/`)
   - **API-nÃ¸gle**: Enten `KEY 1` eller `KEY 2` fra din ressource

2. GÃ¥ til [Azure AI Foundry](https://ai.azure.com/) og distribuer en model.

   :::note
   Distributionsnavnet skal matche modelnavnet for at voracode skal fungere korrekt.
   :::

3. KÃ¸r kommandoen `/connect` og sÃ¸g efter **Azure Cognitive Services**.

   ```txt
   /connect
   ```

4. Indtast API-nÃ¸glen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

5. Angiv dit ressourcenavn som en miljÃ¸variabel:

   ```bash
   AZURE_COGNITIVE_SERVICES_RESOURCE_NAME=XXX voracode
   ```

   Eller tilfÃ¸j den til din bash-profil:

   ```bash title="~/.bash_profile"
   export AZURE_COGNITIVE_SERVICES_RESOURCE_NAME=XXX
   ```

6. KÃ¸r kommandoen `/models` for at vÃ¦lge den distribuerede model.

   ```txt
   /models
   ```

---

### Baseten

1. GÃ¥ til [Baseten](https://app.baseten.co/), opret en konto og generer en API-nÃ¸gle.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **Baseten**.

   ```txt
   /connect
   ```

3. Indtast din Baseten API-nÃ¸gle.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge en model.

   ```txt
   /models
   ```

---

### Cerebras

1. GÃ¥ til [Cerebras-konsollen](https://inference.cerebras.ai/), opret en konto og generer en API-nÃ¸gle.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **Cerebras**.

   ```txt
   /connect
   ```

3. Indtast Cerebras API-nÃ¸glen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge en model som _Qwen 3 Coder 480B_.

   ```txt
   /models
   ```

---

### Cloudflare AI Gateway

Cloudflare AI Gateway lader dig fÃ¥ adgang til modeller fra OpenAI, Anthropic, Workers AI og flere gennem et samlet endpoint. Med [Unified Billing](https://developers.cloudflare.com/ai-gateway/features/unified-billing/) behÃ¸ver du ikke separate API-nÃ¸gler for hver udbyder.

1. GÃ¥ til [Cloudflare-dashboardet](https://dash.cloudflare.com/), naviger til **AI** > **AI Gateway**, og opret en ny gateway.

2. Angiv konto-ID og gateway-ID som miljÃ¸variabler.

   ```bash title="~/.bash_profile"
   export CLOUDFLARE_ACCOUNT_ID=your-32-character-account-id
   export CLOUDFLARE_GATEWAY_ID=your-gateway-id
   ```

3. KÃ¸r kommandoen `/connect` og sÃ¸g efter **Cloudflare AI Gateway**.

   ```txt
   /connect
   ```

4. Indtast din Cloudflare API-token.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

   Eller angiv den som en miljÃ¸variabel.

   ```bash title="~/.bash_profile"
   export CLOUDFLARE_API_TOKEN=your-api-token
   ```

5. KÃ¸r kommandoen `/models` for at vÃ¦lge en model.

   ```txt
   /models
   ```

   Du kan ogsÃ¥ tilfÃ¸je modeller gennem voracode-konfigurationen.

   ```json title="voracode.json"
   {
     "$schema": "https://voracode.ai/config.json",
     "provider": {
       "cloudflare-ai-gateway": {
         "models": {
           "openai/gpt-4o": {},
           "anthropic/claude-sonnet-4": {}
         }
       }
     }
   }
   ```

---

### Cortecs

1. GÃ¥ til [Cortecs-konsollen](https://cortecs.ai/), opret en konto og generer en API-nÃ¸gle.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **Cortecs**.

   ```txt
   /connect
   ```

3. Indtast Cortecs API-nÃ¸glen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge en model som _Kimi K2 Instruct_.

   ```txt
   /models
   ```

---

### DeepSeek

1. GÃ¥ til [DeepSeek-konsollen](https://platform.deepseek.com/), opret en konto og klik pÃ¥ **Create API Key**.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **DeepSeek**.

   ```txt
   /connect
   ```

3. Indtast DeepSeek API-nÃ¸glen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge en DeepSeek-model som _DeepSeek V4 Pro_.

   ```txt
   /models
   ```

---

### Deep Infra

1. GÃ¥ til [Deep Infra-dashboardet](https://deepinfra.com/dash), opret en konto og generer en API-nÃ¸gle.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **Deep Infra**.

   ```txt
   /connect
   ```

3. Indtast Deep Infra API-nÃ¸glen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge en model.

   ```txt
   /models
   ```

---

### FrogBot

1. GÃ¥ til [FrogBot dashboard](https://app.frogbot.ai/signup), opret en konto og generer en API-nÃ¸gle.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **FrogBot**.

   ```txt
   /connect
   ```

3. Indtast frogbot API-nÃ¸glen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge en model.

   ```txt
   /models
   ```

---

### Fireworks AI

1. GÃ¥ til [Fireworks AI-konsollen](https://app.fireworks.ai/), opret en konto og klik pÃ¥ **Create API Key**.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **Fireworks AI**.

   ```txt
   /connect
   ```

3. Indtast Fireworks AI API-nÃ¸glen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge en model som _Kimi K2 Instruct_.

   ```txt
   /models
   ```

---

### GitLab Duo

GitLab Duo giver AI-drevet agentchat med native vÃ¦rktÃ¸jskaldsfunktioner gennem GitLabs anthropiske proxy.

1. KÃ¸r kommandoen `/connect` og vÃ¦lg GitLab.

   ```txt
   /connect
   ```

2. VÃ¦lg din godkendelsesmetode:

   ```txt
   â”Œ Select auth method
   â”‚
   â”‚ OAuth (Recommended)
   â”‚ Personal Access Token
   â””
   ```

   #### Brug OAuth (anbefalet)

   VÃ¦lg **OAuth** og din browser Ã¥bner for autorisation.

   #### Brug personlig adgangstoken
   1. GÃ¥ til [GitLab User Settings > Access Tokens](https://gitlab.com/-/user_settings/personal_access_tokens)
   2. Klik pÃ¥ **Add new token**
   3. Navn: `Voracode`, omfang: `api`
   4. Kopier tokenet (starter med `glpat-`)
   5. Indtast den i terminalen

3. KÃ¸r kommandoen `/models` for at se tilgÃ¦ngelige modeller.

   ```txt
   /models
   ```

   Tre Claude-baserede modeller er tilgÃ¦ngelige:
   - **duo-chat-haiku-4-5** (standard) - Hurtige svar til hurtige opgaver
   - **duo-chat-sonnet-4-5** - Balanceret ydeevne til de fleste arbejdsgange
   - **duo-chat-opus-4-5** - Mest egnet til kompleks analyse

:::note
Du kan ogsÃ¥ specificere 'GITLAB_TOKEN' miljÃ¸variabel, hvis du ikke vil
gemme tokenet i voracode auth-lagring.
:::

##### Selvhostet GitLab

:::note[overholdelsesbemÃ¦rkning]
Voracode bruger en lille model til nogle AI-opgaver som at generere sessionstitlen.
Den er konfigureret til at bruge gpt-5-nano som standard, hostet af Zen. For at lÃ¥se Voracode
til kun at bruge din egen GitLab-hostede instans, tilfÃ¸j fÃ¸lgende i din
`voracode.json` fil. Det anbefales ogsÃ¥ at deaktivere sessionsdeling.

```json
{
  "$schema": "https://voracode.ai/config.json",
  "small_model": "gitlab/duo-chat-haiku-4-5",
  "share": "disabled"
}
```

:::

For selvhostede GitLab-instanser:

```bash
export GITLAB_INSTANCE_URL=https://gitlab.company.com
export GITLAB_TOKEN=glpat-...
```

Hvis din instans kÃ¸rer en brugerdefineret AI-gateway:

```bash
GITLAB_AI_GATEWAY_URL=https://ai-gateway.company.com
```

Eller tilfÃ¸j til din bash-profil:

```bash title="~/.bash_profile"
export GITLAB_INSTANCE_URL=https://gitlab.company.com
export GITLAB_AI_GATEWAY_URL=https://ai-gateway.company.com
export GITLAB_TOKEN=glpat-...
```

:::note
Din GitLab-administrator skal aktivere fÃ¸lgende:

1. [Duo Agent Platform](https://docs.gitlab.com/user/duo_agent_platform/turn_on_off/) for brugeren, gruppen eller instansen
2. Funktionsflag (via Rails-konsollen):
   - `agent_platform_claude_code`
   - `third_party_agents_enabled`
     :::

##### OAuth for selvhostede instanser

For at fÃ¥ Oauth til at fungere for din selvhostede instans, skal du oprette
en ny applikation (Indstillinger â†’ Applikationer) med
callback URL `http://127.0.0.1:8080/callback` og fÃ¸lgende omfang:

- api (FÃ¥ adgang til API pÃ¥ dine vegne)
- read_user (LÃ¦s din personlige information)
- read_repository (tillader skrivebeskyttet adgang til depotet)

UdsÃ¦t derefter applikations-ID som miljÃ¸variabel:

```bash
export GITLAB_OAUTH_CLIENT_ID=your_application_id_here
```

Mere dokumentation pÃ¥ [voracode-gitlab-auth](https://www.npmjs.com/package/voracode-gitlab-auth) hjemmesiden.

##### Konfiguration

Tilpas gennem `voracode.json`:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "gitlab": {
      "options": {
        "instanceUrl": "https://gitlab.com"
      }
    }
  }
}
```

##### GitLab API-vÃ¦rktÃ¸jer (valgfrit, men stÃ¦rkt anbefalet)

For at fÃ¥ adgang til GitLab-vÃ¦rktÃ¸jer (merge requests, problemer, pipelines, CI/CD, etc.):

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "plugin": ["voracode-gitlab-plugin"]
}
```

Denne plugin giver omfattende GitLab-repository-administrationsfunktioner, inklusiv MR-anmeldelser, problemsporing, pipeline-overvÃ¥gning og mere.

---

### GitHub Copilot

SÃ¥dan bruger du GitHub Copilot-abonnementet med Voracode:

:::note
Nogle modeller kan krÃ¦ve et [Pro+
abonnement](https://github.com/features/copilot/plans) at bruge.

Nogle modeller skal aktiveres manuelt i [GitHub Copilot-indstillingerne](https://docs.github.com/en/copilot/how-tos/use-ai-models/configure-access-to-ai-models#setup-for-individual-use).
:::

1. KÃ¸r kommandoen `/connect` og sÃ¸g efter GitHub Copilot.

   ```txt
   /connect
   ```

2. Naviger til [github.com/login/device](https://github.com/login/device) og indtast koden.

   ```txt
   â”Œ Login with GitHub Copilot
   â”‚
   â”‚ https://github.com/login/device
   â”‚
   â”‚ Enter code: 8F43-6FCF
   â”‚
   â”‚ Waiting for authorization...
   ```

3. KÃ¸r nu kommandoen `/models` for at vÃ¦lge modellen du Ã¸nsker.

   ```txt
   /models
   ```

---

### Google Vertex AI

SÃ¥dan bruger du Google Vertex AI med Voracode:

1. GÃ¥ til **Model Garden** i Google Cloud Console og tjek
   modeller tilgÃ¦ngelig i din region.

   :::note
   Du skal have et Google Cloud-projekt med Vertex AI API aktiveret.
   :::

2. Angiv de nÃ¸dvendige miljÃ¸variabler:
   - `GOOGLE_CLOUD_PROJECT`: Dit Google Cloud-projekt ID
   - `VERTEX_LOCATION` (valgfrit): Regionen for Vertex AI (standard til `global`)
   - Godkendelse (vÃ¦lg en):
     - `GOOGLE_APPLICATION_CREDENTIALS`: Sti til tjenestekontoens JSON-nÃ¸glefil
     - Godkend med gcloud CLI: `gcloud auth application-default login`

   SÃ¦t dem mens du kÃ¸rer voracode.

   ```bash
   GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json GOOGLE_CLOUD_PROJECT=your-project-id voracode
   ```

   Eller tilfÃ¸j dem til din bash-profil.

   ```bash title="~/.bash_profile"
   export GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
   export GOOGLE_CLOUD_PROJECT=your-project-id
   export VERTEX_LOCATION=global
   ```

:::tip
`global`-regionen forbedrer tilgÃ¦ngeligheden og reducerer fejl uden ekstra omkostninger. Brug regionale endpoints (f.eks. `us-central1`) for krav til dataophold. [LÃ¦s mere](https://cloud.google.com/vertex-ai/generative-ai/docs/partner-models/use-partner-models#regional_and_global_endpoints)
:::

3. KÃ¸r kommandoen `/models` for at vÃ¦lge modellen du Ã¸nsker.

   ```txt
   /models
   ```

---

### Groq

1. GÃ¥ til [Groq-konsollen](https://console.groq.com/), klik pÃ¥ **Create API Key**, og kopier nÃ¸glen.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter Groq.

   ```txt
   /connect
   ```

3. Indtast API-nÃ¸glen for udbyderen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge den du Ã¸nsker.

   ```txt
   /models
   ```

---

### Hugging Face

[Hugging Face Inference Providers](https://huggingface.co/docs/inference-providers) giver adgang til Ã¥bne modeller som stÃ¸ttes af 17+ udbydere.

1. GÃ¥ til [Hugging Face-indstillinger](https://huggingface.co/settings/tokens/new?ownUserPermissions=inference.serverless.write&tokenType=fineGrained) for at oprette et token med tilladelse til at kalde inferensudbydere.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **Hugging Face**.

   ```txt
   /connect
   ```

3. Indtast dit Hugging Face-token.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge en model som _Kimi-K2-Instruct_ eller _GLM-4.6_.

   ```txt
   /models
   ```

---

### Helicone

[Helicone](https://helicone.ai) er en LLM observerbarhedsplatform som giver logning, overvÃ¥gning og analyser for dine AI-applikationer. Helicone AI Gateway ruter dine anmodninger til den rigtige udbyder automatisk baseret pÃ¥ modellen.

1. GÃ¥ til [Helicone](https://helicone.ai), opret en konto og generer en API-nÃ¸gle fra dashboardet.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **Helicone**.

   ```txt
   /connect
   ```

3. Indtast Helicone API-nÃ¸glen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge en model.

   ```txt
   /models
   ```

For flere udbydere og avancerede funktioner som caching og hastighedsbegrÃ¦nsning, tjek [Helicone-dokumentationen](https://docs.helicone.ai).

#### Valgfrie konfigurationer

I tilfÃ¦lde af at du ser en funktion eller model fra Helicone som ikke konfigureres automatisk gennem voracode, kan du altid konfigurere den selv.

Her er [Helicone's Model Directory](https://helicone.ai/models), du skal bruge denne for at hente ID'erne til de modeller du vil tilfÃ¸je.

```jsonc title="~/.config/voracode/voracode.jsonc"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "helicone": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Helicone",
      "options": {
        "baseURL": "https://ai-gateway.helicone.ai",
      },
      "models": {
        "gpt-4o": {
          // Model ID (from Helicone's model directory page)
          "name": "GPT-4o", // Your own custom name for the model
        },
        "claude-sonnet-4-20250514": {
          "name": "Claude Sonnet 4",
        },
      },
    },
  },
}
```

#### Brugerdefinerede headers

Helicone understÃ¸tter tilpassede headers for funktioner som caching, brugersporing og sessionsadministration. LÃ¦g dem til din udbyderkonfiguration ved at bruge `options.headers`:

```jsonc title="~/.config/voracode/voracode.jsonc"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "helicone": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Helicone",
      "options": {
        "baseURL": "https://ai-gateway.helicone.ai",
        "headers": {
          "Helicone-Cache-Enabled": "true",
          "Helicone-User-Id": "voracode",
        },
      },
    },
  },
}
```

##### Sessionssporing

Helicones [Sessions](https://docs.helicone.ai/features/sessions) funktion lader dig gruppe relaterede LLM anmodninger sammen. Brug [voracode-helicone-session](https://github.com/H2Shami/voracode-helicone-session) plugin for automatisk at logge hver Voracode-samtale som en session i Helicone.

```bash
npm install -g voracode-helicone-session
```

TilfÃ¸j den til din konfiguration.

```json title="voracode.json"
{
  "plugin": ["voracode-helicone-session"]
}
```

Programmatisk injicerer `Helicone-Session-Id` og `Helicone-Session-Name` headers i dine anmodninger. PÃ¥ Helicones Sessions-side vil du se hver Voracode-samtale opfÃ¸rt som en separat session.

##### Almindelige Helicone-headers

| Header                     | Beskrivelse                                                               |
| -------------------------- | ------------------------------------------------------------------------- |
| `Helicone-Cache-Enabled`   | Aktiver respons-caching (`true`/`false`)                                  |
| `Helicone-User-Id`         | Spor beregninger efter bruger                                             |
| `Helicone-Property-[Name]` | TilfÃ¸j egendefinerede egenskaber (f.eks. `Helicone-Property-Environment`) |
| `Helicone-Prompt-Id`       | Knyt anmodninger til prompt-versioner                                     |

Se [Helicone Header Directory](https://docs.helicone.ai/helicone-headers/header-directory) for alle tilgÃ¦ngelige headers.

---

### llama.cpp

Du kan konfigurere voracode til at bruge lokale modeller gennem [llama.cpps](https://github.com/ggml-org/llama.cpp) llama-server-vÃ¦rktÃ¸j

```json title="voracode.json" "llama.cpp" {5, 6, 8, 10-15}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "llama.cpp": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "llama-server (local)",
      "options": {
        "baseURL": "http://127.0.0.1:8080/v1"
      },
      "models": {
        "qwen3-coder:a3b": {
          "name": "Qwen3-Coder: a3b-30b (local)",
          "limit": {
            "context": 128000,
            "output": 65536
          }
        }
      }
    }
  }
}
```

I dette eksempel:

- `llama.cpp` er den tilpassede udbyder ID. Dette kan vÃ¦re hvilken som helst streng du vil.
- `npm` specificerer pakken som skal bruges for denne udbyder. Her bruges `@ai-sdk/openai-compatible` for enhver OpenAI-kompatibel API.
- `name` er visningsnavnet for udbyderen i UI.
- `options.baseURL` er endpointet for den lokale server.
- `models` er et kort over model-ID'er til deres konfigurationer. Modelnavnet vil vises i modelvalglisten.

---

### IO.NET

IO.NET tilbyder 17 modeller optimeret for forskellige brugstilfÃ¦lde:

1. GÃ¥ til [IO.NET-konsollen](https://ai.io.net/), opret en konto og generer en API-nÃ¸gle.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **IO.NET**.

   ```txt
   /connect
   ```

3. Indtast nÃ¸glen IO.NET API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge en model.

   ```txt
   /models
   ```

---

### LM Studio

Du kan konfigurere voracode til at bruge lokale modeller gennem LM Studio.

```json title="voracode.json" "lmstudio" {5, 6, 8, 10-14}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "lmstudio": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "LM Studio (local)",
      "options": {
        "baseURL": "http://127.0.0.1:1234/v1"
      },
      "models": {
        "google/gemma-3n-e4b": {
          "name": "Gemma 3n-e4b (local)"
        }
      }
    }
  }
}
```

I dette eksempel:

- `lmstudio` er den tilpassede udbyder ID. Dette kan vÃ¦re hvilken som helst streng du vil.
- `npm` specificerer pakken som skal bruges for denne udbyder. Her bruges `@ai-sdk/openai-compatible` for enhver OpenAI-kompatibel API.
- `name` er visningsnavnet for udbyderen i UI.
- `options.baseURL` er endpointet for den lokale server.
- `models` er et kort over model-ID'er til deres konfigurationer. Modelnavnet vil vises i modelvalglisten.

---

### Moonshot AI

SÃ¥dan bruger du Kimi K2 fra Moonshot AI:

1. GÃ¥ til [Moonshot AI-konsollen](https://platform.moonshot.ai/console), opret en konto og klik pÃ¥ **Create API Key**.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **Moonshot AI**.

   ```txt
   /connect
   ```

3. Indtast Moonshot API-nÃ¸glen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge _Kimi K2_.

   ```txt
   /models
   ```

---

### MiniMax

1. GÃ¥ til [MiniMax API-konsollen](https://platform.minimax.io/login), opret en konto og generer en API-nÃ¸gle.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **MiniMax**.

   ```txt
   /connect
   ```

3. Indtast MiniMax API-nÃ¸glen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge en model som _M2.1_.

   ```txt
   /models
   ```

---

### Nebius Token Factory

1. GÃ¥ til [Nebius Token Factory-konsollen](https://tokenfactory.nebius.com/), opret en konto og klik pÃ¥ **Add Key**.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **Nebius Token Factory**.

   ```txt
   /connect
   ```

3. Indtast Nebius Token Factory API-nÃ¸glen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge en model som _Kimi K2 Instruct_.

   ```txt
   /models
   ```

---

### Ollama

Du kan konfigurere voracode til at bruge lokale modeller gennem Ollama.

:::tip
Ollama kan automatisk konfigurere sig selv for Voracode. Se [Ollama-integrationsdokumenterne](https://docs.ollama.com/integrations/voracode) for detaljer.
:::

```json title="voracode.json" "ollama" {5, 6, 8, 10-14}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "ollama": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Ollama (local)",
      "options": {
        "baseURL": "http://localhost:11434/v1"
      },
      "models": {
        "llama2": {
          "name": "Llama 2"
        }
      }
    }
  }
}
```

I dette eksempel:

- `ollama` er den tilpassede udbyder ID. Dette kan vÃ¦re hvilken som helst streng du vil.
- `npm` specificerer pakken som skal bruges for denne udbyder. Her bruges `@ai-sdk/openai-compatible` for enhver OpenAI-kompatibel API.
- `name` er visningsnavnet for udbyderen i UI.
- `options.baseURL` er endpointet for den lokale server.
- `models` er et kort over model-ID'er til deres konfigurationer. Modelnavnet vil vises i modelvalglisten.

:::tip
Hvis vÃ¦rktÃ¸jskald ikke fungerer, prÃ¸v at Ã¸ge `num_ctx` i Ollama. Start omkring 16k - 32k.
:::

---

### Ollama Cloud

SÃ¥dan bruger du Ollama Cloud med Voracode:

1. GÃ¥ til [https://ollama.com/](https://ollama.com/) og log pÃ¥ eller opret en konto.

2. Naviger til **Settings** > **Keys** og klik pÃ¥ **Add API Key** for at generere en ny API-nÃ¸gle.

3. Kopier API-nÃ¸glen til brug i Voracode.

4. KÃ¸r kommandoen `/connect` og sÃ¸g efter **Ollama Cloud**.

   ```txt
   /connect
   ```

5. Indtast din Ollama Cloud API-nÃ¸gle.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

6. **Vigtigt**: FÃ¸r du bruger skymodeller i Voracode, skal du hente modelinformationen lokalt:

   ```bash
   ollama pull gpt-oss:20b-cloud
   ```

7. KÃ¸r kommandoen `/models` for at vÃ¦lge din Ollama Cloud-model.

   ```txt
   /models
   ```

---

### OpenAI

Vi anbefaler at du registrerer dig for [ChatGPT Plus eller Pro](https://chatgpt.com/pricing).

1. NÃ¥r du har registreret dig, kÃ¸r kommandoen `/connect` og vÃ¦lg OpenAI.

   ```txt
   /connect
   ```

2. Her kan du vÃ¦lge muligheden **ChatGPT Plus/Pro** og det Ã¥bner din browser
   og beder dig om at godkende.

   ```txt
   â”Œ Select auth method
   â”‚
   â”‚ ChatGPT Plus/Pro
   â”‚ Manually enter API Key
   â””
   ```

3. Nu skal alle OpenAI-modellerne vÃ¦re tilgÃ¦ngelige, nÃ¥r du bruger kommandoen `/models`.

   ```txt
   /models
   ```

##### Brug API-nÃ¸gler

Hvis du allerede har en API-nÃ¸gle, kan du vÃ¦lge **Manually enter API Key** og indsÃ¦tte den i terminalen.

---

### Voracode Zen

Voracode Zen er en liste over testede og verificerede modeller leveret af Voracode-teamet. [LÃ¦s mere](/docs/zen).

1. Log pÃ¥ **<a href={console}>Voracode Zen</a>** og klik pÃ¥ **Create API Key**.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **Voracode Zen**.

   ```txt
   /connect
   ```

3. Indtast Voracode API-nÃ¸glen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge en model som _Qwen 3 Coder 480B_.

   ```txt
   /models
   ```

---

### OpenRouter

1. GÃ¥ til [OpenRouter-dashboardet](https://openrouter.ai/settings/keys), klik pÃ¥ **Create API Key**, og kopier nÃ¸glen.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter OpenRouter.

   ```txt
   /connect
   ```

3. Indtast API-nÃ¸glen for udbyderen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. Mange OpenRouter-modeller er forudindlÃ¦st som standard, kÃ¸r kommandoen `/models` for at vÃ¦lge den du Ã¸nsker.

   ```txt
   /models
   ```

   Du kan ogsÃ¥ tilfÃ¸je flere modeller gennem voracode-konfigurationen.

   ```json title="voracode.json" {6}
   {
     "$schema": "https://voracode.ai/config.json",
     "provider": {
       "openrouter": {
         "models": {
           "somecoolnewmodel": {}
         }
       }
     }
   }
   ```

5. Du kan ogsÃ¥ tilpasse dem gennem voracode-konfigurationen. Her er et eksempel pÃ¥ at specificere en udbyder

   ```json title="voracode.json"
   {
     "$schema": "https://voracode.ai/config.json",
     "provider": {
       "openrouter": {
         "models": {
           "moonshotai/kimi-k2": {
             "options": {
               "provider": {
                 "order": ["baseten"],
                 "allow_fallbacks": false
               }
             }
           }
         }
       }
     }
   }
   ```

---

### SAP AI Core

SAP AI Core giver adgang til 40+ modeller fra OpenAI, Anthropic, Google, Amazon, Meta, Mistral og AI21 gennem en samlet platform.

1. GÃ¥ til din [SAP BTP Cockpit](https://account.hana.ondemand.com/), naviger til din SAP AI kerne-tjenesteinstans, og opret en tjenestenÃ¸gle.

   :::tip
   TjenestenÃ¸glen er et JSON-objekt som indeholder `clientid`, `clientsecret`, `url` og `serviceurls.AI_API_URL`. Du finder din AI-kerneinstans under **Services** > **Instances and Subscriptions** i BTP cockpittet.
   :::

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **SAP AI Core**.

   ```txt
   /connect
   ```

3. Indtast tjenestenÃ¸glen JSON.

   ```txt
   â”Œ Service key
   â”‚
   â”‚
   â”” enter
   ```

   Eller angiv miljÃ¸variablen `AICORE_SERVICE_KEY`:

   ```bash
   AICORE_SERVICE_KEY='{"clientid":"...","clientsecret":"...","url":"...","serviceurls":{"AI_API_URL":"..."}}' voracode
   ```

   Eller tilfÃ¸j den til din bash-profil:

   ```bash title="~/.bash_profile"
   export AICORE_SERVICE_KEY='{"clientid":"...","clientsecret":"...","url":"...","serviceurls":{"AI_API_URL":"..."}}'
   ```

4. Angiv eventuelt deployment-ID og ressourcegruppe:

   ```bash
   AICORE_DEPLOYMENT_ID=your-deployment-id AICORE_RESOURCE_GROUP=your-resource-group voracode
   ```

   :::note
   Disse indstillinger er valgfrie og bÃ¸r konfigureres i henhold til SAP AI kerne-opsÃ¦tningen.
   :::

5. KÃ¸r kommandoen `/models` for at vÃ¦lge fra 40+ tilgÃ¦ngelige modeller.

   ```txt
   /models
   ```

---

### STACKIT

STACKIT AI Model Serving leverer fuldt administreret suverÃ¦nt hostingmiljÃ¸ til AI-modeller, med fokus pÃ¥ LLM'er som Llama, Mistral og Qwen, med maksimal datasuverÃ¦nitet pÃ¥ europÃ¦isk infrastruktur.

1. GÃ¥ til [STACKIT Portal](https://portal.stackit.cloud), naviger til **AI Model Serving**, og opret en auth-token til dit projekt.

   :::tip
   Du skal have en STACKIT-kundekonto, brugerkonto og projekt, fÃ¸r du opretter auth-tokens.
   :::

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **STACKIT**.

   ```txt
   /connect
   ```

3. Indtast din STACKIT AI Model Serving auth-token.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge fra tilgÃ¦ngelige modeller som _Qwen3-VL 235B_ eller _Llama 3.3 70B_.

   ```txt
   /models
   ```

---

### OVHcloud AI Endpoints

1. GÃ¥ til [OVHcloud-panelet](https://ovh.com/manager). Naviger til `Public Cloud`-delen, `AI & Machine Learning` > `AI Endpoints` og i `API Keys`-fanen klikker du pÃ¥ **Opret en ny API-nÃ¸gle**.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **OVHcloud AI Endpoints**.

   ```txt
   /connect
   ```

3. Indtast OVHcloud AI Endpoints API-nÃ¸gle.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge en model som _gpt-oss-120b_.

   ```txt
   /models
   ```

---

### Scaleway

SÃ¥dan bruger du [Scaleway Generative APIs](https://www.scaleway.com/en/docs/generative-apis/) med Voracode:

1. GÃ¥ til [Scaleway Console IAM indstillinger](https://console.scaleway.com/iam/api-keys) for at generere en ny API-nÃ¸gle.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **Scaleway**.

   ```txt
   /connect
   ```

3. Indtast Scaleway API-nÃ¸glen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge en model som _devstral-2-123b-instruct-2512_ eller _gpt-oss-120b_.

   ```txt
   /models
   ```

---

### Together AI

1. GÃ¥ til [Together AI-konsollen](https://api.together.ai), opret en konto og klik pÃ¥ **Add Key**.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **Together AI**.

   ```txt
   /connect
   ```

3. Indtast Together AI API-nÃ¸glen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge en model som _Kimi K2 Instruct_.

   ```txt
   /models
   ```

---

### Venice AI

1. GÃ¥ til [Venice AI-konsollen](https://venice.ai), opret en konto og generer en API-nÃ¸gle.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **Venice AI**.

   ```txt
   /connect
   ```

3. Indtast Venice AI API-nÃ¸glen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge en model som _Llama 3.3 70B_.

   ```txt
   /models
   ```

---

### Vercel AI Gateway

Vercel AI Gateway lader dig fÃ¥ adgang til modeller fra OpenAI, Anthropic, Google, xAI og mere gennem et samlet endpoint. Modeller tilbydes til listepris uden pÃ¥slag.

1. GÃ¥ til [Vercel dashboard](https://vercel.com/), naviger til fanen **AI Gateway**, og klik pÃ¥ **API Keys** for at oprette en ny API-nÃ¸gle.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **Vercel AI Gateway**.

   ```txt
   /connect
   ```

3. Indtast Vercel AI Gateway API-nÃ¸gle.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge en model.

   ```txt
   /models
   ```

Du kan ogsÃ¥ tilpasse modeller gennem voracode-konfigurationen. Her er et eksempel pÃ¥ specificering af udbyder-rutingsrÃ¦kkefÃ¸lge.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "vercel": {
      "models": {
        "anthropic/claude-sonnet-4": {
          "options": {
            "order": ["anthropic", "vertex"]
          }
        }
      }
    }
  }
}
```

Nogle nyttige rutealternativer:

| Alternativ          | Beskrivelse                                              |
| ------------------- | -------------------------------------------------------- |
| `order`             | Providersekvens for at prÃ¸ve                             |
| `only`              | BegrÃ¦ns til specifikke udbydere                          |
| `zeroDataRetention` | Brug kun udbydere med nul retningslinjer for datalagring |

---

### xAI

1. GÃ¥ til [xAI-konsollen](https://console.x.ai/), opret en konto og generer en API-nÃ¸gle.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **xAI**.

   ```txt
   /connect
   ```

3. Indtast xAI API-nÃ¸glen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge en model som _Grok Beta_.

   ```txt
   /models
   ```

---

### Z.AI

1. GÃ¥ til [Z.AI API-konsollen](https://z.ai/manage-apikey/apikey-list), opret en konto og klik pÃ¥ **Create new API Key**.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter **Z.AI**.

   ```txt
   /connect
   ```

   Hvis du abonnerer pÃ¥ **GLM Coding Plan**, vÃ¦lg **Z.AI Coding Plan**.

3. Indtast Z.AI API-nÃ¸glen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KÃ¸r kommandoen `/models` for at vÃ¦lge en model som _GLM-4.7_.

   ```txt
   /models
   ```

---

### ZenMux

1. GÃ¥ til [ZenMux-dashboardet](https://zenmux.ai/settings/keys), klik pÃ¥ **Create API Key**, og kopier nÃ¸glen.

2. KÃ¸r kommandoen `/connect` og sÃ¸g efter ZenMux.

   ```txt
   /connect
   ```

3. Indtast API-nÃ¸glen for udbyderen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. Mange ZenMux-modeller er forudindlÃ¦st som standard, kÃ¸r kommandoen `/models` for at vÃ¦lge den du Ã¸nsker.

   ```txt
   /models
   ```

   Du kan ogsÃ¥ tilfÃ¸je flere modeller gennem voracode-konfigurationen.

   ```json title="voracode.json" {6}
   {
     "$schema": "https://voracode.ai/config.json",
     "provider": {
       "zenmux": {
         "models": {
           "somecoolnewmodel": {}
         }
       }
     }
   }
   ```

---

## Brugerdefineret udbyder

SÃ¥dan tilfÃ¸jer du en **OpenAI-kompatibel** udbyder som ikke er opfÃ¸rt i `/connect`-kommandoen:

:::tip
Du kan bruge hvilken som helst OpenAI-kompatibel udbyder med voracode. De fleste moderne AI-udbydere tilbyder OpenAI-kompatible API'er.
:::

1. KÃ¸r kommandoen `/connect` og rul ned til **Other**.

   ```bash
   $ /connect

   â”Œ  Add credential
   â”‚
   â—†  Select provider
   â”‚  ...
   â”‚  â— Other
   â””
   ```

2. Indtast et unikt ID for udbyderen.

   ```bash
   $ /connect

   â”Œ  Add credential
   â”‚
   â—‡  Enter provider id
   â”‚  myprovider
   â””
   ```

   :::note
   VÃ¦lg et mindevÃ¦rdigt ID, du vil bruge dette i din konfigurationsfil.
   :::

3. Indtast API-nÃ¸glen for udbyderen.

   ```bash
   $ /connect

   â”Œ  Add credential
   â”‚
   â–²  This only stores a credential for myprovider - you will need to configure it in voracode.json, check the docs for examples.
   â”‚
   â—‡  Enter your API key
   â”‚  sk-...
   â””
   ```

4. Opret eller opdater `voracode.json`-filen i projektkataloget:

   ```json title="voracode.json" ""myprovider"" {5-15}
   {
     "$schema": "https://voracode.ai/config.json",
     "provider": {
       "myprovider": {
         "npm": "@ai-sdk/openai-compatible",
         "name": "My AI ProviderDisplay Name",
         "options": {
           "baseURL": "https://api.myprovider.com/v1"
         },
         "models": {
           "my-model-name": {
             "name": "My Model Display Name"
           }
         }
       }
     }
   }
   ```

   Her er konfigurationsmulighederne:
   - **npm**: AI SDK pakke at bruge, `@ai-sdk/openai-compatible` for OpenAI-kompatible udbydere
   - **name**: Visningsnavn i UI.
   - **models**: TilgÃ¦ngelige modeller.
   - **options.baseURL**: API endpoint URL.
   - **options.apiKey**: Angiv API-nÃ¸glen hvis du ikke bruger auth.
   - **options.headers**: Angiv egendefinerede headers.

   Mere om de avancerede muligheder i eksemplet nedenfor.

5. KÃ¸r kommandoen `/models` og din egendefinerede udbyder og modeller vil vises i udvalgslisten.

---

##### Eksempel

Her er et eksempel pÃ¥ indstilling af mulighederne `apiKey`, `headers` og model `limit`.

```json title="voracode.json" {9,11,17-20}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "myprovider": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "My AI ProviderDisplay Name",
      "options": {
        "baseURL": "https://api.myprovider.com/v1",
        "apiKey": "{env:ANTHROPIC_API_KEY}",
        "headers": {
          "Authorization": "Bearer custom-token"
        }
      },
      "models": {
        "my-model-name": {
          "name": "My Model Display Name",
          "limit": {
            "context": 200000,
            "output": 65536
          }
        }
      }
    }
  }
}
```

Konfigurationsdetaljer:

- **apiKey**: Angiv med `env` variabel syntaks, [lÃ¦s mere](/docs/config#env-vars).
- **headers**: Egendefinerede headers sendt med hver anmodning.
- **limit.context**: Maksimalt input-tokens som modellen accepterer.
- **limit.output**: Maksimalt antal tokens modellen kan generere.

`limit`-felterne lader Voracode forstÃ¥ hvor meget kontekst du har tilbage. Standardudbydere henter disse automatisk fra models.dev.

---

## Fejlfinding

Hvis du har problemer med at konfigurere en udbyder, tjek fÃ¸lgende:

1. **Tjek godkendelsesopsÃ¦tningen**: KÃ¸r `voracode auth list` for at se om legitimationsoplysningerne
   for udbyderen er tilfÃ¸jet til din konfiguration.

   Dette gÃ¦lder ikke udbydere som Amazon Bedrock, som er afhÃ¦ngige af miljÃ¸variabler for godkendelse.

2. For tilpassede udbydere, tjek voracode-konfigurationen og:
   - SÃ¸rg for at udbyderens ID som bruges i `/connect`-kommandoen matcher ID i voracode-konfigurationen.
   - Den rigtige npm-pakke bruges for udbyderen. Brug for eksempel `@ai-sdk/cerebras` for Cerebras. Og for alle andre OpenAI-kompatible udbydere, brug `@ai-sdk/openai-compatible`.
   - Kontroller at korrekt API-endpoint er brugt i `options.baseURL`-feltet.
