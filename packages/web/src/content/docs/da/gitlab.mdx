---
title: GitLab
description: Brug Voracode i GitLab-problemer og Merge Requests.
---

Voracode integreres med din GitLab-arbejdsgang gennem din GitLab CI/CD-pipeline eller med GitLab Duo.

I begge tilfÃ¦lde vil Voracode kÃ¸re pÃ¥ dine GitLab-lÃ¸bere.

---

## GitLab CI

Voracode fungerer i en almindelig GitLab-pipeline. Du kan bygge det ind i en pipeline som en [CI component](https://docs.gitlab.com/ee/ci/components/)

Her bruger vi en community-skabt CI/CD-komponent til Voracode â€” [nagyv/gitlab-voracode](https://gitlab.com/nagyv/gitlab-voracode).

---

### Funktioner

- **Brug brugerdefineret konfiguration pr. job**: Konfigurer Voracode med en brugerdefineret konfigurationsmappe, for eksempel `./config/#custom-directory` for at aktivere eller deaktivere funktionalitet pr. Voracode pÃ¥kald.
- **Minimal opsÃ¦tning**: CI-komponenten sÃ¦tter Voracode op i baggrunden, du behÃ¸ver kun at oprette Voracode-konfigurationen og den indledende prompt.
- **Fleksibel**: CI-komponenten understÃ¸tter flere input til at tilpasse dens adfÃ¦rd

---

### OpsÃ¦tning

1. Gem din Voracode-autentificering JSON som en filtype CI-miljÃ¸variabel under **Indstillinger** > **CI/CD** > **Variabler**. SÃ¸rg for at markere dem som "Maskede og skjulte".
2. TilfÃ¸j fÃ¸lgende til din `.gitlab-ci.yml` fil.

   ```yaml title=".gitlab-ci.yml"
   include:
     - component: $CI_SERVER_FQDN/nagyv/gitlab-voracode/voracode@2
       inputs:
         config_dir: ${CI_PROJECT_DIR}/voracode-config
         auth_json: $VORACODE_AUTH_JSON # The variable name for your Voracode authentication JSON
         command: optional-custom-command
         message: "Your prompt here"
   ```

For flere input og use cases [check out the docs](https://gitlab.com/explore/catalog/nagyv/gitlab-voracode) for denne komponent.

---

## GitLab Duo

Voracode integreres med din GitLab-arbejdsgang.
NÃ¦vn `@voracode` i en kommentar, og Voracode vil udfÃ¸re opgaver i din GitLab CI-pipeline.

---

### Funktioner

- **Triageproblemer**: Bed Voracode om at undersÃ¸ge et problem og forklare dig det.
- **Ret og implementer**: Bed Voracode om at lÃ¸se et problem eller implementere en funktion.
  Det vil oprette en ny branch og rejse en Merge Request med Ã¦ndringer.
- **Sikker**: Voracode kÃ¸rer pÃ¥ dine GitLab-lÃ¸bere.

---

### OpsÃ¦tning

Voracode kÃ¸rer i din GitLab CI/CD pipeline, her er hvad du skal bruge for at konfigurere det:

:::tip
Tjek [**GitLab docs**](https://docs.gitlab.com/user/duo_agent_platform/agent_assistant/) for opdaterede instruktioner.
:::

1.  Konfigurer dit GitLab-miljÃ¸
2.  Konfigurer CI/CD
3.  FÃ¥ en API-nÃ¸gle til en AI-modeludbyder
4.  Opret en servicekonto
5.  Konfigurer CI/CD variabler
6.  Opret en flow-konfigurationsfil, her er et eksempel:

        <details>

    <summary>Flowkonfiguration</summary>

    ```yaml
    image: node:22-slim
    commands:
      - echo "Installing voracode"
      - npm install --global voracode-ai
      - echo "Installing glab"
      - export GITLAB_TOKEN=$GITLAB_TOKEN_VORACODE
      - apt-get update --quiet && apt-get install --yes curl wget gpg git && rm --recursive --force /var/lib/apt/lists/*
      - curl --silent --show-error --location "https://raw.githubusercontent.com/upciti/wakemeops/main/assets/install_repository" | bash
      - apt-get install --yes glab
      - echo "Configuring glab"
      - echo $GITLAB_HOST
      - echo "Creating Voracode auth configuration"
      - mkdir --parents ~/.local/share/voracode
      - |
        cat > ~/.local/share/voracode/auth.json << EOF
        {
          "anthropic": {
            "type": "api",
            "key": "$ANTHROPIC_API_KEY"
          }
        }
        EOF
      - echo "Configuring git"
      - git config --global user.email "voracode@gitlab.com"
      - git config --global user.name "Voracode"
      - echo "Testing glab"
      - glab issue list
      - echo "Running Voracode"
      - |
        voracode run "
        You are an AI assistant helping with GitLab operations.

        Context: $AI_FLOW_CONTEXT
        Task: $AI_FLOW_INPUT
        Event: $AI_FLOW_EVENT

        Please execute the requested task using the available GitLab tools.
        Be thorough in your analysis and provide clear explanations.

        <important>
        Please use the glab CLI to access data from GitLab. The glab CLI has already been authenticated. You can run the corresponding commands.

        If you are asked to summarize an MR or issue or asked to provide more information then please post back a note to the MR/Issue so that the user can see it.
        You don't need to commit or push up changes, those will be done automatically based on the file changes you make.
        </important>
        "
      - git checkout --branch $CI_WORKLOAD_REF origin/$CI_WORKLOAD_REF
      - echo "Checking for git changes and pushing if any exist"
      - |
        if ! git diff --quiet || ! git diff --cached --quiet || [ --not --zero "$(git ls-files --others --exclude-standard)" ]; then
          echo "Git changes detected, adding and pushing..."
          git add .
          if git diff --cached --quiet; then
            echo "No staged changes to commit"
          else
            echo "Committing changes to branch: $CI_WORKLOAD_REF"
            git commit --message "Codex changes"
            echo "Pushing changes up to $CI_WORKLOAD_REF"
            git push https://gitlab-ci-token:$GITLAB_TOKEN@$GITLAB_HOST/gl-demo-ultimate-dev-ai-epic-17570/test-java-project.git $CI_WORKLOAD_REF
            echo "Changes successfully pushed"
          fi
        else
          echo "No git changes detected, skipping push"
        fi
    variables:
      - ANTHROPIC_API_KEY
      - GITLAB_TOKEN_VORACODE
      - GITLAB_HOST
    ```

        </details>

Du kan henvise til [GitLab CLI agents docs](https://docs.gitlab.com/user/duo_agent_platform/agent_assistant/) for detaljerede instruktioner.

---

### Eksempler

Her er nogle eksempler pÃ¥, hvordan du kan bruge Voracode i GitLab.

:::tip
Du kan konfigurere til at bruge en anden udlÃ¸sersÃ¦tning end `@voracode`.
:::

- **Forklar et problem**

  TilfÃ¸j denne kommentar i et GitLab-problem.

  ```
  @voracode explain this issue
  ```

  Voracode vil lÃ¦se spÃ¸rgsmÃ¥let og svare med en klar forklaring.

- **LÃ¸s et problem**

  I et GitLab-nummer skal du sige:

  ```
  @voracode fix this
  ```

  Voracode vil oprette en ny branch, implementere Ã¦ndringer og Ã¥bne en Merge Request med Ã¦ndringer.

- **GennemgÃ¥ Merge Requests**

  Efterlad fÃ¸lgende kommentar til en GitLab-Merge Request.

  ```
  @voracode review this merge request
  ```

  Voracode vil gennemgÃ¥ din Merge Request og give feedback.
