---
title: Windows (WSL)
description: KÃ¸r Voracode pÃ¥ Windows med WSL for den bedste oplevelse.
---

import { Steps } from "@astrojs/starlight/components"

Selvom Voracode kan kÃ¸re direkte pÃ¥ Windows, anbefaler vi at bruge [Windows Subsystem for Linux (WSL)](https://learn.microsoft.com/en-us/windows/wsl/install) for den bedste oplevelse. WSL giver et Linux-miljÃ¸, der fungerer problemfrit med Voracodes funktioner.

:::tip[Hvorfor WSL?]
WSL giver bedre filsystemydelse, fuld terminalunderstÃ¸ttelse og kompatibilitet med de udviklingsvÃ¦rktÃ¸jer, som Voracode er afhÃ¦ngig af.
:::

---

## OpsÃ¦tning

<Steps>

1. **Installer WSL**

   Hvis du ikke allerede har gjort det, sÃ¥ [installer WSL](https://learn.microsoft.com/en-us/windows/wsl/install) med Microsofts officielle vejledning.

2. **Installer Voracode i WSL**

   NÃ¥r WSL er sat op, skal du Ã¥bne din WSL-terminal og installere Voracode med en af [installationsmetoderne](/docs/).

   ```bash
   curl -fsSL https://voracode.ai/install | bash
   ```

3. **Brug Voracode fra WSL**

   GÃ¥ til din projektmappe (fÃ¥ adgang til Windows-filer via `/mnt/c/`, `/mnt/d/` osv.) og kÃ¸r Voracode.

   ```bash
   cd /mnt/c/Users/YourName/project
   voracode
   ```

</Steps>

---

## Desktop-app + WSL-server

Hvis du foretrÃ¦kker Voracode Desktop-appen, men vil kÃ¸re serveren i WSL:

1. **Start serveren i WSL** med `--hostname 0.0.0.0` for at tillade eksterne forbindelser:

   ```bash
   voracode serve --hostname 0.0.0.0 --port 4096
   ```

2. **Forbind Desktop-appen** til `http://localhost:4096`

:::note
Hvis `localhost` ikke virker i din opsÃ¦tning, sÃ¥ forbind i stedet med WSL-IP-adressen (fra WSL: `hostname -I`) og brug `http://<wsl-ip>:4096`.
:::

:::caution
NÃ¥r du bruger `--hostname 0.0.0.0`, skal du sÃ¦tte `VORACODE_SERVER_PASSWORD` for at sikre serveren.

```bash
VORACODE_SERVER_PASSWORD=your-password voracode serve --hostname 0.0.0.0
```

:::

---

## Webklient + WSL

For den bedste weboplevelse pÃ¥ Windows:

1. **KÃ¸r `voracode web` i WSL-terminalen** i stedet for PowerShell:

   ```bash
   voracode web --hostname 0.0.0.0
   ```

2. **Ã…bn fra din Windows-browser** pÃ¥ `http://localhost:<port>` (Voracode udskriver URL'en)

NÃ¥r du kÃ¸rer `voracode web` fra WSL, fÃ¥r du korrekt filsystemadgang og terminalintegration, samtidig med at det stadig er tilgÃ¦ngeligt fra din Windows-browser.

---

## Adgang til Windows-filer

WSL kan fÃ¥ adgang til alle dine Windows-filer via `/mnt/`-mappen:

- `C:`-drev â†’ `/mnt/c/`
- `D:`-drev â†’ `/mnt/d/`
- Og sÃ¥ videre...

Eksempel:

```bash
cd /mnt/c/Users/YourName/Documents/project
voracode
```

:::tip
For den mest problemfri oplevelse kan du overveje at klone/kopiere dit repo ind i WSL-filsystemet (for eksempel under `~/code/`) og kÃ¸re Voracode derfra.
:::

---

## Tips

- Lad Voracode kÃ¸re i WSL for projekter, der ligger pÃ¥ Windows-drev - filadgangen er problemfri
- Brug VS Codes [WSL-udvidelse](https://code.visualstudio.com/docs/remote/wsl) sammen med Voracode for et integreret udviklingsworkflow
- Din Voracode-konfiguration og dine sessioner gemmes i WSL-miljÃ¸et i `~/.local/share/voracode/`
