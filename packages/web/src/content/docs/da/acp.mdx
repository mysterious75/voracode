---
title: ACP-understÃ¸ttelse
description: Brug Voracode i enhver ACP-kompatibel editor.
---

Voracode understÃ¸tter [Agent Client Protocol](https://agentclientprotocol.com) eller (ACP), sÃ¥ du kan bruge det direkte i kompatible editorer og IDE'er.

:::tip
For en liste over editorer og vÃ¦rktÃ¸jer, der understÃ¸tter ACP, tjek [ACP fremskridtsrapport](https://zed.dev/blog/acp-progress-report#available-now).
:::

ACP er en Ã¥ben protokol, der standardiserer kommunikation mellem kodeeditorer og AI-kodningsagenter.

---

## Konfiguration

For at bruge Voracode via ACP, konfigurer din editor til at kÃ¸re kommandoen `voracode acp`.

Kommandoen starter Voracode som en ACP-kompatibel underproces, der kommunikerer med din editor over JSON-RPC via stdio.

Nedenfor er eksempler pÃ¥ populÃ¦re editorer, der understÃ¸tter ACP.

---

### Zed

FÃ¸j til din [Zed](https://zed.dev)-konfiguration (`~/.config/zed/settings.json`):

```json title="~/.config/zed/settings.json"
{
  "agent_servers": {
    "Voracode": {
      "command": "voracode",
      "args": ["acp"]
    }
  }
}
```

For at Ã¥bne den skal du bruge handlingen `agent: new thread` i **Kommandopaletten**.

Du kan ogsÃ¥ binde en tastaturgenvej ved at redigere din `keymap.json`:

```json title="keymap.json"
[
  {
    "bindings": {
      "cmd-alt-o": [
        "agent::NewExternalAgentThread",
        {
          "agent": {
            "custom": {
              "name": "Voracode",
              "command": {
                "command": "voracode",
                "args": ["acp"]
              }
            }
          }
        }
      ]
    }
  }
]
```

---

### JetBrains IDE'er

TilfÃ¸j til din [JetBrains IDE](https://www.jetbrains.com/) acp.json i henhold til [dokumentationen](https://www.jetbrains.com/help/ai-assistant/acp.html):

```json title="acp.json"
{
  "agent_servers": {
    "Voracode": {
      "command": "/absolute/path/bin/voracode",
      "args": ["acp"]
    }
  }
}
```

For at Ã¥bne den skal du bruge den nye 'Voracode'-agent i AI Chat-agentvÃ¦lgeren.

---

### Avante.nvim

FÃ¸j til din [Avante.nvim](https://github.com/yetone/avante.nvim)-konfiguration:

```lua
{
  acp_providers = {
    ["voracode"] = {
      command = "voracode",
      args = { "acp" }
    }
  }
}
```

Hvis du har brug for at videregive miljÃ¸variabler:

```lua {6-8}
{
  acp_providers = {
    ["voracode"] = {
      command = "voracode",
      args = { "acp" },
      env = {
        VORACODE_API_KEY = os.getenv("VORACODE_API_KEY")
      }
    }
  }
}
```

---

### CodeCompanion.nvim

For at bruge Voracode som en ACP-agent i [CodeCompanion.nvim](https://github.com/olimorris/codecompanion.nvim), skal du tilfÃ¸je fÃ¸lgende til din Neovim-konfiguration:

```lua
require("codecompanion").setup({
  interactions = {
    chat = {
      adapter = {
        name = "voracode",
        model = "claude-sonnet-4",
      },
    },
  },
})
```

Denne konfiguration sÃ¦tter CodeCompanion til at bruge Voracode som ACP-agent til chat.

Hvis du har brug for at sende miljÃ¸variabler (som `VORACODE_API_KEY`), henvises til [Konfiguration af adaptere: MiljÃ¸variabler](https://codecompanion.olimorris.dev/getting-started#setting-an-api-key) i CodeCompanion.nvim-dokumentationen for alle detaljer.

## UnderstÃ¸ttelse

Voracode fungerer pÃ¥ samme mÃ¥de via ACP som i terminalen. Alle funktioner understÃ¸tter:

:::note
Nogle indbyggede slash-kommandoer som `/undo` og `/redo` er i Ã¸jeblikket ikke understÃ¸ttet.
:::

- Indbyggede vÃ¦rktÃ¸jer (filoperationer, terminalkommandoer osv.)
- Brugerdefinerede vÃ¦rktÃ¸jer og slash-kommandoer
- MCP-servere konfigureret i din Voracode-konfiguration
- Projektspecifikke regler fra `AGENTS.md`
- Brugerdefinerede formatere og linters
- Agenter og tilladelsessystem
