---
title: MCP-servere
description: TilfÃ¸j lokale og eksterne MCP vÃ¦rktÃ¸jer.
---

Du kan tilfÃ¸je eksterne vÃ¦rktÃ¸jer til Voracode ved hjÃ¦lp af _Model Context Protocol_ eller MCP. Voracode understÃ¸tter bÃ¥de lokale og eksterne servere.

NÃ¥r de er tilfÃ¸jet, er MCP automatisk tilgÃ¦ngelig for LLM sammen med indbyggede vÃ¦rktÃ¸jer.

---

#### Forbehold

NÃ¥r du bruger en MCP-server, fÃ¸jes den til konteksten. Dette kan hurtigt stige, hvis du har mange vÃ¦rktÃ¸jer. SÃ¥ vi anbefaler at vÃ¦re forsigtig med hvilke MCP-servere du bruger.

:::tip
MCP-servere tilfÃ¸jer til din kontekst, sÃ¥ du vil vÃ¦re forsigtig med, hvilke du aktiverer.
:::

Visse MCP-servere, som GitHub MCP-servere, har en tendens til at tilfÃ¸je mange tokens og kan nemt overskride kontekstgrÃ¦nsen.

---

## Aktivering

Du kan definere MCP-servere i din [Voracode Config](https://voracode.ai/docs/config/) under `mcp`. TilfÃ¸j hver MCP med et unikt navn. Du kan henvise til denne MCP ved navn, nÃ¥r du spÃ¸rger LLM.

```jsonc title="voracode.jsonc" {6}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "name-of-mcp-server": {
      // ...
      "enabled": true,
    },
    "name-of-other-mcp-server": {
      // ...
    },
  },
}
```

Du kan ogsÃ¥ deaktivere en server ved at indstille `enabled` til `false`. Dette er nyttigt, hvis du midlertidigt vil deaktivere en server uden at fjerne den fra din konfiguration.

---

### TilsidesÃ¦ttelse af eksterne standarder

Organisationer kan levere standard MCP-servere via deres `.well-known/voracode`-slutpunkt. Disse servere kan vÃ¦re deaktiveret som standard, hvilket giver brugerne mulighed for at tilmelde sig dem, de har brug for.

For at aktivere en specifik server fra din organisationers fjernkonfiguration skal du tilfÃ¸je den til din lokale konfiguration med `enabled: true`:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "jira": {
      "type": "remote",
      "url": "https://jira.example.com/mcp",
      "enabled": true
    }
  }
}
```

Dine lokale konfigurationsvÃ¦rdier tilsidesÃ¦tter fjernstandarderne. Se [config precedence](/docs/config#precedence-order) for flere detaljer.

---

## Lokal

TilfÃ¸j lokale MCP-servere ved hjÃ¦lp af `type` til `"local"` i MCP-objektet.

```jsonc title="voracode.jsonc" {15}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-local-mcp-server": {
      "type": "local",
      // Or ["bun", "x", "my-mcp-command"]
      "command": ["npx", "-y", "my-mcp-command"],
      "enabled": true,
      "environment": {
        "MY_ENV_VAR": "my_env_var_value",
      },
    },
  },
}
```

Kommandoen er, hvordan den lokale MCP-server starter. Du kan ogsÃ¥ sende en liste over miljÃ¸variabler ind.

For eksempel kan du tilfÃ¸je test-MCP-serveren [`@modelcontextprotocol/server-everything`](https://www.npmjs.com/package/@modelcontextprotocol/server-everything).

```jsonc title="voracode.jsonc"
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "mcp_everything": {
      "type": "local",
      "command": ["npx", "-y", "@modelcontextprotocol/server-everything"],
    },
  },
}
```

Og for at bruge det kan jeg tilfÃ¸je `use the mcp_everything tool` til mine prompter.

```txt "mcp_everything"
use the mcp_everything tool to add the number 3 and 4
```

---

#### Indstillinger

Her er alle muligheder for at konfigurere en lokal MCP-server.

| Mulighed      | Skriv   | PÃ¥krÃ¦vet | Beskrivelse                                                                          |
| ------------- | ------- | -------- | ------------------------------------------------------------------------------------ |
| `type`        | String  | Y        | Type MCP serverforbindelse, skal vÃ¦re `"local"`.                                     |
| `command`     | Array   | Y        | Kommando og argumenter til at kÃ¸re MCP-serveren.                                     |
| `environment` | Object  |          | MiljÃ¸variabler, der skal indstilles, nÃ¥r serveren kÃ¸rer.                             |
| `enabled`     | Boolean |          | Aktiver eller deaktiver MCP-serveren ved opstart.                                    |
| `timeout`     | Nummer  |          | Timeout i ms for at hente vÃ¦rktÃ¸jer fra MCP-serveren. Standard er 5000 (5 sekunder). |

---

## Ekstern

TilfÃ¸j ekstern MCP-server ved at indstille `type` til `"remote"`.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-remote-mcp": {
      "type": "remote",
      "url": "https://my-mcp-server.com",
      "enabled": true,
      "headers": {
        "Authorization": "Bearer MY_API_KEY"
      }
    }
  }
}
```

`url` er URL for den eksterne MCP server, og med indstillingen `headers` kan du sende en liste over overskrifter ind.

---

#### Indstillinger

| Mulighed  | Skriv   | PÃ¥krÃ¦vet | Beskrivelse                                                                          |
| --------- | ------- | -------- | ------------------------------------------------------------------------------------ |
| `type`    | String  | Y        | Type MCP serverforbindelse, skal vÃ¦re `"remote"`.                                    |
| `url`     | String  | Y        | URL af den eksterne MCP-server.                                                      |
| `enabled` | Boolean |          | Aktiver eller deaktiver MCP-serveren ved opstart.                                    |
| `headers` | Object  |          | Overskrifter til at sende med anmodningen.                                           |
| `oauth`   | Object  |          | OAuth-godkendelseskonfiguration. Se afsnittet [OAuth](#oauth) nedenfor.              |
| `timeout` | Nummer  |          | Timeout i ms for at hente vÃ¦rktÃ¸jer fra MCP-serveren. Standard er 5000 (5 sekunder). |

---

## OAuth

Voracode hÃ¥ndterer automatisk OAuth-godkendelse for eksterne MCP-servere. NÃ¥r en server krÃ¦ver godkendelse, vil Voracode:

1. Registrer 401-svaret, og start OAuth-flowet
2. Brug **Dynamic Client Registration (RFC 7591)**, hvis det understÃ¸tter af serveren
3. Opbevar tokens sikkert til fremtidige anmodninger

---

### Automatisk

For de fleste OAuth-aktiverede MCP-servere krÃ¦ver ingen speciel konfiguration. Bare konfigureret fjernserver:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-oauth-server": {
      "type": "remote",
      "url": "https://mcp.example.com/mcp"
    }
  }
}
```

Hvis serveren krÃ¦ver godkendelse, vil Voracode bede dig om at godkende, nÃ¥r du prÃ¸ver at bruge den fÃ¸rste gang. Hvis ikke, kan du [manually trigger the flow](#authenticating) med `voracode mcp auth <server-name>`.

---

### ForhÃ¥ndsregistreret

Hvis du har klientlegitimationsoplysninger fra MCP-serverudbyderen, kan du konfigurere dem:

```json title="voracode.json" {7-11}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-oauth-server": {
      "type": "remote",
      "url": "https://mcp.example.com/mcp",
      "oauth": {
        "clientId": "{env:MY_MCP_CLIENT_ID}",
        "clientSecret": "{env:MY_MCP_CLIENT_SECRET}",
        "scope": "tools:read tools:execute"
      }
    }
  }
}
```

---

### Godkendelse

Du kan manuelt udlÃ¸se godkendelse eller administrere legitimationsoplysninger.

Godkendt med en specifik MCP-server:

```bash
voracode mcp auth my-oauth-server
```

Liste over alle MCP-servere og deres godkendelsesstatus:

```bash
voracode mcp list
```

Fjern gemte legitimationsoplysninger:

```bash
voracode mcp logout my-oauth-server
```

Kommandoen `mcp auth` Ã¥bner din browser for godkendelse. NÃ¥r du har godkendt, vil Voracode opbevare tokens sikkert i `~/.local/share/voracode/mcp-auth.json`.

---

#### Deaktivering af OAuth

Hvis du vil deaktivere automatisk OAuth for en server (f.eks. for servere, der bruger API-nÃ¸gler i stedet), skal du indstille `oauth` til `false`:

```json title="voracode.json" {7}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-api-key-server": {
      "type": "remote",
      "url": "https://mcp.example.com/mcp",
      "oauth": false,
      "headers": {
        "Authorization": "Bearer {env:MY_API_KEY}"
      }
    }
  }
}
```

---

#### OAuth-indstillinger

| Mulighed       | Skriv           | Beskrivelse                                                                               |
| -------------- | --------------- | ----------------------------------------------------------------------------------------- |
| `oauth`        | Object \| false | OAuth-konfigurationsobjekt eller `false` for at deaktivere OAuth-auto-detektion.          |
| `clientId`     | String          | OAuth-klient-id. Hvis det ikke er angivet, vil dynamisk klientregistrering blive forsÃ¸gt. |
| `clientSecret` | String          | OAuth-klienthemmelighed, hvis det krÃ¦ves af autorisationsserveren.                        |
| `scope`        | String          | OAuth-omfang at anmode om under godkendelse.                                              |

#### Fejlfinding

Hvis en ekstern MCP-server ikke kan godkendes, kan du diagnosticere problemer med:

```bash
# View auth status for all OAuth-capable servers
voracode mcp auth list

# Debug connection and OAuth flow for a specific server
voracode mcp debug my-oauth-server
```

Kommandoen `mcp debug` viser den aktuelle godkendelsesstatus, tester HTTP-forbindelse og forsÃ¸ger OAuth-opdagelsesflowet.

---

## Administration

Dine MCPs er tilgÃ¦ngelige som vÃ¦rktÃ¸jer i **TK\_** sammen med indbyggede vÃ¦rktÃ¸jer. SÃ¥ du kan administrere dem gennem Voracode-konfigurationen som ethvert andet vÃ¦rktÃ¸j.

---

### Global

Det betyder, at du kan aktivere eller deaktivere dem globalt.

```json title="voracode.json" {14}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-mcp-foo": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command-foo"]
    },
    "my-mcp-bar": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command-bar"]
    }
  },
  "tools": {
    "my-mcp-foo": false
  }
}
```

Vi kan ogsÃ¥ bruge et globmÃ¸nster til at deaktivere alle matchende MCPs.

```json title="voracode.json" {14}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-mcp-foo": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command-foo"]
    },
    "my-mcp-bar": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command-bar"]
    }
  },
  "tools": {
    "my-mcp*": false
  }
}
```

Her bruger vi globmÃ¸nsteret `my-mcp*` til at deaktivere alle MCPs.

---

### Per agent

Hvis du har et stort antal MCP-servere, vil du mÃ¥ske kun aktivere dem pr. agent og deaktivere dem globalt. SÃ¥dan gÃ¸r du:

1. Deaktiver det som et vÃ¦rktÃ¸j globalt.
2. Aktiver MCP-serveren som et vÃ¦rktÃ¸j i din [agent config](/docs/agents#tools).

```json title="voracode.json" {11, 14-18}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-mcp": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command"],
      "enabled": true
    }
  },
  "tools": {
    "my-mcp*": false
  },
  "agent": {
    "my-agent": {
      "tools": {
        "my-mcp*": true
      }
    }
  }
}
```

---

#### Glob-mÃ¸nstre

Glob-mÃ¸nsteret bruger simple regex-globing-mÃ¸nstre:

- `*` matcher nul eller flere af ethvert tegn (f.eks. `"my-mcp*"` matcher `my-mcp_search`, `my-mcp_list` osv.)
- `?` matcher nÃ¸jagtigt Ã©t tegn
- Alle andre karakterer matcher bogstaveligt talt

:::note
MCP servervÃ¦rktÃ¸jer er registreret med servernavn som prÃ¦fiks, sÃ¥ for at deaktivere alle vÃ¦rktÃ¸jer til en server skal du blot bruge:

```
"mymcpservername_*": false
```

:::

---

## Eksempler

Nedenfor er eksempler pÃ¥ nogle almindelige MCP-servere. Du kan indsende en PR, hvis du vil dokumentere andre servere.

---

### Sentry

TilfÃ¸j [Sentry MCP server](https://mcp.sentry.dev) for at interagere med dine Sentry-projekter og -problemer.

```json title="voracode.json" {4-8}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "sentry": {
      "type": "remote",
      "url": "https://mcp.sentry.dev/mcp",
      "oauth": {}
    }
  }
}
```

Efter tilfÃ¸jelse af konfigurationen skal du godkende med Sentry:

```bash
voracode mcp auth sentry
```

Dette Ã¥bner et browservindue for at fuldfÃ¸re OAuth-flowet og forbinde Voracode til din Sentry-konto.

NÃ¥r du er godkendt, kan du bruge Sentry-vÃ¦rktÃ¸jer i dine prompter til at forespÃ¸rge om problemer, projekter og fejldata.

```txt "use sentry"
Show me the latest unresolved issues in my project. use sentry
```

---

### Context7

TilfÃ¸j [Context7 MCP server](https://github.com/upstash/context7) for at sÃ¸ge i dokumenter.

```json title="voracode.json" {4-7}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "context7": {
      "type": "remote",
      "url": "https://mcp.context7.com/mcp"
    }
  }
}
```

Hvis du har tilmeldt dig en gratis konto, kan du bruge din API-nÃ¸gle og fÃ¥ hÃ¸jere satsgrÃ¦nser.

```json title="voracode.json" {7-9}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "context7": {
      "type": "remote",
      "url": "https://mcp.context7.com/mcp",
      "headers": {
        "CONTEXT7_API_KEY": "{env:CONTEXT7_API_KEY}"
      }
    }
  }
}
```

Her antager vi, at du har sat miljÃ¸variablen `CONTEXT7_API_KEY`.

FÃ¸j `use context7` til dine prompter for at bruge Context7 MCP server.

```txt "use context7"
Configure a Cloudflare Worker script to cache JSON API responses for five minutes. use context7
```

Alternativt kan du tilfÃ¸je noget som dette til din [AGENTS.md](/docs/rules/).

```md title="AGENTS.md"
When you need to search docs, use `context7` tools.
```

---

### Grep by Vercel

TilfÃ¸j serveren [Grep by Vercel](https://grep.app) MCP for at sÃ¸ge gennem kodestykker pÃ¥ GitHub.

```json title="voracode.json" {4-7}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "gh_grep": {
      "type": "remote",
      "url": "https://mcp.grep.app"
    }
  }
}
```

Da vi navngav vores MCP-server `gh_grep`, kan du tilfÃ¸je `use the gh_grep tool` til dine prompter for at fÃ¥ agenten til at bruge den.

```txt "use the gh_grep tool"
What's the right way to set a custom domain in an SST Astro component? use the gh_grep tool
```

Alternativt kan du tilfÃ¸je noget som dette til din [AGENTS.md](/docs/rules/).

```md title="AGENTS.md"
If you are unsure how to do something, use `gh_grep` to search code examples from GitHub.
```
