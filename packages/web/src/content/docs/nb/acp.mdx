---
title: ACP-stÃ¸tte
description: Bruk voracode i en hvilken som helst ACP-kompatibel editor.
---

Voracode stÃ¸tter [Agent Client Protocol](https://agentclientprotocol.com) eller (ACP), slik at du kan bruke den direkte i kompatible editorer og IDE-er.

:::tip
For en liste over editorer og verktÃ¸y som stÃ¸tter ACP, sjekk ut [ACP fremdriftsrapport](https://zed.dev/blog/acp-progress-report#available-now).
:::

ACP er en Ã¥pen protokoll som standardiserer kommunikasjon mellom kodeditorer og AI-kodeagenter.

---

## Konfigurasjon

For Ã¥ bruke Voracode via ACP, konfigurer redigeringsprogrammet til Ã¥ kjÃ¸re kommandoen `voracode acp`.

Kommandoen starter Voracode som en ACP-kompatibel underprosess som kommuniserer med editoren din over JSON-RPC via stdio.

Nedenfor er eksempler pÃ¥ populÃ¦re editorer som stÃ¸tter ACP.

---

### Zed

Legg til i [Zed](https://zed.dev)-konfigurasjonen (`~/.config/zed/settings.json`):

```json title="~/.config/zed/settings.json"
{
  "agent_servers": {
    "Voracode": {
      "command": "voracode",
      "args": ["acp"]
    }
  }
}
```

For Ã¥ Ã¥pne den, bruk handlingen `agent: new thread` i **Kommandopaletten**.

Du kan ogsÃ¥ binde en hurtigtast ved Ã¥ redigere `keymap.json`:

```json title="keymap.json"
[
  {
    "bindings": {
      "cmd-alt-o": [
        "agent::NewExternalAgentThread",
        {
          "agent": {
            "custom": {
              "name": "Voracode",
              "command": {
                "command": "voracode",
                "args": ["acp"]
              }
            }
          }
        }
      ]
    }
  }
]
```

---

### JetBrains IDE-er

Legg til [JetBrains IDE](https://www.jetbrains.com/) acp.json i henhold til [dokumentasjonen](https://www.jetbrains.com/help/ai-assistant/acp.html):

```json title="acp.json"
{
  "agent_servers": {
    "Voracode": {
      "command": "/absolute/path/bin/voracode",
      "args": ["acp"]
    }
  }
}
```

For Ã¥ Ã¥pne den, bruk den nye 'Voracode'-agenten i AI Chat-agentvelgeren.

---

### Avante.nvim

Legg til i [Avante.nvim](https://github.com/yetone/avante.nvim)-konfigurasjonen:

```lua
{
  acp_providers = {
    ["voracode"] = {
      command = "voracode",
      args = { "acp" }
    }
  }
}
```

Hvis du trenger Ã¥ sende miljÃ¸variabler:

```lua {6-8}
{
  acp_providers = {
    ["voracode"] = {
      command = "voracode",
      args = { "acp" },
      env = {
        VORACODE_API_KEY = os.getenv("VORACODE_API_KEY")
      }
    }
  }
}
```

---

### CodeCompanion.nvim

For Ã¥ bruke Voracode som en ACP-agent i [CodeCompanion.nvim](https://github.com/olimorris/codecompanion.nvim), legg til fÃ¸lgende i Neovim-konfigurasjonen:

```lua
require("codecompanion").setup({
  interactions = {
    chat = {
      adapter = {
        name = "voracode",
        model = "claude-sonnet-4",
      },
    },
  },
})
```

Denne konfigurasjonen setter opp CodeCompanion.nvim til Ã¥ bruke Voracode som ACP-agent for chat.

Hvis du trenger Ã¥ sende miljÃ¸variabler (som `VORACODE_API_KEY`), se [Konfigurere adaptere: miljÃ¸variabler](https://codecompanion.olimorris.dev/getting-started#setting-an-api-key) i CodeCompanion.nvim-dokumentasjonen for fullstendige detaljer.

## StÃ¸tte

Voracode fungerer pÃ¥ samme mÃ¥te via ACP som i terminalen. Alle funksjoner stÃ¸ttes:

:::note
Noen innebygde slash-kommandoer som `/undo` og `/redo` stÃ¸ttes for Ã¸yeblikket ikke.
:::

- Innebygde verktÃ¸y (filoperasjoner, terminalkommandoer, etc.)
- Egendefinerte verktÃ¸y og slash-kommandoer
- MCP servere konfigurert i Voracode-konfigurasjonen
- Prosjektspesifikke regler fra `AGENTS.md`
- Tilpassede formatere og linters
- Agenter og tillatelsessystem
