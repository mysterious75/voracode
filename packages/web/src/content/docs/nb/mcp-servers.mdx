---
title: MCP-servere
description: Legg til lokale og eksterne MCP-verktÃ¸y.
---

Du kan legge til eksterne verktÃ¸y i Voracode ved Ã¥ bruke _Model Context Protocol_ eller MCP. Voracode stÃ¸tter bÃ¥de lokale og eksterne servere.

NÃ¥r de er lagt til, er MCP-verktÃ¸y automatisk tilgjengelige for LLM sammen med innebygde verktÃ¸y.

---

#### Advarsler

NÃ¥r du bruker en MCP-server, legger den til konteksten. Dette kan fort gÃ¥ opp hvis du har mange verktÃ¸y. SÃ¥ vi anbefaler Ã¥ vÃ¦re forsiktig med hvilke MCP-servere du bruker.

:::tip
MCP-servere legger til konteksten din, sÃ¥ du vil vÃ¦re forsiktig med hvilke du aktiverer.
:::

Enkelte MCP-servere, som GitHub MCP-serveren, har en tendens til Ã¥ legge til mange tokens og kan lett overskride kontekstgrensen.

---

## Aktivering

Du kan definere MCP-servere i [Voracode Config](https://voracode.ai/docs/config/) under `mcp`. Legg til hver MCP med et unikt navn. Du kan referere til den MCP ved navn nÃ¥r du spÃ¸r LLM-en.

```jsonc title="voracode.jsonc" {6}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "name-of-mcp-server": {
      // ...
      "enabled": true,
    },
    "name-of-other-mcp-server": {
      // ...
    },
  },
}
```

Du kan ogsÃ¥ deaktivere en server ved Ã¥ sette `enabled` til `false`. Dette er nyttig hvis du vil deaktivere en server midlertidig uten Ã¥ fjerne den fra konfigurasjonen.

---

### Overstyring av fjernstandarder

Organisasjoner kan tilby standard MCP-servere via deres `.well-known/voracode`-endepunkt. Disse serverne kan vÃ¦re deaktivert som standard, slik at brukere kan melde seg pÃ¥ de de trenger.

For Ã¥ aktivere en spesifikk server fra organisasjonens eksterne konfigurasjon, legg den til i din lokale konfigurasjon med `enabled: true`:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "jira": {
      "type": "remote",
      "url": "https://jira.example.com/mcp",
      "enabled": true
    }
  }
}
```

Dine lokale konfigurasjonsverdier overstyrer de eksterne standardinnstillingene. Se [config precedence](/docs/config#precedence-order) for flere detaljer.

---

## Lokal

Legg til lokale MCP-servere ved Ã¥ bruke `type` til `"local"` i MCP-objektet.

```jsonc title="voracode.jsonc" {15}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-local-mcp-server": {
      "type": "local",
      // Or ["bun", "x", "my-mcp-command"]
      "command": ["npx", "-y", "my-mcp-command"],
      "enabled": true,
      "environment": {
        "MY_ENV_VAR": "my_env_var_value",
      },
    },
  },
}
```

Kommandoen er hvordan den lokale MCP-serveren startes. Du kan ogsÃ¥ sende inn en liste over miljÃ¸variabler.

For eksempel, her er hvordan du kan legge til test [`@modelcontextprotocol/server-everything`](https://www.npmjs.com/package/@modelcontextprotocol/server-everything) MCP serveren.

```jsonc title="voracode.jsonc"
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "mcp_everything": {
      "type": "local",
      "command": ["npx", "-y", "@modelcontextprotocol/server-everything"],
    },
  },
}
```

Og for Ã¥ bruke den kan du legge til `use the mcp_everything tool` i ledetekstene mine.

```txt "mcp_everything"
use the mcp_everything tool to add the number 3 and 4
```

---

#### Alternativer

Her er alle alternativene for Ã¥ konfigurere en lokal MCP-server.

| Alternativ    | Type    | PÃ¥krevd | Beskrivelse                                                                              |
| ------------- | ------- | ------- | ---------------------------------------------------------------------------------------- |
| `type`        | String  | Y       | Type MCP servertilkobling, mÃ¥ vÃ¦re `"local"`.                                            |
| `command`     | Array   | Y       | Kommando og argumenter for Ã¥ kjÃ¸re MCP-serveren.                                         |
| `environment` | Object  |         | MiljÃ¸variabler som skal angis nÃ¥r serveren kjÃ¸res.                                       |
| `enabled`     | Boolean |         | Aktiver eller deaktiver MCP-serveren ved oppstart.                                       |
| `timeout`     | Number  |         | Tidsavbrudd i ms for henting av verktÃ¸y fra MCP-serveren. Standard er 5000 (5 sekunder). |

---

## Ekstern

Legg til eksterne MCP-servere ved Ã¥ sette `type` til `"remote"`.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-remote-mcp": {
      "type": "remote",
      "url": "https://my-mcp-server.com",
      "enabled": true,
      "headers": {
        "Authorization": "Bearer MY_API_KEY"
      }
    }
  }
}
```

`url` er URL til den eksterne MCP serveren og med alternativet `headers` kan du sende inn en liste over headere.

---

#### Alternativer

| Alternativ | Type    | PÃ¥krevd | Beskrivelse                                                                              |
| ---------- | ------- | ------- | ---------------------------------------------------------------------------------------- |
| `type`     | String  | Y       | Type MCP servertilkobling, mÃ¥ vÃ¦re `"remote"`.                                           |
| `url`      | String  | Y       | URL av den eksterne MCP-serveren.                                                        |
| `enabled`  | Boolean |         | Aktiver eller deaktiver MCP-serveren ved oppstart.                                       |
| `headers`  | Object  |         | Headere Ã¥ sende med forespÃ¸rselen.                                                       |
| `oauth`    | Object  |         | OAuth-autentiseringskonfigurasjon. Se delen [OAuth](#oauth) nedenfor.                    |
| `timeout`  | Number  |         | Tidsavbrudd i ms for henting av verktÃ¸y fra MCP-serveren. Standard er 5000 (5 sekunder). |

---

## OAuth

Voracode hÃ¥ndterer automatisk OAuth-autentisering for eksterne MCP-servere. NÃ¥r en server krever autentisering, vil Voracode:

1. Oppdag 401-svaret og start OAuth-flyten
2. Bruk **Dynamisk klientregistrering (RFC 7591)** hvis det stÃ¸ttes av serveren
3. Lagre tokens sikkert for fremtidige forespÃ¸rsler

---

### Automatisk

For de fleste OAuth-aktiverte MCP-servere er ingen spesiell konfigurasjon nÃ¸dvendig. Bare konfigurer den eksterne serveren:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-oauth-server": {
      "type": "remote",
      "url": "https://mcp.example.com/mcp"
    }
  }
}
```

Hvis serveren krever autentisering, vil Voracode be deg om Ã¥ autentisere nÃ¥r du prÃ¸ver Ã¥ bruke den for fÃ¸rste gang. Hvis ikke, kan du [utlÃ¸se flyten manuelt](#authenticating) med `voracode mcp auth <server-name>`.

---

### ForhÃ¥ndsregistrert

Hvis du har klientlegitimasjon fra MCP-serverleverandÃ¸ren, kan du konfigurere dem:

```json title="voracode.json" {7-11}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-oauth-server": {
      "type": "remote",
      "url": "https://mcp.example.com/mcp",
      "oauth": {
        "clientId": "{env:MY_MCP_CLIENT_ID}",
        "clientSecret": "{env:MY_MCP_CLIENT_SECRET}",
        "scope": "tools:read tools:execute"
      }
    }
  }
}
```

---

### Autentisering

Du kan utlÃ¸se autentisering manuelt eller administrere legitimasjon.

Autentiser med en spesifikk MCP-server:

```bash
voracode mcp auth my-oauth-server
```

Liste alle MCP-servere og deres autentiseringsstatus:

```bash
voracode mcp list
```

Fjern lagret legitimasjon:

```bash
voracode mcp logout my-oauth-server
```

Kommandoen `mcp auth` Ã¥pner nettleseren din for autorisasjon. Etter at du har godkjent, vil Voracode lagre tokens sikkert i `~/.local/share/voracode/mcp-auth.json`.

---

#### Deaktivering av OAuth

Hvis du vil deaktivere automatisk OAuth for en server (f.eks. for servere som bruker API-nÃ¸kler i stedet), setter du `oauth` til `false`:

```json title="voracode.json" {7}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-api-key-server": {
      "type": "remote",
      "url": "https://mcp.example.com/mcp",
      "oauth": false,
      "headers": {
        "Authorization": "Bearer {env:MY_API_KEY}"
      }
    }
  }
}
```

---

#### OAuth-alternativer

| Alternativ     | Type            | Beskrivelse                                                                            |
| -------------- | --------------- | -------------------------------------------------------------------------------------- |
| `oauth`        | Object \| false | OAuth-konfigurasjonsobjekt, eller `false` for Ã¥ deaktivere automatisk OAuth-deteksjon. |
| `clientId`     | String          | OAuth-klient ID. Hvis ikke oppgitt, vil dynamisk klientregistrering bli forsÃ¸kt.       |
| `clientSecret` | String          | OAuth-klienthemmelighet, hvis det kreves av autorisasjonsserveren.                     |
| `scope`        | String          | OAuth-omfang Ã¥ be om under autorisasjon.                                               |

#### FeilsÃ¸king

Hvis en ekstern MCP-server ikke klarer Ã¥ autentisere, kan du diagnostisere problemer med:

```bash
# View auth status for all OAuth-capable servers
voracode mcp auth list

# Debug connection and OAuth flow for a specific server
voracode mcp debug my-oauth-server
```

Kommandoen `mcp debug` viser gjeldende autentiseringsstatus, tester HTTP-tilkoblingen og forsÃ¸ker OAuth-oppdagelsesflyten.

---

## Administrering

MCP-ene dine er tilgjengelige som verktÃ¸y i Voracode, sammen med innebygde verktÃ¸y. SÃ¥ du kan administrere dem gjennom Voracode-konfigurasjonen som alle andre verktÃ¸y.

---

### Globalt

Dette betyr at du kan aktivere eller deaktivere dem globalt.

```json title="voracode.json" {14}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-mcp-foo": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command-foo"]
    },
    "my-mcp-bar": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command-bar"]
    }
  },
  "tools": {
    "my-mcp-foo": false
  }
}
```

Vi kan ogsÃ¥ bruke et globmÃ¸nster for Ã¥ deaktivere alle matchende MCP-er.

```json title="voracode.json" {14}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-mcp-foo": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command-foo"]
    },
    "my-mcp-bar": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command-bar"]
    }
  },
  "tools": {
    "my-mcp*": false
  }
}
```

Her bruker vi globmÃ¸nsteret `my-mcp*` for Ã¥ deaktivere alle MCP-er.

---

### Per agent

Hvis du har et stort antall MCP-servere, vil du kanskje bare aktivere dem per agent og deaktivere dem globalt. Slik gjÃ¸r du dette:

1. Deaktiver det som et verktÃ¸y globalt.
2. Aktiver MCP-serveren som et verktÃ¸y i [agent config](/docs/agents#tools).

```json title="voracode.json" {11, 14-18}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-mcp": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command"],
      "enabled": true
    }
  },
  "tools": {
    "my-mcp*": false
  },
  "agent": {
    "my-agent": {
      "tools": {
        "my-mcp*": true
      }
    }
  }
}
```

---

#### Glob-mÃ¸nstre

Glob-mÃ¸nsteret bruker enkle regex-globing-mÃ¸nstre:

- `*` samsvarer med null eller flere av alle tegn (f.eks. `"my-mcp*"` samsvarer med `my-mcp_search`, `my-mcp_list`, osv.)
- `?` samsvarer med nÃ¸yaktig ett tegn
- Alle andre karakterer samsvarer bokstavelig talt

:::note
MCP serververktÃ¸y er registrert med servernavn som prefiks, sÃ¥ for Ã¥ deaktivere alle verktÃ¸y for en server bruker du bare:

```
"mymcpservername_*": false
```

:::

---

## Eksempler

Nedenfor er eksempler pÃ¥ noen vanlige MCP-servere. Du kan sende inn en PR hvis du vil dokumentere andre servere.

---

### Sentry

Legg til [Sentry MCP-serveren](https://mcp.sentry.dev) for Ã¥ samhandle med Sentry-prosjektene og problemene dine.

```json title="voracode.json" {4-8}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "sentry": {
      "type": "remote",
      "url": "https://mcp.sentry.dev/mcp",
      "oauth": {}
    }
  }
}
```

Etter Ã¥ ha lagt til konfigurasjonen, autentiser med Sentry:

```bash
voracode mcp auth sentry
```

Dette Ã¥pner et nettleservindu for Ã¥ fullfÃ¸re OAuth-flyten og koble Voracode til Sentry-kontoen din.

NÃ¥r du er autentisert, kan du bruke Sentry-verktÃ¸y i ledetekstene dine for Ã¥ spÃ¸rre etter problemer, prosjekter og feildata.

```txt "use sentry"
Show me the latest unresolved issues in my project. use sentry
```

---

### Context7

Legg til [Context7 MCP-serveren](https://github.com/upstash/context7) for Ã¥ sÃ¸ke gjennom dokumenter.

```json title="voracode.json" {4-7}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "context7": {
      "type": "remote",
      "url": "https://mcp.context7.com/mcp"
    }
  }
}
```

Hvis du har registrert deg for en gratis konto, kan du bruke API-nÃ¸kkelen din og fÃ¥ hÃ¸yere satsgrenser.

```json title="voracode.json" {7-9}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "context7": {
      "type": "remote",
      "url": "https://mcp.context7.com/mcp",
      "headers": {
        "CONTEXT7_API_KEY": "{env:CONTEXT7_API_KEY}"
      }
    }
  }
}
```

Her antar vi at du har `CONTEXT7_API_KEY` miljÃ¸variabelen satt.

Legg til `use context7` i instruksjonene for Ã¥ bruke Context7 MCP-serveren.

```txt "use context7"
Configure a Cloudflare Worker script to cache JSON API responses for five minutes. use context7
```

Alternativt kan du legge til noe slikt til din [AGENTS.md](/docs/rules/).

```md title="AGENTS.md"
When you need to search docs, use `context7` tools.
```

---

### Grep by Vercel

Legg til [Grep by Vercel](https://grep.app) MCP-serveren for Ã¥ sÃ¸ke gjennom kodebiter pÃ¥ GitHub.

```json title="voracode.json" {4-7}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "gh_grep": {
      "type": "remote",
      "url": "https://mcp.grep.app"
    }
  }
}
```

Siden vi kalte MCP-serveren vÃ¥r `gh_grep`, kan du legge til `use the gh_grep tool` i spÃ¸rsmÃ¥lene dine for Ã¥ fÃ¥ agenten til Ã¥ bruke den.

```txt "use the gh_grep tool"
What's the right way to set a custom domain in an SST Astro component? use the gh_grep tool
```

Alternativt kan du legge til noe slikt til din [AGENTS.md](/docs/rules/).

```md title="AGENTS.md"
If you are unsure how to do something, use `gh_grep` to search code examples from GitHub.
```

---

### Helicone

[Helicone](https://helicone.ai) er en LLM observerbarhetsplattform som gir logging, overvÃ¥king og analyser for AI-applikasjonene dine. Helicone AI Gateway ruter forespÃ¸rslene dine til riktig leverandÃ¸r automatisk basert pÃ¥ modellen.

1. GÃ¥ over til [Helicone](https://helicone.ai), opprett en konto og generer en API nÃ¸kkel fra dashbordet.

2. KjÃ¸r kommandoen `/connect` og sÃ¸k etter **Helicone**.

   ```txt
   /connect
   ```

3. Skriv inn Helicone API-nÃ¸kkelen.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. KjÃ¸r kommandoen `/models` for Ã¥ velge en modell.

   ```txt
   /models
   ```

For flere leverandÃ¸rer og avanserte funksjoner som bufring og hastighetsbegrensning, sjekk [Helicone-dokumentasjonen](https://docs.helicone.ai).

#### Valgfrie konfigurasjoner

I tilfelle du ser en funksjon eller modell fra Helicone som ikke konfigureres automatisk gjennom Voracode, kan du alltid konfigurere den selv.

Her er [Helicone's Model Directory](https://helicone.ai/models), du trenger denne for Ã¥ hente ID-ene til modellene du vil legge til.

```jsonc title="~/.config/voracode/voracode.jsonc"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "helicone": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Helicone",
      "options": {
        "baseURL": "https://ai-gateway.helicone.ai",
      },
      "models": {
        "gpt-4o": {
          // Model ID (from Helicone's model directory page)
          "name": "GPT-4o", // Your own custom name for the model
        },
        "claude-sonnet-4-20250514": {
          "name": "Claude Sonnet 4",
        },
      },
    },
  },
}
```

#### Egendefinerte headere

Helicone stÃ¸tter tilpassede headere for funksjoner som bufring, brukersporing og Ã¸ktadministrasjon. Legg dem til leverandÃ¸rkonfigurasjonen din ved Ã¥ bruke `options.headers`:

```jsonc title="~/.config/voracode/voracode.jsonc"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "helicone": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Helicone",
      "options": {
        "baseURL": "https://ai-gateway.helicone.ai",
        "headers": {
          "Helicone-Cache-Enabled": "true",
          "Helicone-User-Id": "voracode",
        },
      },
    },
  },
}
```

##### Sesjonssporing

Helicones [Sessions](https://docs.helicone.ai/features/sessions) funksjon lar deg gruppere relaterte LLM forespÃ¸rsler sammen. Bruk [voracode-helicone-session](https://github.com/H2Shami/voracode-helicone-session) plugin for automatisk Ã¥ logge hver Voracode-samtale som en Ã¸kt i Helicone.

```bash
npm install -g voracode-helicone-session
```

Legg den til i konfigurasjonen din.

```json title="voracode.json"
{
  "plugin": ["voracode-helicone-session"]
}
```

Programtillegget injiserer `Helicone-Session-Id` og `Helicone-Session-Name` overskrifter i forespÃ¸rslene dine. PÃ¥ Helicones Sessions-side vil du se hver Voracode-samtale oppfÃ¸rt som en separat Ã¸kt.

##### Vanlige Helicone-headere

| Overskrift                 | Beskrivelse                                                                |
| -------------------------- | -------------------------------------------------------------------------- |
| `Helicone-Cache-Enabled`   | Aktiver responsbufring (`true`/`false`)                                    |
| `Helicone-User-Id`         | Spor beregninger etter bruker                                              |
| `Helicone-Property-[Name]` | Legg til egendefinerte egenskaper (f.eks. `Helicone-Property-Environment`) |
| `Helicone-Prompt-Id`       | Knytt forespÃ¸rsler til spÃ¸rsmÃ¥lsversjoner                                  |

Se [Helicone Header Directory](https://docs.helicone.ai/helicone-headers/header-directory) for alle tilgjengelige overskrifter.
