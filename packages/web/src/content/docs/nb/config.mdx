---
title: Konfigurasjon
description: Ved Ã¥ bruke voracode JSON-konfigurasjonen.
---

Du kan konfigurere Voracode ved Ã¥ bruke en JSON konfigurasjonsfil.

---

## Format

Voracode stÃ¸tter bÃ¥de **JSON** og **JSONC** (JSON med kommentarer) formater.

```jsonc title="voracode.jsonc"
{
  "$schema": "https://voracode.ai/config.json",
  "model": "anthropic/claude-sonnet-4-5",
  "autoupdate": true,
  "server": {
    "port": 4096,
  },
}
```

---

## Plasseringer

Du kan plassere konfigurasjonen pÃ¥ et par forskjellige steder, og de har en
annen rekkefÃ¸lge.

:::note
Konfigurasjonsfiler er **slÃ¥tt sammen**, ikke erstattet.
:::

Konfigurasjonsfiler slÃ¥s sammen, ikke erstattes. Innstillinger fra fÃ¸lgende konfigurasjonsplasseringer kombineres. Senere konfigurasjoner overstyrer tidligere kun for motstridende nÃ¸kler. Ikke-motstridende innstillinger fra alle konfigurasjoner er bevart.

For eksempel, hvis din globale konfigurasjon setter `theme: "voracode"` og `autoupdate: true`, og prosjektkonfigurasjonen setter `model: "anthropic/claude-sonnet-4-5"`, vil den endelige konfigurasjonen inkludere alle tre innstillingene.

---

### PrioritetsrekkefÃ¸lge

Konfigurasjonskilder lastes inn i denne rekkefÃ¸lgen (senere kilder overstyrer tidligere):

1. **Ekstern konfigurasjon** (fra `.well-known/voracode`) - organisasjonsstandarder
2. **Global konfigurasjon** (`~/.config/voracode/voracode.json`) - brukerinnstillinger
3. **Egendefinert konfigurasjon** (`VORACODE_CONFIG` env var) - egendefinerte overstyringer
4. **Prosjektkonfigurasjon** (`voracode.json` i prosjekt) - prosjektspesifikke innstillinger
5. **`.voracode` kataloger** - agenter, kommandoer, plugins
6. **Inline config** (`VORACODE_CONFIG_CONTENT` env var) - kjÃ¸retidsoverstyringer

Dette betyr at prosjektkonfigurasjoner kan overstyre globale standardinnstillinger, og globale konfigurasjoner kan overstyre eksterne organisasjonsstandarder.

:::note
`.voracode` og `~/.config/voracode` katalogene bruker **flertallsnavn** pÃ¥ underkataloger: `agents/`, `commands/`, `modes/`, `plugins/`, `skills/`, `tools/` og `themes/`. Enkeltnavn (f.eks. `agent/`) stÃ¸ttes ogsÃ¥ for bakoverkompatibilitet.
:::

---

### Ekstern konfigurasjon

Organisasjoner kan tilby standardkonfigurasjon via `.well-known/voracode`-endepunktet. Dette hentes automatisk nÃ¥r du autentiserer med en leverandÃ¸r som stÃ¸tter det.

Ekstern konfigurasjon lastes fÃ¸rst, og fungerer som grunnlaget. Alle andre konfigurasjonskilder (global, prosjekt) kan overstyre disse standardinnstillingene.

For eksempel, hvis organisasjonen din tilbyr MCP servere som er deaktivert som standard:

```json title="Remote config from .well-known/voracode"
{
  "mcp": {
    "jira": {
      "type": "remote",
      "url": "https://jira.example.com/mcp",
      "enabled": false
    }
  }
}
```

Du kan aktivere spesifikke servere i din lokale konfigurasjon:

```json title="voracode.json"
{
  "mcp": {
    "jira": {
      "type": "remote",
      "url": "https://jira.example.com/mcp",
      "enabled": true
    }
  }
}
```

---

### Global konfigurasjon

Plasser din globale Voracode-konfigurasjon i `~/.config/voracode/voracode.json`. Bruk global konfigurasjon for brukerspesifikke preferanser som temaer, leverandÃ¸rer eller nÃ¸kkelbindinger.

Global konfigurasjon overstyrer eksterne organisasjonsstandarder.

---

### Per prosjekt

Legg til `voracode.json` i prosjektroten din. Prosjektkonfigurasjon har den hÃ¸yeste prioritet blant standard config-filer - den overstyrer bÃ¥de globale og eksterne konfigurasjoner.

:::tip
Plasser prosjektspesifikk konfigurasjon i roten til prosjektet ditt.
:::

NÃ¥r Voracode starter opp, ser den etter en konfigurasjonsfil i gjeldende katalog eller gÃ¥r opp til nÃ¦rmeste Git-katalog.

Det er ogsÃ¥ trygt Ã¥ sjekke inn dette i Git og bruker samme skjema som det globale.

---

### Egendefinert bane

Spesifiser en tilpasset konfigurasjonsfilbane ved Ã¥ bruke miljÃ¸variabelen `VORACODE_CONFIG`.

```bash
export VORACODE_CONFIG=/path/to/my/custom-config.json
voracode run "Hello world"
```

Egendefinert konfigurasjon lastes inn mellom globale og prosjektkonfigurasjoner i prioritetsrekkefÃ¸lge.

---

### Egendefinert katalog

Spesifiser en tilpasset konfigurasjonskatalog ved Ã¥ bruke `VORACODE_CONFIG_DIR`
miljÃ¸variabel. Denne katalogen vil bli sÃ¸kt etter agenter, kommandoer,
moduser og plugins akkurat som standard `.voracode` katalog, og bÃ¸r
fÃ¸lge samme struktur.

```bash
export VORACODE_CONFIG_DIR=/path/to/my/config-directory
voracode run "Hello world"
```

Den egendefinerte katalogen lastes inn etter den globale konfigurasjonen og `.voracode`-katalogene, slik at den **kan overstyre** innstillingene deres.

---

## Skjema

Konfigurasjonsfilen har et skjema som er definert i [**`voracode.ai/config.json`**](https://voracode.ai/config.json).

RedaktÃ¸ren din skal kunne validere og autofullfÃ¸re basert pÃ¥ skjemaet.

---

### TUI

Du kan konfigurere TUI-spesifikke innstillinger gjennom alternativet `tui`.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "tui": {
    "scroll_speed": 3,
    "scroll_acceleration": {
      "enabled": true
    },
    "diff_style": "auto"
  }
}
```

Tilgjengelige alternativer:

- `scroll_acceleration.enabled` - Aktiver rulleakselerasjon i macOS-stil. **Tar forrang over `scroll_speed`.**
- `scroll_speed` - Egendefinert rullehastighetsmultiplikator (standard: `3`, minimum: `1`). Ignorert hvis `scroll_acceleration.enabled` er `true`.
- `diff_style` - Kontroller diff-gjengivelse. `"auto"` tilpasser seg terminalbredden, `"stacked"` viser alltid en kolonne.

[Les mer om bruk av TUI her](/docs/tui).

---

### Tjener

Du kan konfigurere serverinnstillinger for kommandoene `voracode serve` og `voracode web` gjennom alternativet `server`.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "server": {
    "port": 4096,
    "hostname": "0.0.0.0",
    "mdns": true,
    "mdnsDomain": "myproject.local",
    "cors": ["http://localhost:5173"]
  }
}
```

Tilgjengelige alternativer:

- `port` - Port Ã¥ lytte pÃ¥.
- `hostname` - Vertsnavn Ã¥ lytte pÃ¥. NÃ¥r `mdns` er aktivert og ingen vertsnavn er angitt, er standard `0.0.0.0`.
- `mdns` - Aktiver mDNS-tjenesteoppdagelse. Dette lar andre enheter pÃ¥ nettverket oppdage Voracode-serveren din.
- `mdnsDomain` - Egendefinert domenenavn for mDNS-tjeneste. Standard er `voracode.local`. Nyttig for Ã¥ kjÃ¸re flere forekomster pÃ¥ samme nettverk.
- `cors` - Ytterligere opprinnelser for Ã¥ tillate CORS nÃ¥r du bruker HTTP-serveren fra en nettleserbasert klient. Verdiene mÃ¥ vÃ¦re fullstendige opprinnelser (skjema + vert + valgfri port), f.eks. `https://app.example.com`.

[Les mer om serveren her](/docs/server).

---

### VerktÃ¸y

Du kan administrere verktÃ¸yene en LLM kan bruke gjennom alternativet `tools`.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "tools": {
    "write": false,
    "bash": false
  }
}
```

[Les mer om verktÃ¸y her](/docs/tools).

---

### Modeller

Du kan konfigurere leverandÃ¸rene og modellene du vil bruke i Voracode-konfigurasjonen gjennom alternativene `provider`, `model` og `small_model`.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {},
  "model": "anthropic/claude-sonnet-4-5",
  "small_model": "anthropic/claude-haiku-4-5"
}
```

Alternativet `small_model` konfigurerer en egen modell for lette oppgaver som tittelgenerering. Som standard prÃ¸ver Voracode Ã¥ bruke en rimeligere modell hvis en er tilgjengelig fra leverandÃ¸ren din, ellers faller den tilbake til hovedmodellen din.

LeverandÃ¸ralternativer kan inkludere `timeout` og `setCacheKey`:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "anthropic": {
      "options": {
        "timeout": 600000,
        "setCacheKey": true
      }
    }
  }
}
```

- `timeout` - Tidsavbrudd for forespÃ¸rsler i millisekunder (standard: 300 000). Sett til `false` for Ã¥ deaktivere.
- `setCacheKey` - SÃ¸rg for at en hurtigbuffernÃ¸kkel alltid settes for valgt leverandÃ¸r.

Du kan ogsÃ¥ konfigurere [lokale modeller](/docs/models#local). [Finn ut mer](/docs/models).

---

#### LeverandÃ¸rspesifikke alternativer

Noen leverandÃ¸rer stÃ¸tter flere konfigurasjonsalternativer utover de generiske innstillingene `timeout` og `apiKey`.

##### Amazon Bedrock

Amazon Bedrock stÃ¸tter AWS-spesifikk konfigurasjon:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "amazon-bedrock": {
      "options": {
        "region": "us-east-1",
        "profile": "my-aws-profile",
        "endpoint": "https://bedrock-runtime.us-east-1.vpce-xxxxx.amazonaws.com"
      }
    }
  }
}
```

- `region` - AWS-region for Bedrock (standard er `AWS_REGION` env var eller `us-east-1`)
- `profile` - AWS navngitt profil fra `~/.aws/credentials` (standard til `AWS_PROFILE` env var)
- `endpoint` - Egendefinert endepunkt URL for VPC endepunkter. Dette er et alias for det generiske `baseURL`-alternativet som bruker AWS-spesifikk terminologi. Hvis begge er spesifisert, har `endpoint` forrang.

:::note
BÃ¦rer-tokens (`AWS_BEARER_TOKEN_BEDROCK` eller `/connect`) har forrang over profilbasert autentisering. Se [autentiseringsprioritet](/docs/providers#authentication-precedence) for detaljer.
:::

[Les mer om konfigurasjon av Amazon Bedrock](/docs/providers#amazon-bedrock).

---

### Temaer

Angi UI-temaet ditt i `tui.json`.

```json title="tui.json"
{
  "$schema": "https://voracode.ai/tui.json",
  "theme": "tokyonight"
}
```

[Les mer her](/docs/themes).

---

### Agenter

Du kan konfigurere spesialiserte agenter for spesifikke oppgaver gjennom alternativet `agent`.

```jsonc title="voracode.jsonc"
{
  "$schema": "https://voracode.ai/config.json",
  "agent": {
    "code-reviewer": {
      "description": "Reviews code for best practices and potential issues",
      "model": "anthropic/claude-sonnet-4-5",
      "prompt": "You are a code reviewer. Focus on security, performance, and maintainability.",
      "tools": {
        // Disable file modification tools for review-only agent
        "write": false,
        "edit": false,
      },
    },
  },
}
```

Du kan ogsÃ¥ definere agenter ved Ã¥ bruke markdown-filer i `~/.config/voracode/agents/` eller `.voracode/agents/`. [Les mer her](/docs/agents).

---

### Standardagent

Du kan angi standard agent ved Ã¥ bruke alternativet `default_agent`. Dette bestemmer hvilken agent som brukes nÃ¥r ingen er eksplisitt spesifisert.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "default_agent": "plan"
}
```

Standardagenten mÃ¥ vÃ¦re en primÃ¦ragent (ikke en underagent). Dette kan vÃ¦re en innebygd agent som `"build"` eller `"plan"`, eller en [egendefinert agent](/docs/agents) du har definert. Hvis den angitte agenten ikke eksisterer eller er en subagent, vil Voracode falle tilbake til `"build"` med en advarsel.

Denne innstillingen gjelder for alle grensesnitt: TUI, CLI (`voracode run`), skrivebordsapp og GitHub Action.

---

### Deling

Du kan konfigurere [share](/docs/share)-funksjonen gjennom alternativet `share`.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "share": "manual"
}
```

Dette krever:

- `"manual"` - Tillat manuell deling via kommandoer (standard)
- `"auto"` - Del nye samtaler automatisk
- `"disabled"` - Deaktiver deling helt

Som standard er deling satt til manuell modus der du eksplisitt mÃ¥ dele samtaler ved Ã¥ bruke kommandoen `/share`.

---

### Kommandoer

Du kan konfigurere egendefinerte kommandoer for repeterende oppgaver gjennom alternativet `command`.

```jsonc title="voracode.jsonc"
{
  "$schema": "https://voracode.ai/config.json",
  "command": {
    "test": {
      "template": "Run the full test suite with coverage report and show any failures.\nFocus on the failing tests and suggest fixes.",
      "description": "Run tests with coverage",
      "agent": "build",
      "model": "anthropic/claude-haiku-4-5",
    },
    "component": {
      "template": "Create a new React component named $ARGUMENTS with TypeScript support.\nInclude proper typing and basic structure.",
      "description": "Create a new component",
    },
  },
}
```

Du kan ogsÃ¥ definere kommandoer ved Ã¥ bruke markdown-filer i `~/.config/voracode/commands/` eller `.voracode/commands/`. [Les mer her](/docs/commands).

---

### Tastebindinger

Tilpass tastebindinger i `tui.json`.

```json title="tui.json"
{
  "$schema": "https://voracode.ai/tui.json",
  "keybinds": {}
}
```

[Les mer her](/docs/keybinds).

---

### Autooppdatering

Voracode vil automatisk laste ned eventuelle nye oppdateringer nÃ¥r den starter opp. Du kan deaktivere dette med alternativet `autoupdate`.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "autoupdate": false
}
```

Hvis du ikke vil ha oppdateringer, men Ã¸nsker Ã¥ bli varslet nÃ¥r en ny versjon er tilgjengelig, sett `autoupdate` til `"notify"`.
Legg merke til at dette bare fungerer hvis det ikke ble installert med en pakkebehandling som Homebrew.

---

### Formatere

Du kan konfigurere kodeformatere gjennom alternativet `formatter`.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "formatter": {
    "prettier": {
      "disabled": true
    },
    "custom-prettier": {
      "command": ["npx", "prettier", "--write", "$FILE"],
      "environment": {
        "NODE_ENV": "development"
      },
      "extensions": [".js", ".ts", ".jsx", ".tsx"]
    }
  }
}
```

[Les mer om formattere her](/docs/formatters).

---

### Tillatelser

Som standard **tillater Voracode alle operasjoner** uten Ã¥ kreve eksplisitt godkjenning. Du kan endre dette ved Ã¥ bruke alternativet `permission`.

Slik sikrer du at verktÃ¸yene `edit` og `bash` krever brukergodkjenning:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "edit": "ask",
    "bash": "ask"
  }
}
```

[Les mer om tillatelser her](/docs/permissions).

---

### Komprimering

Du kan styre kontekstkomprimering gjennom alternativet `compaction`.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "compaction": {
    "auto": true,
    "prune": false,
    "reserved": 10000
  }
}
```

- `auto` - Komprimer Ã¸kten automatisk nÃ¥r konteksten er full (standard: `true`).
- `prune` - Fjern gamle verktÃ¸yutdata for Ã¥ spare tokens (standard: `false`).
- `reserved` - Token-buffer for komprimering. Etterlater nok vindu til Ã¥ unngÃ¥ overflyt under komprimering

---

### FilovervÃ¥ker

Du kan konfigurere ignoreringsmÃ¸nstre for filovervÃ¥king gjennom alternativet `watcher`.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "watcher": {
    "ignore": ["node_modules/**", "dist/**", ".git/**"]
  }
}
```

MÃ¸nstre fÃ¸lger glob-syntaks. Bruk dette for Ã¥ ekskludere stÃ¸yende kataloger fra filovervÃ¥king.

---

### MCP servere

Du kan konfigurere MCP-servere du vil bruke gjennom alternativet `mcp`.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {}
}
```

[Les mer her](/docs/mcp-servers).

---

### Utvidelser

[Plugins](/docs/plugins) utvider Voracode med tilpassede verktÃ¸y, kroker og integrasjoner.

Plasser plugin-filer i `.voracode/plugins/` eller `~/.config/voracode/plugins/`. Du kan ogsÃ¥ laste inn plugins fra npm gjennom alternativet `plugin`.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "plugin": ["voracode-helicone-session", "@my-org/custom-plugin"]
}
```

[Les mer her](/docs/plugins).

---

### Instruksjoner

Du kan konfigurere instruksjoner for modellen du bruker gjennom alternativet `instructions`.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "instructions": ["CONTRIBUTING.md", "docs/guidelines.md", ".cursor/rules/*.md"]
}
```

Dette krever en liste med baner og globmÃ¸nstre til instruksjonsfiler. [Les mer
om regler her](/docs/rules).

---

### Deaktiverte leverandÃ¸rer

Du kan deaktivere leverandÃ¸rer som lastes automatisk gjennom alternativet `disabled_providers`. Dette er nyttig nÃ¥r du vil forhindre at enkelte leverandÃ¸rer lastes inn selv om deres legitimasjon er tilgjengelig.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "disabled_providers": ["openai", "gemini"]
}
```

:::note
`disabled_providers` har prioritet over `enabled_providers`.
:::

Alternativet `disabled_providers` godtar en rekke leverandÃ¸r-ID-er. NÃ¥r en leverandÃ¸r er deaktivert:

- Den blir ikke lastet selv om miljÃ¸variabler er angitt.
- Den vil ikke bli lastet inn selv om API-nÃ¸kler er konfigurert gjennom `/connect`-kommandoen.
- LeverandÃ¸rens modeller vises ikke i modellvalglisten.

---

### Aktiverte leverandÃ¸rer

Du kan spesifisere en godkjenningsliste over leverandÃ¸rer gjennom alternativet `enabled_providers`. NÃ¥r angitt, vil bare de angitte leverandÃ¸rene vÃ¦re aktivert og alle andre vil bli ignorert.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "enabled_providers": ["anthropic", "openai"]
}
```

Dette er nyttig nÃ¥r du vil begrense Voracode til kun Ã¥ bruke spesifikke leverandÃ¸rer i stedet for Ã¥ deaktivere dem Ã©n etter Ã©n.

:::note
`disabled_providers` har prioritet over `enabled_providers`.
:::

Hvis en leverandÃ¸r vises i bÃ¥de `enabled_providers` og `disabled_providers`, har `disabled_providers` prioritet for bakoverkompatibilitet.

---

### Eksperimentelt

`experimental`-nÃ¸kkelen inneholder alternativer som er under aktiv utvikling.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "experimental": {}
}
```

:::caution
Eksperimentelle alternativer er ikke stabile. De kan endres eller fjernes uten varsel.
:::

---

## Variabler

Du kan bruke variabelerstatning i konfigurasjonsfilene dine for Ã¥ referere til miljÃ¸variabler og filinnhold.

---

### MiljÃ¸variabler

Bruk `{env:VARIABLE_NAME}` for Ã¥ erstatte miljÃ¸variabler:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "model": "{env:VORACODE_MODEL}",
  "provider": {
    "anthropic": {
      "models": {},
      "options": {
        "apiKey": "{env:ANTHROPIC_API_KEY}"
      }
    }
  }
}
```

Hvis miljÃ¸variabelen ikke er angitt, vil den bli erstattet med en tom streng.

---

### Filer

Bruk `{file:path/to/file}` for Ã¥ erstatte innholdet i en fil:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "instructions": ["./custom-instructions.md"],
  "provider": {
    "openai": {
      "options": {
        "apiKey": "{file:~/.secrets/openai-key}"
      }
    }
  }
}
```

Filbaner kan vÃ¦re:

- I forhold til konfigurasjonsfilkatalogen
- Eller absolutte baner som begynner med `/` eller `~`

Disse er nyttige for:

- Oppbevaring av sensitive data som API-nÃ¸kler i separate filer.
- Inkludert store instruksjonsfiler uten Ã¥ rote konfigurasjonen din.
- Deling av vanlige konfigurasjonsbiter pÃ¥ tvers av flere konfigurasjonsfiler.
