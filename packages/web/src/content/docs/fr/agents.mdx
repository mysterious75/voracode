---
title: Agents
description: Configurez et utilisez des agents spÃ©cialisÃ©s.
---

Les agents sont des assistants IA spÃ©cialisÃ©s qui peuvent Ãªtre configurÃ©s pour des tÃ¢ches et des flux de travail spÃ©cifiques. Ils vous permettent de crÃ©er des outils ciblÃ©s avec des prompts, des modÃ¨les et un accÃ¨s aux outils personnalisÃ©s.

:::tip
Utilisez l'agent de plan pour analyser le code et examiner les suggestions sans apporter de modifications au code.
:::

Vous pouvez basculer entre les agents au cours d'une session ou les appeler avec la mention `@`.

---

## Types

Il existe deux types d'agents dans Voracode ; agents primaires et sous-agents.

---

### Agents primaires

Les agents primaires sont les principaux assistants avec lesquels vous interagissez directement. Vous pouvez les parcourir Ã  l'aide de la touche **Tab** ou de votre combinaison de touches `switch_agent` configurÃ©e. Ces agents gÃ¨rent votre conversation principale. L'accÃ¨s aux outils est configurÃ© via les autorisations : par exemple, Build a tous les outils activÃ©s tandis que Plan est restreint.

:::tip
Vous pouvez utiliser la touche **Tab** pour basculer entre les agents primaires au cours d'une session.
:::

Voracode est livrÃ© avec deux agents primaires intÃ©grÃ©s, **Build** et **Plan**.
Nous les verrons ci-dessous.

---

### Sous-agents

Les sous-agents sont des assistants spÃ©cialisÃ©s que les agents primaires peuvent appeler pour des tÃ¢ches spÃ©cifiques. Vous pouvez Ã©galement les invoquer manuellement en **@ les mentionnant** dans vos messages.

Voracode est livrÃ© avec trois sous-agents intÃ©grÃ©s, **General**, **Explore** et **Scout**. Nous les verrons ci-dessous.

---

## Agents intÃ©grÃ©s

Voracode est livrÃ© avec deux agents primaires intÃ©grÃ©s et trois sous-agents intÃ©grÃ©s.

---

### Agent Build

_Mode_ : `primary`

Build est l'agent primaire **par dÃ©faut** avec tous les outils activÃ©s. Il s'agit de l'agent standard pour les travaux de dÃ©veloppement oÃ¹ vous avez besoin d'un accÃ¨s complet aux opÃ©rations sur les fichiers et aux commandes systÃ¨me.

---

### Agent Plan

_Mode_ : `primary`

Un agent restreint conÃ§u pour la planification et l'analyse. Nous utilisons un systÃ¨me d'autorisation pour vous donner plus de contrÃ´le et empÃªcher toute modification involontaire.
Par dÃ©faut, tous les Ã©lÃ©ments suivants sont dÃ©finis sur `ask` :

- `file edits` : toutes les Ã©critures, correctifs et modifications
- `bash` : toutes les commandes bash

Cet agent est utile lorsque vous souhaitez que le LLM analyse le code, suggÃ¨re des modifications ou crÃ©e des plans sans apporter de modifications rÃ©elles Ã  votre base de code.

---

### Agent General

_Mode_ : `subagent`

Un agent polyvalent pour traiter des questions complexes et exÃ©cuter des tÃ¢ches en plusieurs Ã©tapes. Dispose d'un accÃ¨s complet aux outils (sauf todo), il peut donc apporter des modifications aux fichiers en cas de besoin. Utilisez-le pour exÃ©cuter plusieurs unitÃ©s de travail en parallÃ¨le.

---

### Agent Explore

_Mode_ : `subagent`

Un agent rapide en lecture seule pour explorer les bases de code. Impossible de modifier les fichiers. Utilisez-le lorsque vous avez besoin de rechercher rapidement des fichiers par modÃ¨les, de rechercher du code par mots-clÃ©s ou de rÃ©pondre Ã  des questions sur la base de code.

---

### Agent Scout

_Mode_ : `subagent`

Un agent en lecture seule pour la recherche sur la documentation externe et les dÃ©pendances. Utilisez-le lorsque vous devez cloner le dÃ©pÃ´t d'une dÃ©pendance dans le cache gÃ©rÃ© d'Voracode, inspecter le code source d'une bibliothÃ¨que ou recouper le code local avec les implÃ©mentations upstream sans modifier votre espace de travail.

---

### Agent Compaction

_Mode_ : `primary`

Agent systÃ¨me cachÃ© qui compacte un contexte long en un rÃ©sumÃ© plus petit. Il s'exÃ©cute automatiquement en cas de besoin et n'est pas sÃ©lectionnable dans l'interface utilisateur.

---

### Agent Title

_Mode_ : `primary`

Agent systÃ¨me cachÃ© qui gÃ©nÃ¨re des titres de session courts. Il s'exÃ©cute automatiquement et n'est pas sÃ©lectionnable dans l'interface utilisateur.

---

### Agent Summary

_Mode_ : `primary`

Agent systÃ¨me cachÃ© qui crÃ©e des rÃ©sumÃ©s de session. Il s'exÃ©cute automatiquement et n'est pas sÃ©lectionnable dans l'interface utilisateur.

---

## Utilisation

1. Pour les agents primaires, utilisez la touche **Tab** pour les parcourir au cours d'une session. Vous pouvez Ã©galement utiliser votre raccourci clavier `switch_agent` configurÃ©.

2. Les sous-agents peuvent Ãªtre invoquÃ©s :
   - **Automatiquement** par les agents primaires pour des tÃ¢ches spÃ©cialisÃ©es en fonction de leurs descriptions.
   - Manuellement en **@ mentionnant** un sous-agent dans votre message. Par exemple :

     ```txt frame="none"
     @general help me search for this function
     ```

3. **Navigation entre les sessions** : lorsque les sous-agents crÃ©ent leurs propres sessions enfants, vous pouvez naviguer entre la session parent et toutes les sessions enfants en utilisant :
   - **\<Leader>+Right** (ou votre combinaison de touches `session_child_cycle` configurÃ©e) pour avancer via parent â†’ enfant1 â†’ enfant2 â†’ ... â†’ parent
   - **\<Leader>+Left** (ou votre combinaison de touches `session_child_cycle_reverse` configurÃ©e) pour revenir en arriÃ¨re dans parent â† enfant1 â† enfant2 â† ... â† parent

Cela vous permet de basculer en toute transparence entre la conversation principale et le travail spÃ©cialisÃ© des sous-agents.

---

## Configuration

Vous pouvez personnaliser les agents intÃ©grÃ©s ou crÃ©er les vÃ´tres via la configuration. Les agents peuvent Ãªtre configurÃ©s de deux maniÃ¨res :

---

### JSON

Configurez les agents dans votre fichier de configuration `voracode.json` :

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "agent": {
    "build": {
      "mode": "primary",
      "model": "anthropic/claude-sonnet-4-20250514",
      "prompt": "{file:./prompts/build.txt}",
      "tools": {
        "write": true,
        "edit": true,
        "bash": true
      }
    },
    "plan": {
      "mode": "primary",
      "model": "anthropic/claude-haiku-4-20250514",
      "tools": {
        "write": false,
        "edit": false,
        "bash": false
      }
    },
    "code-reviewer": {
      "description": "Reviews code for best practices and potential issues",
      "mode": "subagent",
      "model": "anthropic/claude-sonnet-4-20250514",
      "prompt": "You are a code reviewer. Focus on security, performance, and maintainability.",
      "tools": {
        "write": false,
        "edit": false
      }
    }
  }
}
```

---

### Markdown

Vous pouvez Ã©galement dÃ©finir des agents Ã  l'aide de fichiers markdown. Placez-les dans :

- Global : `~/.config/voracode/agents/`
- Par projet : `.voracode/agents/`

```markdown title="~/.config/voracode/agents/review.md"
---
description: Reviews code for quality and best practices
mode: subagent
model: anthropic/claude-sonnet-4-20250514
temperature: 0.1
tools:
  write: false
  edit: false
  bash: false
---

You are in code review mode. Focus on:

- Code quality and best practices
- Potential bugs and edge cases
- Performance implications
- Security considerations

Provide constructive feedback without making direct changes.
```

Le nom du fichier Markdown devient le nom de l'agent. Par exemple, `review.md` crÃ©e un agent `review`.

---

## Options

Examinons ces options de configuration en dÃ©tail.

---

### Description

Utilisez l'option `description` pour fournir une brÃ¨ve description de ce que fait l'agent et quand l'utiliser.

```json title="voracode.json"
{
  "agent": {
    "review": {
      "description": "Reviews code for best practices and potential issues"
    }
  }
}
```

Il s'agit d'une option de configuration **obligatoire**.

---

### TempÃ©rature

ContrÃ´lez le caractÃ¨re alÃ©atoire et la crÃ©ativitÃ© des rÃ©ponses du LLM avec la configuration `temperature`.

Des valeurs faibles rendent les rÃ©ponses plus ciblÃ©es et dÃ©terministes, tandis que des valeurs plus Ã©levÃ©es augmentent la crÃ©ativitÃ© et la variabilitÃ©.

```json title="voracode.json"
{
  "agent": {
    "plan": {
      "temperature": 0.1
    },
    "creative": {
      "temperature": 0.8
    }
  }
}
```

Les valeurs de tempÃ©rature varient gÃ©nÃ©ralement de 0,0 Ã  1,0 :

- **0,0-0,2** : rÃ©ponses trÃ¨s ciblÃ©es et dÃ©terministes, idÃ©ales pour l'analyse et la planification du code
- **0,3-0,5** : rÃ©ponses Ã©quilibrÃ©es avec une certaine crÃ©ativitÃ©, idÃ©ales pour les tÃ¢ches de dÃ©veloppement gÃ©nÃ©rales
- **0,6-1,0** : rÃ©ponses plus crÃ©atives et variÃ©es, utiles pour le brainstorming et l'exploration

```json title="voracode.json"
{
  "agent": {
    "analyze": {
      "temperature": 0.1,
      "prompt": "{file:./prompts/analysis.txt}"
    },
    "build": {
      "temperature": 0.3
    },
    "brainstorm": {
      "temperature": 0.7,
      "prompt": "{file:./prompts/creative.txt}"
    }
  }
}
```

Si aucune tempÃ©rature n'est spÃ©cifiÃ©e, Voracode utilise les valeurs par dÃ©faut spÃ©cifiques au modÃ¨le ; gÃ©nÃ©ralement 0 pour la plupart des modÃ¨les, 0,55 pour les modÃ¨les Qwen.

---

### Ã‰tapes maximum

ContrÃ´lez le nombre maximum d'itÃ©rations que l'agent peut effectuer avant d'Ãªtre obligÃ© de rÃ©pondre avec du texte uniquement. Cela permet aux utilisateurs qui souhaitent contrÃ´ler les coÃ»ts de fixer une limite aux actions agents.

Si cela n'est pas dÃ©fini, l'agent continuera Ã  itÃ©rer jusqu'Ã  ce que le modÃ¨le choisisse de s'arrÃªter ou que l'utilisateur interrompe la session.

```json title="voracode.json"
{
  "agent": {
    "quick-thinker": {
      "description": "Fast reasoning with limited iterations",
      "prompt": "You are a quick thinker. Solve problems with minimal steps.",
      "steps": 5
    }
  }
}
```

Lorsque la limite est atteinte, l'agent reÃ§oit un prompt systÃ¨me spÃ©cial lui demandant de rÃ©pondre avec un rÃ©sumÃ© de son travail et des tÃ¢ches restantes recommandÃ©es.

:::caution
Lâ€™ancien champ `maxSteps` est obsolÃ¨te. Utilisez plutÃ´t `steps`.
:::

---

### DÃ©sactiver

DÃ©finissez sur `true` pour dÃ©sactiver l'agent.

```json title="voracode.json"
{
  "agent": {
    "review": {
      "disable": true
    }
  }
}
```

---

### Invite

SpÃ©cifiez un fichier de prompt systÃ¨me personnalisÃ© pour cet agent avec la configuration `prompt`. Le fichier de prompt doit contenir des instructions spÃ©cifiques Ã  l'objectif de l'agent.

```json title="voracode.json"
{
  "agent": {
    "review": {
      "prompt": "{file:./prompts/code-review.txt}"
    }
  }
}
```

Ce chemin est relatif Ã  l'emplacement du fichier de configuration. Cela fonctionne donc Ã  la fois pour la configuration globale Voracode et pour la configuration spÃ©cifique au projet.

---

### ModÃ¨le

Utilisez la configuration `model` pour remplacer le modÃ¨le de cet agent. Utile pour utiliser diffÃ©rents modÃ¨les optimisÃ©s pour diffÃ©rentes tÃ¢ches. Par exemple, un modÃ¨le de planification plus rapide, un modÃ¨le de mise en Å“uvre plus performant.

:::tip
Si vous ne spÃ©cifiez pas de modÃ¨le, les agents primaires utilisent le [modÃ¨le globalement configurÃ©](/docs/config#models) tandis que les sous-agents utiliseront le modÃ¨le de l'agent primaire qui a appelÃ© le sous-agent.
:::

```json title="voracode.json"
{
  "agent": {
    "plan": {
      "model": "anthropic/claude-haiku-4-20250514"
    }
  }
}
```

L'ID de modÃ¨le dans votre configuration Voracode utilise le format `provider/model-id`. Par exemple, si vous utilisez [Voracode Zen](/docs/zen), vous utiliserez `voracode/gpt-5.1-codex` pour GPT 5.1 Codex.

---

### Outils

ContrÃ´lez quels outils sont disponibles dans cet agent avec la configuration `tools`. Vous pouvez activer ou dÃ©sactiver des outils spÃ©cifiques en les dÃ©finissant sur `true` ou `false`.

```json title="voracode.json" {3-6,9-12}
{
  "$schema": "https://voracode.ai/config.json",
  "tools": {
    "write": true,
    "bash": true
  },
  "agent": {
    "plan": {
      "tools": {
        "write": false,
        "bash": false
      }
    }
  }
}
```

:::note
La configuration spÃ©cifique Ã  l'agent remplace la configuration globale.
:::

Vous pouvez Ã©galement utiliser des caractÃ¨res gÃ©nÃ©riques pour contrÃ´ler plusieurs outils Ã  la fois. Par exemple, pour dÃ©sactiver tous les outils d'un serveur MCP :

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "agent": {
    "readonly": {
      "tools": {
        "mymcp_*": false,
        "write": false,
        "edit": false
      }
    }
  }
}
```

[En savoir plus sur les outils](/docs/tools).

---

### Autorisations

Vous pouvez configurer des autorisations pour gÃ©rer les actions qu'un agent peut effectuer. Actuellement, les autorisations pour les outils `edit`, `bash` et `webfetch` peuvent Ãªtre configurÃ©es pour :

- `"ask"` â€” Demander l'approbation avant d'exÃ©cuter l'outil
- `"allow"` â€” Autoriser toutes les opÃ©rations sans approbation
- `"deny"` â€“ DÃ©sactiver l'outil

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "edit": "deny"
  }
}
```

Vous pouvez remplacer ces autorisations par agent.

```json title="voracode.json" {3-5,8-10}
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "edit": "deny"
  },
  "agent": {
    "build": {
      "permission": {
        "edit": "ask"
      }
    }
  }
}
```

Vous pouvez Ã©galement dÃ©finir des autorisations dans les agents Markdown.

```markdown title="~/.config/voracode/agents/review.md"
---
description: Code review without edits
mode: subagent
permission:
  edit: deny
  bash:
    "*": ask
    "git diff": allow
    "git log*": allow
    "grep *": allow
  webfetch: deny
---

Only analyze code and suggest changes.
```

Vous pouvez dÃ©finir des autorisations pour des commandes bash spÃ©cifiques.

```json title="voracode.json" {7}
{
  "$schema": "https://voracode.ai/config.json",
  "agent": {
    "build": {
      "permission": {
        "bash": {
          "git push": "ask",
          "grep *": "allow"
        }
      }
    }
  }
}
```

Cela peut prendre un pattern glob.

```json title="voracode.json" {7}
{
  "$schema": "https://voracode.ai/config.json",
  "agent": {
    "build": {
      "permission": {
        "bash": {
          "git *": "ask"
        }
      }
    }
  }
}
```

Et vous pouvez Ã©galement utiliser le caractÃ¨re gÃ©nÃ©rique `*` pour gÃ©rer les autorisations pour toutes les commandes.
Puisque la derniÃ¨re rÃ¨gle correspondante est prioritaire, placez le caractÃ¨re gÃ©nÃ©rique `*` en premier et les rÃ¨gles spÃ©cifiques aprÃ¨s.

```json title="voracode.json" {8}
{
  "$schema": "https://voracode.ai/config.json",
  "agent": {
    "build": {
      "permission": {
        "bash": {
          "*": "ask",
          "git status *": "allow"
        }
      }
    }
  }
}
```

[En savoir plus sur les autorisations](/docs/permissions).

---

### Mode

ContrÃ´lez le mode de l'agent avec la configuration `mode`. L'option `mode` est utilisÃ©e pour dÃ©terminer comment l'agent peut Ãªtre utilisÃ©.

```json title="voracode.json"
{
  "agent": {
    "review": {
      "mode": "subagent"
    }
  }
}
```

L'option `mode` peut Ãªtre dÃ©finie sur `primary`, `subagent` ou `all`. Si aucun `mode` nâ€™est spÃ©cifiÃ©, la valeur par dÃ©faut est `all`.

---

### MasquÃ©

Masquez un sous-agent du menu de saisie semi-automatique `@` avec `hidden: true`. Utile pour les sous-agents internes qui ne doivent Ãªtre invoquÃ©s que par programme par d'autres agents via l'outil TÃ¢che.

```json title="voracode.json"
{
  "agent": {
    "internal-helper": {
      "mode": "subagent",
      "hidden": true
    }
  }
}
```

Cela affecte uniquement la visibilitÃ© de l'utilisateur dans le menu de saisie semi-automatique. Les agents masquÃ©s peuvent toujours Ãªtre invoquÃ©s par le modÃ¨le via l'outil TÃ¢che si les autorisations le permettent.

:::note
S'applique uniquement aux agents `mode: subagent`.
:::

---

### Autorisations de tÃ¢ches

ContrÃ´lez quels sous-agents un agent peut appeler via l'outil TÃ¢che avec `permission.task`. Utilise des patterns glob pour une correspondance flexible.

```json title="voracode.json"
{
  "agent": {
    "orchestrator": {
      "mode": "primary",
      "permission": {
        "task": {
          "*": "deny",
          "orchestrator-*": "allow",
          "code-reviewer": "ask"
        }
      }
    }
  }
}
```

Lorsqu'il est dÃ©fini sur `deny`, le sous-agent est entiÃ¨rement supprimÃ© de la description de l'outil TÃ¢che, de sorte que le modÃ¨le ne tentera pas de l'invoquer.

:::tip
Les rÃ¨gles sont Ã©valuÃ©es dans l'ordre et la **derniÃ¨re rÃ¨gle correspondante l'emporte**. Dans l'exemple ci-dessus, `orchestrator-planner` correspond Ã  la fois Ã  `*` (refuser) et Ã  `orchestrator-*` (autoriser), mais comme `orchestrator-*` vient aprÃ¨s `*`, le rÃ©sultat est `allow`.
:::

:::tip
Les utilisateurs peuvent toujours appeler n'importe quel sous-agent directement via le menu de saisie semi-automatique `@`, mÃªme si les autorisations de tÃ¢che de l'agent le refuseraient.
:::

---

### Couleur

Personnalisez l'apparence visuelle de l'agent dans l'interface utilisateur avec l'option `color`. Cela affecte la faÃ§on dont l'agent apparaÃ®t dans l'interface.

Utilisez une couleur hexadÃ©cimale valide (par exemple, `#FF5733`) ou une couleur de thÃ¨me : `primary`, `secondary`, `accent`, `success`, `warning`, `error`, `info`.

```json title="voracode.json"
{
  "agent": {
    "creative": {
      "color": "#ff6b6b"
    },
    "code-reviewer": {
      "color": "accent"
    }
  }
}
```

---

### Top P

ContrÃ´lez la diversitÃ© des rÃ©ponses avec lâ€™option `top_p`. Alternative Ã  la tempÃ©rature pour contrÃ´ler le caractÃ¨re alÃ©atoire.

```json title="voracode.json"
{
  "agent": {
    "brainstorm": {
      "top_p": 0.9
    }
  }
}
```

Les valeurs vont de 0,0 Ã  1,0. Les valeurs infÃ©rieures sont plus ciblÃ©es, les valeurs Ã©levÃ©es plus diversifiÃ©es.

---

### Options supplÃ©mentaires

Toutes les autres options que vous spÃ©cifiez dans la configuration de votre agent seront **transmises directement** au fournisseur en tant qu'options de modÃ¨le. Cela vous permet d'utiliser des fonctionnalitÃ©s et des paramÃ¨tres spÃ©cifiques au fournisseur.

Par exemple, avec les modÃ¨les de raisonnement d'OpenAI, vous pouvez contrÃ´ler l'effort de raisonnement :

```json title="voracode.json" {6,7}
{
  "agent": {
    "deep-thinker": {
      "description": "Agent that uses high reasoning effort for complex problems",
      "model": "openai/gpt-5",
      "reasoningEffort": "high",
      "textVerbosity": "low"
    }
  }
}
```

Ces options supplÃ©mentaires sont spÃ©cifiques au modÃ¨le et au fournisseur. Consultez la documentation de votre fournisseur pour connaÃ®tre les paramÃ¨tres disponibles.

:::tip
ExÃ©cutez `voracode models` pour voir une liste des modÃ¨les disponibles.
:::

---

## CrÃ©ation d'agents

Vous pouvez crÃ©er de nouveaux agents Ã  l'aide de la commande suivante :

```bash
voracode agent create
```

Cette commande interactive va :

1. Demander oÃ¹ enregistrer l'agent ; global ou spÃ©cifique Ã  un projet.
2. Demander une description de ce que l'agent doit faire.
3. GÃ©nÃ©rer un prompt systÃ¨me et un identifiant appropriÃ©s.
4. Vous permettre de sÃ©lectionner les outils auxquels l'agent peut accÃ©der.
5. Enfin, crÃ©er un fichier markdown avec la configuration de l'agent.

---

## Cas d'utilisation

Voici quelques cas dâ€™utilisation courants pour diffÃ©rents agents.

- **Agent de build** : travail de dÃ©veloppement complet avec tous les outils activÃ©s
- **Agent de plan** : analyse et planification sans apporter de modifications
- **Agent de rÃ©vision** : rÃ©vision de code avec accÃ¨s en lecture seule et outils de documentation
- **Agent de dÃ©bogage** : axÃ© sur l'investigation avec les outils bash et read activÃ©s
- **Agent Docs** : Ã©criture de documentation avec des opÃ©rations sur les fichiers mais pas de commandes systÃ¨me

---

## Exemples

Voici quelques exemples dâ€™agents qui pourraient vous Ãªtre utiles.

:::tip
Avez-vous un agent que vous aimeriez partager ? [Soumettez un PR](https://github.com/mysterious75/voracode).
:::

---

### Agent de documentation

```markdown title="~/.config/voracode/agents/docs-writer.md"
---
description: Writes and maintains project documentation
mode: subagent
tools:
  bash: false
---

You are a technical writer. Create clear, comprehensive documentation.

Focus on:

- Clear explanations
- Proper structure
- Code examples
- User-friendly language
```

---

### Auditeur de sÃ©curitÃ©

```markdown title="~/.config/voracode/agents/security-auditor.md"
---
description: Performs security audits and identifies vulnerabilities
mode: subagent
tools:
  write: false
  edit: false
---

You are a security expert. Focus on identifying potential security issues.

Look for:

- Input validation vulnerabilities
- Authentication and authorization flaws
- Data exposure risks
- Dependency vulnerabilities
- Configuration security issues
```
