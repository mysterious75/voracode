---
title: Serveurs LSP
description: Voracode s'intÃ¨gre Ã  vos serveurs LSP.
---

Voracode peut s'intÃ©grer aux serveurs Language Server Protocol (LSP) pour utiliser les diagnostics comme retour pour l'agent.

---

## Serveurs intÃ©grÃ©s

Voracode est livrÃ© avec plusieurs serveurs LSP intÃ©grÃ©s pour les langages populaires :

| Serveur LSP        | Extensions                                                          | PrÃ©requis                                                               |
| ------------------ | ------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| astro              | .astro                                                              | Installation automatique pour les projets Astro                         |
| bash               | .sh, .bash, .zsh, .ksh                                              | Installe automatiquement le serveur bash-language-server                |
| clangd             | .c, .cpp, .cc, .cxx, .c++, .h, .hpp, .hh, .hxx, .h++                | Installation automatique pour les projets C/C++                         |
| csharp             | .cs                                                                 | `.NET SDK` installÃ©                                                     |
| clojure-lsp        | .clj, .cljs, .cljc, .edn                                            | Commande `clojure-lsp` disponible                                       |
| dart               | .dart                                                               | Commande `dart` disponible                                              |
| deno               | .ts, .tsx, .js, .jsx, .mjs                                          | Commande `deno` disponible (dÃ©tection automatique deno.json/deno.jsonc) |
| elixir-ls          | .ex, .ex                                                            | Commande `elixir` disponible                                            |
| eslint             | .ts, .tsx, .js, .jsx, .mjs, .cjs, .mts, .cts, .vue                  | DÃ©pendance `eslint` dans le projet                                      |
| fsharp             | .fs, .fsi, .fsx, .fsscript                                          | `.NET SDK` installÃ©                                                     |
| gleam              | .gleam                                                              | Commande `gleam` disponible                                             |
| gopls              | .go                                                                 | Commande `go` disponible                                                |
| hls                | .hs, .lhs                                                           | Commande `haskell-language-server-wrapper` disponible                   |
| jdtls              | .java                                                               | `Java SDK (version 21+)` installÃ©                                       |
| julials            | .jl                                                                 | `julia` et `LanguageServer.jl` installÃ©s                                |
| kotlin-ls          | .kt, .kts                                                           | Installation automatique pour les projets Kotlin                        |
| lua-ls             | .lua                                                                | Installation automatique pour les projets Lua                           |
| nixd               | .nix                                                                | Commande `nixd` disponible                                              |
| ocaml-lsp          | .ml, .mli                                                           | Commande `ocamllsp` disponible                                          |
| oxlint             | .ts, .tsx, .js, .jsx, .mjs, .cjs, .mts, .cts, .vue, .astro, .svelte | DÃ©pendance `oxlint` dans le projet                                      |
| php intelephense   | .php                                                                | Installation automatique pour les projets PHP                           |
| prisma             | .prisma                                                             | Commande `prisma` disponible                                            |
| pyright            | .py, .pyi                                                           | DÃ©pendance `pyright` installÃ©e                                          |
| ruby-lsp (rubocop) | .rb, .rake, .gemspec, .ru                                           | Commandes `ruby` et `gem` disponibles                                   |
| rust               | .rs                                                                 | Commande `rust-analyzer` disponible                                     |
| sourcekit-lsp      | .swift, .objc, .objcpp                                              | `swift` installÃ© (`xcode` sur macOS)                                    |
| svelte             | .svelte                                                             | Installation automatique pour les projets Svelte                        |
| terraform          | .tf, .tfvars                                                        | Installation automatique Ã  partir des versions GitHub                   |
| tinymist           | .typ, .typc                                                         | Installation automatique Ã  partir des versions GitHub                   |
| typescript         | .ts, .tsx, .js, .jsx, .mjs, .cjs, .mts, .cts                        | DÃ©pendance `typescript` dans le projet                                  |
| vue                | .vue                                                                | Installation automatique pour les projets Vue                           |
| yaml-ls            | .yaml, .yml                                                         | Installe automatiquement le serveur yaml-language-server de Red Hat     |
| zls                | .zig, .zon                                                          | Commande `zig` disponible                                               |

LSP est dÃ©sactivÃ© par dÃ©faut. Lorsqu'il est activÃ©, les serveurs dÃ©marrent lorsqu'une des extensions de fichier ci-dessus est dÃ©tectÃ©e et que les exigences sont remplies.

:::note
Vous pouvez dÃ©sactiver les tÃ©lÃ©chargements automatiques du serveur LSP en dÃ©finissant la variable d'environnement `VORACODE_DISABLE_LSP_DOWNLOAD` sur `true`.
:::

---

## Comment Ã§a marche

Lorsque LSP est activÃ© et qu'voracode ouvre un fichier, il :

1. VÃ©rifie l'extension du fichier par rapport Ã  tous les serveurs LSP activÃ©s.
2. DÃ©marre le serveur LSP appropriÃ© s'il n'est pas dÃ©jÃ  en cours d'exÃ©cution.

---

## Bonnes pratiques

LSP peut aider l'agent Ã  trouver et corriger des problÃ¨mes en fournissant les diagnostics des serveurs de langage. C'est utile dans certains projets, mais ce n'est pas toujours un gain net.

Les serveurs de langage peuvent se dÃ©synchroniser, consommer beaucoup de mÃ©moire, varier selon la version ou le projet, et ralentir les flux de travail de l'agent. Dans de nombreux projets, il est prÃ©fÃ©rable de faire exÃ©cuter directement par l'agent le lint, le typecheck ou d'autres outils CLI de diagnostic, afin que les erreurs reviennent dans la boucle de l'agent sans ces compromis. Documentez ces commandes dans des fichiers d'instructions comme `AGENTS.md` ou des skills afin que l'agent sache quoi exÃ©cuter. Activez LSP lorsque votre projet bÃ©nÃ©ficie d'un retour supplÃ©mentaire des serveurs de langage.

---

## Configuration

Vous pouvez activer et personnaliser les serveurs LSP via la section `lsp` de votre configuration voracode.

Pour activer tous les serveurs LSP intÃ©grÃ©s, dÃ©finissez `lsp` sur `true`.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "lsp": true
}
```

Utilisez un objet pour conserver les serveurs intÃ©grÃ©s activÃ©s tout en configurant des remplacements ou des serveurs personnalisÃ©s.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "lsp": {}
}
```

Chaque serveur LSP prend en charge les Ã©lÃ©ments suivants :

| PropriÃ©tÃ©        | Type     | Description                                                      |
| ---------------- | -------- | ---------------------------------------------------------------- |
| `disabled`       | boolÃ©en  | DÃ©finissez ceci sur `true` pour dÃ©sactiver le serveur LSP        |
| `command`        | chaÃ®ne[] | La commande pour dÃ©marrer le serveur LSP                         |
| `extensions`     | chaÃ®ne[] | Extensions de fichiers que ce serveur LSP doit gÃ©rer             |
| `env`            | objet    | Variables d'environnement Ã  dÃ©finir lors du dÃ©marrage du serveur |
| `initialization` | objet    | Options d'initialisation Ã  envoyer au serveur LSP                |

Regardons quelques exemples.

---

### Variables d'environnement

Utilisez la propriÃ©tÃ© `env` pour dÃ©finir les variables d'environnement lors du dÃ©marrage du serveur LSP :

```json title="voracode.json" {5-7}
{
  "$schema": "https://voracode.ai/config.json",
  "lsp": {
    "rust": {
      "env": {
        "RUST_LOG": "debug"
      }
    }
  }
}
```

---

### Options d'initialisation

Utilisez la propriÃ©tÃ© `initialization` pour transmettre les options d'initialisation au serveur LSP. Il s'agit de paramÃ¨tres spÃ©cifiques au serveur envoyÃ©s lors de la requÃªte LSP `initialize` :

```json title="voracode.json" {5-9}
{
  "$schema": "https://voracode.ai/config.json",
  "lsp": {
    "typescript": {
      "initialization": {
        "preferences": {
          "importModuleSpecifierPreference": "relative"
        }
      }
    }
  }
}
```

:::note
Les options d'initialisation varient selon le serveur LSP. Consultez la documentation de votre serveur LSP pour connaÃ®tre les options disponibles.
:::

---

### DÃ©sactivation des serveurs LSP

Si `lsp` est omis, tous les serveurs LSP sont dÃ©sactivÃ©s. Pour dÃ©sactiver tous les serveurs LSP aprÃ¨s qu'une autre configuration les a activÃ©s, dÃ©finissez `lsp` sur `false` :

```json title="voracode.json" {3}
{
  "$schema": "https://voracode.ai/config.json",
  "lsp": false
}
```

Pour dÃ©sactiver un serveur LSP **spÃ©cifique**, dÃ©finissez `disabled` sur `true` :

```json title="voracode.json" {5}
{
  "$schema": "https://voracode.ai/config.json",
  "lsp": {
    "typescript": {
      "disabled": true
    }
  }
}
```

---

### Serveurs LSP personnalisÃ©s

Vous pouvez ajouter des serveurs LSP personnalisÃ©s en spÃ©cifiant les extensions de commande et de fichier :

```json title="voracode.json" {4-7}
{
  "$schema": "https://voracode.ai/config.json",
  "lsp": {
    "custom-lsp": {
      "command": ["custom-lsp-server", "--stdio"],
      "extensions": [".custom"]
    }
  }
}
```

---

## Informations supplÃ©mentaires

### PHP Intelephense

PHP Intelephense offre des fonctionnalitÃ©s premium via une clÃ© de licence. Vous pouvez fournir une clÃ© de licence en plaÃ§ant (uniquement) la clÃ© dans un fichier texte Ã  l'adresse :

- macOS/Linux : `$HOME/intelephense/license.txt`
- Windows : `%USERPROFILE%/intelephense/license.txt`

Le fichier doit contenir uniquement la clÃ© de licence sans contenu supplÃ©mentaire.
