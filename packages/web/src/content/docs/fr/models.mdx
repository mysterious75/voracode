---
title: ModÃ¨les
description: Configuration d'un fournisseur et d'un modÃ¨le LLM.
---

Voracode utilise [AI SDK](https://ai-sdk.dev/) et [Models.dev](https://models.dev) pour prendre en charge **75+ fournisseurs LLM** et prend en charge l'exÃ©cution de modÃ¨les locaux.

---

## Fournisseurs

Les fournisseurs les plus populaires sont prÃ©chargÃ©s par dÃ©faut. Si vous avez ajoutÃ© les informations d'identification d'un fournisseur via la commande `/connect`, elles seront disponibles lorsque vous dÃ©marrerez Voracode.

En savoir plus sur [fournisseurs](/docs/providers).

---

## SÃ©lection d'un modÃ¨le

Une fois que vous avez configurÃ© votre fournisseur, vous pouvez sÃ©lectionner le modÃ¨le souhaitÃ© en tapant :

```bash frame="none"
/models
```

---

## ModÃ¨les recommandÃ©s

Il existe de nombreux modÃ¨les et de nouveaux modÃ¨les sortent chaque semaine.

:::tip
Pensez Ã  utiliser lâ€™un des modÃ¨les que nous recommandons.
:::

Cependant, seuls quelques-uns dâ€™entre eux savent Ã  la fois gÃ©nÃ©rer du code et appeler des outils.

Voici plusieurs modÃ¨les qui fonctionnent bien avec Voracode, sans ordre particulier. (Cette liste nâ€™est pas exhaustive et nâ€™est pas nÃ©cessairement Ã  jour) :

- GPT 5.2
- Codex GPT 5.1
- Claude Opus 4.5
- Claude Sonnet 4.5
- Minimax M2.1
- Gemini 3 Pro

---

## DÃ©finir une valeur par dÃ©faut

Pour dÃ©finir l'un d'entre eux comme modÃ¨le par dÃ©faut, vous pouvez dÃ©finir la clÃ© `model` dans votre configuration Voracode.

```json title="voracode.json" {3}
{
  "$schema": "https://voracode.ai/config.json",
  "model": "lmstudio/google/gemma-3n-e4b"
}
```

Ici, l'identifiant complet est `provider_id/model_id`. Par exemple, si vous utilisez [Voracode Zen](/docs/zen), vous utiliserez `voracode/gpt-5.1-codex` pour GPT 5.1 Codex.

Si vous avez configurÃ© un [fournisseur personnalisÃ©](/docs/providers#custom), le `provider_id` est la clÃ© de la partie `provider` de votre configuration et le `model_id` est la clÃ© de `provider.models`.

---

## Configuration des modÃ¨les

Vous pouvez configurer globalement les options d'un modÃ¨le via le fichier config.

```jsonc title="voracode.jsonc" {7-12,19-24}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "openai": {
      "models": {
        "gpt-5": {
          "options": {
            "reasoningEffort": "high",
            "textVerbosity": "low",
            "reasoningSummary": "auto",
            "include": ["reasoning.encrypted_content"],
          },
        },
      },
    },
    "anthropic": {
      "models": {
        "claude-sonnet-4-5-20250929": {
          "options": {
            "thinking": {
              "type": "enabled",
              "budgetTokens": 16000,
            },
          },
        },
      },
    },
  },
}
```

Ici, nous configurons les paramÃ¨tres globaux pour deux modÃ¨les intÃ©grÃ©s : `gpt-5` lorsqu'il est accessible via le fournisseur `openai` et `claude-sonnet-4-20250514` lorsqu'il est accessible via le fournisseur `anthropic`.
Les noms du fournisseur intÃ©grÃ© et des modÃ¨les peuvent Ãªtre trouvÃ©s sur [Models.dev](https://models.dev).

Vous pouvez Ã©galement configurer ces options pour tous les agents que vous utilisez. La configuration de l'agent remplace ici toutes les options globales. [En savoir plus](/docs/agents/#additional).

Vous pouvez Ã©galement dÃ©finir des variantes personnalisÃ©es qui Ã©tendent celles intÃ©grÃ©es. Les variantes vous permettent de configurer diffÃ©rents paramÃ¨tres pour le mÃªme modÃ¨le sans crÃ©er d'entrÃ©es en double :

```jsonc title="voracode.jsonc" {6-21}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "voracode": {
      "models": {
        "gpt-5": {
          "variants": {
            "high": {
              "reasoningEffort": "high",
              "textVerbosity": "low",
              "reasoningSummary": "auto",
            },
            "low": {
              "reasoningEffort": "low",
              "textVerbosity": "low",
              "reasoningSummary": "auto",
            },
          },
        },
      },
    },
  },
}
```

---

## Variantes

De nombreux modÃ¨les prennent en charge plusieurs variantes avec diffÃ©rentes configurations. Voracode est livrÃ© avec des variantes par dÃ©faut intÃ©grÃ©es pour les fournisseurs populaires.

### Variantes intÃ©grÃ©es

Voracode est livrÃ© avec des variantes par dÃ©faut pour de nombreux fournisseurs :

**Anthropic** :

- `high` - Budget de rÃ©flexion Ã©levÃ© (par dÃ©faut)
- `max` - Budget de rÃ©flexion maximum

**OpenAI** :

Varie selon le modÃ¨le mais en gros :

- `none` - Aucun raisonnement
- `minimal` - Effort de raisonnement minimal
- `low` - Faible effort de raisonnement
- `medium` - Effort de raisonnement moyen
- `high` â€“ Effort de raisonnement Ã©levÃ©
- `xhigh` - Effort de raisonnement trÃ¨s Ã©levÃ©

**Google**:

- `low` â€“ Budget d'effort/jetons rÃ©duit
- `high` â€“ Budget d'effort/de jetons plus Ã©levÃ©

:::tip
Cette liste n'est pas exhaustive. De nombreux autres fournisseurs ont Ã©galement des paramÃ¨tres par dÃ©faut intÃ©grÃ©s.
:::

### Variantes personnalisÃ©es

Vous pouvez remplacer les variantes existantes ou ajouter les vÃ´tres :

```jsonc title="voracode.jsonc" {7-18}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "openai": {
      "models": {
        "gpt-5": {
          "variants": {
            "thinking": {
              "reasoningEffort": "high",
              "textVerbosity": "low",
            },
            "fast": {
              "disabled": true,
            },
          },
        },
      },
    },
  },
}
```

### Cycle des variantes

Utilisez le raccourci clavier `variant_cycle` pour basculer rapidement entre les variantes. [En savoir plus](/docs/keybinds).

---

## Chargement des modÃ¨les

Lorsque Voracode dÃ©marre, il recherche les modÃ¨les dans l'ordre de prioritÃ© suivant :

1. Le flag de ligne de commande `--model` ou `-m`. Le format est le mÃªme que dans le fichier de configuration : `provider_id/model_id`.

2. La liste des modÃ¨les dans la configuration Voracode.

   ```json title="voracode.json"
   {
     "$schema": "https://voracode.ai/config.json",
     "model": "anthropic/claude-sonnet-4-20250514"
   }
   ```

Le format ici est `provider/model`.

3. Le dernier modÃ¨le utilisÃ©.

4. Le premier modÃ¨le utilisant une prioritÃ© interne.
