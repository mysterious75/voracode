---
title: GitLab
description: Utilisez Voracode dans les issues GitLab et les merge requests.
---

Voracode s'intÃ¨gre Ã  votre flux de travail GitLab via votre pipeline CI/CD GitLab ou avec GitLab Duo.

Dans les deux cas, Voracode fonctionnera sur vos runners GitLab.

---

## GitLab CI

Voracode fonctionne dans un pipeline GitLab rÃ©gulier. Vous pouvez l'intÃ©grer dans un pipeline en tant que [composant CI](https://docs.gitlab.com/ee/ci/components/)

Ici, nous utilisons un composant CI/CD crÃ©Ã© par la communautÃ© pour Voracode â€” [nagyv/gitlab-voracode](https://gitlab.com/nagyv/gitlab-voracode).

---

### FonctionnalitÃ©s

- **Utiliser une configuration personnalisÃ©e par tÃ¢che** : configurez Voracode avec un rÃ©pertoire de configuration personnalisÃ©, par exemple `./config/#custom-directory` pour activer ou dÃ©sactiver la fonctionnalitÃ© pour chaque appel d'Voracode.
- **Configuration minimale** : le composant CI configure Voracode en arriÃ¨re-plan, il vous suffit de crÃ©er la configuration Voracode et le prompt initial.
- **Flexible** : le composant CI prend en charge plusieurs entrÃ©es pour personnaliser son comportement

---

### Installation

1. Stockez votre authentification Voracode JSON en tant que variables d'environnement CI de type fichier sous **ParamÃ¨tres** > **CI/CD** > **Variables**. Assurez-vous de les marquer comme Â« MasquÃ©es et protÃ©gÃ©es Â».
2. Ajoutez ce qui suit Ã  votre fichier `.gitlab-ci.yml`.

   ```yaml title=".gitlab-ci.yml"
   include:
     - component: $CI_SERVER_FQDN/nagyv/gitlab-voracode/voracode@2
       inputs:
         config_dir: ${CI_PROJECT_DIR}/voracode-config
         auth_json: $VORACODE_AUTH_JSON # The variable name for your Voracode authentication JSON
         command: optional-custom-command
         message: "Your prompt here"
   ```

Pour plus d'entrÃ©es et de cas d'utilisation [consultez la doc](https://gitlab.com/explore/catalog/nagyv/gitlab-voracode) pour ce composant.

---

## GitLab Duo

Voracode s'intÃ¨gre Ã  votre flux de travail GitLab.
Mentionnez `@voracode` dans un commentaire et Voracode exÃ©cutera les tÃ¢ches au sein de votre pipeline CI GitLab.

---

### FonctionnalitÃ©s

- **Triage d'issues** : demandez Ã  Voracode d'examiner une issue et de vous l'expliquer.
- **Correction et implÃ©mentation** : demandez Ã  Voracode de rÃ©soudre une issue ou d'implÃ©menter une fonctionnalitÃ©.
  Cela crÃ©era une nouvelle branche et dÃ©clenchera une merge request avec les modifications.
- **SÃ©curisÃ©** : Voracode fonctionne sur vos runners GitLab.

---

### Installation

Voracode s'exÃ©cute dans votre pipeline CI/CD GitLab. Voici ce dont vous aurez besoin pour le configurer :

:::tip
Consultez la [**documentation GitLab**](https://docs.gitlab.com/user/duo_agent_platform/agent_assistant/) pour obtenir des instructions Ã  jour.
:::

1.  Configurez votre environnement GitLab
2.  Configurer CI/CD
3.  Obtenez la clÃ© API d'un fournisseur de modÃ¨les d'IA
4.  CrÃ©er un compte de service
5.  Configurer les variables CI/CD
6.  CrÃ©ez un fichier de configuration de flux, voici un exemple :

        <details>

    <summary>Configuration du flux</summary>

    ```yaml
    image: node:22-slim
    commands:
      - echo "Installing voracode"
      - npm install --global voracode-ai
      - echo "Installing glab"
      - export GITLAB_TOKEN=$GITLAB_TOKEN_VORACODE
      - apt-get update --quiet && apt-get install --yes curl wget gpg git && rm --recursive --force /var/lib/apt/lists/*
      - curl --silent --show-error --location "https://raw.githubusercontent.com/upciti/wakemeops/main/assets/install_repository" | bash
      - apt-get install --yes glab
      - echo "Configuring glab"
      - echo $GITLAB_HOST
      - echo "Creating Voracode auth configuration"
      - mkdir --parents ~/.local/share/voracode
      - |
        cat > ~/.local/share/voracode/auth.json << EOF
        {
          "anthropic": {
            "type": "api",
            "key": "$ANTHROPIC_API_KEY"
          }
        }
        EOF
      - echo "Configuring git"
      - git config --global user.email "voracode@gitlab.com"
      - git config --global user.name "Voracode"
      - echo "Testing glab"
      - glab issue list
      - echo "Running Voracode"
      - |
        voracode run "
        You are an AI assistant helping with GitLab operations.

        Context: $AI_FLOW_CONTEXT
        Task: $AI_FLOW_INPUT
        Event: $AI_FLOW_EVENT

        Please execute the requested task using the available GitLab tools.
        Be thorough in your analysis and provide clear explanations.

        <important>
        Please use the glab CLI to access data from GitLab. The glab CLI has already been authenticated. You can run the corresponding commands.

        If you are asked to summarize an MR or issue or asked to provide more information then please post back a note to the MR/Issue so that the user can see it.
        You don't need to commit or push up changes, those will be done automatically based on the file changes you make.
        </important>
        "
      - git checkout --branch $CI_WORKLOAD_REF origin/$CI_WORKLOAD_REF
      - echo "Checking for git changes and pushing if any exist"
      - |
        if ! git diff --quiet || ! git diff --cached --quiet || [ --not --zero "$(git ls-files --others --exclude-standard)" ]; then
          echo "Git changes detected, adding and pushing..."
          git add .
          if git diff --cached --quiet; then
            echo "No staged changes to commit"
          else
            echo "Committing changes to branch: $CI_WORKLOAD_REF"
            git commit --message "Codex changes"
            echo "Pushing changes up to $CI_WORKLOAD_REF"
            git push https://gitlab-ci-token:$GITLAB_TOKEN@$GITLAB_HOST/gl-demo-ultimate-dev-ai-epic-17570/test-java-project.git $CI_WORKLOAD_REF
            echo "Changes successfully pushed"
          fi
        else
          echo "No git changes detected, skipping push"
        fi
    variables:
      - ANTHROPIC_API_KEY
      - GITLAB_TOKEN_VORACODE
      - GITLAB_HOST
    ```

        </details>

Vous pouvez vous rÃ©fÃ©rer aux [GitLab CLI agents docs](https://docs.gitlab.com/user/duo_agent_platform/agent_assistant/) pour des instructions dÃ©taillÃ©es.

---

### Exemples

Voici quelques exemples de la faÃ§on dont vous pouvez utiliser Voracode dans GitLab.

:::tip
Vous pouvez configurer pour utiliser une phrase de dÃ©clenchement diffÃ©rente de `@voracode`.
:::

- **Expliquer une issue**

Ajoutez ce commentaire dans une issue GitLab.

```
  @voracode explain this issue
```

Voracode lira l'issue et rÃ©pondra avec une explication claire.

- **RÃ©soudre une issue**

Dans une issue GitLab, dites :

```
  @voracode fix this
```

Voracode crÃ©era une nouvelle branche, mettra en Å“uvre les modifications et ouvrira une merge request avec les modifications.

- **Revue de merge request**

Laissez le commentaire suivant sur une merge request GitLab.

```
  @voracode review this merge request
```

Voracode examinera la merge request et fournira des commentaires.
