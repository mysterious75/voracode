---
title: Introduction
description: Commencez avec Voracode.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"
import config from "../../../../config.mjs"
export const console = config.console

[**Voracode**](/) est un agent de codage d'IA open source. Il est disponible sous forme d'interface basÃ©e sur un terminal, d'application de bureau ou d'extension IDE.

![Voracode TUI avec le thÃ¨me voracode](../../../assets/lander/screenshot.png)

CommenÃ§ons.

---

#### PrÃ©requis

Pour utiliser Voracode dans votre terminal, vous aurez besoin de :

1. Un Ã©mulateur de terminal moderne comme :
   - [WezTerm](https://wezterm.org), multiplateforme
   - [Alacritty](https://alacritty.org), multiplateforme
   - [Ghostty](https://ghostty.org), Linux et macOS
   - [Kitty](https://sw.kovidgoyal.net/kitty/), Linux et macOS

2. ClÃ©s API pour les fournisseurs de LLM que vous souhaitez utiliser.

---

## Installation

Le moyen le plus simple d'installer Voracode est d'utiliser le script d'installation.

```bash
curl -fsSL https://voracode.ai/install | bash
```

Vous pouvez Ã©galement l'installer avec les commandes suivantes :

- **Node.js**

    <Tabs>

      <TabItem label="npm">
      ```bash
      npm install -g voracode-ai
      ```

      </TabItem>

      <TabItem label="Bun">
      ```bash
      bun install -g voracode-ai
      ```

      </TabItem>

      <TabItem label="pnpm">
      ```bash
      pnpm install -g voracode-ai
      ```

      </TabItem>

      <TabItem label="Yarn">
      ```bash
      yarn global add voracode-ai
      ```

      </TabItem>

  </Tabs>

- **Via Homebrew sur macOS et Linux**

  ```bash
  brew install mysterious75/tap/voracode
  ```

  > Nous vous recommandons d'utiliser le tap Voracode pour les versions les plus rÃ©centes. La formule officielle `brew install voracode` est maintenue par l'Ã©quipe Homebrew et est mise Ã  jour moins frÃ©quemment.

- **Via Paru sur Arch Linux**

  ```bash
  sudo pacman -S voracode           # Arch Linux (Stable)
  paru -S voracode-bin              # Arch Linux (Latest from AUR)
  ```

#### Windows

:::tip[RecommandÃ© : utilisez WSL]
Pour une expÃ©rience optimale sur Windows, nous vous recommandons d'utiliser le [Windows Subsystem for Linux (WSL)](/docs/windows-wsl). Il offre de meilleures performances et une compatibilitÃ© totale avec les fonctionnalitÃ©s de Voracode.
:::

- **Via Chocolatey**

  ```bash
  choco install voracode
  ```

- **Via Scoop**

  ```bash
  scoop install voracode
  ```

- **Via NPM**

  ```bash
  npm install -g voracode-ai
  ```

- **Via Mise**

  ```bash
  mise use -g github:mysterious75/voracode
  ```

- **Via Docker**

  ```bash
  docker run -it --rm ghcr.io/mysterious75/voracode
  ```

Le support de l'installation de Voracode sur Windows Ã  l'aide de Bun est actuellement en cours de dÃ©veloppement.

Vous pouvez Ã©galement rÃ©cupÃ©rer le binaire dans les [Releases](https://github.com/mysterious75/voracode/releases).

---

## Configuration

Avec Voracode, vous pouvez utiliser n'importe quel fournisseur de LLM en configurant ses clÃ©s API.

Si vous dÃ©butez avec les fournisseurs de LLM, nous vous recommandons d'utiliser [Voracode Zen](/docs/zen).
C'est une liste curÃ©e de modÃ¨les qui ont Ã©tÃ© testÃ©s et vÃ©rifiÃ©s par l'Ã©quipe Voracode.

1. ExÃ©cutez la commande `/connect` dans le TUI, sÃ©lectionnez voracode et allez sur [voracode.ai/auth](https://voracode.ai/auth).

   ```txt
   /connect
   ```

2. Connectez-vous, ajoutez vos informations de facturation et copiez votre clÃ© API.

3. Collez votre clÃ© API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

Vous pouvez Ã©galement sÃ©lectionner l'un des autres fournisseurs. [En savoir plus](/docs/providers#directory).

---

## Initialisation

Maintenant que vous avez configurÃ© un fournisseur, vous pouvez accÃ©der Ã  un projet sur lequel vous voulez travailler.

```bash
cd /path/to/project
```

Et exÃ©cutez Voracode.

```bash
voracode
```

Ensuite, initialisez Voracode pour le projet en exÃ©cutant la commande suivante.

```bash frame="none"
/init
```

Cela permettra Ã  Voracode d'analyser votre projet et de crÃ©er un fichier `AGENTS.md` Ã  la racine du projet.

:::tip
Vous devez valider le fichier `AGENTS.md` de votre projet dans Git.
:::

Cela aide Voracode Ã  comprendre la structure du projet et les modÃ¨les de codage utilisÃ©s.

---

## Utilisation

Vous Ãªtes maintenant prÃªt Ã  utiliser Voracode pour travailler sur votre projet. N'hÃ©sitez pas Ã  lui demander n'importe quoi !

Si vous dÃ©butez dans l'utilisation d'un agent de codage IA, voici quelques exemples qui pourraient aider.

---

### Poser des questions

Vous pouvez demander Ã  Voracode de vous expliquer la base de code.

:::tip
Utilisez la touche `@` pour effectuer une recherche floue de fichiers dans le projet.
:::

```txt frame="none" "@packages/functions/src/api/index.ts"
How is authentication handled in @packages/functions/src/api/index.ts
```

Ceci est utile s'il y a une partie de la base de code sur laquelle vous n'avez pas travaillÃ©.

---

### Ajouter des fonctionnalitÃ©s

Vous pouvez demander Ã  Voracode d'ajouter de nouvelles fonctionnalitÃ©s Ã  votre projet. Cependant, nous recommandons dâ€™abord de lui demander de crÃ©er un plan.

1. **CrÃ©er un plan**

Voracode dispose d'un _Mode Plan_ qui dÃ©sactive sa capacitÃ© Ã  apporter des modifications et suggÃ¨re plutÃ´t _comment_ il implÃ©mentera la fonctionnalitÃ©.

AccÃ©dez-y Ã  l'aide de la touche **Tab**. Vous verrez un indicateur Ã  cet effet dans le coin infÃ©rieur droit.

```bash frame="none" title="Switch to Plan mode"
   <TAB>
```

DÃ©crivons maintenant ce que nous voulons qu'il fasse.

```txt frame="none"
   When a user deletes a note, we'd like to flag it as deleted in the database.
   Then create a screen that shows all the recently deleted notes.
   From this screen, the user can undelete a note or permanently delete it.
```

Vous souhaitez donner Ã  Voracode suffisamment de dÃ©tails pour comprendre ce que vous voulez. Ã‡a aide pour lui parler comme si vous parliez Ã  un dÃ©veloppeur junior de votre Ã©quipe.

:::tip
Donnez Ã  Voracode beaucoup de contexte et d'exemples pour l'aider Ã  comprendre ce que vous voulez.
:::

2. **ItÃ©rer sur le plan**

Une fois qu'il vous donne un plan, vous pouvez lui faire part de vos commentaires ou ajouter plus de dÃ©tails.

```txt frame="none"
   We'd like to design this new screen using a design I've used before.
   [Image #1] Take a look at this image and use it as a reference.
```

:::tip
Glissez-dÃ©posez des images dans le terminal pour les ajouter Ã  l'invite.
:::

Voracode peut scanner toutes les images que vous lui donnez et les ajouter Ã  l'invite. Vous pouvez le faire en glissant et en dÃ©posant une image dans le terminal.

3. **CrÃ©ez la fonctionnalitÃ©**

Une fois que vous vous sentez Ã  l'aise avec le plan, revenez au _Mode Build_ en appuyant Ã  nouveau sur la touche **Tab**.

```bash frame="none"
   <TAB>
```

Et demandez-lui d'apporter les modifications.

```bash frame="none"
   Sounds good! Go ahead and make the changes.
```

---

### Apporter des modifications

Pour des modifications plus simples, vous pouvez demander Ã  Voracode de construire directement sans avoir Ã  revoir le plan au prÃ©alable.

```txt frame="none" "@packages/functions/src/settings.ts" "@packages/functions/src/notes.ts"
We need to add authentication to the /settings route. Take a look at how this is
handled in the /notes route in @packages/functions/src/notes.ts and implement
the same logic in @packages/functions/src/settings.ts
```

Vous devez fournir suffisamment de dÃ©tails pour qu'Voracode effectue les bonnes modifications.

---

### Annuler les modifications

Disons que vous demandez Ã  Voracode d'apporter quelques modifications.

```txt frame="none" "@packages/functions/src/api/index.ts"
Can you refactor the function in @packages/functions/src/api/index.ts?
```

Mais vous rÃ©alisez que ce nâ€™est pas ce que vous vouliez. Vous **pouvez annuler** les modifications Ã  l'aide de la commande `/undo`.

```bash frame="none"
/undo
```

Voracode annulera dÃ©sormais les modifications que vous avez apportÃ©es et affichera votre message d'origine encore.

```txt frame="none" "@packages/functions/src/api/index.ts"
Can you refactor the function in @packages/functions/src/api/index.ts?
```

Ã€ partir de lÃ , vous pouvez modifier l'invite et demander Ã  Voracode de rÃ©essayer.

:::tip
Vous pouvez exÃ©cuter `/undo` plusieurs fois pour annuler plusieurs modifications.
:::

Ou vous **pouvez refaire** les modifications Ã  l'aide de la commande `/redo`.

```bash frame="none"
/redo
```

---

## Partager

Les conversations que vous avez avec Voracode peuvent Ãªtre [partagÃ©es avec votre Ã©quipe](/docs/share).

```bash frame="none"
/share
```

Cela crÃ©era un lien vers la conversation en cours et le copiera dans votre presse-papiers.

:::note
Les conversations ne sont pas partagÃ©es par dÃ©faut.
:::

Voici un [exemple de conversation](https://voracode.ai/s/4XP1fce5) avec Voracode.

---

## Personnaliser

Et c'est tout ! Vous Ãªtes dÃ©sormais un pro de l'utilisation de Voracode.

Pour vous l'approprier, nous vous recommandons de [choisir un thÃ¨me](/docs/themes), [de personnaliser les raccourcis clavier](/docs/keybinds), de [configurer les formateurs de code](/docs/formatters), de [crÃ©er des commandes personnalisÃ©es](/docs/commands) ou de jouer avec la [Voracode Config](/docs/config).
