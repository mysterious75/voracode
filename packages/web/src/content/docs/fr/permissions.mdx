---
title: Autorisations
description: ContrÃ´lez quelles actions nÃ©cessitent une approbation pour Ãªtre exÃ©cutÃ©es.
---

Voracode utilise la configuration `permission` pour dÃ©cider si une action donnÃ©e doit s'exÃ©cuter automatiquement, vous inviter ou Ãªtre bloquÃ©e.

Depuis `v1.1.1`, l'ancienne configuration boolÃ©enne `tools` est obsolÃ¨te et a Ã©tÃ© fusionnÃ©e dans `permission`. L'ancienne configuration `tools` est toujours prise en charge pour des raisons de compatibilitÃ© ascendante.

---

## Actions

Chaque rÃ¨gle d'autorisation se rÃ©sout en l'une des suivantes :

- `"allow"` â€“ exÃ©cuter sans approbation
- `"ask"` â€“ demande d'approbation
- `"deny"` â€” bloque l'action

---

## Configuration

Vous pouvez dÃ©finir des autorisations globalement (avec `*`) et remplacer des outils spÃ©cifiques.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "*": "ask",
    "bash": "allow",
    "edit": "deny"
  }
}
```

Vous pouvez Ã©galement dÃ©finir toutes les autorisations en mÃªme temps :

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": "allow"
}
```

---

## RÃ¨gles granulaires (syntaxe d'objet)

Pour la plupart des autorisations, vous pouvez utiliser un objet pour appliquer diffÃ©rentes actions en fonction de l'entrÃ©e de l'outil.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "bash": {
      "*": "ask",
      "git *": "allow",
      "npm *": "allow",
      "rm *": "deny",
      "grep *": "allow"
    },
    "edit": {
      "*": "deny",
      "packages/web/src/content/docs/*.mdx": "allow"
    }
  }
}
```

Les rÃ¨gles sont Ã©valuÃ©es par correspondance de modÃ¨le, la **derniÃ¨re rÃ¨gle correspondante Ã©tant gagnante**. Un modÃ¨le courant consiste Ã  placer la rÃ¨gle fourre-tout `"*"` en premier, et les rÃ¨gles plus spÃ©cifiques aprÃ¨s.

### CaractÃ¨res gÃ©nÃ©riques

Les modÃ¨les d'autorisation utilisent une simple correspondance de caractÃ¨res gÃ©nÃ©riques :

- `*` correspond Ã  zÃ©ro ou plusieurs caractÃ¨res
- `?` correspond exactement Ã  un caractÃ¨re
- Tous les autres caractÃ¨res correspondent littÃ©ralement

### Extension du rÃ©pertoire personnel

Vous pouvez utiliser `~` ou `$HOME` au dÃ©but d'un modÃ¨le pour rÃ©fÃ©rencer votre rÃ©pertoire personnel. Ceci est particuliÃ¨rement utile pour les rÃ¨gles [`external_directory`](#external-directories).

- `~/projects/*` -> `/Users/username/projects/*`
- `$HOME/projects/*` -> `/Users/username/projects/*`
- `~` -> `/Users/username`

### RÃ©pertoires externes

Utilisez `external_directory` pour autoriser les appels d'outils qui touchent des chemins en dehors du rÃ©pertoire de travail oÃ¹ Voracode a Ã©tÃ© dÃ©marrÃ©. Cela s'applique Ã  tout outil qui prend un chemin en entrÃ©e (par exemple `read`, `edit`, `glob`, `grep` et de nombreuses commandes `bash`).

L'expansion du rÃ©pertoire personnel (comme `~/...`) n'affecte que la faÃ§on dont un modÃ¨le est Ã©crit. Cela n'intÃ¨gre pas un chemin externe Ã  l'espace de travail actuel, donc les chemins en dehors du rÃ©pertoire de travail doivent toujours Ãªtre autorisÃ©s via `external_directory`.

Par exemple, cela permet d'accÃ©der Ã  tout ce qui se trouve sous `~/projects/personal/` :

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "external_directory": {
      "~/projects/personal/**": "allow"
    }
  }
}
```

Tout rÃ©pertoire autorisÃ© ici hÃ©rite des mÃªmes valeurs par dÃ©faut que l'espace de travail actuel. Ã‰tant donnÃ© que [`read` est par dÃ©faut `allow`](#defaults), les lectures sont Ã©galement autorisÃ©es pour les entrÃ©es sous `external_directory`, sauf dÃ©rogation. Ajoutez des rÃ¨gles explicites lorsqu'un outil doit Ãªtre restreint dans ces chemins, comme bloquer les modifications tout en conservant les lectures :

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "external_directory": {
      "~/projects/personal/**": "allow"
    },
    "edit": {
      "~/projects/personal/**": "deny"
    }
  }
}
```

Gardez la liste centrÃ©e sur les chemins approuvÃ©s et superposez des rÃ¨gles d'autorisation ou de refus supplÃ©mentaires selon les besoins pour d'autres outils (par exemple `bash`).

---

## Autorisations disponibles

Les autorisations Voracode sont classÃ©es par nom d'outil, plus quelques garde-fous de sÃ©curitÃ© :

- `read` â€” lecture d'un fichier (correspond au chemin du fichier)
- `edit` â€” toutes les modifications de fichiers (couvre `edit`, `write`, `patch`)
- `glob` â€” globalisation de fichiers (correspond au modÃ¨le global)
- `grep` â€” recherche de contenu (correspond au modÃ¨le regex)
- `bash` - exÃ©cution de commandes shell (correspond aux commandes analysÃ©es comme `git status --porcelain`)
- `task` â€” lancement de sous-agents (correspond au type de sous-agent)
- `skill` â€” chargement d'une compÃ©tence (correspond au nom de la compÃ©tence)
- `lsp` â€” exÃ©cution de requÃªtes LSP (actuellement non granulaires)
- `webfetch` â€” rÃ©cupÃ©rer une URL (correspond Ã  l'URL)
- `websearch` â€” recherche Web (correspond Ã  la requÃªte)
- `external_directory` - dÃ©clenchÃ© lorsqu'un outil touche des chemins en dehors du rÃ©pertoire de travail du projet
- `doom_loop` â€” dÃ©clenchÃ© lorsque le mÃªme appel d'outil se rÃ©pÃ¨te 3 fois avec une entrÃ©e identique

---

## Valeurs par dÃ©faut

Si vous ne spÃ©cifiez rien, Voracode dÃ©marre avec les valeurs par dÃ©faut permissives :

- La plupart des autorisations sont par dÃ©faut `"allow"`.
- `doom_loop` et `external_directory` sont par dÃ©faut `"ask"`.
- `read` est `"allow"`, mais les fichiers `.env` sont refusÃ©s par dÃ©faut :

```json title="voracode.json"
{
  "permission": {
    "read": {
      "*": "allow",
      "*.env": "deny",
      "*.env.*": "deny",
      "*.env.example": "allow"
    }
  }
}
```

---

## Que fait Â« Demander Â» ?

Lorsque Voracode demande l'approbation, l'interface utilisateur propose trois rÃ©sultats :

- `once` â€” approuve uniquement cette demande
- `always` â€” approuve les futures demandes correspondant aux modÃ¨les suggÃ©rÃ©s (pour le reste de la session Voracode en cours)
- `reject` â€” refuser la demande

L'ensemble des modÃ¨les que `always` approuverait est fourni par l'outil (par exemple, les approbations bash mettent gÃ©nÃ©ralement sur liste blanche un prÃ©fixe de commande sÃ»r comme `git status*`).

---

## Agents

Vous pouvez remplacer les autorisations par agent. Les autorisations des agents sont fusionnÃ©es avec la configuration globale et les rÃ¨gles des agents sont prioritaires. [En savoir plus](/docs/agents#permissions) sur les autorisations des agents.

:::note
Reportez-vous Ã  la section [RÃ¨gles granulaires (syntaxe d'objet)](#granular-rules-object-syntax) ci-dessus pour des exemples de correspondance de modÃ¨les plus dÃ©taillÃ©s.
:::

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "bash": {
      "*": "ask",
      "git *": "allow",
      "git commit *": "deny",
      "git push *": "deny",
      "grep *": "allow"
    }
  },
  "agent": {
    "build": {
      "permission": {
        "bash": {
          "*": "ask",
          "git *": "allow",
          "git commit *": "ask",
          "git push *": "deny",
          "grep *": "allow"
        }
      }
    }
  }
}
```

Vous pouvez Ã©galement configurer les autorisations des agents dans Markdown :

```markdown title="~/.config/voracode/agents/review.md"
---
description: Code review without edits
mode: subagent
permission:
  edit: deny
  bash: ask
  webfetch: deny
---

Only analyze code and suggest changes.
```

:::tip
Utilisez la correspondance de modÃ¨les pour les commandes avec des arguments. `"grep *"` autorise `grep pattern file.txt`, tandis que `"grep"` seul le bloquerait. Les commandes comme `git status` fonctionnent pour le comportement par dÃ©faut mais nÃ©cessitent une autorisation explicite (comme `"git status *"`) lorsque des arguments sont passÃ©s.
:::
