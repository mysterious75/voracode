---
title: Serveurs MCP
description: Ajoutez des outils MCP locaux et distants.
---

Vous pouvez ajouter des outils externes Ã  Voracode Ã  l'aide du _Model Context Protocol_ ou MCP. Voracode prend en charge les serveurs locaux et distants.

Une fois ajoutÃ©s, les outils MCP sont automatiquement disponibles pour le LLM aux cÃ´tÃ©s des outils intÃ©grÃ©s.

---

#### Mises en garde

Lorsque vous utilisez un serveur MCP, cela ajoute au contexte. Cela peut vite sâ€™additionner si vous disposez de beaucoup dâ€™outils. Nous vous recommandons donc de faire attention aux serveurs MCP que vous utilisez.

:::tip
Les serveurs MCP s'ajoutent Ã  votre contexte, vous devez donc faire attention Ã  ceux que vous activez.
:::

Certains serveurs MCP, comme le serveur GitHub MCP, ont tendance Ã  ajouter beaucoup de jetons et peuvent facilement dÃ©passer la limite de contexte.

---

## Activation

Vous pouvez dÃ©finir des serveurs MCP dans votre [Voracode Config](https://voracode.ai/docs/config/) sous `mcp`. Ajoutez chaque MCP avec un nom unique. Vous pouvez faire rÃ©fÃ©rence Ã  ce MCP par son nom lorsque vous demandez le LLM.

```jsonc title="voracode.jsonc" {6}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "name-of-mcp-server": {
      // ...
      "enabled": true,
    },
    "name-of-other-mcp-server": {
      // ...
    },
  },
}
```

Vous pouvez Ã©galement dÃ©sactiver un serveur en dÃ©finissant `enabled` sur `false`. Ceci est utile si vous souhaitez dÃ©sactiver temporairement un serveur sans le supprimer de votre configuration.

---

### Remplacement des valeurs par dÃ©faut distantes

Les organisations peuvent fournir des serveurs MCP par dÃ©faut via leur point de terminaison `.well-known/voracode`. Ces serveurs peuvent Ãªtre dÃ©sactivÃ©s par dÃ©faut, permettant aux utilisateurs de choisir ceux dont ils ont besoin.

Pour activer un serveur spÃ©cifique Ã  partir de la configuration distante de votre organisation, ajoutez-le Ã  votre configuration locale avec `enabled: true` :

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "jira": {
      "type": "remote",
      "url": "https://jira.example.com/mcp",
      "enabled": true
    }
  }
}
```

Vos valeurs de configuration locales remplacent les valeurs par dÃ©faut distantes. Voir [config precedence](/docs/config#precedence-order) pour plus de dÃ©tails.

---

## Local

Ajoutez des serveurs MCP locaux en dÃ©finissant `type` sur `"local"` dans l'objet MCP.

```jsonc title="voracode.jsonc" {15}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-local-mcp-server": {
      "type": "local",
      // Or ["bun", "x", "my-mcp-command"]
      "command": ["npx", "-y", "my-mcp-command"],
      "enabled": true,
      "environment": {
        "MY_ENV_VAR": "my_env_var_value",
      },
    },
  },
}
```

La commande indique comment le serveur MCP local est dÃ©marrÃ©. Vous pouvez Ã©galement transmettre une liste de variables dâ€™environnement.

Par exemple, voici comment ajouter le serveur de test [`@modelcontextprotocol/server-everything`](https://www.npmjs.com/package/@modelcontextprotocol/server-everything) MCP.

```jsonc title="voracode.jsonc"
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "mcp_everything": {
      "type": "local",
      "command": ["npx", "-y", "@modelcontextprotocol/server-everything"],
    },
  },
}
```

Et pour l'utiliser, je peux ajouter `use the mcp_everything tool` Ã  mes prompts.

```txt "mcp_everything"
use the mcp_everything tool to add the number 3 and 4
```

---

#### Options

Voici toutes les options pour configurer un serveur MCP local.

| Options       | Type    | Obligatoire | Description                                                                                                                  |
| ------------- | ------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------- |
| `type`        | ChaÃ®ne  | Oui         | Le type de connexion au serveur MCP doit Ãªtre `"local"`.                                                                     |
| `command`     | Tableau | Oui         | Commande et arguments pour exÃ©cuter le serveur MCP.                                                                          |
| `environment` | Objet   |             | Variables d'environnement Ã  dÃ©finir lors de l'exÃ©cution du serveur.                                                          |
| `enabled`     | BoolÃ©en |             | Activez ou dÃ©sactivez le serveur MCP au dÃ©marrage.                                                                           |
| `timeout`     | NumÃ©ro  |             | DÃ©lai d'expiration en ms pour la rÃ©cupÃ©ration des outils depuis le serveur MCP. La valeur par dÃ©faut est 5 000 (5 secondes). |

---

## Distant

Ajoutez des serveurs MCP distants en dÃ©finissant `type` sur `"remote"`.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-remote-mcp": {
      "type": "remote",
      "url": "https://my-mcp-server.com",
      "enabled": true,
      "headers": {
        "Authorization": "Bearer MY_API_KEY"
      }
    }
  }
}
```

Le champ `url` est l'URL du serveur MCP distant et l'option `headers` vous permet de transmettre des en-tÃªtes.

---

#### Options

| Options   | Type    | Obligatoire | Description                                                                                                                  |
| --------- | ------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------- |
| `type`    | ChaÃ®ne  | Oui         | Le type de connexion au serveur MCP doit Ãªtre `"remote"`.                                                                    |
| `url`     | ChaÃ®ne  | Oui         | URL du serveur MCP distant.                                                                                                  |
| `enabled` | BoolÃ©en |             | Activez ou dÃ©sactivez le serveur MCP au dÃ©marrage.                                                                           |
| `headers` | Objet   |             | En-tÃªtes Ã  envoyer avec la demande.                                                                                          |
| `oauth`   | Objet   |             | Configuration de l'authentification OAuth. Voir la section [OAuth](#oauth) ci-dessous.                                       |
| `timeout` | NumÃ©ro  |             | DÃ©lai d'expiration en ms pour la rÃ©cupÃ©ration des outils depuis le serveur MCP. La valeur par dÃ©faut est 5 000 (5 secondes). |

---

## OAuth

Voracode gÃ¨re automatiquement l'authentification OAuth pour les serveurs MCP distants. Lorsqu'un serveur nÃ©cessite une authentification, Voracode :

1. DÃ©tecte la rÃ©ponse 401 et lance le flux OAuth
2. Utilise **Enregistrement client dynamique (RFC 7591)** s'il est pris en charge par le serveur.
3. Stocke les jetons en toute sÃ©curitÃ© pour les demandes futures

---

### Automatique

Pour la plupart des serveurs MCP compatibles OAuth, aucune configuration particuliÃ¨re n'est nÃ©cessaire. Configurez simplement le serveur distant :

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-oauth-server": {
      "type": "remote",
      "url": "https://mcp.example.com/mcp"
    }
  }
}
```

Si le serveur nÃ©cessite une authentification, Voracode vous demandera de vous authentifier lorsque vous essayez de l'utiliser pour la premiÃ¨re fois. Sinon, vous pouvez [dÃ©clencher manuellement le flux](#authenticating) avec `voracode mcp auth <server-name>`.

---

### PrÃ©-enregistrÃ©

Si vous disposez des informations d'identification client du fournisseur de serveur MCP, vous pouvez les configurer :

```json title="voracode.json" {7-11}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-oauth-server": {
      "type": "remote",
      "url": "https://mcp.example.com/mcp",
      "oauth": {
        "clientId": "{env:MY_MCP_CLIENT_ID}",
        "clientSecret": "{env:MY_MCP_CLIENT_SECRET}",
        "scope": "tools:read tools:execute"
      }
    }
  }
}
```

---

### Authentification

Vous pouvez dÃ©clencher manuellement l'authentification ou gÃ©rer les informations d'identification.

Authentifiez-vous auprÃ¨s d'un serveur MCP spÃ©cifique :

```bash
voracode mcp auth my-oauth-server
```

RÃ©pertoriez tous les serveurs MCP et leur statut d'authentification :

```bash
voracode mcp list
```

Supprimez les informations d'identification stockÃ©es :

```bash
voracode mcp logout my-oauth-server
```

La commande `mcp auth` ouvrira votre navigateur pour autorisation. AprÃ¨s votre autorisation, Voracode stockera les jetons en toute sÃ©curitÃ© dans `~/.local/share/voracode/mcp-auth.json`.

---

#### DÃ©sactivation d'OAuth

Si vous souhaitez dÃ©sactiver le OAuth automatique pour un serveur (par exemple, pour les serveurs qui utilisent les clÃ©s API Ã  la place), dÃ©finissez `oauth` sur `false` :

```json title="voracode.json" {7}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-api-key-server": {
      "type": "remote",
      "url": "https://mcp.example.com/mcp",
      "oauth": false,
      "headers": {
        "Authorization": "Bearer {env:MY_API_KEY}"
      }
    }
  }
}
```

---

#### Options OAuth

| Options        | Type          | Description                                                                              |
| -------------- | ------------- | ---------------------------------------------------------------------------------------- |
| `oauth`        | Objet \| faux | Objet de configuration OAuth, ou `false` pour dÃ©sactiver la dÃ©tection automatique OAuth. |
| `clientId`     | ChaÃ®ne        | ID client OAuth. Sâ€™il nâ€™est pas fourni, lâ€™enregistrement dynamique du client sera tentÃ©. |
| `clientSecret` | ChaÃ®ne        | OAuth secret client, si requis par le serveur d'autorisation.                            |
| `scope`        | ChaÃ®ne        | OAuth scopes Ã  demander lors de l'autorisation.                                          |

#### DÃ©bogage

Si un serveur MCP distant ne parvient pas Ã  s'authentifier, vous pouvez diagnostiquer les problÃ¨mes avec :

```bash
# View auth status for all OAuth-capable servers
voracode mcp auth list

# Debug connection and OAuth flow for a specific server
voracode mcp debug my-oauth-server
```

La commande `mcp debug` affiche l'Ã©tat d'authentification actuel, teste la connectivitÃ© HTTP et tente le flux de dÃ©couverte OAuth.

---

## Gestion

Vos MCP sont disponibles sous forme d'outils dans Voracode, aux cÃ´tÃ©s des outils intÃ©grÃ©s. Vous pouvez donc les gÃ©rer via la configuration Voracode comme n'importe quel autre outil.

---

### Global

Cela signifie que vous pouvez les activer ou les dÃ©sactiver globalement.

```json title="voracode.json" {14}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-mcp-foo": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command-foo"]
    },
    "my-mcp-bar": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command-bar"]
    }
  },
  "tools": {
    "my-mcp-foo": false
  }
}
```

Nous pouvons Ã©galement utiliser un modÃ¨le global pour dÃ©sactiver tous les MCP correspondants.

```json title="voracode.json" {14}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-mcp-foo": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command-foo"]
    },
    "my-mcp-bar": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command-bar"]
    }
  },
  "tools": {
    "my-mcp*": false
  }
}
```

Ici, nous utilisons le modÃ¨le global `my-mcp*` pour dÃ©sactiver tous les MCP.

---

### Par agent

Si vous disposez d'un grand nombre de serveurs MCP, vous souhaiterez peut-Ãªtre les activer uniquement par agent et les dÃ©sactiver globalement. Pour ce faire :

1. DÃ©sactivez-le en tant qu'outil Ã  l'Ã©chelle mondiale.
2. Dans votre [agent config](/docs/agents#tools), activez le serveur MCP en tant qu'outil.

```json title="voracode.json" {11, 14-18}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-mcp": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command"],
      "enabled": true
    }
  },
  "tools": {
    "my-mcp*": false
  },
  "agent": {
    "my-agent": {
      "tools": {
        "my-mcp*": true
      }
    }
  }
}
```

---

#### ModÃ¨les globaux

Le modÃ¨le glob utilise des modÃ¨les de globbing regex simples :

- `*` correspond Ã  zÃ©ro ou plusieurs caractÃ¨res (par exemple, `"my-mcp*"` correspond Ã  `my-mcp_search`, `my-mcp_list`, etc.)
- `?` correspond exactement Ã  un caractÃ¨re
- Tous les autres caractÃ¨res correspondent littÃ©ralement

:::note
Les outils serveur MCP sont enregistrÃ©s avec le nom du serveur comme prÃ©fixe, donc pour dÃ©sactiver tous les outils d'un serveur, utilisez simplement :

```
"mymcpservername_*": false
```

:::

---

## Exemples

Vous trouverez ci-dessous des exemples de serveurs MCP courants. Vous pouvez soumettre un PR si vous souhaitez documenter d'autres serveurs.

---

### Sentry

Ajoutez le [serveur Sentry MCP](https://mcp.sentry.dev) pour interagir avec vos projets et problÃ¨mes Sentry.

```json title="voracode.json" {4-8}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "sentry": {
      "type": "remote",
      "url": "https://mcp.sentry.dev/mcp",
      "oauth": {}
    }
  }
}
```

AprÃ¨s avoir ajoutÃ© la configuration, authentifiez-vous auprÃ¨s de Sentry :

```bash
voracode mcp auth sentry
```

Cela ouvrira une fenÃªtre de navigateur pour terminer le flux OAuth et connecter Voracode Ã  votre compte Sentry.

Une fois authentifiÃ©, vous pouvez utiliser les outils Sentry dans vos prompts pour interroger les problÃ¨mes, les projets et les donnÃ©es d'erreur.

```txt "use sentry"
Show me the latest unresolved issues in my project. use sentry
```

---

### Context7

Ajoutez le [Context7 MCP server](https://github.com/upstash/context7) pour effectuer une recherche dans les documents.

```json title="voracode.json" {4-7}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "context7": {
      "type": "remote",
      "url": "https://mcp.context7.com/mcp"
    }
  }
}
```

Si vous avez crÃ©Ã© un compte gratuit, vous pouvez utiliser votre clÃ© API et obtenir des limites de dÃ©bit plus Ã©levÃ©es.

```json title="voracode.json" {7-9}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "context7": {
      "type": "remote",
      "url": "https://mcp.context7.com/mcp",
      "headers": {
        "CONTEXT7_API_KEY": "{env:CONTEXT7_API_KEY}"
      }
    }
  }
}
```

Ici, nous supposons que la variable d'environnement `CONTEXT7_API_KEY` est dÃ©finie.

Ajoutez `use context7` Ã  vos prompts pour utiliser le serveur Context7 MCP.

```txt "use context7"
Configure a Cloudflare Worker script to cache JSON API responses for five minutes. use context7
```

Alternativement, vous pouvez ajouter quelque chose comme ceci Ã  votre [AGENTS.md](/docs/rules/).

```md title="AGENTS.md"
When you need to search docs, use `context7` tools.
```

---

### Grep by Vercel

Ajoutez le serveur [Grep by Vercel](https://grep.app) MCP pour rechercher des extraits de code sur GitHub.

```json title="voracode.json" {4-7}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "gh_grep": {
      "type": "remote",
      "url": "https://mcp.grep.app"
    }
  }
}
```

Puisque nous avons nommÃ© notre serveur MCP `gh_grep`, vous pouvez ajouter `use the gh_grep tool` Ã  vos prompts pour que l'agent l'utilise.

```txt "use the gh_grep tool"
What's the right way to set a custom domain in an SST Astro component? use the gh_grep tool
```

Alternativement, vous pouvez ajouter quelque chose comme ceci Ã  votre [AGENTS.md](/docs/rules/).

```md title="AGENTS.md"
If you are unsure how to do something, use `gh_grep` to search code examples from GitHub.
```
