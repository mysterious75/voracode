---
title: Fournisseurs
description: Utiliser nâ€™importe quel fournisseur LLM en Voracode.
---

import config from "../../../../config.mjs"
export const console = config.console

Voracode utilise [AI SDK](https://ai-sdk.dev/) et [Models.dev](https://models.dev) pour prendre en charge **75+ fournisseurs LLM** et prend en charge l'exÃ©cution de modÃ¨les locaux.

Pour ajouter un fournisseur, vous devezÂ :

1. Ajoutez les clÃ©s API pour le fournisseur Ã  l'aide de la commande `/connect`.
2. Configurez le fournisseur dans votre configuration Voracode.

---

### Informations d'identification

Lorsque vous ajoutez les clÃ©s API d'un fournisseur avec la commande `/connect`, elles sont stockÃ©es
en `~/.local/share/voracode/auth.json`.

---

### Configuration

Vous pouvez personnaliser les fournisseurs via la section `provider` de votre Voracode
configuration.

---

#### Socle URL

Vous pouvez personnaliser la base URL pour n'importe quel fournisseur en dÃ©finissant l'option `baseURL`. Ceci est utile lors de l'utilisation de services proxy ou de points de terminaison personnalisÃ©s.

```json title="voracode.json" {6}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "anthropic": {
      "options": {
        "baseURL": "https://api.anthropic.com/v1"
      }
    }
  }
}
```

---

## Voracode Zen

Voracode Zen est une liste de modÃ¨les fournis par l'Ã©quipe Voracode qui ont Ã©tÃ©
testÃ© et vÃ©rifiÃ© pour fonctionner correctement avec Voracode. [En savoir plus](/docs/zen).

:::tip
Si vous Ãªtes nouveau, nous vous recommandons de commencer par Voracode Zen.
:::

1. ExÃ©cutez la commande `/connect` dans le TUI, sÃ©lectionnez voracode et dirigez-vous vers [voracode.ai/auth](https://voracode.ai/auth).

   ```txt
   /connect
   ```

2. Connectez-vous, ajoutez vos informations de facturation et copiez votre clÃ© API.

3. Collez votre clÃ© API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez `/models` dans le TUI pour voir la liste des modÃ¨les que nous recommandons.

   ```txt
   /models
   ```

Il fonctionne comme nâ€™importe quel autre fournisseur dans Voracode et son utilisation est totalement facultative.

---

## Voracode Go

Voracode Go est un plan d'abonnement Ã  faible coÃ»t qui offre un accÃ¨s fiable aux modÃ¨les de codage ouverts populaires fournis par l'Ã©quipe Voracode qui ont Ã©tÃ©
testÃ© et vÃ©rifiÃ© pour fonctionner correctement avec Voracode.

1. ExÃ©cutez la commande `/connect` dans le TUI, sÃ©lectionnez `Voracode Go` et rendez-vous sur [voracode.ai/auth](https://voracode.ai/zen).

   ```txt
   /connect
   ```

2. Connectez-vous, ajoutez vos informations de facturation et copiez votre clÃ© API.

3. Collez votre clÃ© API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez `/models` dans le TUI pour voir la liste des modÃ¨les que nous recommandons.

   ```txt
   /models
   ```

Il fonctionne comme nâ€™importe quel autre fournisseur dans Voracode et son utilisation est totalement facultative.

---

## Annuaire

Examinons certains fournisseurs en dÃ©tail. Si vous souhaitez ajouter un fournisseur au
liste, n'hÃ©sitez pas Ã  ouvrir un PR.

:::note
Vous ne voyez pas de fournisseur ici ? Soumettez un PR.
:::

---

### 302.AI

1. Rendez-vous sur la [console 302.AI](https://302.ai/), crÃ©ez un compte et gÃ©nÃ©rez une clÃ© API.

2. ExÃ©cutez la commande `/connect` et recherchez **302.AI**.

   ```txt
   /connect
   ```

3. Saisissez votre clÃ© 302.AI API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le.

   ```txt
   /models
   ```

---

### Amazon Bedrock

Pour utiliser Amazon Bedrock avec VoracodeÂ :

1. Rendez-vous sur le **Catalogue de modÃ¨les** dans la console Amazon Bedrock et demandez
   accÃ©dez aux modÃ¨les que vous souhaitez.

   :::tip
   Vous devez avoir accÃ¨s au modÃ¨le souhaitÃ© dans Amazon Bedrock.
   :::

2. **Configurez l'authentification** Ã  l'aide de l'une des mÃ©thodes suivantesÂ :

   #### Variables d'environnement (dÃ©marrage rapide)

DÃ©finissez l'une de ces variables d'environnement lors de l'exÃ©cution de voracodeÂ :

```bash
   # Option 1: Using AWS access keys
   AWS_ACCESS_KEY_ID=XXX AWS_SECRET_ACCESS_KEY=YYY voracode

   # Option 2: Using named AWS profile
   AWS_PROFILE=my-profile voracode

   # Option 3: Using Bedrock bearer token
   AWS_BEARER_TOKEN_BEDROCK=XXX voracode
```

Ou ajoutez-les Ã  votre profil bashÂ :

```bash title="~/.bash_profile"
   export AWS_PROFILE=my-dev-profile
   export AWS_REGION=us-east-1
```

#### Fichier de configuration (recommandÃ©)

Pour une configuration spÃ©cifique au projet ou persistante, utilisez `voracode.json`Â :

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "amazon-bedrock": {
      "options": {
        "region": "us-east-1",
        "profile": "my-aws-profile"
      }
    }
  }
}
```

**Options disponiblesÂ :**

- `region` - RÃ©gion AWS (par exemple, `us-east-1`, `eu-west-1`)
- `profile` - Profil nommÃ© AWS de `~/.aws/credentials`
- `endpoint` - URL de point de terminaison personnalisÃ©e pour les endpoints VPC (alias de l'option gÃ©nÃ©rique `baseURL`)

:::tip
Les options du fichier de configuration sont prioritaires sur les variables d'environnement.
:::

#### AvancÃ©Â : points de terminaison d'un VPC

Si vous utilisez des points de terminaison d'un VPC pour BedrockÂ :

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "amazon-bedrock": {
      "options": {
        "region": "us-east-1",
        "profile": "production",
        "endpoint": "https://bedrock-runtime.us-east-1.vpce-xxxxx.amazonaws.com"
      }
    }
  }
}
```

:::note
L'option `endpoint` est un alias pour l'option gÃ©nÃ©rique `baseURL`, utilisant la terminologie spÃ©cifique Ã  AWS. Si `endpoint` et `baseURL` sont spÃ©cifiÃ©s, `endpoint` est prioritaire.
:::

#### MÃ©thodes d'authentification

- **`AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY`**Â : crÃ©ez un utilisateur IAM et gÃ©nÃ©rez des clÃ©s d'accÃ¨s dans la console AWS
- **`AWS_PROFILE`**Â : utilisez les profils nommÃ©s de `~/.aws/credentials`. Configurez d'abord avec `aws configure --profile my-profile` ou `aws sso login`
- **`AWS_BEARER_TOKEN_BEDROCK`**Â : GÃ©nÃ©rez des clÃ©s API Ã  long terme Ã  partir de la console Amazon Bedrock
- **`AWS_WEB_IDENTITY_TOKEN_FILE` / `AWS_ROLE_ARN`**Â : pour EKS IRSA (rÃ´les IAM pour les comptes de service) ou d'autres environnements Kubernetes avec fÃ©dÃ©ration OIDC. Ces variables d'environnement sont automatiquement injectÃ©es par Kubernetes lors de l'utilisation des annotations de compte de service.

#### PrioritÃ© d'authentification

Amazon Bedrock utilise la prioritÃ© d'authentification suivanteÂ :

1.  **Bearer Token** - Variable d'environnement `AWS_BEARER_TOKEN_BEDROCK` ou jeton de la commande `/connect`
2.  **AWS Credential Chain** - Profil, clÃ©s d'accÃ¨s, informations d'identification partagÃ©es, rÃ´les IAM, jetons d'identitÃ© Web (EKS IRSA), mÃ©tadonnÃ©es d'instance

:::note
Lorsqu'un jeton de porteur est dÃ©fini (via `/connect` ou `AWS_BEARER_TOKEN_BEDROCK`), il est prioritaire sur toutes les mÃ©thodes d'identification AWS, y compris les profils configurÃ©s.
:::

3. ExÃ©cutez la commande `/models` pour sÃ©lectionner le modÃ¨le souhaitÃ©.

   ```txt
   /models
   ```

:::note
Pour les profils d'infÃ©rence personnalisÃ©s, utilisez le nom du modÃ¨le et du fournisseur dans la clÃ© et dÃ©finissez la propriÃ©tÃ© `id` sur l'arn. Cela garantit une mise en cache correcteÂ :

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "amazon-bedrock": {
      // ...
      "models": {
        "anthropic-claude-sonnet-4.5": {
          "id": "arn:aws:bedrock:us-east-1:xxx:application-inference-profile/yyy"
        }
      }
    }
  }
}
```

:::

---

### Anthropic

1. Une fois inscrit, exÃ©cutez la commande `/connect` et sÃ©lectionnez Anthropic.

   ```txt
   /connect
   ```

2. Ici, vous pouvez sÃ©lectionner l'option **Claude Pro/Max** et cela ouvrira votre navigateur.
   et vous demande de vous authentifier.

   ```txt
   â”Œ Select auth method
   â”‚
   â”‚ Claude Pro/Max
   â”‚ Create an API Key
   â”‚ Manually enter API Key
   â””
   ```

3. DÃ©sormais, tous les modÃ¨les Anthropic devraient Ãªtre disponibles lorsque vous utilisez la commande `/models`.

   ```txt
   /models
   ```

:::info
L'utilisation de votre abonnement Claude Pro/Max dans Voracode n'est pas officiellement prise en charge par [Anthropic](https://anthropic.com).
:::

##### Utilisation des clÃ©s API

Vous pouvez Ã©galement sÃ©lectionner **CrÃ©er une clÃ© API** si vous n'avez pas d'abonnement Pro/Max. Il ouvrira Ã©galement votre navigateur et vous demandera de vous connecter Ã  Anthropic et vous donnera un code que vous pourrez coller dans votre terminal.

Ou si vous disposez dÃ©jÃ  d'une clÃ© API, vous pouvez sÃ©lectionner **Entrer manuellement la clÃ© API** et la coller dans votre terminal.

---

### Atomic Chat

Vous pouvez configurer voracode pour utiliser des modÃ¨les locaux via [Atomic Chat](https://atomic.chat), une application de bureau qui exÃ©cute des LLM locaux derriÃ¨re un serveur API compatible OpenAI (point de terminaison par dÃ©faut `http://127.0.0.1:1337/v1`).

```json title="voracode.json" "atomic-chat" {5, 6, 8, 10-14}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "atomic-chat": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Atomic Chat (local)",
      "options": {
        "baseURL": "http://127.0.0.1:1337/v1"
      },
      "models": {
        "<your-model-id>": {
          "name": "<your-model-name>"
        }
      }
    }
  }
}
```

Dans cet exemple :

- `atomic-chat` est l'identifiant personnalisÃ© du fournisseur. Il peut s'agir de n'importe quelle chaÃ®ne.
- `npm` spÃ©cifie le paquet Ã  utiliser pour ce fournisseur. Ici, `@ai-sdk/openai-compatible` est utilisÃ© pour toute API compatible OpenAI.
- `name` est le nom du fournisseur affichÃ© dans l'interface.
- `options.baseURL` est le point de terminaison du serveur local. Modifiez l'hÃ´te et le port selon votre configuration Atomic Chat.
- `models` est une carte d'ID de modÃ¨les vers leurs noms d'affichage. Chaque ID doit correspondre Ã  l'`id` renvoyÃ© par `GET /v1/models` â€” exÃ©cutez `curl http://127.0.0.1:1337/v1/models` pour lister les ID actuellement chargÃ©s dans Atomic Chat.

:::tip
Si les appels d'outils ne fonctionnent pas bien, choisissez un modÃ¨le chargÃ© avec un bon support du tool calling (par exemple, une variante Qwen-Coder ou DeepSeek-Coder).
:::

---

### Azure OpenAI

:::note
Si vous rencontrez des erreurs Â« Je suis dÃ©solÃ©, mais je ne peux pas vous aider avec cette demande Â», essayez de modifier le filtre de contenu de **DefaultV2** Ã  **Default** dans votre ressource Azure.
:::

1. Rendez-vous sur le [portail Azure](https://portal.azure.com/) et crÃ©ez une ressource **Azure OpenAI**. Vous aurez besoin deÂ :
   - **Nom de la ressource**Â : cela fait partie de votre point de terminaison API (`https://RESOURCE_NAME.openai.azure.com/`)
   - **ClÃ© API**Â : soit `KEY 1` ou `KEY 2` de votre ressource

2. AccÃ©dez Ã  [Azure AI Foundry](https://ai.azure.com/) et dÃ©ployez un modÃ¨le.

   :::note
   Le nom du dÃ©ploiement doit correspondre au nom du modÃ¨le pour que voracode fonctionne correctement.
   :::

3. ExÃ©cutez la commande `/connect` et recherchez **Azure**.

   ```txt
   /connect
   ```

4. Entrez votre clÃ© API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

5. DÃ©finissez le nom de votre ressource comme variable d'environnementÂ :

   ```bash
   AZURE_RESOURCE_NAME=XXX voracode
   ```

Ou ajoutez-le Ã  votre profil bashÂ :

```bash title="~/.bash_profile"
   export AZURE_RESOURCE_NAME=XXX
```

6. ExÃ©cutez la commande `/models` pour sÃ©lectionner votre modÃ¨le dÃ©ployÃ©.

   ```txt
   /models
   ```

---

### Azure Cognitive Services

1. Rendez-vous sur le [portail Azure](https://portal.azure.com/) et crÃ©ez une ressource **Azure OpenAI**. Vous aurez besoin deÂ :
   - **Nom de la ressource**Â : cela fait partie de votre point de terminaison API (`https://AZURE_COGNITIVE_SERVICES_RESOURCE_NAME.cognitiveservices.azure.com/`)
   - **ClÃ© API**Â : soit `KEY 1` ou `KEY 2` de votre ressource

2. AccÃ©dez Ã  [Azure AI Foundry](https://ai.azure.com/) et dÃ©ployez un modÃ¨le.

   :::note
   Le nom du dÃ©ploiement doit correspondre au nom du modÃ¨le pour que voracode fonctionne correctement.
   :::

3. ExÃ©cutez la commande `/connect` et recherchez **Azure Cognitive Services**.

   ```txt
   /connect
   ```

4. Entrez votre clÃ© API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

5. DÃ©finissez le nom de votre ressource comme variable d'environnementÂ :

   ```bash
   AZURE_COGNITIVE_SERVICES_RESOURCE_NAME=XXX voracode
   ```

Ou ajoutez-le Ã  votre profil bashÂ :

```bash title="~/.bash_profile"
   export AZURE_COGNITIVE_SERVICES_RESOURCE_NAME=XXX
```

6. ExÃ©cutez la commande `/models` pour sÃ©lectionner votre modÃ¨le dÃ©ployÃ©.

   ```txt
   /models
   ```

---

### Baseten

1. Rendez-vous sur [Baseten](https://app.baseten.co/), crÃ©ez un compte et gÃ©nÃ©rez une clÃ© API.

2. ExÃ©cutez la commande `/connect` et recherchez **Baseten**.

   ```txt
   /connect
   ```

3. Entrez votre clÃ© Baseten API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le.

   ```txt
   /models
   ```

---

### Cerebras

1. Rendez-vous sur la [console Cerebras](https://inference.cerebras.ai/), crÃ©ez un compte et gÃ©nÃ©rez une clÃ© API.

2. ExÃ©cutez la commande `/connect` et recherchez **Cerebras**.

   ```txt
   /connect
   ```

3. Entrez votre clÃ© Cerebras API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le tel que _Qwen 3 Coder 480B_.

   ```txt
   /models
   ```

---

### Cloudflare AI Gateway

Cloudflare AI Gateway vous permet d'accÃ©der aux modÃ¨les de OpenAI, Anthropic, Workers AI et bien plus encore via un point de terminaison unifiÃ©. Avec [Unified Billing](https://developers.cloudflare.com/ai-gateway/features/unified-billing/), vous n'avez pas besoin de clÃ©s API distinctes pour chaque fournisseur.

1. Rendez-vous sur le [tableau de bord Cloudflare](https://dash.cloudflare.com/), accÃ©dez Ã  **AI** > **AI Gateway** et crÃ©ez une nouvelle passerelle.

2. DÃ©finissez votre ID de compte et votre ID de passerelle comme variables d'environnement.

   ```bash title="~/.bash_profile"
   export CLOUDFLARE_ACCOUNT_ID=your-32-character-account-id
   export CLOUDFLARE_GATEWAY_ID=your-gateway-id
   ```

3. ExÃ©cutez la commande `/connect` et recherchez **Cloudflare AI Gateway**.

   ```txt
   /connect
   ```

4. Entrez votre jeton Cloudflare API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

Ou dÃ©finissez-le comme variable d'environnement.

```bash title="~/.bash_profile"
   export CLOUDFLARE_API_TOKEN=your-api-token
```

5. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le.

   ```txt
   /models
   ```

Vous pouvez Ã©galement ajouter des modÃ¨les via votre configuration voracode.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "cloudflare-ai-gateway": {
      "models": {
        "openai/gpt-4o": {},
        "anthropic/claude-sonnet-4": {}
      }
    }
  }
}
```

---

### Cortecs

1. Rendez-vous sur la [console Cortecs](https://cortecs.ai/), crÃ©ez un compte et gÃ©nÃ©rez une clÃ© API.

2. ExÃ©cutez la commande `/connect` et recherchez **Cortecs**.

   ```txt
   /connect
   ```

3. Entrez votre clÃ© Cortecs API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le tel que _Kimi K2 Instruct_.

   ```txt
   /models
   ```

---

### DeepSeek

1. Rendez-vous sur la [console DeepSeek](https://platform.deepseek.com/), crÃ©ez un compte et cliquez sur **CrÃ©er une nouvelle clÃ© API**.

2. ExÃ©cutez la commande `/connect` et recherchez **DeepSeek**.

   ```txt
   /connect
   ```

3. Entrez votre clÃ© DeepSeek API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le DeepSeek tel que _DeepSeek V4 Pro_.

   ```txt
   /models
   ```

---

### Deep Infra

1. Rendez-vous sur le [tableau de bord Deep Infra](https://deepinfra.com/dash), crÃ©ez un compte et gÃ©nÃ©rez une clÃ© API.

2. ExÃ©cutez la commande `/connect` et recherchez **Deep Infra**.

   ```txt
   /connect
   ```

3. Entrez votre clÃ© Deep Infra API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le.

   ```txt
   /models
   ```

---

### FrogBot

1. Rendez-vous sur le [Tableau de bord du micrologiciel](https://app.frogbot.ai/signup), crÃ©ez un compte et gÃ©nÃ©rez une clÃ© API.

2. ExÃ©cutez la commande `/connect` et recherchez **FrogBot**.

   ```txt
   /connect
   ```

3. Entrez la clÃ© API de votre micrologiciel.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le.

   ```txt
   /models
   ```

---

### Fireworks AI

1. Rendez-vous sur la [console Fireworks AI](https://app.fireworks.ai/), crÃ©ez un compte et cliquez sur **CrÃ©er une clÃ© API**.

2. ExÃ©cutez la commande `/connect` et recherchez **Fireworks AI**.

   ```txt
   /connect
   ```

3. Entrez votre clÃ© Fireworks AI API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le tel que _Kimi K2 Instruct_.

   ```txt
   /models
   ```

---

### GitLab Duo

GitLab Duo fournit un chat agent basÃ© sur l'IA avec des capacitÃ©s d'appel d'outils natives via le proxy Anthropic de GitLab.

1. ExÃ©cutez la commande `/connect` et sÃ©lectionnez GitLab.

   ```txt
   /connect
   ```

2. Choisissez votre mÃ©thode d'authentificationÂ :

   ```txt
   â”Œ Select auth method
   â”‚
   â”‚ OAuth (Recommended)
   â”‚ Personal Access Token
   â””
   ```

   #### Utilisation de OAuth (recommandÃ©)

SÃ©lectionnez **OAuth** et votre navigateur s'ouvrira pour autorisation.

#### Utilisation d'un jeton d'accÃ¨s personnel

1.  AccÃ©dez Ã  [GitLab ParamÃ¨tres utilisateur > Jetons d'accÃ¨s](https://gitlab.com/-/user_settings/personal_access_tokens)
2.  Cliquez sur **Ajouter un nouveau jeton**
3.  NomÂ : `Voracode`, PortÃ©esÂ : `api`
4.  Copiez le jeton (commence par `glpat-`)
5.  Entrez-le dans le terminal

6.  ExÃ©cutez la commande `/models` pour voir les modÃ¨les disponibles.

    ```txt
    /models
    ```

````

Trois modÃ¨les basÃ©s sur Claude sont disponiblesÂ :

- **duo-chat-haiku-4-5** (Par dÃ©faut) - RÃ©ponses rapides pour des tÃ¢ches rapides
- **duo-chat-sonnet-4-5** - Performances Ã©quilibrÃ©es pour la plupart des flux de travail
- **duo-chat-opus-4-5** - Le plus capable pour les analyses complexes

:::note
Vous pouvez Ã©galement spÃ©cifier la variable d'environnement 'GITLAB_TOKEN' si vous ne souhaitez pas
pour stocker le jeton dans le stockage d'authentification voracode.
:::

##### GitLab auto-hÃ©bergÃ©

:::note[note de conformitÃ©]
Voracode utilise un petit modÃ¨le pour certaines tÃ¢ches d'IA telles que la gÃ©nÃ©ration du titre de la session.
Il est configurÃ© pour utiliser gpt-5-nano par dÃ©faut, hÃ©bergÃ© par Zen. Pour verrouiller Voracode
pour utiliser uniquement votre propre instance hÃ©bergÃ©e par GitLab, ajoutez ce qui suit Ã  votre
Fichier `voracode.json`. Il est Ã©galement recommandÃ© de dÃ©sactiver le partage de session.

```json
{
"$schema": "https://voracode.ai/config.json",
"small_model": "gitlab/duo-chat-haiku-4-5",
"share": "disabled"
}
````

:::

Pour les instances GitLab auto-hÃ©bergÃ©esÂ :

```bash
export GITLAB_INSTANCE_URL=https://gitlab.company.com
export GITLAB_TOKEN=glpat-...
```

Si votre instance exÃ©cute une AI Gateway personnalisÃ©eÂ :

```bash
GITLAB_AI_GATEWAY_URL=https://ai-gateway.company.com
```

Ou ajoutez Ã  votre profil bashÂ :

```bash title="~/.bash_profile"
export GITLAB_INSTANCE_URL=https://gitlab.company.com
export GITLAB_AI_GATEWAY_URL=https://ai-gateway.company.com
export GITLAB_TOKEN=glpat-...
```

:::note
Votre administrateur GitLab doit activer les Ã©lÃ©ments suivantsÂ :

1. [Duo Agent Platform](https://docs.gitlab.com/user/duo_agent_platform/turn_on_off/) pour l'utilisateur, le groupe ou l'instance
2. Indicateurs de fonctionnalitÃ©s (via la console Rails)Â :
   - `agent_platform_claude_code`
   - `third_party_agents_enabled`
     :::

##### OAuth pour les instances auto-hÃ©bergÃ©es

Afin que Oauth fonctionne pour votre instance auto-hÃ©bergÃ©e, vous devez crÃ©er
une nouvelle application (ParamÃ¨tres â†’ Applications) avec le
rappel URL `http://127.0.0.1:8080/callback` et Ã©tendues suivantesÂ :

- api (AccÃ©dez au API en votre nom)
- read_user (Lire vos informations personnelles)
- read_repository (Autorise l'accÃ¨s en lecture seule au rÃ©fÃ©rentiel)

Exposez ensuite l'ID de l'application en tant que variable d'environnementÂ :

```bash
export GITLAB_OAUTH_CLIENT_ID=your_application_id_here
```

Plus de documentation sur la page d'accueil [voracode-gitlab-auth](https://www.npmjs.com/package/voracode-gitlab-auth).

##### Configuration

Personnalisez via `voracode.json`Â :

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "gitlab": {
      "options": {
        "instanceUrl": "https://gitlab.com"
      }
    }
  }
}
```

##### Outils GitLab API (facultatif, mais fortement recommandÃ©)

Pour accÃ©der aux outils GitLab (demandes de fusion, tickets, pipelines, CI/CD, etc.)Â :

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "plugin": ["voracode-gitlab-plugin"]
}
```

Ce plugin fournit des fonctionnalitÃ©s complÃ¨tes de gestion du rÃ©fÃ©rentiel GitLab, notamment les examens MR, le suivi des problÃ¨mes, la surveillance du pipeline, etc.

---

### GitHub Copilot

Pour utiliser votre abonnement GitHub Copilot avec voracodeÂ :

:::note
Certains modÃ¨les peuvent avoir besoin d'un [Pro+
abonnement](https://github.com/features/copilot/plans) Ã  utiliser.

Certains modÃ¨les doivent Ãªtre activÃ©s manuellement dans vos [GitHub Copilot paramÃ¨tres](https://docs.github.com/en/copilot/how-tos/use-ai-models/configure-access-to-ai-models#setup-for-individual-use).
:::

1. ExÃ©cutez la commande `/connect` et recherchez GitHub Copilot.

   ```txt
   /connect
   ```

2. AccÃ©dez Ã  [github.com/login/device](https://github.com/login/device) et entrez le code.

   ```txt
   â”Œ Login with GitHub Copilot
   â”‚
   â”‚ https://github.com/login/device
   â”‚
   â”‚ Enter code: 8F43-6FCF
   â”‚
   â”” Waiting for authorization...
   ```

3. ExÃ©cutez maintenant la commande `/models` pour sÃ©lectionner le modÃ¨le souhaitÃ©.

   ```txt
   /models
   ```

---

### Google VertexÂ AI

Pour utiliser Google VertexÂ AI avec VoracodeÂ :

1. Rendez-vous sur **Model Garden** dans Google Cloud Console et vÃ©rifiez les
   modÃ¨les disponibles dans votre rÃ©gion.

   :::note
   Vous devez disposer d'un projet GoogleÂ Cloud avec VertexÂ AI API activÃ©.
   :::

2. DÃ©finissez les variables d'environnement requisesÂ :
   - `GOOGLE_CLOUD_PROJECT`Â :Â ID de votre projet GoogleÂ Cloud
   - `VERTEX_LOCATION` (facultatif)Â : rÃ©gion pour VertexÂ AI (par dÃ©fautÂ : `global`)
   - Authentification (au choix)Â :
     - `GOOGLE_APPLICATION_CREDENTIALS`Â : chemin d'accÃ¨s au fichier de clÃ© JSON de votre compte de service
     - Authentifiez-vous Ã  l'aide de gcloudÂ CLIÂ : `gcloud auth application-default login`

DÃ©finissez-les lors de l'exÃ©cution de voracode.

```bash
   GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json GOOGLE_CLOUD_PROJECT=your-project-id voracode
```

Ou ajoutez-les Ã  votre profil bash.

```bash title="~/.bash_profile"
   export GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
   export GOOGLE_CLOUD_PROJECT=your-project-id
   export VERTEX_LOCATION=global
```

:::tip
La rÃ©gion `global` amÃ©liore la disponibilitÃ© et rÃ©duit les erreurs sans frais supplÃ©mentaires. Utilisez des points de terminaison rÃ©gionaux (par exemple, `us-central1`) pour les exigences de rÃ©sidence des donnÃ©es. [En savoir plus](https://cloud.google.com/vertex-ai/generative-ai/docs/partner-models/use-partner-models#regional_and_global_endpoints)
:::

3. ExÃ©cutez la commande `/models` pour sÃ©lectionner le modÃ¨le souhaitÃ©.

   ```txt
   /models
   ```

---

### Groq

1. Rendez-vous sur la [console Groq](https://console.groq.com/), cliquez sur **CrÃ©er une clÃ© API** et copiez la clÃ©.

2. ExÃ©cutez la commande `/connect` et recherchez Groq.

   ```txt
   /connect
   ```

3. Saisissez la clÃ© API du fournisseur.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner celle que vous souhaitez.

   ```txt
   /models
   ```

---

### Hugging Face

[Hugging Face Inference Providers](https://huggingface.co/docs/inference-providers) donne accÃ¨s Ã  des modÃ¨les ouverts pris en charge par plus de 17 fournisseurs.

1. Rendez-vous sur [Hugging Face settings](https://huggingface.co/settings/tokens/new?ownUserPermissions=inference.serverless.write&tokenType=fineGrained) pour crÃ©er un jeton avec l'autorisation de passer des appels aux fournisseurs d'infÃ©rence.

2. ExÃ©cutez la commande `/connect` et recherchez **Hugging Face**.

   ```txt
   /connect
   ```

3. Entrez votre jeton Hugging Face.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le comme _Kimi-K2-Instruct_ ou _GLM-4.6_.

   ```txt
   /models
   ```

---

### Helicone

[Helicone](https://helicone.ai) est une plate-forme d'observabilitÃ© LLM qui fournit la journalisation, la surveillance et l'analyse de vos applications d'IA. L'Helicone AI Gateway achemine automatiquement vos demandes vers le fournisseur appropriÃ© en fonction du modÃ¨le.

1. Rendez-vous sur [Helicone](https://helicone.ai), crÃ©ez un compte et gÃ©nÃ©rez une clÃ© API Ã  partir de votre tableau de bord.

2. ExÃ©cutez la commande `/connect` et recherchez **Helicone**.

   ```txt
   /connect
   ```

3. Entrez votre clÃ© Helicone API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le.

   ```txt
   /models
   ```

Pour plus de fournisseurs et de fonctionnalitÃ©s avancÃ©es telles que la mise en cache et la limitation de dÃ©bit, consultez la [documentation Helicone](https://docs.helicone.ai).

#### Configurations facultatives

Si vous voyez une fonctionnalitÃ© ou un modÃ¨le d'Helicone qui n'est pas configurÃ© automatiquement via voracode, vous pouvez toujours le configurer vous-mÃªme.

Voici le [RÃ©pertoire des modÃ¨les d'Helicone](https://helicone.ai/models), vous en aurez besoin pour rÃ©cupÃ©rer les identifiants des modÃ¨les que vous souhaitez ajouter.

```jsonc title="~/.config/voracode/voracode.jsonc"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "helicone": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Helicone",
      "options": {
        "baseURL": "https://ai-gateway.helicone.ai",
      },
      "models": {
        "gpt-4o": {
          // Model ID (from Helicone's model directory page)
          "name": "GPT-4o", // Your own custom name for the model
        },
        "claude-sonnet-4-20250514": {
          "name": "Claude Sonnet 4",
        },
      },
    },
  },
}
```

#### En-tÃªtes personnalisÃ©s

Helicone prend en charge les en-tÃªtes personnalisÃ©s pour des fonctionnalitÃ©s telles que la mise en cache, le suivi des utilisateurs et la gestion des sessions. Ajoutez-les Ã  la configuration de votre fournisseur en utilisant `options.headers`Â :

```jsonc title="~/.config/voracode/voracode.jsonc"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "helicone": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Helicone",
      "options": {
        "baseURL": "https://ai-gateway.helicone.ai",
        "headers": {
          "Helicone-Cache-Enabled": "true",
          "Helicone-User-Id": "voracode",
        },
      },
    },
  },
}
```

##### Suivi des sessions

La fonctionnalitÃ© [Sessions](https://docs.helicone.ai/features/sessions) d'Helicone vous permet de regrouper les requÃªtes LLM associÃ©es. Utilisez le plugin [voracode-helicone-session](https://github.com/H2Shami/voracode-helicone-session) pour enregistrer automatiquement chaque conversation Voracode en tant que session dans Helicone.

```bash
npm install -g voracode-helicone-session
```

Ajoutez-le Ã  votre configuration.

```json title="voracode.json"
{
  "plugin": ["voracode-helicone-session"]
}
```

Le plugin injecte les en-tÃªtes `Helicone-Session-Id` et `Helicone-Session-Name` dans vos requÃªtes. Sur la page Sessions d'Helicone, vous verrez chaque conversation Voracode rÃ©pertoriÃ©e comme une session distincte.

##### En-tÃªtes Helicone communs

| En-tÃªte                    | Descriptif                                                                           |
| -------------------------- | ------------------------------------------------------------------------------------ |
| `Helicone-Cache-Enabled`   | Activer la mise en cache des rÃ©ponses (`true`/`false`)                               |
| `Helicone-User-Id`         | Suivre les mÃ©triques par utilisateur                                                 |
| `Helicone-Property-[Name]` | Ajouter des propriÃ©tÃ©s personnalisÃ©es (par exemple, `Helicone-Property-Environment`) |
| `Helicone-Prompt-Id`       | Associer les requÃªtes aux versions d'invite                                          |

Consultez le [Helicone Header Directory](https://docs.helicone.ai/helicone-headers/header-directory) pour tous les en-tÃªtes disponibles.

---

### llama.cpp

Vous pouvez configurer voracode pour utiliser des modÃ¨les locaux via l'utilitaire llama-server de [llama.cpp's](https://github.com/ggml-org/llama.cpp)

```json title="voracode.json" "llama.cpp" {5, 6, 8, 10-15}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "llama.cpp": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "llama-server (local)",
      "options": {
        "baseURL": "http://127.0.0.1:8080/v1"
      },
      "models": {
        "qwen3-coder:a3b": {
          "name": "Qwen3-Coder: a3b-30b (local)",
          "limit": {
            "context": 128000,
            "output": 65536
          }
        }
      }
    }
  }
}
```

Dans cet exempleÂ :

- `llama.cpp` est l'ID du fournisseur personnalisÃ©. Cela peut Ãªtre nâ€™importe quelle chaÃ®ne de votre choix.
- `npm` spÃ©cifie le package Ã  utiliser pour ce fournisseur. Ici, `@ai-sdk/openai-compatible` est utilisÃ© pour tout API compatible OpenAI.
- `name` est le nom d'affichage du fournisseur dans l'interface utilisateur.
- `options.baseURL` est le point de terminaison du serveur local.
- `models` est une carte des ID de modÃ¨le avec leurs configurations. Le nom du modÃ¨le sera affichÃ© dans la liste de sÃ©lection du modÃ¨le.

---

### IO.NET

IO.NET propose 17 modÃ¨les optimisÃ©s pour diffÃ©rents cas d'utilisationÂ :

1. Rendez-vous sur la [console IO.NET](https://ai.io.net/), crÃ©ez un compte et gÃ©nÃ©rez une clÃ© API.

2. ExÃ©cutez la commande `/connect` et recherchez **IO.NET**.

   ```txt
   /connect
   ```

3. Entrez votre clÃ© IO.NET API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le.

   ```txt
   /models
   ```

---

### LM Studio

Vous pouvez configurer voracode pour utiliser des modÃ¨les locaux via LM Studio.

```json title="voracode.json" "lmstudio" {5, 6, 8, 10-14}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "lmstudio": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "LM Studio (local)",
      "options": {
        "baseURL": "http://127.0.0.1:1234/v1"
      },
      "models": {
        "google/gemma-3n-e4b": {
          "name": "Gemma 3n-e4b (local)"
        }
      }
    }
  }
}
```

Dans cet exempleÂ :

- `lmstudio` est l'ID du fournisseur personnalisÃ©. Cela peut Ãªtre nâ€™importe quelle chaÃ®ne de votre choix.
- `npm` spÃ©cifie le package Ã  utiliser pour ce fournisseur. Ici, `@ai-sdk/openai-compatible` est utilisÃ© pour tout API compatible OpenAI.
- `name` est le nom d'affichage du fournisseur dans l'interface utilisateur.
- `options.baseURL` est le point de terminaison du serveur local.
- `models` est une carte des ID de modÃ¨le avec leurs configurations. Le nom du modÃ¨le sera affichÃ© dans la liste de sÃ©lection du modÃ¨le.

---

### Moonshot AI

Pour utiliser Kimi K2 de Moonshot AIÂ :

1. Rendez-vous sur la [console Moonshot AI](https://platform.moonshot.ai/console), crÃ©ez un compte et cliquez sur **CrÃ©er une clÃ© API**.

2. ExÃ©cutez la commande `/connect` et recherchez **Moonshot AI**.

   ```txt
   /connect
   ```

3. Entrez votre clÃ© Moonshot API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner _Kimi K2_.

   ```txt
   /models
   ```

---

### MiniMax

1. Rendez-vous sur la [Console MiniMax API](https://platform.minimax.io/login), crÃ©ez un compte et gÃ©nÃ©rez une clÃ© API.

2. ExÃ©cutez la commande `/connect` et recherchez **MiniMax**.

   ```txt
   /connect
   ```

3. Entrez votre clÃ© MiniMax API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le tel que _M2.1_.

   ```txt
   /models
   ```

---

### Nebius Token Factory

1. Rendez-vous sur la [console Nebius Token Factory](https://tokenfactory.nebius.com/), crÃ©ez un compte et cliquez sur **Ajouter une clÃ©**.

2. ExÃ©cutez la commande `/connect` et recherchez **Nebius Token Factory**.

   ```txt
   /connect
   ```

3. Entrez votre clÃ© Nebius Token Factory API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le tel que _Kimi K2 Instruct_.

   ```txt
   /models
   ```

---

### Ollama

Vous pouvez configurer voracode pour utiliser des modÃ¨les locaux via Ollama.

:::tip
Ollama peut se configurer automatiquement pour Voracode. Voir les [documents d'intÃ©gration Ollama](https://docs.ollama.com/integrations/voracode) pour plus de dÃ©tails.
:::

```json title="voracode.json" "ollama" {5, 6, 8, 10-14}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "ollama": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Ollama (local)",
      "options": {
        "baseURL": "http://localhost:11434/v1"
      },
      "models": {
        "llama2": {
          "name": "Llama 2"
        }
      }
    }
  }
}
```

Dans cet exempleÂ :

- `ollama` est l'ID du fournisseur personnalisÃ©. Cela peut Ãªtre nâ€™importe quelle chaÃ®ne de votre choix.
- `npm` spÃ©cifie le package Ã  utiliser pour ce fournisseur. Ici, `@ai-sdk/openai-compatible` est utilisÃ© pour tout API compatible OpenAI.
- `name` est le nom d'affichage du fournisseur dans l'interface utilisateur.
- `options.baseURL` est le point de terminaison du serveur local.
- `models` est une carte des ID de modÃ¨le avec leurs configurations. Le nom du modÃ¨le sera affichÃ© dans la liste de sÃ©lection du modÃ¨le.

:::tip
Si les appels d'outils ne fonctionnent pas, essayez d'augmenter `num_ctx` dans Ollama. Commencez vers 16k - 32k.
:::

---

### Ollama Cloud

Pour utiliser Ollama Cloud avec VoracodeÂ :

1. Rendez-vous sur [https://ollama.com/](https://ollama.com/) et connectez-vous ou crÃ©ez un compte.

2. AccÃ©dez Ã  **ParamÃ¨tres** > **ClÃ©s** et cliquez sur **Ajouter une clÃ© API** pour gÃ©nÃ©rer une nouvelle clÃ© API.

3. Copiez la clÃ© API Ã  utiliser dans Voracode.

4. ExÃ©cutez la commande `/connect` et recherchez **Ollama Cloud**.

   ```txt
   /connect
   ```

5. Entrez votre clÃ© Ollama Cloud API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

6. **Important**Â : Avant d'utiliser des modÃ¨les cloud dans Voracode, vous devez extraire les informations du modÃ¨le localementÂ :

   ```bash
   ollama pull gpt-oss:20b-cloud
   ```

7. ExÃ©cutez la commande `/models` pour sÃ©lectionner votre modÃ¨le Ollama Cloud.

   ```txt
   /models
   ```

---

### OpenAI

Nous vous recommandons de vous inscrire Ã  [ChatGPT Plus ou Pro](https://chatgpt.com/pricing).

1. Une fois inscrit, exÃ©cutez la commande `/connect` et sÃ©lectionnez OpenAI.

   ```txt
   /connect
   ```

2. Ici, vous pouvez sÃ©lectionner l'option **ChatGPT Plus/Pro** et cela ouvrira votre navigateur.
   et vous demande de vous authentifier.

   ```txt
   â”Œ Select auth method
   â”‚
   â”‚ ChatGPT Plus/Pro
   â”‚ Manually enter API Key
   â””
   ```

3. DÃ©sormais, tous les modÃ¨les OpenAI devraient Ãªtre disponibles lorsque vous utilisez la commande `/models`.

   ```txt
   /models
   ```

##### Utilisation des clÃ©s API

Si vous disposez dÃ©jÃ  d'une clÃ© API, vous pouvez sÃ©lectionner **Entrer manuellement la clÃ© API** et la coller dans votre terminal.

---

### Voracode Zen

Voracode Zen est une liste de modÃ¨les testÃ©s et vÃ©rifiÃ©s fournie par l'Ã©quipe Voracode. [En savoir plus](/docs/zen).

1. Connectez-vous Ã  **<a href={console}>Voracode Zen</a>** et cliquez sur **CrÃ©er une clÃ© API**.

2. ExÃ©cutez la commande `/connect` et recherchez **Voracode Zen**.

   ```txt
   /connect
   ```

3. Entrez votre clÃ© Voracode API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le tel que _Qwen 3 Coder 480B_.

   ```txt
   /models
   ```

---

### OpenRouter

1. Rendez-vous sur le [tableau de bord OpenRouter](https://openrouter.ai/settings/keys), cliquez sur **CrÃ©er une clÃ© API** et copiez la clÃ©.

2. ExÃ©cutez la commande `/connect` et recherchez OpenRouter.

   ```txt
   /connect
   ```

3. Saisissez la clÃ© API du fournisseur.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. De nombreux modÃ¨les OpenRouter sont prÃ©chargÃ©s par dÃ©faut, exÃ©cutez la commande `/models` pour sÃ©lectionner celui que vous souhaitez.

   ```txt
   /models
   ```

Vous pouvez Ã©galement ajouter des modÃ¨les supplÃ©mentaires via votre configuration voracode.

```json title="voracode.json" {6}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "openrouter": {
      "models": {
        "somecoolnewmodel": {}
      }
    }
  }
}
```

5. Vous pouvez Ã©galement les personnaliser via votre configuration voracode. Voici un exemple de spÃ©cification d'un fournisseur

   ```json title="voracode.json"
   {
     "$schema": "https://voracode.ai/config.json",
     "provider": {
       "openrouter": {
         "models": {
           "moonshotai/kimi-k2": {
             "options": {
               "provider": {
                 "order": ["baseten"],
                 "allow_fallbacks": false
               }
             }
           }
         }
       }
     }
   }
   ```

---

### SAP AI Core

SAP AI Core donne accÃ¨s Ã  plus de 40 modÃ¨les de OpenAI, Anthropic, Google, Amazon, Meta, Mistral et AI21 via une plateforme unifiÃ©e.

1. AccÃ©dez Ã  votre [SAP BTP Cockpit](https://account.hana.ondemand.com/), accÃ©dez Ã  votre instance de service SAP AI Core et crÃ©ez une clÃ© de service.

   :::tip
   La clÃ© de service est un objet JSON contenant `clientid`, `clientsecret`, `url` et `serviceurls.AI_API_URL`. Vous pouvez trouver votre instance AI Core sous **Services** > **Instances et abonnements** dans le cockpit BTP.
   :::

2. ExÃ©cutez la commande `/connect` et recherchez **SAP AI Core**.

   ```txt
   /connect
   ```

3. Entrez votre clÃ© de service JSON.

   ```txt
   â”Œ Service key
   â”‚
   â”‚
   â”” enter
   ```

Ou dÃ©finissez la variable d'environnement `AICORE_SERVICE_KEY`Â :

```bash
   AICORE_SERVICE_KEY='{"clientid":"...","clientsecret":"...","url":"...","serviceurls":{"AI_API_URL":"..."}}' voracode
```

Ou ajoutez-le Ã  votre profil bashÂ :

```bash title="~/.bash_profile"
   export AICORE_SERVICE_KEY='{"clientid":"...","clientsecret":"...","url":"...","serviceurls":{"AI_API_URL":"..."}}'
```

4. DÃ©finissez Ã©ventuellement l'ID de dÃ©ploiement et le groupe de ressourcesÂ :

   ```bash
   AICORE_DEPLOYMENT_ID=your-deployment-id AICORE_RESOURCE_GROUP=your-resource-group voracode
   ```

   :::note
   Ces paramÃ¨tres sont facultatifs et doivent Ãªtre configurÃ©s en fonction de votre configuration SAP AI Core.
   :::

5. ExÃ©cutez la commande `/models` pour sÃ©lectionner parmi plus de 40 modÃ¨les disponibles.

   ```txt
   /models
   ```

---

### STACKIT

STACKIT AI Model Serving fournit un environnement d'hÃ©bergement souverain entiÃ¨rement gÃ©rÃ© pour les modÃ¨les d'IA, se concentrant sur les LLM comme Llama, Mistral et Qwen, avec une souverainetÃ© maximale des donnÃ©es sur l'infrastructure europÃ©enne.

1. Rendez-vous sur le [portail STACKIT](https://portal.stackit.cloud), accÃ©dez Ã  **AI Model Serving** et crÃ©ez un jeton d'authentification pour votre projet.

   :::tip
   Vous avez besoin d'un compte client STACKIT, d'un compte utilisateur et d'un projet avant de crÃ©er des jetons d'authentification.
   :::

2. ExÃ©cutez la commande `/connect` et recherchez **STACKIT**.

   ```txt
   /connect
   ```

3. Entrez votre jeton d'authentification STACKIT AI Model Serving.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner parmi les modÃ¨les disponibles tels que _Qwen3-VL 235B_ ou _Llama 3.3 70B_.

   ```txt
   /models
   ```

---

### OVHcloud AI Endpoints

1. Rendez-vous sur le [Panneau OVHcloud](https://ovh.com/manager). AccÃ©dez Ã  la section `Public Cloud`, `AI & Machine Learning` > `AI Endpoints` et dans l'onglet `API Keys`, cliquez sur **CrÃ©er une nouvelle clÃ© API**.

2. ExÃ©cutez la commande `/connect` et recherchez **OVHcloud AI Endpoints**.

   ```txt
   /connect
   ```

3. Saisissez votre clÃ© OVHcloud AI Endpoints API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le tel que _gpt-oss-120b_.

   ```txt
   /models
   ```

---

### Scaleway

Pour utiliser [Scaleway Generative APIs](https://www.scaleway.com/en/docs/generative-apis/) avec VoracodeÂ :

1. Rendez-vous dans les [ParamÃ¨tres IAM de la console Scaleway](https://console.scaleway.com/iam/api-keys) pour gÃ©nÃ©rer une nouvelle clÃ© API.

2. ExÃ©cutez la commande `/connect` et recherchez **Scaleway**.

   ```txt
   /connect
   ```

3. Entrez votre clÃ© Scaleway API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le tel que _devstral-2-123b-instruct-2512_ ou _gpt-oss-120b_.

   ```txt
   /models
   ```

---

### Together AI

1. Rendez-vous sur [Together AI console](https://api.together.ai), crÃ©ez un compte et cliquez sur **Ajouter une clÃ©**.

2. ExÃ©cutez la commande `/connect` et recherchez **Together AI**.

   ```txt
   /connect
   ```

3. Entrez votre clÃ© Together AI API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le tel que _Kimi K2 Instruct_.

   ```txt
   /models
   ```

---

### Venice AI

1. Rendez-vous sur la [Venice AI console](https://venice.ai), crÃ©ez un compte et gÃ©nÃ©rez une clÃ© API.

2. ExÃ©cutez la commande `/connect` et recherchez **Venice AI**.

   ```txt
   /connect
   ```

3. Entrez votre clÃ© Venise AI API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le tel que _Llama 3.3 70B_.

   ```txt
   /models
   ```

---

### Vercel AI Gateway

Vercel AI Gateway vous permet d'accÃ©der aux modÃ¨les de OpenAI, Anthropic, Google, xAI et plus encore via un point de terminaison unifiÃ©. Les modÃ¨les sont proposÃ©s au prix catalogue sans majoration.

1. Rendez-vous sur le [tableau de bord Vercel](https://vercel.com/), accÃ©dez Ã  l'onglet **AI Gateway** et cliquez sur **API Keys** pour crÃ©er une nouvelle clÃ© API.

2. ExÃ©cutez la commande `/connect` et recherchez **Vercel AI Gateway**.

   ```txt
   /connect
   ```

3. Entrez votre clÃ© Vercel AI Gateway API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le.

   ```txt
   /models
   ```

Vous pouvez Ã©galement personnaliser les modÃ¨les via votre configuration voracode. Voici un exemple de spÃ©cification de l'ordre de routage du fournisseur.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "vercel": {
      "models": {
        "anthropic/claude-sonnet-4": {
          "options": {
            "order": ["anthropic", "vertex"]
          }
        }
      }
    }
  }
}
```

Quelques options de routage utilesÂ :

| Options             | Descriptif                                                                                  |
| ------------------- | ------------------------------------------------------------------------------------------- |
| `order`             | SÃ©quence de fournisseur Ã  essayer                                                           |
| `only`              | Restreindre Ã  des fournisseurs spÃ©cifiques                                                  |
| `zeroDataRetention` | Utilisez uniquement des fournisseurs avec des politiques de conservation des donnÃ©es nulles |

---

### xAI

1. Rendez-vous sur la [console xAI](https://console.x.ai/), crÃ©ez un compte et gÃ©nÃ©rez une clÃ© API.

2. ExÃ©cutez la commande `/connect` et recherchez **xAI**.

   ```txt
   /connect
   ```

3. Entrez votre clÃ© xAI API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le tel que _Grok Beta_.

   ```txt
   /models
   ```

---

### Z.AI

1. Rendez-vous sur la [console Z.AI API](https://z.ai/manage-apikey/apikey-list), crÃ©ez un compte et cliquez sur **CrÃ©er une nouvelle clÃ© API**.

2. ExÃ©cutez la commande `/connect` et recherchez **Z.AI**.

   ```txt
   /connect
   ```

Si vous Ãªtes abonnÃ© au **Plan de codage GLM**, sÃ©lectionnez **Plan de codage Z.AI**.

3. Entrez votre clÃ© Z.AI API.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. ExÃ©cutez la commande `/models` pour sÃ©lectionner un modÃ¨le tel que _GLM-4.7_.

   ```txt
   /models
   ```

---

### ZenMux

1. Rendez-vous sur le [tableau de bord ZenMux](https://zenmux.ai/settings/keys), cliquez sur **CrÃ©er une clÃ© API** et copiez la clÃ©.

2. ExÃ©cutez la commande `/connect` et recherchez ZenMux.

   ```txt
   /connect
   ```

3. Saisissez la clÃ© API du fournisseur.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. De nombreux modÃ¨les ZenMux sont prÃ©chargÃ©s par dÃ©faut, exÃ©cutez la commande `/models` pour sÃ©lectionner celui que vous souhaitez.

   ```txt
   /models
   ```

Vous pouvez Ã©galement ajouter des modÃ¨les supplÃ©mentaires via votre configuration voracode.

```json title="voracode.json" {6}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "zenmux": {
      "models": {
        "somecoolnewmodel": {}
      }
    }
  }
}
```

---

## Fournisseur personnalisÃ©

Pour ajouter un fournisseur **OpenAI-compatible** qui n'est pas rÃ©pertoriÃ© dans la commande `/connect`Â :

:::tip
Vous pouvez utiliser n'importe quel fournisseur compatible OpenAI avec voracode. La plupart des fournisseurs d'IA modernes proposent des API compatibles OpenAI.
:::

1. ExÃ©cutez la commande `/connect` et faites dÃ©filer jusqu'Ã  **Autre**.

   ```bash
   $ /connect

   â”Œ  Add credential
   â”‚
   â—†  Select provider
   â”‚  ...
   â”‚  â— Other
   â””
   ```

2. Saisissez un identifiant unique pour le fournisseur.

   ```bash
   $ /connect

   â”Œ  Add credential
   â”‚
   â—‡  Enter provider id
   â”‚  myprovider
   â””
   ```

   :::note
   Choisissez un identifiant mÃ©morable, vous l'utiliserez dans votre fichier de configuration.
   :::

3. Entrez votre clÃ© API pour le fournisseur.

   ```bash
   $ /connect

   â”Œ  Add credential
   â”‚
   â–²  This only stores a credential for myprovider - you will need to configure it in voracode.json, check the docs for examples.
   â”‚
   â—‡  Enter your API key
   â”‚  sk-...
   â””
   ```

4. CrÃ©ez ou mettez Ã  jour votre fichier `voracode.json` dans le rÃ©pertoire de votre projetÂ :

   ```json title="voracode.json" ""myprovider"" {5-15}
   {
     "$schema": "https://voracode.ai/config.json",
     "provider": {
       "myprovider": {
         "npm": "@ai-sdk/openai-compatible",
         "name": "My AI ProviderDisplay Name",
         "options": {
           "baseURL": "https://api.myprovider.com/v1"
         },
         "models": {
           "my-model-name": {
             "name": "My Model Display Name"
           }
         }
       }
     }
   }
   ```

Voici les options de configuration :

- **npm**Â : package AI SDK Ã  utiliser, `@ai-sdk/openai-compatible` pour les fournisseurs compatibles OpenAI
- **nom**Â : nom Ã  afficher dans l'interface utilisateur.
- **modÃ¨les**Â :Â ModÃ¨les disponibles.
- **options.baseURL**Â : URL de l'endpoint API.
- **options.apiKey**Â : dÃ©finissez Ã©ventuellement la clÃ© API, si vous n'utilisez pas d'authentification.
- **options.headers**Â : dÃ©finissez Ã©ventuellement des en-tÃªtes personnalisÃ©s.

En savoir plus sur les options avancÃ©es dans l'exemple ci-dessous.

5. ExÃ©cutez la commande `/models` et votre fournisseur et vos modÃ¨les personnalisÃ©s apparaÃ®tront dans la liste de sÃ©lection.

---

##### Exemple

Voici un exemple de dÃ©finition des options `apiKey`, `headers` et modÃ¨le `limit`.

```json title="voracode.json" {9,11,17-20}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "myprovider": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "My AI ProviderDisplay Name",
      "options": {
        "baseURL": "https://api.myprovider.com/v1",
        "apiKey": "{env:ANTHROPIC_API_KEY}",
        "headers": {
          "Authorization": "Bearer custom-token"
        }
      },
      "models": {
        "my-model-name": {
          "name": "My Model Display Name",
          "limit": {
            "context": 200000,
            "output": 65536
          }
        }
      }
    }
  }
}
```

DÃ©tails de configurationÂ :

- **apiKey**Â : dÃ©fini Ã  l'aide de la syntaxe de variable `env`, [en savoir plus](/docs/config#env-vars).
- **headers**Â : en-tÃªtes personnalisÃ©s envoyÃ©s avec chaque requÃªte.
- **limit.context**Â : nombre maximal de jetons d'entrÃ©e acceptÃ©s par le modÃ¨le.
- **limit.output**Â : nombre maximal de jetons que le modÃ¨le peut gÃ©nÃ©rer.

Les champs `limit` permettent Ã  Voracode de comprendre la quantitÃ© de contexte qu'il vous reste. Les fournisseurs standard les extraient automatiquement de models.dev.

---

## DÃ©pannage

Si vous rencontrez des difficultÃ©s lors de la configuration d'un fournisseur, vÃ©rifiez les points suivantsÂ :

1. **VÃ©rifiez la configuration de l'authentification**Â : exÃ©cutez `voracode auth list` pour voir si les informations d'identification
   pour le fournisseur sont ajoutÃ©s Ã  votre configuration.

Cela ne s'applique pas aux fournisseurs comme Amazon Bedrock, qui s'appuient sur des variables d'environnement pour leur authentification.

2. Pour les fournisseurs personnalisÃ©s, vÃ©rifiez la configuration voracode etÂ :
   - Assurez-vous que l'ID du fournisseur utilisÃ© dans la commande `/connect` correspond Ã  l'ID de votre configuration voracode.
   - Le bon package npm est utilisÃ© pour le fournisseur. Par exemple, utilisez `@ai-sdk/cerebras` pour Cerebras. Et pour tous les autres fournisseurs compatibles OpenAI, utilisez `@ai-sdk/openai-compatible`.
   - VÃ©rifiez que le point de terminaison API correct est utilisÃ© dans le champ `options.baseURL`.
