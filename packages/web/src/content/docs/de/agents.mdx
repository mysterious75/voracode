---
title: Agenten
description: Konfigurieren und verwenden Sie spezielle Agenten.
---

Agenten sind spezialisierte AI-Assistenten, die fÃ¼r bestimmte Aufgaben und ArbeitsablÃ¤ufe konfiguriert werden kÃ¶nnen. Sie ermÃ¶glichen Ihnen die Erstellung gezielter Werkzeuge mit benutzerdefinierten Eingabeaufforderungen, Modellen und Werkzeugzugriff.

:::tip
Verwenden Sie den Planagenten, um Code zu analysieren und VorschlÃ¤ge zu Ã¼berprÃ¼fen, ohne CodeÃ¤nderungen vorzunehmen.
:::

Sie kÃ¶nnen wÃ¤hrend einer Sitzung zwischen Agenten wechseln oder sie mit der ErwÃ¤hnung `@` aufrufen.

---

## Typen

Es gibt zwei Arten von Agenten in Voracode; PrimÃ¤ragenten und Subagenten.

---

### PrimÃ¤ragenten

PrimÃ¤ragenten sind die Hauptassistenten, mit denen Sie direkt interagieren. Sie kÃ¶nnen sie mit der **Tabulatortaste** oder Ihrer konfigurierten Tastenkombination `switch_agent` durchblÃ¤ttern. Diese Agenten kÃ¼mmern sich um Ihr HauptgesprÃ¤ch. Der Werkzeugzugriff wird Ã¼ber Berechtigungen konfiguriert â€“ zum Beispiel sind bei â€žBuildâ€œ alle Werkzeuge aktiviert, wÃ¤hrend â€žPlanâ€œ eingeschrÃ¤nkt ist.

:::tip
Sie kÃ¶nnen die **Tabulatortaste** verwenden, um wÃ¤hrend einer Sitzung zwischen primÃ¤ren Agenten zu wechseln.
:::

Voracode verfÃ¼gt Ã¼ber zwei integrierte PrimÃ¤ragenten: **Build** und **Plan**. Auch
Schauen wir uns diese unten an.

---

### Build

_Modus_: `primary`

Build ist der **Standard**-PrimÃ¤ragent mit allen aktivierten Tools. Dies ist der Standardagent fÃ¼r Entwicklungsarbeiten, bei dem Sie vollen Zugriff auf Dateioperationen und Systembefehle benÃ¶tigen.

---

### Plan

_Modus_: `primary`

Ein eingeschrÃ¤nkter Agent, der fÃ¼r Planung und Analyse entwickelt wurde. Wir verwenden ein Berechtigungssystem, um Ihnen mehr Kontrolle zu geben und unbeabsichtigte Ã„nderungen zu verhindern.
StandardmÃ¤ÃŸig sind alle folgenden Elemente auf `ask` gesetzt:

- `file edits`: Alle SchreibvorgÃ¤nge, Patches und Bearbeitungen
- `bash`: Alle Bash-Befehle

Dieser Agent ist nÃ¼tzlich, wenn Sie mÃ¶chten, dass LLM Code analysiert, Ã„nderungen vorgeschlagen oder PlÃ¤ne erstellt werden, ohne tatsÃ¤chliche Ã„nderungen an Ihrer Codebasis vorzunehmen.

---

### General

_Modus_: `subagent`

Ein Allzweckagent zur Recherche komplexerer Fragen und zur AusfÃ¼hrung mehrstufiger Aufgaben. VerfÃ¼gt Ã¼ber vollstÃ¤ndigen Zugriff auf das Tool (auÃŸer Todo), sodass bei Bedarf DateiÃ¤nderungen vorgenommen werden kÃ¶nnen. Verwenden Sie Matrizen, um mehrere Arbeitseinheiten parallel auszufÃ¼hren.

---

### Explore

_Modus_: `subagent`

Ein schneller, schreibgeschÃ¼tzter Agent zum Erkunden von Codebasen. Dateien kÃ¶nnen nicht geÃ¤ndert werden. Verwenden Sie dies, wenn Sie Dateien schnell anhand von Mustern finden, Code nach SchlÃ¼sselwÃ¶rtern durchsuchen oder Fragen zur Codebasis beantworten mÃ¼ssen.

---

### Scout

_Modus_: `subagent`

Ein schreibgeschÃ¼tzter Agent fÃ¼r externe Dokumentation und Dependency-Recherche. Verwenden Sie ihn, wenn Sie ein Dependency-Repository in den von Voracode verwalteten Cache klonen, den Quellcode einer Bibliothek untersuchen oder lokalen Code mit Upstream-Implementierungen abgleichen mÃ¼ssen, ohne Ihren Workspace zu verÃ¤ndern.

---

### Compaction

_Modus_: `primary`

Versteckter Systemagent, der lange Kontext in einer kleineren Zusammenfassung komprimiert. Es wird bei Bedarf automatisch ausgefÃ¼hrt und ist im UI nicht auswÃ¤hlbar.

---

### Title

_Modus_: `primary`

Versteckter Systemagent, der kurze Sitzungstitel generiert. Es lÃ¤uft automatisch und ist im UI nicht auswÃ¤hlbar.

---

### Summary

_Modus_: `primary`

Versteckter Systemagent, der Sitzungszusammenfassungen erstellt. Es lÃ¤uft automatisch und ist im UI nicht auswÃ¤hlbar.

---

## Verwendung

1. Verwenden Sie fÃ¼r primÃ¤re Agenten die **Tabulatortaste**, um wÃ¤hrend einer Sitzung durch sie zu blÃ¤ttern. Sie kÃ¶nnen auch Ihre konfigurierte `switch_agent`-Tastenkombination verwenden.

2. Subagenten kÃ¶nnen aufgerufen werden:
   - **Automatisch** durch primÃ¤re Agenten fÃ¼r spezielle Aufgaben basierend auf ihren Beschreibungen.
   - Manuell durch **@ ErwÃ¤hnung** eines Subagenten in Ihrer Nachricht. Zum Beispiel.

     ```txt frame="none"
     @general help me search for this function
     ```

3. **Navigation zwischen Sitzungen**: Wenn Subagenten ihre eigenen untergeordneten Sitzungen erstellen, kÃ¶nnen Sie zwischen der Ã¼bergeordneten Sitzung und allen untergeordneten Sitzungen sorgfÃ¤ltig handeln, indem Sie Folgendes verwenden:
   - **\<Leader>+Rechts** (oder Ihre konfigurierte `session_child_cycle`-Tastenkombination), um vorwÃ¤rts durch Parent â†’ Child1 â†’ Child2 â†’ ... â†’ Parent zu blÃ¤ttern
   - **\<Leader>+Links** (oder Ihre konfigurierte `session_child_cycle_reverse`-Tastenkombination), um rÃ¼ckwÃ¤rts durch Parent â† Child1 â† Child2 â† ... â† Parent zu blÃ¤ttern

   Dadurch kÃ¶nnen Sie nahtlos zwischen der Hauptkonversation und der spezialisierten Subagentenarbeit wechseln.

---

## Konfiguration

Sie kÃ¶nnen die integrierten Agenten anpassen oder Ã¼ber die Konfiguration eigene erstellen. Agenten kÃ¶nnen auf zwei Arten konfiguriert werden:

---

### JSON

Konfigurieren Sie Agenten in Ihrer `voracode.json`-Konfigurationsdatei:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "agent": {
    "build": {
      "mode": "primary",
      "model": "anthropic/claude-sonnet-4-20250514",
      "prompt": "{file:./prompts/build.txt}",
      "tools": {
        "write": true,
        "edit": true,
        "bash": true
      }
    },
    "plan": {
      "mode": "primary",
      "model": "anthropic/claude-haiku-4-20250514",
      "tools": {
        "write": false,
        "edit": false,
        "bash": false
      }
    },
    "code-reviewer": {
      "description": "Reviews code for best practices and potential issues",
      "mode": "subagent",
      "model": "anthropic/claude-sonnet-4-20250514",
      "prompt": "You are a code reviewer. Focus on security, performance, and maintainability.",
      "tools": {
        "write": false,
        "edit": false
      }
    }
  }
}
```

---

### Markdown

Sie kÃ¶nnen Agenten auch mithilfe von Markdown-Dateien definieren. Platzieren Sie sie in:

- Global: `~/.config/voracode/agents/`
- Pro Projekt: `.voracode/agents/`

```markdown title="~/.config/voracode/agents/review.md"
---
description: Reviews code for quality and best practices
mode: subagent
model: anthropic/claude-sonnet-4-20250514
temperature: 0.1
tools:
  write: false
  edit: false
  bash: false
---

You are in code review mode. Focus on:

- Code quality and best practices
- Potential bugs and edge cases
- Performance implications
- Security considerations

Provide constructive feedback without making direct changes.
```

Der Name der Markdown-Datei wird zum Agentennamen. Beispielsweise erstellte `review.md` einen `review`-Agenten.

---

## Optionen

Schauen wir uns diese KonfigurationsmÃ¶glichkeiten im Detail an.

---

### Beschreibung

Verwenden Sie die Option `description`, um eine kurze Beschreibung darÃ¼ber bereitzustellen, was der Agent tut und wann er verwendet werden soll.

```json title="voracode.json"
{
  "agent": {
    "review": {
      "description": "Reviews code for best practices and potential issues"
    }
  }
}
```

Dies ist eine **erforderliche** Konfigurationsoption.

---

### Temperatur

Steuern Sie die ZufÃ¤lligkeit und KreativitÃ¤t der Antworten von LLM mit der `temperature`-Konfiguration.

HÃ¶chste Werte machen die Antworten fokussierter und deterministischer, wÃ¤hrend hÃ¶here Werte die KreativitÃ¤t und VariabilitÃ¤t steigern.

```json title="voracode.json"
{
  "agent": {
    "plan": {
      "temperature": 0.1
    },
    "creative": {
      "temperature": 0.8
    }
  }
}
```

Temperaturwerte liegen typischerweise zwischen 0.0 und 1.0:

- **0.0-0.2**: Sehr fokussierte und deterministische Antworten, ideal fÃ¼r Code-Analyse und Planung
- **0.3-0.5**: Ausgewogene Antworten mit etwas KreativitÃ¤t, gut fÃ¼r allgemeine Entwicklungsaufgaben
- **0.6-1.0**: Kreativere und vielfÃ¤ltigere Antworten, nÃ¼tzlich fÃ¼r Brainstorming und Erkundung

```json title="voracode.json"
{
  "agent": {
    "analyze": {
      "temperature": 0.1,
      "prompt": "{file:./prompts/analysis.txt}"
    },
    "build": {
      "temperature": 0.3
    },
    "brainstorm": {
      "temperature": 0.7,
      "prompt": "{file:./prompts/creative.txt}"
    }
  }
}
```

Wenn keine Temperatur angegeben ist, verwendet Voracode modellspezifische Standardwerte; typischerweise 0 fÃ¼r die meisten Modelle, 0.55 fÃ¼r Qwen-Modelle.

---

### Schritte

Steuern Sie die maximale Anzahl an Agenteniterationen, die ein Agent durchfÃ¼hren kann, bevor er gezwungen wird, nur mit Text zu antworten. Dadurch kÃ¶nnen Benutzer, die die Kosten kontrollieren mÃ¶chten, ein Limit fÃ¼r Agentenaktionen festlegen.

Wenn dies nicht festgelegt ist, iteriert der Agent weiter, bis das Modell den Stopp beschlieÃŸt oder der Benutzer die Sitzung unterbricht.

```json title="voracode.json"
{
  "agent": {
    "quick-thinker": {
      "description": "Fast reasoning with limited iterations",
      "prompt": "You are a quick thinker. Solve problems with minimal steps.",
      "steps": 5
    }
  }
}
```

Wenn das Limit erreicht ist, erhÃ¤lt der Agent eine spezielle Systemaufforderung, die ihn anweist, mit einer Zusammenfassung seiner Arbeit und empfohlenen verbleibenden Aufgaben zu antworten.

:::caution
Das alte Feld `maxSteps` ist veraltet. Verwenden Sie stattdessen `steps`.
:::

---

### Deaktivieren

Auf `true` setzen, um den Agenten zu deaktivieren.

```json title="voracode.json"
{
  "agent": {
    "review": {
      "disable": true
    }
  }
}
```

---

### Prompt

Geben Sie mit der `prompt`-Konfiguration eine benutzerdefinierte Systemaufforderungsdatei fÃ¼r diese Agenten an. Die Eingabeaufforderungsdatei sollte Anweisungen enthalten, die speziell auf den Zweck des Agenten zugeschnitten sind.

```json title="voracode.json"
{
  "agent": {
    "review": {
      "prompt": "{file:./prompts/code-review.txt}"
    }
  }
}
```

Dieser Pfad ist relativ zum Speicherort der Konfigurationsdatei. Dies funktioniert sowohl fÃ¼r die globale Voracode-Konfiguration als auch fÃ¼r die projektspezifische Konfiguration.

---

### Modell

Verwenden Sie die `model`-Konfiguration, um das Modell fÃ¼r diese Agenten zu Ã¼berschreiben. NÃ¼tzlich fÃ¼r die Verwendung verschiedener Modelle, die fÃ¼r verschiedene Aufgaben optimiert sind. Zum Beispiel ein schnelleres Modell fÃ¼r die Planung, ein leistungsfÃ¤higeres Modell fÃ¼r die Umsetzung.

:::tip
Wenn Sie kein Modell angeben, verwenden Sie den primÃ¤ren Agenten [model globally configured](/docs/config#models), wÃ¤hrend die Subagenten das Modell des primÃ¤ren Agenten verwenden, der den Subagenten aufgerufen hat.
:::

```json title="voracode.json"
{
  "agent": {
    "plan": {
      "model": "anthropic/claude-haiku-4-20250514"
    }
  }
}
```

Die Modell-ID in Ihrer Voracode-Konfiguration verwendet das Format `provider/model-id`. Wenn Sie beispielsweise [Voracode Zen](/docs/zen) verwenden, wÃ¼rden Sie `voracode/gpt-5.1-codex` fÃ¼r GPT 5.1 Codex verwenden.

---

### Werkzeuge

Steuern Sie mit der `tools`-Konfiguration, welche Tools in diesem Agent verfÃ¼gbar sind. Sie kÃ¶nnen bestimmte Tools aktivieren oder deaktivieren, indem Sie sie auf `true` oder `false` setzen.

```json title="voracode.json" {3-6,9-12}
{
  "$schema": "https://voracode.ai/config.json",
  "tools": {
    "write": true,
    "bash": true
  },
  "agent": {
    "plan": {
      "tools": {
        "write": false,
        "bash": false
      }
    }
  }
}
```

:::note
Die agentenspezifische Konfiguration Ã¼berschreibt die globale Konfiguration.
:::

Sie kÃ¶nnen Platzhalter auch verwenden, um mehrere Werkzeuge gleichzeitig zu steuern. So deaktivieren Sie beispielsweise alle Tools von einem MCP-Server:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "agent": {
    "readonly": {
      "tools": {
        "mymcp_*": false,
        "write": false,
        "edit": false
      }
    }
  }
}
```

[Learn more about tools](/docs/tools).

---

### Berechtigungen

Sie kÃ¶nnen Berechtigungen konfigurieren, um zu verwalten, welche Aktionen ein Agent ausfÃ¼hren kann. Derzeit kÃ¶nnen die Berechtigungen fÃ¼r die Tools `edit`, `bash` und `webfetch` wie folgt konfiguriert werden:

- `"ask"` â€“ Bitten Sie um Genehmigung, bevor Sie das Tool ausfÃ¼hren
- `"allow"` â€“ Alle VorgÃ¤nge ohne Genehmigung zulassen
- `"deny"` â€“ Deaktivieren Sie das Tool

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "edit": "deny"
  }
}
```

Sie kÃ¶nnen diese Berechtigungen pro Agent Ã¼berschreiben.

```json title="voracode.json" {3-5,8-10}
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "edit": "deny"
  },
  "agent": {
    "build": {
      "permission": {
        "edit": "ask"
      }
    }
  }
}
```

Sie kÃ¶nnen Berechtigungen auch in Markdown-Agenten festlegen.

```markdown title="~/.config/voracode/agents/review.md"
---
description: Code review without edits
mode: subagent
permission:
  edit: deny
  bash:
    "*": ask
    "git diff": allow
    "git log*": allow
    "grep *": allow
  webfetch: deny
---

Only analyze code and suggest changes.
```

Sie kÃ¶nnen Berechtigungen fÃ¼r bestimmte Bash-Befehle festlegen.

```json title="voracode.json" {7}
{
  "$schema": "https://voracode.ai/config.json",
  "agent": {
    "build": {
      "permission": {
        "bash": {
          "git push": "ask",
          "grep *": "allow"
        }
      }
    }
  }
}
```

Dies kann ein Glob-Muster annehmen.

```json title="voracode.json" {7}
{
  "$schema": "https://voracode.ai/config.json",
  "agent": {
    "build": {
      "permission": {
        "bash": {
          "git *": "ask"
        }
      }
    }
  }
}
```

Und Sie kÃ¶nnen den Platzhalter `*` auch verwenden, um Berechtigungen fÃ¼r alle Befehle zu verwalten.
Da die letzte Ã¼bereinstimmende Regel Vorrang hat, setzen Sie zuerst den Platzhalter `*` und danach bestimmte Regeln.

```json title="voracode.json" {8}
{
  "$schema": "https://voracode.ai/config.json",
  "agent": {
    "build": {
      "permission": {
        "bash": {
          "*": "ask",
          "git status *": "allow"
        }
      }
    }
  }
}
```

[Learn more about permissions](/docs/permissions).

---

### Modus

Steuern Sie den Modus des Agenten mit der `mode`-Konfiguration. Mit der Option `mode` wird festgelegt, wie der Agent verwendet werden kann.

```json title="voracode.json"
{
  "agent": {
    "review": {
      "mode": "subagent"
    }
  }
}
```

Die Option `mode` kann auf `primary`, `subagent` oder `all` gesetzt werden. Wenn kein `mode` angegeben ist, wird standardmÃ¤ÃŸig `all` verwendet.

---

### Versteckt

Verstecken Sie einen Subagenten im AutovervollstÃ¤ndigungsmenÃ¼ `@` mit `hidden: true`. NÃ¼tzlich fÃ¼r interne Subagenten, die nur programmgesteuert von anderen Agenten Ã¼ber das Task-Tool aufgerufen werden sollten.

```json title="voracode.json"
{
  "agent": {
    "internal-helper": {
      "mode": "subagent",
      "hidden": true
    }
  }
}
```

Dies wirkt sich nur auf die Benutzersichtbarkeit im AutovervollstÃ¤ndigungsmenÃ¼ aus. Versteckte Agenten kÃ¶nnen weiterhin vom Modell Ã¼ber das Aufgabentool aufgerufen werden, sofern die Berechtigungen dies zulassen.

:::note
Gilt nur fÃ¼r `mode: subagent`-Agenten.
:::

---

### Task-Berechtigungen

Steuern Sie, welche Subagenten ein Agent Ã¼ber das Task-Tool mit `permission.task` aufrufen kann. Verwendet Glob-Muster fÃ¼r eine flexible Zuordnung.

```json title="voracode.json"
{
  "agent": {
    "orchestrator": {
      "mode": "primary",
      "permission": {
        "task": {
          "*": "deny",
          "orchestrator-*": "allow",
          "code-reviewer": "ask"
        }
      }
    }
  }
}
```

Bei der Einstellung `deny` wird der Subagent vollstÃ¤ndig aus der Beschreibung des Aufgabentools entfernt, sodass das Modell nicht versucht, ihn aufzurufen.

:::tip
Die Regeln werden der Reihe nach ausgewertet und die **letzte Ã¼bereinstimmende Regel gewinnt**. Im obigen Beispiel entspricht `orchestrator-planner` sowohl `*` (verweigern) als auch `orchestrator-*` (zulassen), aber da `orchestrator-*` nach `*` kommt, ist das Ergebnis `allow`.
:::

:::tip
Benutzer kÃ¶nnen jeden Subagenten immer direkt Ã¼ber das AutovervollstÃ¤ndigungsmenÃ¼ `@` aufrufen, auch wenn die Aufgabenberechtigungen des Agenten dies verweigern wÃ¼rden.
:::

---

### Farbe

Passen Sie das visuelle Erscheinungsbild des Agenten im UI mit der Option `color` an. Dies wirkt sich darauf aus, wie der Agent in der BenutzeroberflÃ¤che angezeigt wird.

Verwenden Sie eine gÃ¼ltige Hex-Farbe (z. B. `#FF5733`) oder Themenfarbe: `primary`, `secondary`, `accent`, `success`, `warning`, `error`, `info`.

```json title="voracode.json"
{
  "agent": {
    "creative": {
      "color": "#ff6b6b"
    },
    "code-reviewer": {
      "color": "accent"
    }
  }
}
```

---

### Top P

Steuern Sie die Antwortvielfalt mit der Option `top_p`. Alternative zur Temperatur zur Kontrolle des Zufalls.

```json title="voracode.json"
{
  "agent": {
    "brainstorm": {
      "top_p": 0.9
    }
  }
}
```

Die Werte reichen von 0.0 bis 1.0. Niedrigere Werte sind fokussierter, hÃ¶here Werte vielfÃ¤ltiger.

---

### ZusÃ¤tzliche Optionen

Alle anderen Optionen, die Sie in Ihrer Agentenkonfiguration angeben, werden als Modelloptionen **direkt** an den Anbieter weitergeleitet. Dadurch kÃ¶nnen Sie anbieterspezifische Funktionen und Parameter nutzen.

Mit den Argumentationsmodellen von OpenAI kÃ¶nnen Sie beispielsweise den Argumentationsaufwand steuern:

```json title="voracode.json" {6,7}
{
  "agent": {
    "deep-thinker": {
      "description": "Agent that uses high reasoning effort for complex problems",
      "model": "openai/gpt-5",
      "reasoningEffort": "high",
      "textVerbosity": "low"
    }
  }
}
```

Diese zusÃ¤tzlichen Optionen sind modell- und anbieterspezifisch. ÃœberprÃ¼fen Sie die Dokumentation Ihres Anbieters auf verfÃ¼gbare Parameter.

:::tip
FÃ¼hren Sie `voracode models` aus, um eine Liste der verfÃ¼gbaren Modelle anzubieten.
:::

---

## Agenten erstellen

Mit dem folgenden Befehl kÃ¶nnen Sie neue Agenten erstellen:

```bash
voracode agent create
```

Dieser interaktive Befehl fÃ¼hrt zu Folgendem:

1. Fragen Sie, wo der Agent gespeichert werden soll. global oder projektspezifisch.
2. Beschreibung dessen, was der Agent tun soll.
3. Generieren Sie eine entsprechende Systemaufforderung und -kennung.
4. Hier kÃ¶nnen Sie auswÃ¤hlen, auf welche Tools der Agent zugreifen kann.
5. Erstellen Sie abschlieÃŸend eine Markdown-Datei mit der Agentenkonfiguration.

---

## AnwendungsfÃ¤lle

Hier sind einige hÃ¤ufige AnwendungsfÃ¤lle fÃ¼r verschiedene Agenten.

- **Build-Agent**: VollstÃ¤ndige Entwicklungsarbeit mit allen aktivierten Tools
- **Planagent**: Analyse und Planung ohne Ã„nderungen
- **Review-Agent**: CodeÃ¼berprÃ¼fung mit schreibgeschÃ¼tztem Zugriff plus Dokumentationstools
- **Debug-Agent**: Konzentriert sich auf die Untersuchung mit aktivierten Bash- und Lesetools
- **Docs-Agent**: Schreiben von Dokumentationen mit Dateioperationen, aber ohne Systembefehle

---

## Beispiele

Hier sind einige Beispielagenten, die fÃ¼r Sie nÃ¼tzlich sein kÃ¶nnten.

:::tip
Haben Sie einen Agenten, den Sie gerne teilen mÃ¶chten? [Reichen Sie einen PR ein](https://github.com/mysterious75/voracode).
:::

---

### Dokumentationsagent

```markdown title="~/.config/voracode/agents/docs-writer.md"
---
description: Writes and maintains project documentation
mode: subagent
tools:
  bash: false
---

You are a technical writer. Create clear, comprehensive documentation.

Focus on:

- Clear explanations
- Proper structure
- Code examples
- User-friendly language
```

---

### SicherheitsprÃ¼fer

```markdown title="~/.config/voracode/agents/security-auditor.md"
---
description: Performs security audits and identifies vulnerabilities
mode: subagent
tools:
  write: false
  edit: false
---

You are a security expert. Focus on identifying potential security issues.

Look for:

- Input validation vulnerabilities
- Authentication and authorization flaws
- Data exposure risks
- Dependency vulnerabilities
- Configuration security issues
```
