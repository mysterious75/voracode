---
title: Plugins
description: Schreiben Sie Ihre eigenen Plugins, um Voracode zu erweitern.
---

Mit Plugins kÃ¶nnen Sie Voracode erweitern, indem Sie verschiedene Ereignisse einbinden und das Verhalten anpassen. Sie kÃ¶nnen Plugins erstellen, um neue Funktionen hinzuzufÃ¼gen, externe Dienste zu integrieren oder das Standardverhalten von Voracode zu Ã¤ndern.

Beispiele finden Sie im von der Community erstellten [plugins](/docs/ecosystem#plugins).

---

## Plugins verwenden

Es gibt zwei MÃ¶glichkeiten, Plugins zu laden.

---

### Aus lokalen Dateien

Platzieren Sie JavaScript- oder TypeScript-Dateien im Plugin-Verzeichnis.

- `.voracode/plugins/` â€“ Plugins auf Projektebene
- `~/.config/voracode/plugins/` â€“ Globale Plugins

Dateien in diesen Verzeichnissen werden beim Start automatisch geladen.

---

### Von npm

Geben Sie npm-Pakete in Ihrer Konfigurationsdatei an.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "plugin": ["voracode-helicone-session", "voracode-wakatime", "@my-org/custom-plugin"]
}
```

Es werden sowohl regulÃ¤re als auch bereichsbezogene NPM-Pakete unterstÃ¼tzt.

Durchsuchen Sie die verfÃ¼gbaren Plugins im [ecosystem](/docs/ecosystem#plugins).

---

### Plugin-Installation

**NPM-Plugins** werden beim Start automatisch mit Bun installiert. Pakete und ihre AbhÃ¤ngigkeiten werden in `~/.cache/voracode/node_modules/` zwischengespeichert.

**Lokale Plugins** werden direkt aus dem Plugin-Verzeichnis geladen. Um externe Pakete zu verwenden, mÃ¼ssen Sie ein `package.json` in Ihrem Konfigurationsverzeichnis erstellen (siehe [Dependencies](#dependencies)) oder das Plugin auf npm und [add it to your config](/docs/config#plugins) verÃ¶ffentlichen.

---

### Ladereihenfolge

Plugins werden aus allen Quellen geladen und alle Hooks werden nacheinander ausgefÃ¼hrt. Die Ladereihenfolge lautet:

1. Globale Konfiguration (`~/.config/voracode/voracode.json`)
2. Projektkonfiguration (`voracode.json`)
3. Globales Plugin-Verzeichnis (`~/.config/voracode/plugins/`)
4. Projekt-Plugin-Verzeichnis (`.voracode/plugins/`)

Doppelte npm-Pakete mit demselben Namen und derselben Version werden einmal geladen. Allerdings werden ein lokales Plugin und ein NPM-Plugin mit Ã¤hnlichen Namen beide separat geladen.

---

## Plugins erstellen

Ein Plugin ist ein **JavaScript/TypeScript-Modul**, das eine oder mehrere Plugin-Funktionen exportiert. Jede Funktion empfÃ¤ngt ein Kontextobjekt und gibt ein Hooks-Objekt zurÃ¼ck.

---

### AbhÃ¤ngigkeiten

Lokale Plugins und benutzerdefinierte Tools kÃ¶nnen externe npm-Pakete verwenden. FÃ¼gen Sie Ihrem Konfigurationsverzeichnis ein `package.json` mit den benÃ¶tigten AbhÃ¤ngigkeiten hinzu.

```json title=".voracode/package.json"
{
  "dependencies": {
    "shescape": "^2.1.0"
  }
}
```

Voracode fÃ¼hrt beim Start `bun install` aus, um diese zu installieren. Ihre Plugins und Tools kÃ¶nnen sie dann importieren.

```ts title=".voracode/plugins/my-plugin.ts"
import { escape } from "shescape"

export const MyPlugin = async (ctx) => {
  return {
    "tool.execute.before": async (input, output) => {
      if (input.tool === "bash") {
        output.args.command = escape(output.args.command)
      }
    },
  }
}
```

---

### Grundstruktur

```js title=".voracode/plugins/example.js"
export const MyPlugin = async ({ project, client, $, directory, worktree }) => {
  console.log("Plugin initialized!")

  return {
    // Hook implementations go here
  }
}
```

Die Plugin-Funktion erhÃ¤lt:

- `project`: Die aktuellen Projektinformationen.
- `directory`: Das aktuelle Arbeitsverzeichnis.
- `worktree`: Der Git-Worktree-Pfad.
- `client`: Ein Voracode-SDK-Client fÃ¼r die Interaktion mit AI.
- `$`: Buns [shell API](https://bun.com/docs/runtime/shell) zum AusfÃ¼hren von Befehlen.

---

### TypeScript-UnterstÃ¼tzung

FÃ¼r TypeScript-Plugins kÃ¶nnen Sie Typen aus dem Plugin-Paket importieren:

```ts title="my-plugin.ts" {1}
import type { Plugin } from "@voracode-ai/plugin"

export const MyPlugin: Plugin = async ({ project, client, $, directory, worktree }) => {
  return {
    // Type-safe hook implementations
  }
}
```

---

### Ereignisse

Plugins kÃ¶nnen Ereignisse abonnieren, wie unten im Abschnitt â€žBeispieleâ€œ gezeigt. Hier finden Sie eine Liste der verschiedenen verfÃ¼gbaren Veranstaltungen.

#### Befehlsereignisse

- `command.executed`

#### Dateiereignisse

- `file.edited`
- `file.watcher.updated`

#### Installationsereignisse

- `installation.updated`

#### LSP-Ereignisse

- `lsp.client.diagnostics`
- `lsp.updated`

#### Nachrichtenereignisse

- `message.part.removed`
- `message.part.updated`
- `message.removed`
- `message.updated`

#### Berechtigungsereignisse

- `permission.asked`
- `permission.replied`

#### Serverereignisse

- `server.connected`

#### Sitzungsereignisse

- `session.created`
- `session.compacted`
- `session.deleted`
- `session.diff`
- `session.error`
- `session.idle`
- `session.status`
- `session.updated`

#### Todo-Ereignisse

- `todo.updated`

#### Shell-Ereignisse

- `shell.env`

#### Tool-Ereignisse

- `tool.execute.after`
- `tool.execute.before`

#### TUI-Ereignisse

- `tui.prompt.append`
- `tui.command.execute`
- `tui.toast.show`

---

## Beispiele

Hier sind einige Beispiele fÃ¼r Plugins, mit denen Sie Voracode erweitern kÃ¶nnen.

---

### Benachrichtigungen senden

Senden Sie Benachrichtigungen, wenn bestimmte Ereignisse eintreten:

```js title=".voracode/plugins/notification.js"
export const NotificationPlugin = async ({ project, client, $, directory, worktree }) => {
  return {
    event: async ({ event }) => {
      // Send notification on session completion
      if (event.type === "session.idle") {
        await $`osascript -e 'display notification "Session completed!" with title "voracode"'`
      }
    },
  }
}
```

Wir verwenden `osascript`, um AppleScript unter macOS auszufÃ¼hren. Hier verwenden wir es zum Versenden von Benachrichtigungen.

:::note
Wenn Sie die Voracode-Desktop-App verwenden, kann diese automatisch Systembenachrichtigungen senden, wenn eine Antwort bereit ist oder wenn in einer Sitzung ein Fehler auftritt.
:::

---

### .env-Schutz

Verhindern Sie, dass Voracode `.env`-Dateien liest:

```javascript title=".voracode/plugins/env-protection.js"
export const EnvProtection = async ({ project, client, $, directory, worktree }) => {
  return {
    "tool.execute.before": async (input, output) => {
      if (input.tool === "read" && output.args.filePath.includes(".env")) {
        throw new Error("Do not read .env files")
      }
    },
  }
}
```

---

### Umgebungsvariablen einfÃ¼gen

FÃ¼gen Sie Umgebungsvariablen in alle Shell-AusfÃ¼hrungen ein (AI-Tools und Benutzerterminals):

```javascript title=".voracode/plugins/inject-env.js"
export const InjectEnvPlugin = async () => {
  return {
    "shell.env": async (input, output) => {
      output.env.MY_API_KEY = "secret"
      output.env.PROJECT_ROOT = input.cwd
    },
  }
}
```

---

### Benutzerdefinierte Tools

Plugins kÃ¶nnen Voracode auch benutzerdefinierte Tools hinzufÃ¼gen:

```ts title=".voracode/plugins/custom-tools.ts"
import { type Plugin, tool } from "@voracode-ai/plugin"

export const CustomToolsPlugin: Plugin = async (ctx) => {
  return {
    tool: {
      mytool: tool({
        description: "This is a custom tool",
        args: {
          foo: tool.schema.string(),
        },
        async execute(args, context) {
          const { directory, worktree } = context
          return `Hello ${args.foo} from ${directory} (worktree: ${worktree})`
        },
      }),
    },
  }
}
```

Der `tool`-Helfer erstellt ein benutzerdefiniertes Tool, das Voracode aufrufen kann. Es verwendet eine Zod-Schemafunktion und gibt eine Tooldefinition zurÃ¼ck mit:

- `description`: Was das Tool macht
- `args`: Zod-Schema fÃ¼r die Argumente des Tools
- `execute`: Funktion, die ausgefÃ¼hrt wird, wenn das Tool aufgerufen wird

Ihre benutzerdefinierten Tools stehen neben den integrierten Tools fÃ¼r Voracode zur VerfÃ¼gung.

---

### Protokollierung

Verwenden Sie `client.app.log()` anstelle von `console.log` fÃ¼r die strukturierte Protokollierung:

```ts title=".voracode/plugins/my-plugin.ts"
export const MyPlugin = async ({ client }) => {
  await client.app.log({
    body: {
      service: "my-plugin",
      level: "info",
      message: "Plugin initialized",
      extra: { foo: "bar" },
    },
  })
}
```

Ebenen: `debug`, `info`, `warn`, `error`. Weitere Informationen finden Sie unter [SDK documentation](https://voracode.ai/docs/sdk).

---

### Compaction-Hooks

Passen Sie den Kontext an, der beim Komprimieren einer Sitzung einbezogen wird:

```ts title=".voracode/plugins/compaction.ts"
import type { Plugin } from "@voracode-ai/plugin"

export const CompactionPlugin: Plugin = async (ctx) => {
  return {
    "experimental.session.compacting": async (input, output) => {
      // Inject additional context into the compaction prompt
      output.context.push(`
## Custom Context

Include any state that should persist across compaction:
- Current task status
- Important decisions made
- Files being actively worked on
`)
    },
  }
}
```

Der Hook `experimental.session.compacting` wird ausgelÃ¶st, bevor der Hook LLM eine Fortsetzungszusammenfassung generiert. Verwenden Sie es, um domÃ¤nenspezifischen Kontext einzufÃ¼gen, der der standardmÃ¤ÃŸigen Komprimierungsaufforderung entgehen wÃ¼rde.

Sie kÃ¶nnen die Komprimierungsaufforderung auch vollstÃ¤ndig ersetzen, indem Sie `output.prompt` festlegen:

```ts title=".voracode/plugins/custom-compaction.ts"
import type { Plugin } from "@voracode-ai/plugin"

export const CustomCompactionPlugin: Plugin = async (ctx) => {
  return {
    "experimental.session.compacting": async (input, output) => {
      // Replace the entire compaction prompt
      output.prompt = `
You are generating a continuation prompt for a multi-agent swarm session.

Summarize:
1. The current task and its status
2. Which files are being modified and by whom
3. Any blockers or dependencies between agents
4. The next steps to complete the work

Format as a structured prompt that a new agent can use to resume work.
`
    },
  }
}
```

Wenn `output.prompt` festgelegt ist, ersetzt es die standardmÃ¤ÃŸige Komprimierungsaufforderung vollstÃ¤ndig. Das Array `output.context` wird in diesem Fall ignoriert.
