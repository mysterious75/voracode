---
title: Anbieter
description: Verwendung eines beliebigen LLM-Anbieters in Voracode.
---

import config from "../../../../config.mjs"
export const console = config.console

Voracode verwendet [AI SDK](https://ai-sdk.dev/) und [Models.dev](https://models.dev) zur UnterstÃ¼tzung von **75+ LLM-Anbietern** und unterstÃ¼tzt die AusfÃ¼hrung lokaler Modelle.

Um einen Anbieter hinzuzufÃ¼gen, mÃ¼ssen Sie:

1. FÃ¼gen Sie die API-SchlÃ¼ssel fÃ¼r den Anbieter mit dem Befehl `/connect` hinzu.
2. Konfigurieren Sie den Anbieter in Ihrer Voracode-Konfiguration.

---

### Anmeldeinformationen

Wenn Sie die API-SchlÃ¼ssel eines Anbieters mit dem Befehl `/connect` hinzufÃ¼gen, werden diese gespeichert
in `~/.local/share/voracode/auth.json`.

---

### Konfiguration

Sie kÃ¶nnen die Anbieter Ã¼ber den Abschnitt `provider` in Ihrem Voracode anpassen
config.

---

#### Base URL

Sie kÃ¶nnen die Base URL fÃ¼r jeden Anbieter anpassen, indem Sie die Option `baseURL` festlegen. Das ist nuetzlich, wenn Sie Proxy-Dienste oder benutzerdefinierte Endpunkte verwenden.

```json title="voracode.json" {6}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "anthropic": {
      "options": {
        "baseURL": "https://api.anthropic.com/v1"
      }
    }
  }
}
```

---

## Voracode Zen

Voracode Zen ist eine Liste von Modellen, die vom Voracode-Team bereitgestellt wurden
Getestet und verifiziert, dass es gut mit Voracode funktioniert. [Learn more](/docs/zen).

:::tip
Wenn Sie neu sind, empfehlen wir Ihnen, mit Voracode Zen zu beginnen.
:::

1. FÃ¼hren Sie den Befehl `/connect` in TUI aus, wÃ¤hlen Sie Voracode aus und gehen Sie zu [voracode.ai/auth](https://voracode.ai/auth).

   ```txt
   /connect
   ```

2. Melden Sie sich an, geben Sie Ihre Rechnungsdaten ein und kopieren Sie Ihren API-SchlÃ¼ssel.

3. FÃ¼gen Sie Ihren API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie `/models` in TUI aus, um die Liste der von uns empfohlenen Modelle anzuzeigen.

   ```txt
   /models
   ```

Es funktioniert wie jeder andere Anbieter in Voracode und ist vÃ¶llig optional.

---

## Voracode Go

Voracode Go ist ein kostenguenstiges Abonnement, das zuverlaessigen Zugriff auf beliebte Open-Coding-Modelle bietet, die vom Voracode-Team getestet und verifiziert wurden, dass sie gut mit Voracode funktionieren.

1. FÃ¼hren Sie den Befehl `/connect` in der TUI aus, waehlen Sie `Voracode Go` und gehen Sie zu [voracode.ai/auth](https://voracode.ai/zen).

   ```txt
   /connect
   ```

2. Melden Sie sich an, geben Sie Ihre Rechnungsdaten ein und kopieren Sie Ihren API-SchlÃ¼ssel.

3. FÃ¼gen Sie Ihren API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie `/models` in der TUI aus, um die Liste der empfohlenen Modelle zu sehen.

   ```txt
   /models
   ```

Es funktioniert wie jeder andere Anbieter in Voracode und ist vÃ¶llig optional.

---

## Verzeichnis

Schauen wir uns einige der Anbieter im Detail an. Wenn Sie einen Anbieter hinzufÃ¼gen mÃ¶chten
Liste, zÃ¶gern Sie nicht, ein PR zu Ã¶ffnen.

:::note
Sehen Sie hier keinen Anbieter? Senden Sie einen PR.
:::

---

### 302.AI

1. Gehen Sie zu [302.AI console](https://302.ai/), erstellen Sie ein Konto und generieren Sie einen API-SchlÃ¼ssel.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **302.AI**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren 302.AI API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um ein Modell auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### Amazon Bedrock

So verwenden Sie Amazon Bedrock mit Voracode:

1. Gehen Sie zum **Modellkatalog** in der Amazon Bedrock-Konsole und fordern Sie an
   Zugriff auf die gewÃ¼nschten Modelle.

   :::tip
   Sie mÃ¼ssen Zugriff auf das gewÃ¼nschte Modell in Amazon Bedrock haben.
   :::

2. **Konfigurieren Sie die Authentifizierung** mit einer der folgenden Methoden:

   #### Umgebungsvariablen (Schnellstart)

   Legen Sie eine dieser Umgebungsvariablen fest, wÃ¤hrend Sie Voracode ausfÃ¼hren:

   ```bash
   # Option 1: Using AWS access keys
   AWS_ACCESS_KEY_ID=XXX AWS_SECRET_ACCESS_KEY=YYY voracode

   # Option 2: Using named AWS profile
   AWS_PROFILE=my-profile voracode

   # Option 3: Using Bedrock bearer token
   AWS_BEARER_TOKEN_BEDROCK=XXX voracode
   ```

   Oder fÃ¼gen Sie sie Ihrem Bash-Profil hinzu:

   ```bash title="~/.bash_profile"
   export AWS_PROFILE=my-dev-profile
   export AWS_REGION=us-east-1
   ```

   #### Konfigurationsdatei (empfohlen)

   FÃ¼r eine projektspezifische oder dauerhafte Konfiguration verwenden Sie `voracode.json`:

   ```json title="voracode.json"
   {
     "$schema": "https://voracode.ai/config.json",
     "provider": {
       "amazon-bedrock": {
         "options": {
           "region": "us-east-1",
           "profile": "my-aws-profile"
         }
       }
     }
   }
   ```

   **VerfÃ¼gbare Optionen:**
   - `region` - AWS region (e.g., `us-east-1`, `eu-west-1`)
   - `profile` - AWS named profile from `~/.aws/credentials`
   - `endpoint` â€“ Benutzerdefinierter Endpunkt URL fÃ¼r VPC-Endpunkte (Alias â€‹â€‹fÃ¼r generische `baseURL`-Option)

   :::tip
   Konfigurationsdateioptionen haben Vorrang vor Umgebungsvariablen.
   :::

   #### Erweitert: VPC Endpunkte

   Wenn Sie VPC-Endpunkte fÃ¼r Bedrock verwenden:

   ```json title="voracode.json"
   {
     "$schema": "https://voracode.ai/config.json",
     "provider": {
       "amazon-bedrock": {
         "options": {
           "region": "us-east-1",
           "profile": "production",
           "endpoint": "https://bedrock-runtime.us-east-1.vpce-xxxxx.amazonaws.com"
         }
       }
     }
   }
   ```

   :::note
   Die Option `endpoint` ist ein Alias â€‹â€‹fÃ¼r die generische Option `baseURL` und verwendet AWS-spezifische Terminologie. Wenn sowohl `endpoint` als auch `baseURL` angegeben sind, hat `endpoint` Vorrang.
   :::

   #### Authentifizierungsmethoden
   - **`AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY`**: Erstellen Sie einen IAM-Benutzer und generieren Sie ZugriffsschlÃ¼ssel in der AWS-Konsole
   - **`AWS_PROFILE`**: Benannte Profile von `~/.aws/credentials` verwenden. Zuerst mit `aws configure --profile my-profile` oder `aws sso login` konfigurieren
   - **`AWS_BEARER_TOKEN_BEDROCK`**: Generieren Sie langfristige API-SchlÃ¼ssel aus der Amazon Bedrock-Konsole
   - **`AWS_WEB_IDENTITY_TOKEN_FILE` / `AWS_ROLE_ARN`**: FÃ¼r EKS IRSA (IAM Rollen fÃ¼r Dienstkonten) oder andere Kubernetes-Umgebungen mit OIDC-Verbund. Diese Umgebungsvariablen werden von Kubernetes automatisch eingefÃ¼gt, wenn Dienstkontoanmerkungen verwendet werden.

   #### AuthentifizierungsprioritÃ¤t

   Amazon Bedrock verwendet die folgende AuthentifizierungsprioritÃ¤t:
   1. **Bearer-Token** â€“ `AWS_BEARER_TOKEN_BEDROCK`-Umgebungsvariable oder Token aus dem `/connect`-Befehl
   2. **AWS Anmeldeinformationskette** â€“ Profil, ZugriffsschlÃ¼ssel, gemeinsame Anmeldeinformationen, IAM Rollen, Web-IdentitÃ¤tstoken (EKS IRSA), Instanzmetadaten

   :::note
   Wenn ein Inhabertoken festgelegt ist (Ã¼ber `/connect` oder `AWS_BEARER_TOKEN_BEDROCK`), hat es Vorrang vor allen AWS-Anmeldeinformationsmethoden, einschlieÃŸlich konfigurierter Profile.
   :::

3. FÃ¼hren Sie den Befehl `/models` aus, um das gewÃ¼nschte Modell auszuwÃ¤hlen.

   ```txt
   /models
   ```

:::note
Verwenden Sie fÃ¼r benutzerdefinierte Inferenzprofile den Modell- und Anbieternamen im SchlÃ¼ssel und legen Sie die Eigenschaft `id` auf den Arn fest. Dadurch wird ein korrektes Caching sichergestellt:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "amazon-bedrock": {
      // ...
      "models": {
        "anthropic-claude-sonnet-4.5": {
          "id": "arn:aws:bedrock:us-east-1:xxx:application-inference-profile/yyy"
        }
      }
    }
  }
}
```

:::

---

### Anthropic

1. Sobald Sie sich angemeldet haben, fÃ¼hren Sie den Befehl `/connect` aus und wÃ¤hlen Sie Anthropic aus.

   ```txt
   /connect
   ```

2. Hier kÃ¶nnen Sie die Option **Claude Pro/Max** auswÃ¤hlen und Ihr Browser wird geÃ¶ffnet
   und bitten Sie, sich zu authentifizieren.

   ```txt
   â”Œ Select auth method
   â”‚
   â”‚ Claude Pro/Max
   â”‚ Create an API Key
   â”‚ Manually enter API Key
   â””
   ```

3. Jetzt sollten alle Anthropic-Modelle verfÃ¼gbar sein, wenn Sie den Befehl `/models` verwenden.

   ```txt
   /models
   ```

:::info
Die Verwendung Ihres Claude Pro/Max-Abonnements in Voracode wird von [Anthropic](https://anthropic.com) nicht offiziell unterstÃ¼tzt.
:::

##### Verwendung von API-Keys

Sie kÃ¶nnen auch **Erstellen eines API-SchlÃ¼ssels** auswÃ¤hlen, wenn Sie kein Pro/Max-Abonnement haben. AuÃŸerdem wird Ihr Browser geÃ¶ffnet, Sie werden aufgefordert, sich bei Anthropic anzumelden, und Sie erhalten einen Code, den Sie in Ihr Terminal einfÃ¼gen kÃ¶nnen.

Wenn Sie bereits Ã¼ber einen API-SchlÃ¼ssel verfÃ¼gen, kÃ¶nnen Sie **API-SchlÃ¼ssel manuell eingeben** auswÃ¤hlen und ihn in Ihr Terminal einfÃ¼gen.

---

### Atomic Chat

Sie kÃ¶nnen voracode so konfigurieren, dass es lokale Modelle Ã¼ber [Atomic Chat](https://atomic.chat) verwendet â€” eine Desktop-Anwendung, die lokale LLMs hinter einem OpenAI-kompatiblen API-Server bereitstellt (Standard-Endpunkt `http://127.0.0.1:1337/v1`).

```json title="voracode.json" "atomic-chat" {5, 6, 8, 10-14}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "atomic-chat": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Atomic Chat (local)",
      "options": {
        "baseURL": "http://127.0.0.1:1337/v1"
      },
      "models": {
        "<your-model-id>": {
          "name": "<your-model-name>"
        }
      }
    }
  }
}
```

In diesem Beispiel:

- `atomic-chat` ist die benutzerdefinierte Provider-ID. Dies kann eine beliebige Zeichenkette sein.
- `npm` gibt das fÃ¼r diesen Provider zu verwendende Paket an. Hier wird `@ai-sdk/openai-compatible` fÃ¼r jede OpenAI-kompatible API verwendet.
- `name` ist der in der UI angezeigte Name des Providers.
- `options.baseURL` ist der Endpunkt des lokalen Servers. Passen Sie Host und Port an Ihre Atomic-Chat-Konfiguration an.
- `models` ist eine Zuordnung von Modell-IDs zu ihren Anzeigenamen. Jede ID muss dem `id`-Wert entsprechen, den `GET /v1/models` zurÃ¼ckgibt â€” fÃ¼hren Sie `curl http://127.0.0.1:1337/v1/models` aus, um die aktuell in Atomic Chat geladenen IDs aufzulisten.

:::tip
Wenn Tool-Aufrufe nicht zuverlÃ¤ssig funktionieren, wÃ¤hlen Sie ein geladenes Modell mit starker Tool-Calling-UnterstÃ¼tzung (zum Beispiel eine Qwen-Coder- oder DeepSeek-Coder-Variante).
:::

---

### Azure OpenAI

:::note
Wenn Sie auf die Fehlermeldung â€žEs tut mir leid, aber ich kann Ihnen bei dieser Anfrage nicht weiterhelfenâ€œ stoÃŸen, versuchen Sie, den Inhaltsfilter in Ihrer Azure-Ressource von **DefaultV2** in **Default** zu Ã¤ndern.
:::

1. Gehen Sie zu [Azure portal](https://portal.azure.com/) und erstellen Sie eine **Azure OpenAI**-Ressource. Sie benÃ¶tigen:
   - **Ressourcenname**: Dies wird Teil Ihres API-Endpunkts (`https://RESOURCE_NAME.openai.azure.com/`)
   - **API-SchlÃ¼ssel**: Entweder `KEY 1` oder `KEY 2` aus Ihrer Ressource

2. Gehen Sie zu [Azure AI Foundry](https://ai.azure.com/) und stellen Sie ein Modell bereit.

   :::note
   Der Bereitstellungsname muss mit dem Modellnamen Ã¼bereinstimmen, damit Voracode ordnungsgemÃ¤ÃŸ funktioniert.
   :::

3. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **Azure**.

   ```txt
   /connect
   ```

4. Geben Sie Ihren API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

5. Legen Sie Ihren Ressourcennamen als Umgebungsvariable fest:

   ```bash
   AZURE_RESOURCE_NAME=XXX voracode
   ```

   Oder fÃ¼gen Sie es Ihrem Bash-Profil hinzu:

   ```bash title="~/.bash_profile"
   export AZURE_RESOURCE_NAME=XXX
   ```

6. FÃ¼hren Sie den Befehl `/models` aus, um Ihr bereitgestelltes Modell auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### Azure Cognitive Services

1. Gehen Sie zu [Azure portal](https://portal.azure.com/) und erstellen Sie eine **Azure OpenAI**-Ressource. Sie benÃ¶tigen:
   - **Ressourcenname**: Dies wird Teil Ihres API-Endpunkts (`https://AZURE_COGNITIVE_SERVICES_RESOURCE_NAME.cognitiveservices.azure.com/`)
   - **API-SchlÃ¼ssel**: Entweder `KEY 1` oder `KEY 2` aus Ihrer Ressource

2. Gehen Sie zu [Azure AI Foundry](https://ai.azure.com/) und stellen Sie ein Modell bereit.

   :::note
   Der Bereitstellungsname muss mit dem Modellnamen Ã¼bereinstimmen, damit Voracode ordnungsgemÃ¤ÃŸ funktioniert.
   :::

3. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **Azure Cognitive Services**.

   ```txt
   /connect
   ```

4. Geben Sie Ihren API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

5. Legen Sie Ihren Ressourcennamen als Umgebungsvariable fest:

   ```bash
   AZURE_COGNITIVE_SERVICES_RESOURCE_NAME=XXX voracode
   ```

   Oder fÃ¼gen Sie es Ihrem Bash-Profil hinzu:

   ```bash title="~/.bash_profile"
   export AZURE_COGNITIVE_SERVICES_RESOURCE_NAME=XXX
   ```

6. FÃ¼hren Sie den Befehl `/models` aus, um Ihr bereitgestelltes Modell auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### Baseten

1. Gehen Sie zu [Baseten](https://app.baseten.co/), erstellen Sie ein Konto und generieren Sie einen API-SchlÃ¼ssel.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **Baseten**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren Baseten API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um ein Modell auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### Cerebras

1. Gehen Sie zu [Cerebras console](https://inference.cerebras.ai/), erstellen Sie ein Konto und generieren Sie einen API-SchlÃ¼ssel.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **Cerebras**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren Cerebras-API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um ein Modell wie _Qwen 3 Coder 480B_ auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### Cloudflare AI Gateway

Mit dem Cloudflare AI Gateway kÃ¶nnen Sie Ã¼ber einen einheitlichen Endpunkt auf Modelle von OpenAI, Anthropic, Workers AI und mehr zugreifen. Mit [Unified Billing](https://developers.cloudflare.com/ai-gateway/features/unified-billing/) benÃ¶tigen Sie nicht fÃ¼r jeden Anbieter separate API-SchlÃ¼ssel.

1. Gehen Sie zu [Cloudflare dashboard](https://dash.cloudflare.com/), navigieren Sie zu **AI** > **AI Gateway** und erstellen Sie ein neues Gateway.

2. Legen Sie Ihr Konto ID und Ihr Gateway ID als Umgebungsvariablen fest.

   ```bash title="~/.bash_profile"
   export CLOUDFLARE_ACCOUNT_ID=your-32-character-account-id
   export CLOUDFLARE_GATEWAY_ID=your-gateway-id
   ```

3. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **Cloudflare AI Gateway**.

   ```txt
   /connect
   ```

4. Geben Sie Ihr Cloudflare-API-Token ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

   Oder legen Sie es als Umgebungsvariable fest.

   ```bash title="~/.bash_profile"
   export CLOUDFLARE_API_TOKEN=your-api-token
   ```

5. FÃ¼hren Sie den Befehl `/models` aus, um ein Modell auszuwÃ¤hlen.

   ```txt
   /models
   ```

   Sie kÃ¶nnen Modelle auch Ã¼ber Ihre Voracode-Konfiguration hinzufÃ¼gen.

   ```json title="voracode.json"
   {
     "$schema": "https://voracode.ai/config.json",
     "provider": {
       "cloudflare-ai-gateway": {
         "models": {
           "openai/gpt-4o": {},
           "anthropic/claude-sonnet-4": {}
         }
       }
     }
   }
   ```

---

### Cortecs

1. Gehen Sie zu [Cortecs console](https://cortecs.ai/), erstellen Sie ein Konto und generieren Sie einen API-SchlÃ¼ssel.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **Cortecs**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren Cortecs-API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um ein Modell wie _Kimi K2 Instruct_ auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### DeepSeek

1. Gehen Sie zu [DeepSeek console](https://platform.deepseek.com/), erstellen Sie ein Konto und klicken Sie auf **Neuen API-SchlÃ¼ssel erstellen**.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **DeepSeek**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren DeepSeek API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um ein DeepSeek-Modell wie _DeepSeek V4 Pro_ auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### Deep Infra

1. Gehen Sie zu [Deep Infra dashboard](https://deepinfra.com/dash), erstellen Sie ein Konto und generieren Sie einen API-SchlÃ¼ssel.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **Deep Infra**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren Deep Infra API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um ein Modell auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### FrogBot

1. Gehen Sie zu [FrogBot dashboard](https://app.frogbot.ai/signup), erstellen Sie ein Konto und generieren Sie einen API-SchlÃ¼ssel.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **FrogBot**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren FrogBot API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um ein Modell auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### Fireworks AI

1. Gehen Sie zu [Fireworks AI console](https://app.fireworks.ai/), erstellen Sie ein Konto und klicken Sie auf **API-SchlÃ¼ssel erstellen**.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **Fireworks AI**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren Fireworks AI API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um ein Modell wie _Kimi K2 Instruct_ auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### GitLab Duo

GitLab Duo bietet AI-gestÃ¼tzten Agenten-Chat mit nativen Tool-Aufruffunktionen Ã¼ber den Anthropic-Proxy von GitLab.

1. FÃ¼hren Sie den Befehl `/connect` aus und wÃ¤hlen Sie GitLab aus.

   ```txt
   /connect
   ```

2. WÃ¤hlen Sie Ihre Authentifizierungsmethode:

   ```txt
   â”Œ Select auth method
   â”‚
   â”‚ OAuth (Recommended)
   â”‚ Personal Access Token
   â””
   ```

   #### Verwendung von OAuth (empfohlen)

   WÃ¤hlen Sie **OAuth** und Ihr Browser wird zur Autorisierung geÃ¶ffnet.

   #### Verwendung eines persÃ¶nlichen Zugriffstokens
   1. Gehe zu [GitLab User Settings > Access Tokens](https://gitlab.com/-/user_settings/personal_access_tokens)
   2. Klicken Sie auf **Neues Token hinzufÃ¼gen**
   3. Name: `Voracode`, Bereiche: `api`
   4. Kopieren Sie das Token (beginnt mit `glpat-`)
   5. Geben Sie es im Terminal ein

3. FÃ¼hren Sie den Befehl `/models` aus, um verfÃ¼gbare Modelle anzuzeigen.

   ```txt
   /models
   ```

   Es stehen drei Claude-basierte Modelle zur VerfÃ¼gung:
   - **duo-chat-haiku-4-5** (Standard) â€“ Schnelle Antworten fÃ¼r schnelle Aufgaben
   - **duo-chat-sonnet-4-5** â€“ Ausgewogene Leistung fÃ¼r die meisten ArbeitsablÃ¤ufe
   - **duo-chat-opus-4-5** â€“ Am besten geeignet fÃ¼r komplexe Analysen

:::note
Sie kÃ¶nnen auch die Umgebungsvariable â€žGITLAB_TOKENâ€œ angeben, wenn Sie dies nicht mÃ¶chten
um das Token im Voracode-Authentifizierungsspeicher zu speichern.
:::

##### Selbstgehostetes GitLab

:::note[Compliance-Hinweis]
Voracode verwendet ein kleines Modell fÃ¼r einige AI-Aufgaben wie das Generieren des Sitzungstitels.
Es ist standardmÃ¤ÃŸig fÃ¼r die Verwendung von gpt-5-nano konfiguriert und wird von Zen gehostet. Zum Sperren von Voracode
Um nur Ihre eigene von GitLab gehostete Instanz zu verwenden, fÃ¼gen Sie Folgendes zu Ihrer hinzu
`voracode.json`-Datei. Es wird auÃŸerdem empfohlen, die Sitzungsfreigabe zu deaktivieren.

```json
{
  "$schema": "https://voracode.ai/config.json",
  "small_model": "gitlab/duo-chat-haiku-4-5",
  "share": "disabled"
}
```

:::

FÃ¼r selbstgehostete GitLab-Instanzen:

```bash
export GITLAB_INSTANCE_URL=https://gitlab.company.com
export GITLAB_TOKEN=glpat-...
```

Wenn Ihre Instanz ein benutzerdefiniertes AI-Gateway ausfÃ¼hrt:

```bash
GITLAB_AI_GATEWAY_URL=https://ai-gateway.company.com
```

Oder fÃ¼gen Sie zu Ihrem Bash-Profil hinzu:

```bash title="~/.bash_profile"
export GITLAB_INSTANCE_URL=https://gitlab.company.com
export GITLAB_AI_GATEWAY_URL=https://ai-gateway.company.com
export GITLAB_TOKEN=glpat-...
```

:::note
Ihr GitLab-Administrator muss Folgendes aktivieren:

1. [Duo Agent Platform](https://docs.gitlab.com/user/duo_agent_platform/turn_on_off/) fÃ¼r den Benutzer, die Gruppe oder die Instanz
2. Feature-Flags (Ã¼ber die Rails-Konsole):
   - `agent_platform_claude_code`
   - `third_party_agents_enabled`
     :::

##### OAuth fÃ¼r selbstgehostete Instanzen

Damit OAuth fÃ¼r Ihre selbst gehostete Instanz funktioniert, mÃ¼ssen Sie eine erstellen
eine neue Anwendung (Einstellungen â†’ Anwendungen) mit dem
RÃ¼ckruf URL `http://127.0.0.1:8080/callback` und folgende Bereiche:

- API (Greifen Sie in Ihrem Namen auf API zu)
- read_user (Lesen Sie Ihre persÃ¶nlichen Daten)
- read_repository (ErmÃ¶glicht schreibgeschÃ¼tzten Zugriff auf das Repository)

Stellen Sie dann die Anwendung ID als Umgebungsvariable bereit:

```bash
export GITLAB_OAUTH_CLIENT_ID=your_application_id_here
```

Weitere Dokumentation auf der [voracode-gitlab-auth](https://www.npmjs.com/package/voracode-gitlab-auth)-Homepage.

##### Konfiguration

Anpassen Ã¼ber `voracode.json`:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "gitlab": {
      "options": {
        "instanceUrl": "https://gitlab.com"
      }
    }
  }
}
```

##### GitLab API Tools (Optional, aber dringend empfohlen)

So greifen Sie auf GitLab-Tools zu (ZusammenfÃ¼hrungsanfragen, Probleme, Pipelines, CI/CD usw.):

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "plugin": ["voracode-gitlab-plugin"]
}
```

Dieses Plugin bietet umfassende GitLab-Repository-Verwaltungsfunktionen, einschlieÃŸlich MR-ÃœberprÃ¼fungen, Problemverfolgung, Pipeline-Ãœberwachung und mehr.

---

### GitHub Copilot

So verwenden Sie Ihr GitHub Copilot-Abonnement mit Voracode:

:::note
Einige Modelle benÃ¶tigen mÃ¶glicherweise ein [Pro+
Abonnement](https://github.com/features/copilot/plans) zur Verwendung.

Einige Modelle mÃ¼ssen in Ihrem [GitHub Copilot settings](https://docs.github.com/en/copilot/how-tos/use-ai-models/configure-access-to-ai-models#setup-for-individual-use) manuell aktiviert werden.
:::

1. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach GitHub Copilot.

   ```txt
   /connect
   ```

2. Navigieren Sie zu [github.com/login/device](https://github.com/login/device) und geben Sie den Code ein.

   ```txt
   â”Œ Login with GitHub Copilot
   â”‚
   â”‚ https://github.com/login/device
   â”‚
   â”‚ Enter code: 8F43-6FCF
   â”‚
   â”” Waiting for authorization...
   ```

3. FÃ¼hren Sie nun den Befehl `/models` aus, um das gewÃ¼nschte Modell auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### Google Vertex AI

So verwenden Sie Google Vertex AI mit Voracode:

1. Gehen Sie zum **Model Garden** in der Google Cloud Console und Ã¼berprÃ¼fen Sie die
   Modelle, die in Ihrer Region verfÃ¼gbar sind.

   :::note
   Sie benÃ¶tigen ein Google Cloud-Projekt mit aktiviertem Vertex AI API.
   :::

2. Legen Sie die erforderlichen Umgebungsvariablen fest:
   - `GOOGLE_CLOUD_PROJECT`: Ihr Google Cloud-Projekt ID
   - `VERTEX_LOCATION` (optional): Die Region fÃ¼r Vertex AI (standardmÃ¤ÃŸig `global`)
   - Authentifizierung (wÃ¤hlen Sie eine aus):
     - `GOOGLE_APPLICATION_CREDENTIALS`: Pfad zur SchlÃ¼sseldatei Ihres Dienstkontos JSON
     - Authentifizieren Sie sich mit gcloud CLI: `gcloud auth application-default login`

   Legen Sie sie fest, wÃ¤hrend Sie Voracode ausfÃ¼hren.

   ```bash
   GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json GOOGLE_CLOUD_PROJECT=your-project-id voracode
   ```

   Oder fÃ¼gen Sie sie Ihrem Bash-Profil hinzu.

   ```bash title="~/.bash_profile"
   export GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
   export GOOGLE_CLOUD_PROJECT=your-project-id
   export VERTEX_LOCATION=global
   ```

:::tip
Die Region `global` verbessert die VerfÃ¼gbarkeit und reduziert Fehler ohne zusÃ¤tzliche Kosten. Verwenden Sie regionale Endpunkte (e.g., `us-central1`) fÃ¼r Datenresidenzanforderungen. [Learn more](https://cloud.google.com/vertex-ai/generative-ai/docs/partner-models/use-partner-models#regional_and_global_endpoints)
:::

3. FÃ¼hren Sie den Befehl `/models` aus, um das gewÃ¼nschte Modell auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### Groq

1. Gehen Sie zu [Groq console](https://console.groq.com/), klicken Sie auf **Create API Key** und kopieren Sie den SchlÃ¼ssel.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach Groq.

   ```txt
   /connect
   ```

3. Geben Sie den SchlÃ¼ssel API fÃ¼r den Anbieter ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um das gewÃ¼nschte auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### Hugging Face

[Hugging Face Inference Providers](https://huggingface.co/docs/inference-providers) bietet Zugriff auf offene Modelle, die von 17+-Anbietern unterstÃ¼tzt werden.

1. Gehen Sie zu [Hugging Face settings](https://huggingface.co/settings/tokens/new?ownUserPermissions=inference.serverless.write&tokenType=fineGrained), um ein Token mit der Berechtigung zum Aufrufen von Inferenzanbietern zu erstellen.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **Hugging Face**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren Hugging Face-Token ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um ein Modell wie _Kimi-K2-Instruct_ oder _GLM-4.6_ auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### Helicone

[Helicone](https://helicone.ai) ist eine LLM-Beobachtbarkeitsplattform, die Protokollierung, Ãœberwachung und Analyse fÃ¼r Ihre AI-Anwendungen bietet. Das Helicone AI Gateway leitet Ihre Anfragen basierend auf dem Modell automatisch an den entsprechenden Anbieter weiter.

1. Gehen Sie zu [Helicone](https://helicone.ai), erstellen Sie ein Konto und generieren Sie einen API-SchlÃ¼ssel Ã¼ber Ihr Dashboard.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **Helicone**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren Helicone-API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um ein Modell auszuwÃ¤hlen.

   ```txt
   /models
   ```

Weitere Anbieter und erweiterte Funktionen wie Caching und Ratenbegrenzung finden Sie unter [Helicone documentation](https://docs.helicone.ai).

#### Optionale Konfigurationen

Falls Sie eine Funktion oder ein Modell von Helicone sehen, das nicht automatisch Ã¼ber Voracode konfiguriert wird, kÃ¶nnen Sie es jederzeit selbst konfigurieren.

Hier ist [Helicone's Model Directory](https://helicone.ai/models). Sie benÃ¶tigen dies, um die IDs der Modelle abzurufen, die Sie hinzufÃ¼gen mÃ¶chten.

```jsonc title="~/.config/voracode/voracode.jsonc"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "helicone": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Helicone",
      "options": {
        "baseURL": "https://ai-gateway.helicone.ai",
      },
      "models": {
        "gpt-4o": {
          // Model ID (from Helicone's model directory page)
          "name": "GPT-4o", // Your own custom name for the model
        },
        "claude-sonnet-4-20250514": {
          "name": "Claude Sonnet 4",
        },
      },
    },
  },
}
```

#### Benutzerdefinierte Header

Helicone unterstÃ¼tzt benutzerdefinierte Header fÃ¼r Funktionen wie Caching, Benutzerverfolgung und Sitzungsverwaltung. FÃ¼gen Sie sie mit `options.headers` zu Ihrer Anbieterkonfiguration hinzu:

```jsonc title="~/.config/voracode/voracode.jsonc"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "helicone": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Helicone",
      "options": {
        "baseURL": "https://ai-gateway.helicone.ai",
        "headers": {
          "Helicone-Cache-Enabled": "true",
          "Helicone-User-Id": "voracode",
        },
      },
    },
  },
}
```

##### Sitzungsverfolgung

Mit der [Sessions](https://docs.helicone.ai/features/sessions)-Funktion von Helicone kÃ¶nnen Sie zusammengehÃ¶rige LLM-Anfragen gruppieren. Verwenden Sie das [voracode-helicone-session](https://github.com/H2Shami/voracode-helicone-session)-Plugin, um jede Voracode-Konversation automatisch als Sitzung in Helicone zu protokollieren.

```bash
npm install -g voracode-helicone-session
```

FÃ¼gen Sie es Ihrer Konfiguration hinzu.

```json title="voracode.json"
{
  "plugin": ["voracode-helicone-session"]
}
```

Das Plugin fÃ¼gt `Helicone-Session-Id`- und `Helicone-Session-Name`-Header in Ihre Anfragen ein. Auf der Seite â€žSitzungenâ€œ von Helicone wird jede Voracode-Konversation als separate Sitzung aufgefÃ¼hrt.

##### GÃ¤ngige Helicone-Header

| Kopfzeile                  | Beschreibung                                                                        |
| -------------------------- | ----------------------------------------------------------------------------------- |
| `Helicone-Cache-Enabled`   | Antwort-Caching aktivieren (`true`/`false`)                                         |
| `Helicone-User-Id`         | Verfolgen Sie Metriken nach Benutzer                                                |
| `Helicone-Property-[Name]` | Benutzerdefinierte Eigenschaften hinzufÃ¼gen (e.g., `Helicone-Property-Environment`) |
| `Helicone-Prompt-Id`       | Anfragen mit Eingabeaufforderungsversionen verknÃ¼pfen                               |

Alle verfÃ¼gbaren Header finden Sie unter [Helicone Header Directory](https://docs.helicone.ai/helicone-headers/header-directory).

---

### llama.cpp

Sie kÃ¶nnen Voracode fÃ¼r die Verwendung lokaler Modelle Ã¼ber das Dienstprogramm [llama.cpp's](https://github.com/ggml-org/llama.cpp) llama-server konfigurieren

```json title="voracode.json" "llama.cpp" {5, 6, 8, 10-15}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "llama.cpp": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "llama-server (local)",
      "options": {
        "baseURL": "http://127.0.0.1:8080/v1"
      },
      "models": {
        "qwen3-coder:a3b": {
          "name": "Qwen3-Coder: a3b-30b (local)",
          "limit": {
            "context": 128000,
            "output": 65536
          }
        }
      }
    }
  }
}
```

In diesem Beispiel:

- `llama.cpp` ist der benutzerdefinierte Anbieter ID. Dies kann eine beliebige Zeichenfolge sein.
- `npm` gibt das Paket an, das fÃ¼r diesen Anbieter verwendet werden soll. Hier wird `@ai-sdk/openai-compatible` fÃ¼r jeden OpenAI-kompatiblen API verwendet.
- `name` ist der Anzeigename fÃ¼r den Anbieter im UI.
- `options.baseURL` ist der Endpunkt fÃ¼r den lokalen Server.
- `models` ist eine Zuordnung von Modell-IDs zu ihren Konfigurationen. Der Modellname wird in der Modellauswahlliste angezeigt.

---

### IO.NET

IO.NET bietet 17-Modelle, die fÃ¼r verschiedene AnwendungsfÃ¤lle optimiert sind:

1. Gehen Sie zu [IO.NET console](https://ai.io.net/), erstellen Sie ein Konto und generieren Sie einen API-SchlÃ¼ssel.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **IO.NET**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren IO.NET API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um ein Modell auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### LM Studio

Sie kÃ¶nnen Voracode Ã¼ber LM Studio fÃ¼r die Verwendung lokaler Modelle konfigurieren.

```json title="voracode.json" "lmstudio" {5, 6, 8, 10-14}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "lmstudio": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "LM Studio (local)",
      "options": {
        "baseURL": "http://127.0.0.1:1234/v1"
      },
      "models": {
        "google/gemma-3n-e4b": {
          "name": "Gemma 3n-e4b (local)"
        }
      }
    }
  }
}
```

In diesem Beispiel:

- `lmstudio` ist der benutzerdefinierte Anbieter ID. Dies kann eine beliebige Zeichenfolge sein.
- `npm` gibt das Paket an, das fÃ¼r diesen Anbieter verwendet werden soll. Hier wird `@ai-sdk/openai-compatible` fÃ¼r jeden OpenAI-kompatiblen API verwendet.
- `name` ist der Anzeigename fÃ¼r den Anbieter im UI.
- `options.baseURL` ist der Endpunkt fÃ¼r den lokalen Server.
- `models` ist eine Zuordnung von Modell-IDs zu ihren Konfigurationen. Der Modellname wird in der Modellauswahlliste angezeigt.

---

### Moonshot AI

So verwenden Sie Kimi K2 von Moonshot AI:

1. Gehen Sie zu [Moonshot AI console](https://platform.moonshot.ai/console), erstellen Sie ein Konto und klicken Sie auf **SchlÃ¼ssel API erstellen**.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **Moonshot AI**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren Moonshot API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um _Kimi K2_ auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### MiniMax

1. Gehen Sie zu [MiniMax API Console](https://platform.minimax.io/login), erstellen Sie ein Konto und generieren Sie einen API-SchlÃ¼ssel.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **MiniMax**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren MiniMax API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um ein Modell wie _M2.1_ auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### Nebius Token Factory

1. Gehen Sie zu [Nebius Token Factory console](https://tokenfactory.nebius.com/), erstellen Sie ein Konto und klicken Sie auf **SchlÃ¼ssel hinzufÃ¼gen**.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **Nebius Token Factory**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren Nebius Token Factory API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um ein Modell wie _Kimi K2 Instruct_ auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### Ollama

Sie kÃ¶nnen Voracode Ã¼ber Ollama fÃ¼r die Verwendung lokaler Modelle konfigurieren.

:::tip
Ollama kann sich automatisch fÃ¼r Voracode konfigurieren. Weitere Informationen finden Sie unter [Ollama integration docs](https://docs.ollama.com/integrations/voracode).
:::

```json title="voracode.json" "ollama" {5, 6, 8, 10-14}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "ollama": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Ollama (local)",
      "options": {
        "baseURL": "http://localhost:11434/v1"
      },
      "models": {
        "llama2": {
          "name": "Llama 2"
        }
      }
    }
  }
}
```

In diesem Beispiel:

- `ollama` ist der benutzerdefinierte Anbieter ID. Dies kann eine beliebige Zeichenfolge sein.
- `npm` gibt das Paket an, das fÃ¼r diesen Anbieter verwendet werden soll. Hier wird `@ai-sdk/openai-compatible` fÃ¼r jeden OpenAI-kompatiblen API verwendet.
- `name` ist der Anzeigename fÃ¼r den Anbieter im UI.
- `options.baseURL` ist der Endpunkt fÃ¼r den lokalen Server.
- `models` ist eine Zuordnung von Modell-IDs zu ihren Konfigurationen. Der Modellname wird in der Modellauswahlliste angezeigt.

:::tip
Wenn Toolaufrufe nicht funktionieren, versuchen Sie, `num_ctx` in Ollama zu erhÃ¶hen. Beginnen Sie bei ca. 16.000 â€“ 32.000 km.
:::

---

### Ollama Cloud

So verwenden Sie Ollama Cloud mit Voracode:

1. Gehen Sie zu [https://ollama.com/](https://ollama.com/) und melden Sie sich an oder erstellen Sie ein Konto.

2. Navigieren Sie zu **Einstellungen** > **SchlÃ¼ssel** und klicken Sie auf **Add API Key**, um einen neuen API-SchlÃ¼ssel zu generieren.

3. Kopieren Sie den API-SchlÃ¼ssel zur Verwendung in Voracode.

4. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **Ollama Cloud**.

   ```txt
   /connect
   ```

5. Geben Sie Ihren Ollama Cloud API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

6. **Wichtig**: Bevor Sie Cloud-Modelle in Voracode verwenden, mÃ¼ssen Sie die Modellinformationen lokal abrufen:

   ```bash
   ollama pull gpt-oss:20b-cloud
   ```

7. FÃ¼hren Sie den Befehl `/models` aus, um Ihr Ollama Cloud-Modell auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### OpenAI

Wir empfehlen, sich fÃ¼r [ChatGPT Plus or Pro](https://chatgpt.com/pricing) anzumelden.

1. FÃ¼hren Sie nach der Anmeldung den Befehl `/connect` aus und wÃ¤hlen Sie OpenAI aus.

   ```txt
   /connect
   ```

2. Hier kÃ¶nnen Sie die Option **ChatGPT Plus/Pro** auswÃ¤hlen und Ihr Browser wird geÃ¶ffnet
   und bitten Sie, sich zu authentifizieren.

   ```txt
   â”Œ Select auth method
   â”‚
   â”‚ ChatGPT Plus/Pro
   â”‚ Manually enter API Key
   â””
   ```

3. Jetzt sollten alle OpenAI-Modelle verfÃ¼gbar sein, wenn Sie den Befehl `/models` verwenden.

   ```txt
   /models
   ```

##### Verwendung von API-Keys

Wenn Sie bereits Ã¼ber einen API-SchlÃ¼ssel verfÃ¼gen, kÃ¶nnen Sie **API-SchlÃ¼ssel manuell eingeben** auswÃ¤hlen und ihn in Ihr Terminal einfÃ¼gen.

---

### Voracode Zen

Voracode Zen ist eine Liste getesteter und verifizierter Modelle, die vom Voracode-Team bereitgestellt werden. [Learn more](/docs/zen).

1. Melden Sie sich bei **<a href={console}>Voracode Zen</a>** an und klicken Sie auf **Create API Key**.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **Voracode Zen**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren Voracode API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um ein Modell wie _Qwen 3 Coder 480B_ auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### OpenRouter

1. Gehen Sie zu [OpenRouter dashboard](https://openrouter.ai/settings/keys), klicken Sie auf **Create API Key** und kopieren Sie den SchlÃ¼ssel.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach OpenRouter.

   ```txt
   /connect
   ```

3. Geben Sie den SchlÃ¼ssel API fÃ¼r den Anbieter ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. Viele OpenRouter-Modelle sind standardmÃ¤ÃŸig vorinstalliert. FÃ¼hren Sie den Befehl `/models` aus, um das gewÃ¼nschte Modell auszuwÃ¤hlen.

   ```txt
   /models
   ```

   Sie kÃ¶nnen auch zusÃ¤tzliche Modelle Ã¼ber Ihre Voracode-Konfiguration hinzufÃ¼gen.

   ```json title="voracode.json" {6}
   {
     "$schema": "https://voracode.ai/config.json",
     "provider": {
       "openrouter": {
         "models": {
           "somecoolnewmodel": {}
         }
       }
     }
   }
   ```

5. Sie kÃ¶nnen sie auch Ã¼ber Ihre Voracode-Konfiguration anpassen. Hier ist ein Beispiel fÃ¼r die Angabe eines Anbieters

   ```json title="voracode.json"
   {
     "$schema": "https://voracode.ai/config.json",
     "provider": {
       "openrouter": {
         "models": {
           "moonshotai/kimi-k2": {
             "options": {
               "provider": {
                 "order": ["baseten"],
                 "allow_fallbacks": false
               }
             }
           }
         }
       }
     }
   }
   ```

---

### SAP AI Core

SAP AI Core bietet Zugriff auf 40+ Modelle von OpenAI, Anthropic, Google, Amazon, Meta, Mistral und AI21 Ã¼ber eine einheitliche Plattform.

1. Gehen Sie zu Ihrem [SAP BTP Cockpit](https://account.hana.ondemand.com/), navigieren Sie zu Ihrer SAP AI Core-Dienstinstanz und erstellen Sie einen DienstschlÃ¼ssel.

   :::tip
   Der DienstschlÃ¼ssel ist ein JSON-Objekt, das `clientid`, `clientsecret`, `url` und `serviceurls.AI_API_URL` enthÃ¤lt. Sie finden Ihre AI Core-Instanz unter **Dienste** > **Instanzen und Abonnements** im BTP Cockpit.
   :::

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **SAP AI Core**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren JSON-Service-Account-Key ein.

   ```txt
   â”Œ Service key
   â”‚
   â”‚
   â”” enter
   ```

   Oder legen Sie die Umgebungsvariable `AICORE_SERVICE_KEY` fest:

   ```bash
   AICORE_SERVICE_KEY='{"clientid":"...","clientsecret":"...","url":"...","serviceurls":{"AI_API_URL":"..."}}' voracode
   ```

   Oder fÃ¼gen Sie es Ihrem Bash-Profil hinzu:

   ```bash title="~/.bash_profile"
   export AICORE_SERVICE_KEY='{"clientid":"...","clientsecret":"...","url":"...","serviceurls":{"AI_API_URL":"..."}}'
   ```

4. Legen Sie optional die Bereitstellung ID und die Ressourcengruppe fest:

   ```bash
   AICORE_DEPLOYMENT_ID=your-deployment-id AICORE_RESOURCE_GROUP=your-resource-group voracode
   ```

   :::note
   Diese Einstellungen sind optional und sollten entsprechend Ihrem SAP AI Core-Setup konfiguriert werden.
   :::

5. FÃ¼hren Sie den Befehl `/models` aus, um aus 40+ verfÃ¼gbaren Modellen auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### STACKIT

STACKIT AI Model Serving bietet eine voll verwaltete, souveraene Hosting-Umgebung fuer AI-Modelle, mit Fokus auf LLMs wie Llama, Mistral und Qwen, mit maximaler Datensouveraenitaet auf europaeischer Infrastruktur.

1. Gehen Sie zum [STACKIT Portal](https://portal.stackit.cloud), navigieren Sie zu **AI Model Serving** und erstellen Sie ein Auth-Token fuer Ihr Projekt.

   :::tip
   Sie benoetigen ein STACKIT-Kundenkonto, Benutzerkonto und Projekt, bevor Sie Auth-Tokens erstellen koennen.
   :::

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **STACKIT**.

   ```txt
   /connect
   ```

3. Geben Sie Ihr STACKIT AI Model Serving Auth-Token ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um aus verfÃ¼gbaren Modellen wie _Qwen3-VL 235B_ oder _Llama 3.3 70B_ auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### OVHcloud AI Endpoints

1. Gehen Sie zum [OVHcloud panel](https://ovh.com/manager). Navigieren Sie zum Abschnitt `Public Cloud`, `AI & Machine Learning` > `AI Endpoints` und klicken Sie auf der Registerkarte `API Keys` auf **Neuen API-SchlÃ¼ssel erstellen**.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **OVHcloud AI Endpoints**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren OVHcloud AI Endpoints API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um ein Modell wie _gpt-oss-120b_ auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### Scaleway

So verwenden Sie [Scaleway Generative APIs](https://www.scaleway.com/en/docs/generative-apis/) mit Voracode:

1. Gehen Sie zu [Scaleway Console IAM settings](https://console.scaleway.com/iam/api-keys), um einen neuen API-SchlÃ¼ssel zu generieren.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **Scaleway**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren Scaleway-API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um ein Modell wie _devstral-2-123b-instruct-2512_ oder _gpt-oss-120b_ auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### Together AI

1. Gehen Sie zu [Together AI console](https://api.together.ai), erstellen Sie ein Konto und klicken Sie auf **SchlÃ¼ssel hinzufÃ¼gen**.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **Together AI**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren Together AI API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um ein Modell wie _Kimi K2 Instruct_ auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### Venice AI

1. Gehen Sie zu [Venice AI console](https://venice.ai), erstellen Sie ein Konto und generieren Sie einen API-SchlÃ¼ssel.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **Venice AI**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren Venice AI API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um ein Modell wie _Llama 3.3 70B_ auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### Vercel AI Gateway

Mit dem Vercel AI Gateway kÃ¶nnen Sie Ã¼ber einen einheitlichen Endpunkt auf Modelle von OpenAI, Anthropic, Google, xAI und mehr zugreifen. Die Modelle werden zum Listenpreis ohne Aufschlag angeboten.

1. Gehen Sie zum [Vercel dashboard](https://vercel.com/), navigieren Sie zur Registerkarte **AI Gateway** und klicken Sie auf **API-SchlÃ¼ssel**, um einen neuen API-SchlÃ¼ssel zu erstellen.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **Vercel AI Gateway**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren Vercel AI Gateway API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um ein Modell auszuwÃ¤hlen.

   ```txt
   /models
   ```

Sie kÃ¶nnen Modelle auch Ã¼ber Ihre Voracode-Konfiguration anpassen. Hier ist ein Beispiel fÃ¼r die Angabe der Provider-Routing-Reihenfolge.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "vercel": {
      "models": {
        "anthropic/claude-sonnet-4": {
          "options": {
            "order": ["anthropic", "vertex"]
          }
        }
      }
    }
  }
}
```

Einige nÃ¼tzliche Routing-Optionen:

| Option              | Beschreibung                                                      |
| ------------------- | ----------------------------------------------------------------- |
| `order`             | Anbietersequenz zum Ausprobieren                                  |
| `only`              | Auf bestimmte Anbieter beschrÃ¤nken                                |
| `zeroDataRetention` | Verwenden Sie nur Anbieter mit Null-Datenaufbewahrungsrichtlinien |

---

### xAI

1. Gehen Sie zu [xAI console](https://console.x.ai/), erstellen Sie ein Konto und generieren Sie einen API-SchlÃ¼ssel.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **xAI**.

   ```txt
   /connect
   ```

3. Geben Sie Ihren xAI API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um ein Modell wie _Grok Beta_ auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### Z.AI

1. Gehen Sie zu [Z.AI API console](https://z.ai/manage-apikey/apikey-list), erstellen Sie ein Konto und klicken Sie auf **Neuen API-SchlÃ¼ssel erstellen**.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach **Z.AI**.

   ```txt
   /connect
   ```

   Wenn Sie den **GLM Coding Plan** abonniert haben, wÃ¤hlen Sie **Z.AI Coding Plan**.

3. Geben Sie Ihren Z.AI API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. FÃ¼hren Sie den Befehl `/models` aus, um ein Modell wie _GLM-4.7_ auszuwÃ¤hlen.

   ```txt
   /models
   ```

---

### ZenMux

1. Gehen Sie zu [ZenMux dashboard](https://zenmux.ai/settings/keys), klicken Sie auf **Create API Key** und kopieren Sie den SchlÃ¼ssel.

2. FÃ¼hren Sie den Befehl `/connect` aus und suchen Sie nach ZenMux.

   ```txt
   /connect
   ```

3. Geben Sie den SchlÃ¼ssel API fÃ¼r den Anbieter ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

4. Viele ZenMux-Modelle sind standardmÃ¤ÃŸig vorinstalliert. FÃ¼hren Sie den Befehl `/models` aus, um das gewÃ¼nschte Modell auszuwÃ¤hlen.

   ```txt
   /models
   ```

   Sie kÃ¶nnen auch zusÃ¤tzliche Modelle Ã¼ber Ihre Voracode-Konfiguration hinzufÃ¼gen.

   ```json title="voracode.json" {6}
   {
     "$schema": "https://voracode.ai/config.json",
     "provider": {
       "zenmux": {
         "models": {
           "somecoolnewmodel": {}
         }
       }
     }
   }
   ```

---

## Benutzerdefinierter Anbieter

So fÃ¼gen Sie einen **OpenAI-kompatiblen** Anbieter hinzu, der nicht im Befehl `/connect` aufgefÃ¼hrt ist:

:::tip
Sie kÃ¶nnen jeden OpenAI-kompatiblen Anbieter mit Voracode verwenden. Die meisten modernen AI-Anbieter bieten OpenAI-kompatible APIs an.
:::

1. FÃ¼hren Sie den Befehl `/connect` aus und scrollen Sie nach unten zu **Andere**.

   ```bash
   $ /connect

   â”Œ  Add credential
   â”‚
   â—†  Select provider
   â”‚  ...
   â”‚  â— Other
   â””
   ```

2. Geben Sie einen eindeutigen ID fÃ¼r den Anbieter ein.

   ```bash
   $ /connect

   â”Œ  Add credential
   â”‚
   â—‡  Enter provider id
   â”‚  myprovider
   â””
   ```

   :::note
   WÃ¤hlen Sie einen einprÃ¤gsamen ID, den Sie in Ihrer Konfigurationsdatei verwenden.
   :::

3. Geben Sie Ihren API-SchlÃ¼ssel fÃ¼r den Anbieter ein.

   ```bash
   $ /connect

   â”Œ  Add credential
   â”‚
   â–²  This only stores a credential for myprovider - you will need to configure it in voracode.json, check the docs for examples.
   â”‚
   â—‡  Enter your API key
   â”‚  sk-...
   â””
   ```

4. Erstellen oder aktualisieren Sie Ihre `voracode.json`-Datei in Ihrem Projektverzeichnis:

   ```json title="voracode.json" ""myprovider"" {5-15}
   {
     "$schema": "https://voracode.ai/config.json",
     "provider": {
       "myprovider": {
         "npm": "@ai-sdk/openai-compatible",
         "name": "My AI ProviderDisplay Name",
         "options": {
           "baseURL": "https://api.myprovider.com/v1"
         },
         "models": {
           "my-model-name": {
             "name": "My Model Display Name"
           }
         }
       }
     }
   }
   ```

   Hier sind die KonfigurationsmÃ¶glichkeiten:
   - **npm**: AI SDK zu verwendendes Paket, `@ai-sdk/openai-compatible` fÃ¼r OpenAI-kompatible Anbieter
   - **Name**: Anzeigename in UI.
   - **Modelle**: VerfÃ¼gbare Modelle.
   - **options.baseURL**: API endpoint URL.
   - **Optionen.apiKey**: Legen Sie optional den SchlÃ¼ssel API fest, wenn Sie keine Authentifizierung verwenden.
   - **options.headers**: Optional benutzerdefinierte Header festlegen.

   Mehr zu den erweiterten Optionen im Beispiel unten.

5. FÃ¼hren Sie den Befehl `/models` aus und Ihr benutzerdefinierter Anbieter und Ihre Modelle werden in der Auswahlliste angezeigt.

---

##### Beispiel

Hier ist ein Beispiel fÃ¼r die Einstellung der Optionen `apiKey`, `headers` und Modell `limit`.

```json title="voracode.json" {9,11,17-20}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "myprovider": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "My AI ProviderDisplay Name",
      "options": {
        "baseURL": "https://api.myprovider.com/v1",
        "apiKey": "{env:ANTHROPIC_API_KEY}",
        "headers": {
          "Authorization": "Bearer custom-token"
        }
      },
      "models": {
        "my-model-name": {
          "name": "My Model Display Name",
          "limit": {
            "context": 200000,
            "output": 65536
          }
        }
      }
    }
  }
}
```

Konfigurationsdetails:

- **apiKey**: Wird mit der Variablensyntax `env`, [learn more](/docs/config#env-vars), festgelegt.
- **Header**: Benutzerdefinierte Header, die mit jeder Anfrage gesendet werden.
- **limit.context**: Maximale Eingabetokens, die das Modell akzeptiert.
- **limit.output**: Maximale Token, die das Modell generieren kann.

Mithilfe der `limit`-Felder kann Voracode erkennen, wie viel Kontext Sie noch haben. Standardanbieter beziehen diese automatisch von models.dev.

---

## Fehlerbehebung

Wenn Sie Probleme beim Konfigurieren eines Anbieters haben, Ã¼berprÃ¼fen Sie Folgendes:

1. **ÃœberprÃ¼fen Sie die Authentifizierungseinrichtung**: FÃ¼hren Sie `voracode auth list` aus, um zu sehen, ob die Anmeldeinformationen vorhanden sind
   fÃ¼r den Anbieter werden Ihrer Konfiguration hinzugefÃ¼gt.

   Dies gilt nicht fÃ¼r Anbieter wie Amazon Bedrock, die fÃ¼r ihre Authentifizierung auf Umgebungsvariablen angewiesen sind.

2. ÃœberprÃ¼fen Sie bei benutzerdefinierten Anbietern die Voracode-Konfiguration und:
   - Stellen Sie sicher, dass der im Befehl `/connect` verwendete Anbieter ID mit dem ID in Ihrer Voracode-Konfiguration Ã¼bereinstimmt.
   - FÃ¼r den Anbieter wird das richtige npm-Paket verwendet. Verwenden Sie beispielsweise `@ai-sdk/cerebras` fÃ¼r Cerebras. Und fÃ¼r alle anderen OpenAI-kompatiblen Anbieter verwenden Sie `@ai-sdk/openai-compatible`.
   - ÃœberprÃ¼fen Sie, ob im Feld `options.baseURL` der richtige Endpunkt API verwendet wird.
