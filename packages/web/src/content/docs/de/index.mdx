---
title: EinfÃ¼hrung
description: Beginnen Sie mit Voracode.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"
import config from "../../../../config.mjs"
export const console = config.console

[**Voracode**](/) ist ein Open-Source-Coding-Agent fÃ¼r AI. Es ist als terminalbasierte Schnittstelle, Desktop-App oder IDE-Erweiterung verfÃ¼gbar.

![Voracode TUI with the voracode theme](../../../assets/lander/screenshot.png)

Fangen wir an.

---

#### Voraussetzungen

Um Voracode in Ihrem Terminal verwenden zu kÃ¶nnen, benÃ¶tigen Sie:

1. Ein moderner Terminal-Emulator, zum Beispiel:
   - [WezTerm](https://wezterm.org), cross-platform
   - [Alacritty](https://alacritty.org), plattformÃ¼bergreifend
   - [Ghostty](https://ghostty.org), Linux und macOS
   - [Kitty](https://sw.kovidgoyal.net/kitty/), Linux und macOS

2. API-SchlÃ¼ssel fÃ¼r die LLM-Anbieter, die Sie verwenden mÃ¶chten.

---

## Installation

Der einfachste Weg, Voracode zu installieren, ist Ã¼ber das Installationsskript.

```bash
curl -fsSL https://voracode.ai/install | bash
```

Sie kÃ¶nnen es auch mit den folgenden Befehlen installieren:

- **Mit Node.js**

        <Tabs>

      <TabItem label="npm">
      ```bash
      npm install -g voracode-ai
      ```

          </TabItem>

        <TabItem label="Bun">
        ```bash
        bun install -g voracode-ai
        ```

          </TabItem>

        <TabItem label="pnpm">
        ```bash
        pnpm install -g voracode-ai
        ```

          </TabItem>

        <TabItem label="Yarn">
        ```bash
        yarn global add voracode-ai
        ```

      </TabItem>

  </Tabs>

- **Homebrew unter macOS und Linux verwenden**

  ```bash
  brew install mysterious75/tap/voracode
  ```

  > Wir empfehlen die Verwendung des Voracode-Taps fÃ¼r die aktuellsten Versionen. Die offizielle `brew install voracode`-Formel wird vom Homebrew-Team gepflegt und seltener aktualisiert.

- **Verwendung von Paru unter Arch Linux**

  ```bash
  sudo pacman -S voracode           # Arch Linux (Stable)
  paru -S voracode-bin              # Arch Linux (Latest from AUR)
  ```

#### Windows

:::tip[Empfohlen: Verwenden Sie WSL]
FÃ¼r das beste Erlebnis unter Windows empfehlen wir die Verwendung von [Windows Subsystem for Linux (WSL)](/docs/windows-wsl). Es bietet eine bessere Leistung und vollstÃ¤ndige KompatibilitÃ¤t mit den Funktionen von Voracode.
:::

- **Verwendung von Chocolatey**

  ```bash
  choco install voracode
  ```

- **Scoop verwenden**

  ```bash
  scoop install voracode
  ```

- **Mit NPM**

  ```bash
  npm install -g voracode-ai
  ```

- **Mit Mise**

  ```bash
  mise use -g github:mysterious75/voracode
  ```

- **Verwendung von Docker**

  ```bash
  docker run -it --rm ghcr.io/mysterious75/voracode
  ```

Die UnterstÃ¼tzung fÃ¼r die Installation von Voracode unter Windows mit Bun ist derzeit in Arbeit.

Sie kÃ¶nnen die BinÃ¤rdatei auch aus [Releases](https://github.com/mysterious75/voracode/releases) abrufen.

---

## Konfiguration

Mit Voracode kÃ¶nnen Sie jeden LLM-Anbieter verwenden, indem Sie dessen API-SchlÃ¼ssel konfigurieren.

Wenn Sie mit der Verwendung von LLM-Anbietern noch nicht vertraut sind, empfehlen wir die Verwendung von [Voracode Zen](/docs/zen).
Es handelt sich um eine kuratierte Liste von Modellen, die vom Voracode Team getestet und verifiziert wurden.

1. FÃ¼hren Sie den Befehl `/connect` in TUI aus, wÃ¤hlen Sie Voracode aus und gehen Sie zu [voracode.ai/auth](https://voracode.ai/auth).

   ```txt
   /connect
   ```

2. Melden Sie sich an, geben Sie Ihre Rechnungsdaten ein und kopieren Sie Ihren API-SchlÃ¼ssel.

3. FÃ¼gen Sie Ihren API-SchlÃ¼ssel ein.

   ```txt
   â”Œ API key
   â”‚
   â”‚
   â”” enter
   ```

Alternativ kÃ¶nnen Sie auch einen der anderen Anbieter auswÃ¤hlen. [Mehr erfahren](/docs/providers#directory).

---

## Initialisierung

Nachdem Sie nun einen Anbieter konfiguriert haben, kÃ¶nnen Sie zu einem Projekt navigieren
an dem Sie arbeiten mÃ¶chten.

```bash
cd /path/to/project
```

Und fÃ¼hren Sie Voracode aus.

```bash
voracode
```

Als nÃ¤chstes initialisieren Sie Voracode fÃ¼r das Projekt, indem Sie den folgenden Befehl ausfÃ¼hren.

```bash frame="none"
/init
```

Dadurch wird Voracode dazu veranlasst, Ihr Projekt zu analysieren und eine `AGENTS.md`-Datei im Projektstammverzeichnis zu erstellen.

:::tip
Sie sollten die `AGENTS.md`-Datei Ihres Projekts an Git Ã¼bergeben.
:::

Dies hilft Voracode, die Projektstruktur und die Codierungsmuster zu verstehen
die verwendet werden.

---

## Verwendung

Sie kÃ¶nnen nun Voracode verwenden, um an Ihrem Projekt zu arbeiten. Fragen Sie ruhig nach
allem!

Wenn Sie mit der Verwendung eines AI-Codierungsagenten noch nicht vertraut sind, finden Sie hier einige Beispiele, die hilfreich sein kÃ¶nnten.

---

### Fragen stellen

Sie kÃ¶nnen Voracode bitten, Ihnen die Codebasis zu erklÃ¤ren.

:::tip
Verwenden Sie die Taste `@`, um eine Fuzzy-Suche nach Dateien im Projekt durchzufÃ¼hren.
:::

```txt frame="none" "@packages/functions/src/api/index.ts"
How is authentication handled in @packages/functions/src/api/index.ts
```

Dies ist hilfreich, wenn es einen Teil der Codebasis gibt, an dem Sie nicht gearbeitet haben.

---

### Funktionen hinzufÃ¼gen

Sie kÃ¶nnen Voracode bitten, Ihrem Projekt neue Funktionen hinzuzufÃ¼gen. Wir empfehlen jedoch, es zunÃ¤chst um die Erstellung eines Plans zu bitten.

1. **Erstellen Sie einen Plan**

   Voracode verfÃ¼gt Ã¼ber einen _Plan Mode_, der seine FÃ¤higkeit, Ã„nderungen vorzunehmen, deaktiviert
   Schlagen Sie stattdessen vor, wie die Funktion implementiert wird.

   Wechseln Sie mit der **Tabulatortaste** dorthin. In der unteren rechten Ecke sehen Sie einen Indikator dafÃ¼r.

   ```bash frame="none" title="In den Plan-Modus wechseln"
   <TAB>
   ```

   Beschreiben wir nun, was es tun soll.

   ```txt frame="none"
   When a user deletes a note, we'd like to flag it as deleted in the database.
   Then create a screen that shows all the recently deleted notes.
   From this screen, the user can undelete a note or permanently delete it.
   ```

   Sie mÃ¶chten Voracode genÃ¼gend Details geben, um zu verstehen, was Sie wollen. Es hilft
   mit ihm zu sprechen, als ob Sie mit einem Junior-Entwickler in Ihrem Team sprechen wÃ¼rden.

   :::tip
   Geben Sie Voracode reichlich Kontext und Beispiele, damit es versteht, was Sie
   wollen.
   :::

2. **Den Plan Ã¼berarbeiten**

   Sobald Sie einen Plan erhalten, kÃ¶nnen Sie ihm Feedback geben oder weitere Details hinzufÃ¼gen.

   ```txt frame="none"
   We'd like to design this new screen using a design I've used before.
   [Image #1] Take a look at this image and use it as a reference.
   ```

   :::tip
   Ziehen Sie Bilder per Drag-and-Drop in das Terminal, um sie zur Eingabeaufforderung hinzuzufÃ¼gen.
   :::

   Voracode kann alle von Ihnen bereitgestellten Bilder scannen und zur Eingabeaufforderung hinzufÃ¼gen. Du kannst
   Ziehen Sie dazu ein Bild per Drag & Drop in das Terminal.

3. **Funktion erstellen**

   Wenn Sie mit dem Plan zufrieden sind, wechseln Sie zurÃ¼ck in den _Build Mode_
   indem Sie erneut die **Tabulatortaste** drÃ¼cken.

   ```bash frame="none"
   <TAB>
   ```

   Und es bitten, die Ã„nderungen vorzunehmen.

   ```bash frame="none"
   Sounds good! Go ahead and make the changes.
   ```

---

### Ã„nderungen vornehmen

FÃ¼r einfachere Ã„nderungen kÃ¶nnen Sie Voracode bitten, es direkt zu erstellen
ohne den Plan vorher Ã¼berprÃ¼fen zu mÃ¼ssen.

```txt frame="none" "@packages/functions/src/settings.ts" "@packages/functions/src/notes.ts"
We need to add authentication to the /settings route. Take a look at how this is
handled in the /notes route in @packages/functions/src/notes.ts and implement
the same logic in @packages/functions/src/settings.ts
```

Sie mÃ¶chten sicherstellen, dass Sie viele Details bereitstellen, damit Voracode die richtigen
Ã„nderungen vornimmt.

---

### Ã„nderungen rÃ¼ckgÃ¤ngig machen

Nehmen wir an, Sie bitten Voracode, einige Ã„nderungen vorzunehmen.

```txt frame="none" "@packages/functions/src/api/index.ts"
Can you refactor the function in @packages/functions/src/api/index.ts?
```

Aber Sie erkennen, dass es nicht das ist, was Sie wollten. Sie kÃ¶nnen die Ã„nderungen **rÃ¼ckgÃ¤ngig machen**
mit dem Befehl `/undo`.

```bash frame="none"
/undo
```

Voracode macht nun die von Ihnen vorgenommenen Ã„nderungen rÃ¼ckgÃ¤ngig und zeigt Ihre ursprÃ¼ngliche Nachricht
wieder an.

```txt frame="none" "@packages/functions/src/api/index.ts"
Can you refactor the function in @packages/functions/src/api/index.ts?
```

Von hier aus kÃ¶nnen Sie die Eingabeaufforderung anpassen und Voracode bitten, es erneut zu versuchen.

:::tip
Sie kÃ¶nnen `/undo` mehrmals ausfÃ¼hren, um mehrere Ã„nderungen rÃ¼ckgÃ¤ngig zu machen.
:::

Oder Sie **kÃ¶nnen** die Ã„nderungen mit dem Befehl `/redo` wiederholen.

```bash frame="none"
/redo
```

---

## Teilen

Die GesprÃ¤che, die Sie mit Voracode fÃ¼hren, kÃ¶nnen [mit Ihrem Team](/docs/share) geteilt werden.

```bash frame="none"
/share
```

Dadurch wird ein Link zur aktuellen Konversation erstellt und in Ihre Zwischenablage kopiert.

:::note
GesprÃ¤che werden standardmÃ¤ÃŸig nicht geteilt.
:::

Hier ist ein [BeispielgesprÃ¤ch](https://voracode.ai/s/4XP1fce5) mit Voracode.

---

## Anpassung

Und das ist es! Sie sind jetzt ein Profi im Umgang mit Voracode.

Um es zu Ihrem eigenen zu machen, empfehlen wir [ein Theme auszuwÃ¤hlen](/docs/themes), [Keybinds anzupassen](/docs/keybinds), [Code-Formatter zu konfigurieren](/docs/formatters), [eigene Commands zu erstellen](/docs/commands) oder mit der [Voracode-Konfiguration](/docs/config) zu experimentieren.
