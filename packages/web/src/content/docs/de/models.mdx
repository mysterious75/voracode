---
title: Modelle
description: Konfigurieren eines LLM-Anbieters und -Modells.
---

Voracode verwendet [AI SDK](https://ai-sdk.dev/) und [Models.dev](https://models.dev) zur UnterstÃ¼tzung von **75+ LLM-Anbietern** und unterstÃ¼tzt die AusfÃ¼hrung lokaler Modelle.

---

## Anbieter

Die meisten gÃ¤ngigen Anbieter sind standardmÃ¤ÃŸig vorinstalliert. Wenn Sie die Anmeldeinformationen fÃ¼r einen Anbieter Ã¼ber den Befehl `/connect` hinzugefÃ¼gt haben, sind diese verfÃ¼gbar, wenn Sie Voracode starten.

Erfahren Sie mehr Ã¼ber [providers](/docs/providers).

---

## Modell auswÃ¤hlen

Sobald Sie Ihren Anbieter konfiguriert haben, kÃ¶nnen Sie das gewÃ¼nschte Modell auswÃ¤hlen, indem Sie Folgendes eingeben:

```bash frame="none"
/models
```

---

## Empfohlene Modelle

Es gibt viele Modelle da drauÃŸen und jede Woche kommen neue Modelle heraus.

:::tip
ErwÃ¤gen Sie die Verwendung eines der von uns empfohlenen Modelle.
:::

Allerdings gibt es nur wenige von ihnen, die sowohl gut darin sind, Code zu generieren als auch Tools aufzurufen.

Hier sind mehrere Modelle, die gut mit Voracode funktionieren, in keiner bestimmten Reihenfolge. (Dies ist weder eine vollstÃ¤ndige noch eine unbedingt aktuelle Liste):

- GPT 5.2
- GPT 5.1 Kodex
- Claude Opus 4.5
- Claude Sonett 4.5
- Minimax M2.1
- Gemini 3 Pro

---

## Standard festlegen

Um eines davon als Standardmodell festzulegen, kÃ¶nnen Sie den SchlÃ¼ssel `model` in Ihrem festlegen
Voracode config.

```json title="voracode.json" {3}
{
  "$schema": "https://voracode.ai/config.json",
  "model": "lmstudio/google/gemma-3n-e4b"
}
```

Hier ist der vollstÃ¤ndige ID `provider_id/model_id`. Wenn Sie beispielsweise [Voracode Zen](/docs/zen) verwenden, wÃ¼rden Sie `voracode/gpt-5.1-codex` fÃ¼r GPT 5.1 Codex verwenden.

Wenn Sie einen [custom provider](/docs/providers#custom) konfiguriert haben, ist `provider_id` der SchlÃ¼ssel aus dem `provider`-Teil Ihrer Konfiguration und `model_id` der SchlÃ¼ssel aus `provider.models`.

---

## Modelle konfigurieren

Sie kÃ¶nnen die Optionen eines Modells global Ã¼ber die Konfiguration konfigurieren.

```jsonc title="voracode.jsonc" {7-12,19-24}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "openai": {
      "models": {
        "gpt-5": {
          "options": {
            "reasoningEffort": "high",
            "textVerbosity": "low",
            "reasoningSummary": "auto",
            "include": ["reasoning.encrypted_content"],
          },
        },
      },
    },
    "anthropic": {
      "models": {
        "claude-sonnet-4-5-20250929": {
          "options": {
            "thinking": {
              "type": "enabled",
              "budgetTokens": 16000,
            },
          },
        },
      },
    },
  },
}
```

Hier konfigurieren wir globale Einstellungen fÃ¼r zwei integrierte Modelle: `gpt-5` bei Zugriff Ã¼ber den `openai`-Anbieter und `claude-sonnet-4-20250514` bei Zugriff Ã¼ber den `anthropic`-Anbieter.
Die integrierten Anbieter- und Modellnamen finden Sie unter [Models.dev](https://models.dev).

Sie kÃ¶nnen diese Optionen auch fÃ¼r alle von Ihnen verwendeten Agenten konfigurieren. Die Agentenkonfiguration beschreibt hier alle globalen Optionen. [Learn more](/docs/agents/#additional).

Sie kÃ¶nnen auch benutzerdefinierte Varianten definieren, die integrierte Varianten erweitern. Mit Varianten kÃ¶nnen Sie unterschiedliche Einstellungen fÃ¼r dasselbe Modell konfigurieren, ohne doppelte EintrÃ¤ge zu erstellen:

```jsonc title="voracode.jsonc" {6-21}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "voracode": {
      "models": {
        "gpt-5": {
          "variants": {
            "high": {
              "reasoningEffort": "high",
              "textVerbosity": "low",
              "reasoningSummary": "auto",
            },
            "low": {
              "reasoningEffort": "low",
              "textVerbosity": "low",
              "reasoningSummary": "auto",
            },
          },
        },
      },
    },
  },
}
```

---

## Varianten

Viele Modelle unterstÃ¼tzen mehrere Varianten mit unterschiedlichen Konfigurationen. Voracode wird mit integrierten Standardvarianten fÃ¼r beliebte Anbieter ausgeliefert.

### Integrierte Varianten

Voracode wird fÃ¼r viele Anbieter mit Standardvarianten ausgeliefert:

**Anthropic**:

- `high` â€“ Budget fÃ¼r hohes Denken (Standard)
- `max` â€“ Maximales Denkbudget

**OpenAI**:

Variiert je nach Modell, aber ungefÃ¤hr:

- `none` â€“ Keine BegrÃ¼ndung
- `minimal` â€“ Minimaler Argumentationsaufwand
- `low` â€“ Geringer Denkaufwand
- `medium` â€“ Mittlerer Denkaufwand
- `high` â€“ Hoher Denkaufwand
- `xhigh` â€“ Extra hoher Argumentationsaufwand

**Google**:

- `low` â€“ Beliebtestes effort/token-Budget
- `high` â€“ HÃ¶heres effort/token-Budget

:::tip
Diese Liste ist nicht vollstÃ¤ndig. Viele andere Anbieter verfÃ¼gen ebenfalls Ã¼ber integrierte Standardeinstellungen.
:::

### Benutzerdefinierte Varianten

Sie kÃ¶nnen vorhandene Varianten Ã¼berschreiben oder eigene hinzufÃ¼gen:

```jsonc title="voracode.jsonc" {7-18}
{
  "$schema": "https://voracode.ai/config.json",
  "provider": {
    "openai": {
      "models": {
        "gpt-5": {
          "variants": {
            "thinking": {
              "reasoningEffort": "high",
              "textVerbosity": "low",
            },
            "fast": {
              "disabled": true,
            },
          },
        },
      },
    },
  },
}
```

### Varianten durchwechseln

Verwenden Sie die Tastenkombination `variant_cycle`, um schnell zwischen Varianten zu wechseln. [Learn more](/docs/keybinds).

---

## Laden von Modellen

Wenn Voracode startet, sucht es nach Modellen in der folgenden PrioritÃ¤tsreihenfolge:

1. Das Befehlszeilenflag `--model` oder `-m`. Das Format ist das gleiche wie in der Konfigurationsdatei: `provider_id/model_id`.

2. Die Modellliste in der Voracode-Konfiguration.

   ```json title="voracode.json"
   {
     "$schema": "https://voracode.ai/config.json",
     "model": "anthropic/claude-sonnet-4-20250514"
   }
   ```

   Das Format hier ist `provider/model`.

3. Das zuletzt verwendete Modell.

4. Das erste Modell, das eine interne PrioritÃ¤t verwendet.
