---
title: GitLab
description: Verwenden Sie Voracode in GitLab-Issues und Merge Requests.
---

Voracode lÃ¤sst sich Ã¼ber Ihre GitLab CI/CD-Pipeline oder mit GitLab Duo in Ihren GitLab-Workflow integrieren.

In beiden FÃ¤llen lÃ¤uft Voracode auf Ihren GitLab Runners.

---

## GitLab CI

Voracode funktioniert in einer regulÃ¤ren GitLab-Pipeline. Sie kÃ¶nnen es als [CI component](https://docs.gitlab.com/ee/ci/components/) in eine Pipeline einbauen.

Hier verwenden wir eine von der Community erstellte CI/CD-Komponente fÃ¼r Voracode â€“ [nagyv/gitlab-voracode](https://gitlab.com/nagyv/gitlab-voracode).

---

### Funktionen

- **Benutzerdefinierte Konfiguration pro Job verwenden**: Konfigurieren Sie Voracode mit einem benutzerdefinierten Konfigurationsverzeichnis, zum Beispiel `./config/#custom-directory`, um die FunktionalitÃ¤t pro Voracode-Aufruf zu aktivieren oder zu deaktivieren.
- **Minimale Einrichtung**: Die CI-Komponente richtet Voracode im Hintergrund ein. Sie mÃ¼ssen lediglich die Voracode-Konfiguration und die erste Eingabeaufforderung erstellen.
- **Flexibel**: Die CI-Komponente unterstÃ¼tzt mehrere Eingaben zum Anpassen ihres Verhaltens

---

### Einrichtung

1. Speichern Sie Ihre Voracode-Authentifizierung JSON als Umgebungsvariablen vom Dateityp CI unter **Einstellungen** > **CI/CD** > **Variablen**. Stellen Sie sicher, dass Sie sie als â€žMaskiert und ausgeblendetâ€œ markieren.
2. FÃ¼gen Sie Folgendes zu Ihrer `.gitlab-ci.yml`-Datei hinzu.

   ```yaml title=".gitlab-ci.yml"
   include:
     - component: $CI_SERVER_FQDN/nagyv/gitlab-voracode/voracode@2
       inputs:
         config_dir: ${CI_PROJECT_DIR}/voracode-config
         auth_json: $VORACODE_AUTH_JSON # Die variable name fÃ¼r ihre Voracode authentifizierung JSON
         command: optional-custom-command
         message: "Your prompt here"
   ```

Weitere Eingaben und AnwendungsfÃ¤lle finden Sie unter [check out the docs](https://gitlab.com/explore/catalog/nagyv/gitlab-voracode) fÃ¼r diese Komponente.

---

## GitLab Duo

Voracode lÃ¤sst sich in Ihren GitLab-Workflow integrieren.
ErwÃ¤hnen Sie `@voracode` in einem Kommentar und Voracode fÃ¼hrt Aufgaben innerhalb Ihrer GitLab CI-Pipeline aus.

---

### Funktionen

- **Issue Triage**: Bitten Sie Voracode, ein Problem zu untersuchen und es Ihnen zu erklÃ¤ren.
- **Reparieren und implementieren**: Bitten Sie Voracode, ein Problem zu beheben oder eine Funktion zu implementieren.
  Es wird ein neuer Branch erstellt und eine Merge Request mit den Ã„nderungen ausgelÃ¶st.
- **Sicher**: Voracode lÃ¤uft auf Ihren GitLab Runners.

---

### Einrichtung

Voracode wird in Ihrer GitLab CI/CD-Pipeline ausgefÃ¼hrt. Folgendes mÃ¼ssen Sie zum Einrichten benÃ¶tigen:

:::tip
Aktuelle Anweisungen finden Sie im [**GitLab docs**](https://docs.gitlab.com/user/duo_agent_platform/agent_assistant/).
:::

1.  Konfigurieren Sie Ihre GitLab-Umgebung
2.  CI/CD einrichten
3.  Holen Sie sich einen AI-ModellanbieterschlÃ¼ssel API
4.  Erstellen Sie ein Dienstkonto
5.  Konfigurieren Sie CI/CD-Variablen
6.  Erstellen Sie eine Flow-Konfigurationsdatei, hier ist ein Beispiel:

        <details>

    <summary>Flow-Konfiguration</summary>

    ```yaml
    image: node:22-slim
    commands:
      - echo "Installing voracode"
      - npm install --global voracode-ai
      - echo "Installing glab"
      - export GITLAB_TOKEN=$GITLAB_TOKEN_VORACODE
      - apt-get update --quiet && apt-get install --yes curl wget gpg git && rm --recursive --force /var/lib/apt/lists/*
      - curl --silent --show-error --location "https://raw.githubusercontent.com/upciti/wakemeops/main/assets/install_repository" | bash
      - apt-get install --yes glab
      - echo "Configuring glab"
      - echo $GITLAB_HOST
      - echo "Creating Voracode auth configuration"
      - mkdir --parents ~/.local/share/voracode
      - |
        cat > ~/.local/share/voracode/auth.json << EOF
        {
          "anthropic": {
            "type": "api",
            "key": "$ANTHROPIC_API_KEY"
          }
        }
        EOF
      - echo "Configuring git"
      - git config --global user.email "voracode@gitlab.com"
      - git config --global user.name "Voracode"
      - echo "Testing glab"
      - glab issue list
      - echo "Running Voracode"
      - |
        voracode run "
        You are an AI assistant helping with GitLab operations.

        Context: $AI_FLOW_CONTEXT
        Task: $AI_FLOW_INPUT
        Event: $AI_FLOW_EVENT

        Please execute the requested task using the available GitLab tools.
        Be thorough in your analysis and provide clear explanations.

        <important>
        Please use the glab CLI to access data from GitLab. The glab CLI has already been authenticated. You can run the corresponding commands.

        If you are asked to summarize an MR or issue or asked to provide more information then please post back a note to the MR/Issue so that the user can see it.
        You don't need to commit or push up changes, those will be done automatically based on the file changes you make.
        </important>
        "
      - git checkout --branch $CI_WORKLOAD_REF origin/$CI_WORKLOAD_REF
      - echo "Checking for git changes and pushing if any exist"
      - |
        if ! git diff --quiet || ! git diff --cached --quiet || [ --not --zero "$(git ls-files --others --exclude-standard)" ]; then
          echo "Git changes detected, adding and pushing..."
          git add .
          if git diff --cached --quiet; then
            echo "No staged changes to commit"
          else
            echo "Committing changes to branch: $CI_WORKLOAD_REF"
            git commit --message "Codex changes"
            echo "Pushing changes up to $CI_WORKLOAD_REF"
            git push https://gitlab-ci-token:$GITLAB_TOKEN@$GITLAB_HOST/gl-demo-ultimate-dev-ai-epic-17570/test-java-project.git $CI_WORKLOAD_REF
            echo "Changes successfully pushed"
          fi
        else
          echo "No git changes detected, skipping push"
        fi
    variables:
      - ANTHROPIC_API_KEY
      - GITLAB_TOKEN_VORACODE
      - GITLAB_HOST
    ```

        </details>

AusfÃ¼hrliche Anweisungen finden Sie unter [GitLab CLI agents docs](https://docs.gitlab.com/user/duo_agent_platform/agent_assistant/).

---

### Beispiele

Hier sind einige Beispiele, wie Sie Voracode in GitLab verwenden kÃ¶nnen.

:::tip
Sie kÃ¶nnen die Verwendung einer anderen Triggerphrase als `@voracode` konfigurieren.
:::

- **ErklÃ¤ren Sie ein Problem**

  FÃ¼gen Sie diesen Kommentar in einer GitLab-Ausgabe hinzu.

  ```
  @voracode explain this issue
  ```

  Voracode liest das Problem und antwortet mit einer klaren ErklÃ¤rung.

- **Ein Problem beheben**

  Sagen Sie in einer GitLab-Ausgabe:

  ```
  @voracode fix this
  ```

  Voracode erstellt einen neuen Branch, implementiert die Ã„nderungen und Ã¶ffnet eine Merge Request mit den Ã„nderungen.

- **Merge Requests prÃ¼fen**

  Hinterlassen Sie den folgenden Kommentar zu einer GitLab Merge Request.

  ```
  @voracode review this merge request
  ```

  Voracode prÃ¼ft die Merge Request und gibt Feedback.
