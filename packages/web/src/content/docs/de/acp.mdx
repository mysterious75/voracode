---
title: ACP-UnterstÃ¼tzung
description: Verwenden Sie Voracode in jedem ACP-kompatiblen Editor.
---

Voracode unterstÃ¼tzt [Agent Client Protocol](https://agentclientprotocol.com) oder (ACP), sodass Sie es direkt in kompatiblen Editoren und IDEs verwenden kÃ¶nnen.

:::tip
Eine Liste der Editoren und Tools, die ACP unterstÃ¼tzen, finden Sie unter [ACP progress report](https://zed.dev/blog/acp-progress-report#available-now).
:::

ACP ist ein offenes Protokoll, das die Kommunikation zwischen Code-Editoren und AI-Coding-Agenten standardisiert.

---

## Konfiguration

Um Voracode Ã¼ber ACP zu verwenden, konfigurieren Sie Ihren Editor fÃ¼r die AusfÃ¼hrung des Befehls `voracode acp`.

Der Befehl startet Voracode als ACP-kompatiblen Unterprozess, der Ã¼ber stdio Ã¼ber JSON-RPC mit Ihrem Editor kommuniziert.

Nachfolgend finden Sie Beispiele fÃ¼r beliebte Editoren, die ACP unterstÃ¼tzen.

---

### Zed

FÃ¼gen Sie Ihrer [Zed](https://zed.dev)-Konfiguration (`~/.config/zed/settings.json`) Folgendes hinzu:

```json title="~/.config/zed/settings.json"
{
  "agent_servers": {
    "Voracode": {
      "command": "voracode",
      "args": ["acp"]
    }
  }
}
```

Um es zu Ã¶ffnen, verwenden Sie die Aktion `agent: new thread` in der **Befehlspalette**.

Sie kÃ¶nnen auch eine Tastenkombination binden, indem Sie Ihren `keymap.json` bearbeiten:

```json title="keymap.json"
[
  {
    "bindings": {
      "cmd-alt-o": [
        "agent::NewExternalAgentThread",
        {
          "agent": {
            "custom": {
              "name": "Voracode",
              "command": {
                "command": "voracode",
                "args": ["acp"]
              }
            }
          }
        }
      ]
    }
  }
]
```

---

### JetBrains IDEs

FÃ¼gen Sie zu Ihrem [JetBrains IDE](https://www.jetbrains.com/) acp.json gemÃ¤ÃŸ [Dokumentation](https://www.jetbrains.com/help/ai-assistant/acp.html) hinzu:

```json title="acp.json"
{
  "agent_servers": {
    "Voracode": {
      "command": "/absolute/path/bin/voracode",
      "args": ["acp"]
    }
  }
}
```

Um es zu Ã¶ffnen, verwenden Sie den neuen Agenten â€žVoracodeâ€œ in der AI Chat Agent Selector.

---

### Avante.nvim

FÃ¼gen Sie Ihrer [Avante.nvim](https://github.com/yetone/avante.nvim)-Konfiguration Folgendes hinzu:

```lua
{
  acp_providers = {
    ["voracode"] = {
      command = "voracode",
      args = { "acp" }
    }
  }
}
```

Wenn Sie Umgebungsvariablen Ã¼bergeben mÃ¼ssen:

```lua {6-8}
{
  acp_providers = {
    ["voracode"] = {
      command = "voracode",
      args = { "acp" },
      env = {
        VORACODE_API_KEY = os.getenv("VORACODE_API_KEY")
      }
    }
  }
}
```

---

### CodeCompanion.nvim

Um Voracode als ACP-Agent in [CodeCompanion.nvim](https://github.com/olimorris/codecompanion.nvim) zu verwenden, fÃ¼gen Sie Folgendes zu Ihrer Neovim-Konfiguration hinzu:

```lua
require("codecompanion").setup({
  interactions = {
    chat = {
      adapter = {
        name = "voracode",
        model = "claude-sonnet-4",
      },
    },
  },
})
```

Diese Konfiguration richtet CodeCompanion so ein, dass Voracode als ACP-Agent fÃ¼r den Chat verwendet wird.

Wenn Sie Umgebungsvariablen (wie `VORACODE_API_KEY`) Ã¼bergeben mÃ¼ssen, finden Sie ausfÃ¼hrliche Informationen zu [Configuring Adapters: Environment Variables](https://codecompanion.olimorris.dev/getting-started#setting-an-api-key) in der Dokumentation zu CodeCompanion.nvim.

## UnterstÃ¼tzung

Voracode funktioniert Ã¼ber ACP genauso wie im Terminal. Alle Funktionen werden unterstÃ¼tzt:

:::note
Einige integrierte Slash-Befehle wie `/undo` und `/redo` werden derzeit nicht unterstÃ¼tzt.
:::

- Integrierte Tools (Dateioperationen, Terminalbefehle usw.)
- Benutzerdefinierte Tools und Slash-Befehle
- MCP-Server, die in Ihrer Voracode-Konfiguration konfiguriert sind
- Projektspezifische Regeln von `AGENTS.md`
- Benutzerdefinierte Formatierer und Linters
- Agenten- und Berechtigungssystem
