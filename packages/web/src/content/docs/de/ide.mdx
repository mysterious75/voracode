---
title: IDE
description: Die Voracode-Erweiterung fÃ¼r VS Code, Cursor und andere IDEs
---

Voracode lÃ¤sst sich in VS Code, Cursor oder jede IDE integrieren, die ein Terminal unterstÃ¼tzt. FÃ¼hren Sie einfach `voracode` im Terminal aus, um zu beginnen.

---

## Verwendung

- **Schnellstart**: Verwenden Sie `Cmd+Esc` (Mac) oder `Ctrl+Esc` (Windows/Linux), um Voracode in einer geteilten Terminalansicht zu Ã¶ffnen, oder fokussieren Sie eine dieser Terminalsitzung, falls bereits eine ausgefÃ¼hrt wird.
- **Neue Sitzung**: Verwenden Sie `Cmd+Shift+Esc` (Mac) oder `Ctrl+Shift+Esc` (Windows/Linux), um eine neue Voracode-Terminalsitzung zu starten, auch wenn eine bereits geÃ¶ffnet ist. Sie kÃ¶nnen auch auf die Voracode-SchaltflÃ¤che im UI klicken.
- **Kontextbewusstsein**: Teilen Sie Ihre aktuelle Auswahl oder Registerkarte automatisch mit Voracode.
- **VerknÃ¼pfungen zu Dateiverweisen**: Verwenden Sie `Cmd+Option+K` (Mac) oder `Alt+Ctrl+K` (Linux/Windows), um Dateiverweisen einzufÃ¼gen. Beispiel: `@File#L37-42`.

---

## Installation

So installieren Sie Voracode in VS Code und beliebten Forks wie Cursor, Windsurf, VSCodium:

1. Ã–ffnen Sie VS Code
2. Ã–ffnen Sie das integrierte Terminal
3. FÃ¼hren Sie `voracode` aus â€“ die Erweiterung wird automatisch installiert

Wenn Sie andererseits Ihre eigene IDE verwenden mÃ¶chten, wenn Sie `/editor` oder `/export` von TUI ausfÃ¼hren, mÃ¼ssen Sie `export EDITOR="code --wait"` festlegen. [Learn more](/docs/tui/#editor-setup).

---

### Manuelle Installation

Suchen Sie im Extension Marketplace nach **Voracode** und klicken Sie auf **Installieren**.

---

### Fehlerbehebung

Wenn die Erweiterung nicht automatisch installiert werden kann:

- Stellen Sie sicher, dass Sie `voracode` im integrierten Terminal ausfÃ¼hren.
- BestÃ¤tigen Sie, dass CLI fÃ¼r Ihre IDE installiert ist:
  - FÃ¼r VS Code: `code` Befehl
  - FÃ¼r Cursor: Befehl `cursor`
  - FÃ¼r Windsurf: Befehl `windsurf`
  - FÃ¼r VSCodium: Befehl `codium`
  - Wenn nicht, fÃ¼hren Sie `Cmd+Shift+P` (Mac) oder `Ctrl+Shift+P` (Windows/Linux) aus und suchen Sie nach â€žShell Command: Install 'code' command in PATHâ€œ (oder dem Ã„quivalent fÃ¼r Ihre IDE).
- Stellen Sie sicher, dass VS Code Ã¼ber die Berechtigung zum Installieren von Erweiterungen verfÃ¼gt
