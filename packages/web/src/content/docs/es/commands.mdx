---
title: Comandos
description: Cree comandos personalizados para tareas repetitivas.
---

Los comandos personalizados le permiten especificar un mensaje que desea ejecutar cuando ese comando se ejecuta en TUI.

```bash frame="none"
/my-command
```

Los comandos personalizados se suman a los comandos integrados como `/init`, `/undo`, `/redo`, `/share`, `/help`. [MÃ¡s informaciÃ³n](/docs/tui#commands).

---

## Crear archivos de comando

Cree archivos de Markdown en el directorio `commands/` para definir comandos personalizados.

Crear `.voracode/commands/test.md`:

```md title=".voracode/commands/test.md"
---
description: Run tests with coverage
agent: build
model: anthropic/claude-3-5-sonnet-20241022
---

Run the full test suite with coverage report and show any failures.
Focus on the failing tests and suggest fixes.
```

El frontmatter define las propiedades del comando. El contenido se convierte en la plantilla.

Utilice el comando escribiendo `/` seguido del nombre del comando.

```bash frame="none"
"/test"
```

---

## ConfiguraciÃ³n

Puede agregar comandos personalizados a travÃ©s de la configuraciÃ³n Voracode o creando archivos de Markdown en el directorio `commands/`.

---

### JSON

Utilice la opciÃ³n `command` en su Voracode [config](/docs/config):

```json title="voracode.jsonc" {4-12}
{
  "$schema": "https://voracode.ai/config.json",
  "command": {
    // This becomes the name of the command
    "test": {
      // This is the prompt that will be sent to the LLM
      "template": "Run the full test suite with coverage report and show any failures.\nFocus on the failing tests and suggest fixes.",
      // This is shown as the description in the TUI
      "description": "Run tests with coverage",
      "agent": "build",
      "model": "anthropic/claude-3-5-sonnet-20241022"
    }
  }
}
```

Ahora puedes ejecutar este comando en TUI:

```bash frame="none"
/test
```

---

### Markdown

TambiÃ©n puede definir comandos utilizando archivos de Markdown. ColÃ³calos en:

- Global: `~/.config/voracode/commands/`
- Por proyecto: `.voracode/commands/`

```markdown title="~/.config/voracode/commands/test.md"
---
description: Run tests with coverage
agent: build
model: anthropic/claude-3-5-sonnet-20241022
---

Run the full test suite with coverage report and show any failures.
Focus on the failing tests and suggest fixes.
```

El nombre del archivo de Markdown se convierte en el nombre del comando. Por ejemplo, `test.md` permite
que lo ejecutes:

```bash frame="none"
/test
```

---

## ConfiguraciÃ³n de indicaciones

Las indicaciones para los comandos personalizados admiten varios marcadores de posiciÃ³n y sintaxis especiales.

---

### Argumentos

Pase argumentos a comandos usando el marcador de posiciÃ³n `$ARGUMENTS`.

```md title=".voracode/commands/component.md"
---
description: Create a new component
---

Create a new React component named $ARGUMENTS with TypeScript support.
Include proper typing and basic structure.
```

Ejecute el comando con argumentos:

```bash frame="none"
/component Button
```

Y `$ARGUMENTS` serÃ¡ reemplazado por `Button`.

TambiÃ©n puede acceder a argumentos individuales utilizando parÃ¡metros posicionales:

- `$1` - Primer argumento
- `$2` - Segundo argumento
- `$3` - Tercer argumento
- EtcÃ©tera...

Por ejemplo:

```md title=".voracode/commands/create-file.md"
---
description: Create a new file with content
---

Create a file named $1 in the directory $2
with the following content: $3
```

Ejecute el comando:

```bash frame="none"
/create-file config.json src "{ \"key\": \"value\" }"
```

Esto reemplaza:

- `$1` con `config.json`
- `$2` con `src`
- `$3` con `{ "key": "value" }`

---

### Salida del shell

Utilice _!`command`_ para inyectar la salida de [comando bash](/docs/tui#bash-commands) en su mensaje.

Por ejemplo, para crear un comando personalizado que analice la cobertura de la prueba:

```md title=".voracode/commands/analyze-coverage.md"
---
description: Analyze test coverage
---

Here are the current test results:
!`npm test`

Based on these results, suggest improvements to increase coverage.
```

O para revisar cambios recientes:

```md title=".voracode/commands/review-changes.md"
---
description: Review recent changes
---

Recent git commits:
!`git log --oneline -10`

Review these changes and suggest any improvements.
```

Los comandos se ejecutan en el directorio raÃ­z de su proyecto y su salida pasa a formar parte del mensaje.

---

### Referencias de archivos

Incluya archivos en su comando usando `@` seguido del nombre del archivo.

```md title=".voracode/commands/review-component.md"
---
description: Review component
---

Review the component in @src/components/Button.tsx.
Check for performance issues and suggest improvements.
```

El contenido del archivo se incluye automÃ¡ticamente en el mensaje.

---

## Opciones

Veamos las opciones de configuraciÃ³n en detalle.

---

### Plantilla

La opciÃ³n `template` define el mensaje que se enviarÃ¡ al LLM cuando se ejecute el comando.

```json title="voracode.json"
{
  "command": {
    "test": {
      "template": "Run the full test suite with coverage report and show any failures.\nFocus on the failing tests and suggest fixes."
    }
  }
}
```

Esta es una opciÃ³n de configuraciÃ³n **obligatoria**.

---

### DescripciÃ³n

Utilice la opciÃ³n `description` para proporcionar una breve descripciÃ³n de lo que hace el comando.

```json title="voracode.json"
{
  "command": {
    "test": {
      "description": "Run tests with coverage"
    }
  }
}
```

Esto se muestra como descripciÃ³n en TUI cuando escribe el comando.

---

### Agente

Utilice la configuraciÃ³n `agent` para especificar opcionalmente quÃ© [agente](/docs/agents) debe ejecutar este comando.
Si se trata de un [subagente](/docs/agents/#subagents), el comando activarÃ¡ una invocaciÃ³n de subagente de forma predeterminada.
Para desactivar este comportamiento, establezca `subtask` en `false`.

```json title="voracode.json"
{
  "command": {
    "review": {
      "agent": "plan"
    }
  }
}
```

Esta es una opciÃ³n de configuraciÃ³n **opcional**. Si no se especifica, el valor predeterminado es su agente actual.

---

### Subtarea

Utilice el booleano `subtask` para forzar que el comando active una invocaciÃ³n de [subagente](/docs/agents/#subagents).
Esto es Ãºtil si desea que el comando no contamine su contexto principal y **obligue** al agente a actuar como subagente.
incluso si `mode` estÃ¡ configurado en `primary` en la configuraciÃ³n de [agente](/docs/agents).

```json title="voracode.json"
{
  "command": {
    "analyze": {
      "subtask": true
    }
  }
}
```

Esta es una opciÃ³n de configuraciÃ³n **opcional**.

---

### Modelo

Utilice la configuraciÃ³n `model` para anular el modelo predeterminado para este comando.

```json title="voracode.json"
{
  "command": {
    "analyze": {
      "model": "anthropic/claude-3-5-sonnet-20241022"
    }
  }
}
```

Esta es una opciÃ³n de configuraciÃ³n **opcional**.

---

## Integrados

voracode incluye varios comandos integrados como `/init`, `/undo`, `/redo`, `/share`, `/help`; [mÃ¡s informaciÃ³n](/docs/tui#commands).

:::note
Los comandos personalizados pueden anular los comandos integrados.
:::

Si define un comando personalizado con el mismo nombre, anularÃ¡ el comando integrado.
