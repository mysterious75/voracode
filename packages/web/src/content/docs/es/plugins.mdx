---
title: Complementos
description: Escriba sus propios complementos para extender Voracode.
---

Los complementos le permiten extender Voracode al conectarse a varios eventos y personalizar el comportamiento. Puede crear complementos para agregar nuevas funciones, integrarlos con servicios externos o modificar el comportamiento predeterminado de Voracode.

Para ver ejemplos, consulte los [complementos](/docs/ecosystem#plugins) creados por la comunidad.

---

## Usar un complemento

Hay dos formas de cargar complementos.

---

### Desde archivos locales

Coloque los archivos JavaScript o TypeScript en el directorio del complemento.

- `.voracode/plugins/` - Complementos a nivel de proyecto
- `~/.config/voracode/plugins/` - Complementos globales

Los archivos en estos directorios se cargan automÃ¡ticamente al inicio.

---

### Desde npm

Especifique paquetes npm en su archivo de configuraciÃ³n.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "plugin": ["voracode-helicone-session", "voracode-wakatime", "@my-org/custom-plugin"]
}
```

Se admiten paquetes npm regulares y de alcance.

Explore los complementos disponibles en el [ecosistema](/docs/ecosystem#plugins).

---

### CÃ³mo se instalan los complementos

Los **complementos npm** se instalan automÃ¡ticamente usando Bun al inicio. Los paquetes y sus dependencias se almacenan en cachÃ© en `~/.cache/voracode/node_modules/`.

**Los complementos locales** se cargan directamente desde el directorio de complementos. Para usar paquetes externos, debe crear un `package.json` dentro de su directorio de configuraciÃ³n (consulte [Dependencias](#dependencies)), o publicar el complemento en npm y [agregarlo a su configuraciÃ³n](/docs/config#plugins).

---

### Orden de carga

Los complementos se cargan desde todas las fuentes y todos los enlaces se ejecutan en secuencia. El orden de carga es:

1. ConfiguraciÃ³n global (`~/.config/voracode/voracode.json`)
2. ConfiguraciÃ³n del proyecto (`voracode.json`)
3. Directorio global de complementos (`~/.config/voracode/plugins/`)
4. Directorio de complementos del proyecto (`.voracode/plugins/`)

Los paquetes npm duplicados con el mismo nombre y versiÃ³n se cargan una vez. Sin embargo, un complemento local y un complemento npm con nombres similares se cargan por separado.

---

## Crear un complemento

Un complemento es un mÃ³dulo **JavaScript/TypeScript** que exporta uno o mÃ¡s complementos.
funciones. Cada funciÃ³n recibe un objeto de contexto y devuelve un objeto de enlace.

---

### Dependencias

Los complementos locales y las herramientas personalizadas pueden utilizar paquetes npm externos. Agregue un `package.json` a su directorio de configuraciÃ³n con las dependencias que necesita.

```json title=".voracode/package.json"
{
  "dependencies": {
    "shescape": "^2.1.0"
  }
}
```

Voracode ejecuta `bun install` al inicio para instalarlos. Luego, sus complementos y herramientas pueden importarlos.

```ts title=".voracode/plugins/my-plugin.ts"
import { escape } from "shescape"

export const MyPlugin = async (ctx) => {
  return {
    "tool.execute.before": async (input, output) => {
      if (input.tool === "bash") {
        output.args.command = escape(output.args.command)
      }
    },
  }
}
```

---

### Estructura bÃ¡sica

```js title=".voracode/plugins/example.js"
export const MyPlugin = async ({ project, client, $, directory, worktree }) => {
  console.log("Plugin initialized!")

  return {
    // Hook implementations go here
  }
}
```

La funciÃ³n del complemento recibe:

- `project`: La informaciÃ³n actual del proyecto.
- `directory`: El directorio de trabajo actual.
- `worktree`: La ruta del Ã¡rbol de trabajo de git.
- `client`: Un cliente SDK voracode para interactuar con la IA.
- `$`: [shell API](https://bun.com/docs/runtime/shell) de Bun para ejecutar comandos.

---

### Soporte TypeScript

Para los complementos TypeScript, puede importar tipos desde el paquete de complementos:

```ts title="my-plugin.ts" {1}
import type { Plugin } from "@voracode-ai/plugin"

export const MyPlugin: Plugin = async ({ project, client, $, directory, worktree }) => {
  return {
    // Type-safe hook implementations
  }
}
```

---

### Eventos

Los complementos pueden suscribirse a eventos como se ve a continuaciÃ³n en la secciÃ³n Ejemplos. AquÃ­ hay una lista de los diferentes eventos disponibles.

#### Eventos de comando

- `command.executed`

#### Eventos de archivo

- `file.edited`
- `file.watcher.updated`

#### Eventos de instalaciÃ³n

- `installation.updated`

#### Eventos LSP

- `lsp.client.diagnostics`
- `lsp.updated`

#### Eventos de mensajes

- `message.part.removed`
- `message.part.updated`
- `message.removed`
- `message.updated`

#### Eventos de permiso

- `permission.asked`
- `permission.replied`

#### Eventos del servidor

- `server.connected`

#### Eventos de sesiÃ³n

- `session.created`
- `session.compacted`
- `session.deleted`
- `session.diff`
- `session.error`
- `session.idle`
- `session.status`
- `session.updated`

#### Eventos de Todo

- `todo.updated`

#### Eventos de Shell

- `shell.env`

#### Eventos de herramientas

- `tool.execute.after`
- `tool.execute.before`

#### Eventos TUI

- `tui.prompt.append`
- `tui.command.execute`
- `tui.toast.show`

---

## Ejemplos

A continuaciÃ³n se muestran algunos ejemplos de complementos que puede utilizar para ampliar voracode.

---

### Enviar notificaciones

Enviar notificaciones cuando ocurran ciertos eventos:

```js title=".voracode/plugins/notification.js"
export const NotificationPlugin = async ({ project, client, $, directory, worktree }) => {
  return {
    event: async ({ event }) => {
      // Send notification on session completion
      if (event.type === "session.idle") {
        await $`osascript -e 'display notification "Session completed!" with title "voracode"'`
      }
    },
  }
}
```

Estamos usando `osascript` para ejecutar AppleScript en macOS. AquÃ­ lo estamos usando para enviar notificaciones.

:::note
Si estÃ¡ utilizando la aplicaciÃ³n de escritorio Voracode, puede enviar notificaciones del sistema automÃ¡ticamente cuando una respuesta estÃ© lista o cuando se produzca un error en una sesiÃ³n.
:::

---

### ProtecciÃ³n .env

Evite que voracode lea archivos `.env`:

```javascript title=".voracode/plugins/env-protection.js"
export const EnvProtection = async ({ project, client, $, directory, worktree }) => {
  return {
    "tool.execute.before": async (input, output) => {
      if (input.tool === "read" && output.args.filePath.includes(".env")) {
        throw new Error("Do not read .env files")
      }
    },
  }
}
```

---

### Inyectar variables de entorno

Inyecte variables de entorno en toda la ejecuciÃ³n del shell (herramientas de inteligencia artificial y terminales de usuario):

```javascript title=".voracode/plugins/inject-env.js"
export const InjectEnvPlugin = async () => {
  return {
    "shell.env": async (input, output) => {
      output.env.MY_API_KEY = "secret"
      output.env.PROJECT_ROOT = input.cwd
    },
  }
}
```

---

### Herramientas personalizadas

Los complementos tambiÃ©n pueden agregar herramientas personalizadas a voracode:

```ts title=".voracode/plugins/custom-tools.ts"
import { type Plugin, tool } from "@voracode-ai/plugin"

export const CustomToolsPlugin: Plugin = async (ctx) => {
  return {
    tool: {
      mytool: tool({
        description: "This is a custom tool",
        args: {
          foo: tool.schema.string(),
        },
        async execute(args, context) {
          const { directory, worktree } = context
          return `Hello ${args.foo} from ${directory} (worktree: ${worktree})`
        },
      }),
    },
  }
}
```

El ayudante `tool` crea una herramienta personalizada a la que voracode puede llamar. Toma una funciÃ³n de esquema Zod y devuelve una definiciÃ³n de herramienta con:

- `description`: QuÃ© hace la herramienta
- `args`: Esquema Zod para los argumentos de la herramienta.
- `execute`: FunciÃ³n que se ejecuta cuando se llama a la herramienta

Sus herramientas personalizadas estarÃ¡n disponibles para voracode junto con las herramientas integradas.

:::note
Si una herramienta de complemento utiliza el mismo nombre que una herramienta integrada, la herramienta de complemento tiene prioridad.
:::

---

### Registro

Utilice `client.app.log()` en lugar de `console.log` para el registro estructurado:

```ts title=".voracode/plugins/my-plugin.ts"
export const MyPlugin = async ({ client }) => {
  await client.app.log({
    body: {
      service: "my-plugin",
      level: "info",
      message: "Plugin initialized",
      extra: { foo: "bar" },
    },
  })
}
```

Niveles: `debug`, `info`, `warn`, `error`. Consulte la [documentaciÃ³n del SDK](https://voracode.ai/docs/sdk) para obtener mÃ¡s detalles.

---

### Hooks de compactaciÃ³n

Personalice el contexto incluido cuando se compacta una sesiÃ³n:

```ts title=".voracode/plugins/compaction.ts"
import type { Plugin } from "@voracode-ai/plugin"

export const CompactionPlugin: Plugin = async (ctx) => {
  return {
    "experimental.session.compacting": async (input, output) => {
      // Inject additional context into the compaction prompt
      output.context.push(`
## Custom Context

Include any state that should persist across compaction:
- Current task status
- Important decisions made
- Files being actively worked on
`)
    },
  }
}
```

El hook `experimental.session.compacting` se activa antes de que LLM genere un resumen de continuaciÃ³n. Ãšselo para inyectar contexto especÃ­fico del dominio que el mensaje de compactaciÃ³n predeterminado omitirÃ­a.

TambiÃ©n puede reemplazar completamente el mensaje de compactaciÃ³n configurando `output.prompt`:

```ts title=".voracode/plugins/custom-compaction.ts"
import type { Plugin } from "@voracode-ai/plugin"

export const CustomCompactionPlugin: Plugin = async (ctx) => {
  return {
    "experimental.session.compacting": async (input, output) => {
      // Replace the entire compaction prompt
      output.prompt = `
You are generating a continuation prompt for a multi-agent swarm session.

Summarize:
1. The current task and its status
2. Which files are being modified and by whom
3. Any blockers or dependencies between agents
4. The next steps to complete the work

Format as a structured prompt that a new agent can use to resume work.
`
    },
  }
}
```

Cuando se configura `output.prompt`, reemplaza completamente el mensaje de compactaciÃ³n predeterminado. En este caso, se ignora la matriz `output.context`.
