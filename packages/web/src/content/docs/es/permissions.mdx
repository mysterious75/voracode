---
title: Permisos
description: Controle quÃ© acciones requieren aprobaciÃ³n para ejecutarse.
---

Voracode usa la configuraciÃ³n `permission` para decidir si una acciÃ³n determinada debe ejecutarse automÃ¡ticamente, avisarle o bloquearse.

A partir de `v1.1.1`, la configuraciÃ³n booleana heredada `tools` estÃ¡ obsoleta y se ha fusionado en `permission`. La antigua configuraciÃ³n `tools` todavÃ­a se admite por compatibilidad con versiones anteriores.

---

## Acciones

Cada regla de permiso se resuelve en una de:

- `"allow"` â€” ejecutar sin aprobaciÃ³n
- `"ask"` â€” solicitar aprobaciÃ³n
- `"deny"` â€” bloquea la acciÃ³n

---

## ConfiguraciÃ³n

Puede establecer permisos globalmente (con `*`) y anular herramientas especÃ­ficas.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "*": "ask",
    "bash": "allow",
    "edit": "deny"
  }
}
```

TambiÃ©n puedes configurar todos los permisos a la vez:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": "allow"
}
```

---

## Reglas granulares (sintaxis de objeto)

Para la mayorÃ­a de los permisos, puede utilizar un objeto para aplicar diferentes acciones segÃºn la entrada de la herramienta.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "bash": {
      "*": "ask",
      "git *": "allow",
      "npm *": "allow",
      "rm *": "deny",
      "grep *": "allow"
    },
    "edit": {
      "*": "deny",
      "packages/web/src/content/docs/*.mdx": "allow"
    }
  }
}
```

Las reglas se evalÃºan segÃºn la coincidencia de patrones, y la **Ãºltima regla coincidente gana**. Un patrÃ³n comÃºn es poner primero la regla general `"*"` y despuÃ©s reglas mÃ¡s especÃ­ficas.

### Comodines

Los patrones de permisos utilizan una simple coincidencia de comodines:

- `*` coincide con cero o mÃ¡s de cualquier carÃ¡cter
- `?` coincide exactamente con un carÃ¡cter
- Todos los demÃ¡s caracteres coinciden literalmente

### ExpansiÃ³n del directorio de inicio

Puede usar `~` o `$HOME` al comienzo de un patrÃ³n para hacer referencia a su directorio de inicio. Esto es particularmente Ãºtil para las reglas [`external_directory`](#external-directories).

- `~/projects/*` -> `/Users/username/projects/*`
- `$HOME/projects/*` -> `/Users/username/projects/*`
- `~` -> `/Users/username`

### Directorios externos

Utilice `external_directory` para permitir llamadas a herramientas que toquen rutas fuera del directorio de trabajo donde se iniciÃ³ Voracode. Esto se aplica a cualquier herramienta que tome una ruta como entrada (por ejemplo, `read`, `edit`, `glob`, `grep` y muchos comandos `bash`).

La expansiÃ³n del hogar (como `~/...`) solo afecta la forma en que se escribe un patrÃ³n. No hace que una ruta externa forme parte del espacio de trabajo actual, por lo que las rutas fuera del directorio de trabajo aÃºn deben permitirse a travÃ©s de `external_directory`.

Por ejemplo, esto permite el acceso a todo lo que se encuentra en `~/projects/personal/`:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "external_directory": {
      "~/projects/personal/**": "allow"
    }
  }
}
```

Cualquier directorio permitido aquÃ­ hereda los mismos valores predeterminados que el espacio de trabajo actual. Dado que [`read` tiene por defecto `allow`](#defaults), tambiÃ©n se permiten lecturas para entradas bajo `external_directory` a menos que se anulen. Agregue reglas explÃ­citas cuando una herramienta deba restringirse en estas rutas, como bloquear ediciones mientras se mantienen las lecturas:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "external_directory": {
      "~/projects/personal/**": "allow"
    },
    "edit": {
      "~/projects/personal/**": "deny"
    }
  }
}
```

Mantenga la lista centrada en rutas confiables y aplique reglas adicionales de permitir o denegar segÃºn sea necesario para otras herramientas (por ejemplo, `bash`).

---

## Permisos disponibles

Los permisos Voracode estÃ¡n codificados por el nombre de la herramienta, ademÃ¡s de un par de medidas de seguridad:

- `read` â€” leer un archivo (coincide con la ruta del archivo)
- `edit` â€” todas las modificaciones de archivos (cubre `edit`, `write`, `patch`)
- `glob` â€” globalizaciÃ³n de archivos (coincide con el patrÃ³n global)
- `grep` â€” bÃºsqueda de contenido (coincide con el patrÃ³n de expresiones regulares)
- `bash`: ejecuta comandos de shell (coincide con comandos analizados como `git status --porcelain`)
- `task` â€” lanzamiento de subagentes (coincide con el tipo de subagente)
- `skill` â€” cargar una habilidad (coincide con el nombre de la habilidad)
- `lsp`: ejecuciÃ³n de consultas LSP (actualmente no granulares)
- `webfetch` â€” obteniendo una URL (coincide con la URL)
- `websearch` â€” bÃºsqueda web (coincide con la consulta)
- `external_directory`: se activa cuando una herramienta toca rutas fuera del directorio de trabajo del proyecto.
- `doom_loop`: se activa cuando la misma llamada de herramienta se repite 3 veces con entrada idÃ©ntica

---

## Valores predeterminados

Si no especifica nada, Voracode comienza desde valores predeterminados permisivos:

- La mayorÃ­a de los permisos estÃ¡n predeterminados en `"allow"`.
- `doom_loop` y `external_directory` por defecto son `"ask"`.
- `read` es `"allow"`, pero los archivos `.env` estÃ¡n denegados de forma predeterminada:

```json title="voracode.json"
{
  "permission": {
    "read": {
      "*": "allow",
      "*.env": "deny",
      "*.env.*": "deny",
      "*.env.example": "allow"
    }
  }
}
```

---

## Â¿QuÃ© significa "preguntar"?

Cuando Voracode solicita aprobaciÃ³n, la interfaz de usuario ofrece tres resultados:

- `once` â€” aprobar solo esta solicitud
- `always`: aprueba solicitudes futuras que coincidan con los patrones sugeridos (para el resto de la sesiÃ³n actual Voracode)
- `reject` â€” rechazar la solicitud

La herramienta proporciona el conjunto de patrones que `always` aprobarÃ­a (por ejemplo, las aprobaciones de bash generalmente incluyen en la lista blanca un prefijo de comando seguro como `git status*`).

---

## Agentes

Puede anular los permisos por agente. Los permisos del agente se combinan con la configuraciÃ³n global y las reglas del agente tienen prioridad. [MÃ¡s informaciÃ³n](/docs/agents#permissions) sobre los permisos de los agentes.

:::note
Consulte la secciÃ³n [Reglas granulares (sintaxis de objeto)](#granular-rules-object-syntax) anterior para obtener ejemplos de coincidencia de patrones mÃ¡s detallados.
:::

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "bash": {
      "*": "ask",
      "git *": "allow",
      "git commit *": "deny",
      "git push *": "deny",
      "grep *": "allow"
    }
  },
  "agent": {
    "build": {
      "permission": {
        "bash": {
          "*": "ask",
          "git *": "allow",
          "git commit *": "ask",
          "git push *": "deny",
          "grep *": "allow"
        }
      }
    }
  }
}
```

TambiÃ©n puede configurar los permisos del agente en Markdown:

```markdown title="~/.config/voracode/agents/review.md"
---
description: Code review without edits
mode: subagent
permission:
  edit: deny
  bash: ask
  webfetch: deny
---

Only analyze code and suggest changes.
```

:::tip
Utilice la coincidencia de patrones para comandos con argumentos. `"grep *"` permite `grep pattern file.txt`, mientras que `"grep"` solo lo bloquearÃ­a. Los comandos como `git status` funcionan para el comportamiento predeterminado pero requieren permiso explÃ­cito (como `"git status *"`) cuando se pasan argumentos.
:::
