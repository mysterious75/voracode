---
title: GitLab
description: Utilice Voracode en problemas GitLab y solicitudes de fusiÃ³n.
---

Voracode se integra con su flujo de trabajo GitLab a travÃ©s de su canal de CI/CD GitLab o con GitLab Duo.

En ambos casos, Voracode se ejecutarÃ¡ en tus corredores GitLab.

---

## GitLab CI

Voracode funciona en una tuberÃ­a GitLab normal. Puede integrarlo en una canalizaciÃ³n como un [componente de CI](https://docs.gitlab.com/ee/ci/components/)

AquÃ­ estamos utilizando un componente CI/CD creado por la comunidad para Voracode â€” [nagyv/gitlab-voracode](https://gitlab.com/nagyv/gitlab-voracode).

---

### CaracterÃ­sticas

- **Usar configuraciÃ³n personalizada por trabajo**: configure Voracode con un directorio de configuraciÃ³n personalizado, por ejemplo `./config/#custom-directory` para habilitar o deshabilitar la funcionalidad por invocaciÃ³n de Voracode.
- **ConfiguraciÃ³n mÃ­nima**: el componente CI configura Voracode en segundo plano, solo necesita crear la configuraciÃ³n Voracode y el mensaje inicial.
- **Flexible**: el componente CI admite varias entradas para personalizar su comportamiento

---

### ConfiguraciÃ³n

1. Guarde su Voracode autenticaciÃ³n JSON como variables de entorno de CI de tipo de archivo en **ConfiguraciÃ³n** > **CI/CD** > **Variables**. AsegÃºrate de marcarlos como "Enmascarados y ocultos".
2. Agregue lo siguiente a su archivo `.gitlab-ci.yml`.

   ```yaml title=".gitlab-ci.yml"
   include:
     - component: $CI_SERVER_FQDN/nagyv/gitlab-voracode/voracode@2
       inputs:
         config_dir: ${CI_PROJECT_DIR}/voracode-config
         auth_json: $VORACODE_AUTH_JSON # The variable name for your Voracode authentication JSON
         command: optional-custom-command
         message: "Your prompt here"
   ```

Para obtener mÃ¡s entradas y casos de uso, [consulte los documentos](https://gitlab.com/explore/catalog/nagyv/gitlab-voracode) de este componente.

---

## GitLab Duo

Voracode se integra con su flujo de trabajo GitLab.
Mencione `@voracode` en un comentario y Voracode ejecutarÃ¡ tareas dentro de su canal de CI GitLab.

---

### CaracterÃ­sticas

- **ClasificaciÃ³n de problemas**: PÃ­dele a Voracode que investigue un problema y te lo explique.
- **Reparar e implementar**: pÃ­dale a Voracode que solucione un problema o implemente una funciÃ³n.
  CrearÃ¡ una nueva rama y generarÃ¡ una solicitud de fusiÃ³n con los cambios.
- **Seguro**: Voracode se ejecuta en tus corredores GitLab.

---

### ConfiguraciÃ³n

Voracode se ejecuta en su canalizaciÃ³n de CI/CD GitLab. Esto es lo que necesitarÃ¡ para configurarlo:

:::tip
Consulte los [**GitLab documentos**](https://docs.gitlab.com/user/duo_agent_platform/agent_assistant/) para obtener instrucciones actualizadas.
:::

1.  Configure su entorno GitLab
2.  Configurar CI/CD
3.  Obtenga una clave API de proveedor de modelo de IA
4.  Crea una cuenta de servicio
5.  Configurar variables CI/CD
6.  Cree un archivo de configuraciÃ³n de flujo, aquÃ­ hay un ejemplo:

        <details>

    <summary>ConfiguraciÃ³n de flujo</summary>

    ```yaml
    image: node:22-slim
    commands:
      - echo "Installing voracode"
      - npm install --global voracode-ai
      - echo "Installing glab"
      - export GITLAB_TOKEN=$GITLAB_TOKEN_VORACODE
      - apt-get update --quiet && apt-get install --yes curl wget gpg git && rm --recursive --force /var/lib/apt/lists/*
      - curl --silent --show-error --location "https://raw.githubusercontent.com/upciti/wakemeops/main/assets/install_repository" | bash
      - apt-get install --yes glab
      - echo "Configuring glab"
      - echo $GITLAB_HOST
      - echo "Creating Voracode auth configuration"
      - mkdir --parents ~/.local/share/voracode
      - |
        cat > ~/.local/share/voracode/auth.json << EOF
        {
          "anthropic": {
            "type": "api",
            "key": "$ANTHROPIC_API_KEY"
          }
        }
        EOF
      - echo "Configuring git"
      - git config --global user.email "voracode@gitlab.com"
      - git config --global user.name "Voracode"
      - echo "Testing glab"
      - glab issue list
      - echo "Running Voracode"
      - |
        voracode run "
        You are an AI assistant helping with GitLab operations.

        Context: $AI_FLOW_CONTEXT
        Task: $AI_FLOW_INPUT
        Event: $AI_FLOW_EVENT

        Please execute the requested task using the available GitLab tools.
        Be thorough in your analysis and provide clear explanations.

        <important>
        Please use the glab CLI to access data from GitLab. The glab CLI has already been authenticated. You can run the corresponding commands.

        If you are asked to summarize an MR or issue or asked to provide more information then please post back a note to the MR/Issue so that the user can see it.
        You don't need to commit or push up changes, those will be done automatically based on the file changes you make.
        </important>
        "
      - git checkout --branch $CI_WORKLOAD_REF origin/$CI_WORKLOAD_REF
      - echo "Checking for git changes and pushing if any exist"
      - |
        if ! git diff --quiet || ! git diff --cached --quiet || [ --not --zero "$(git ls-files --others --exclude-standard)" ]; then
          echo "Git changes detected, adding and pushing..."
          git add .
          if git diff --cached --quiet; then
            echo "No staged changes to commit"
          else
            echo "Committing changes to branch: $CI_WORKLOAD_REF"
            git commit --message "Codex changes"
            echo "Pushing changes up to $CI_WORKLOAD_REF"
            git push https://gitlab-ci-token:$GITLAB_TOKEN@$GITLAB_HOST/gl-demo-ultimate-dev-ai-epic-17570/test-java-project.git $CI_WORKLOAD_REF
            echo "Changes successfully pushed"
          fi
        else
          echo "No git changes detected, skipping push"
        fi
    variables:
      - ANTHROPIC_API_KEY
      - GITLAB_TOKEN_VORACODE
      - GITLAB_HOST
    ```

        </details>

Puede consultar los [GitLab CLI documentos de los agentes](https://docs.gitlab.com/user/duo_agent_platform/agent_assistant/) para obtener instrucciones detalladas.

---

### Ejemplos

A continuaciÃ³n se muestran algunos ejemplos de cÃ³mo puede utilizar Voracode en GitLab.

:::tip
Puede configurarlo para utilizar una frase de activaciÃ³n diferente a `@voracode`.
:::

- **Explica un problema**

  Agregue este comentario en una ediciÃ³n GitLab.

  ```
  @voracode explain this issue
  ```

  Voracode leerÃ¡ el problema y responderÃ¡ con una explicaciÃ³n clara.

- **Solucionar un problema**

  En un problema GitLab, diga:

  ```
  @voracode fix this
  ```

  Voracode crearÃ¡ una nueva rama, implementarÃ¡ los cambios y abrirÃ¡ una solicitud de fusiÃ³n con los cambios.

- **Revisar solicitudes de fusiÃ³n**

  Deje el siguiente comentario sobre una solicitud de fusiÃ³n GitLab.

  ```
  @voracode review this merge request
  ```

  Voracode revisarÃ¡ la solicitud de fusiÃ³n y brindarÃ¡ comentarios.
