---
title: MCP servidores
description: Agregue herramientas MCP locales y remotas.
---

Puede agregar herramientas externas a Voracode utilizando el _Model Context Protocol_ o MCP. Voracode admite servidores locales y remotos.

Una vez agregadas, las herramientas MCP estÃ¡n disponibles automÃ¡ticamente para LLM junto con las herramientas integradas.

---

#### Advertencias

Cuando utiliza un servidor MCP, se suma al contexto. Esto puede acumularse rÃ¡pidamente si tiene muchas herramientas. Por lo que recomendamos tener cuidado con quÃ© servidores MCP utiliza.

:::tip
Los servidores MCP se agregan a su contexto, por lo que debe tener cuidado con cuÃ¡les habilita.
:::

Ciertos servidores MCP, como el servidor GitHub MCP, tienden a agregar muchos tokens y pueden exceder fÃ¡cilmente el lÃ­mite de contexto.

---

## Habilitar

Puede definir servidores MCP en su [Voracode Config](https://voracode.ai/docs/config/) en `mcp`. Agregue cada MCP con un nombre Ãºnico. Puede referirse a ese MCP por su nombre cuando solicite el LLM.

```jsonc title="voracode.jsonc" {6}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "name-of-mcp-server": {
      // ...
      "enabled": true,
    },
    "name-of-other-mcp-server": {
      // ...
    },
  },
}
```

TambiÃ©n puede desactivar un servidor configurando `enabled` en `false`. Esto es Ãºtil si desea desactivar temporalmente un servidor sin eliminarlo de su configuraciÃ³n.

---

### AnulaciÃ³n de los valores predeterminados remotos

Las organizaciones pueden proporcionar servidores MCP predeterminados a travÃ©s de su punto final `.well-known/voracode`. Estos servidores pueden estar deshabilitados de forma predeterminada, lo que permite a los usuarios optar por los que necesitan.

Para habilitar un servidor especÃ­fico desde la configuraciÃ³n remota de su organizaciÃ³n, agrÃ©guelo a su configuraciÃ³n local con `enabled: true`:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "jira": {
      "type": "remote",
      "url": "https://jira.example.com/mcp",
      "enabled": true
    }
  }
}
```

Los valores de configuraciÃ³n local anulan los valores predeterminados remotos. Consulte [precedencia de configuraciÃ³n](/docs/config#precedence-order) para obtener mÃ¡s detalles.

---

## Locales

Agregue servidores MCP locales usando `type` a `"local"` dentro del objeto MCP.

```jsonc title="voracode.jsonc" {15}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-local-mcp-server": {
      "type": "local",
      // Or ["bun", "x", "my-mcp-command"]
      "command": ["npx", "-y", "my-mcp-command"],
      "enabled": true,
      "environment": {
        "MY_ENV_VAR": "my_env_var_value",
      },
    },
  },
}
```

El comando es cÃ³mo se inicia el servidor MCP local. TambiÃ©n puede pasar una lista de variables de entorno.

Por ejemplo, asÃ­ es como puede agregar el servidor de prueba [`@modelcontextprotocol/server-everything`](https://www.npmjs.com/package/@modelcontextprotocol/server-everything) MCP.

```jsonc title="voracode.jsonc"
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "mcp_everything": {
      "type": "local",
      "command": ["npx", "-y", "@modelcontextprotocol/server-everything"],
    },
  },
}
```

Y para usarlo puedo agregar `use the mcp_everything tool` a mis indicaciones.

```txt "mcp_everything"
use the mcp_everything tool to add the number 3 and 4
```

---

#### Opciones

AquÃ­ estÃ¡n todas las opciones para configurar un servidor MCP local.

| OpciÃ³n        | Tipo     | Requerido | DescripciÃ³n                                                                                                        |
| ------------- | -------- | --------- | ------------------------------------------------------------------------------------------------------------------ |
| `type`        | Cadena   | Y         | El tipo de conexiÃ³n del servidor MCP debe ser `"local"`.                                                           |
| `command`     | Matriz   | Y         | Comando y argumentos para ejecutar el servidor MCP.                                                                |
| `environment` | Objeto   |           | Variables de entorno para configurar al ejecutar el servidor.                                                      |
| `enabled`     | booleano |           | Habilite o deshabilite el servidor MCP al inicio.                                                                  |
| `timeout`     | NÃºmero   |           | Tiempo de espera en ms para recuperar herramientas del servidor MCP. El valor predeterminado es 5000 (5 segundos). |

---

## Remoto

Agregue servidores MCP remotos configurando `type` en `"remote"`.

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-remote-mcp": {
      "type": "remote",
      "url": "https://my-mcp-server.com",
      "enabled": true,
      "headers": {
        "Authorization": "Bearer MY_API_KEY"
      }
    }
  }
}
```

`url` es la URL del servidor MCP remoto y con la opciÃ³n `headers` puede pasar una lista de encabezados.

---

#### Opciones

| OpciÃ³n    | Tipo     | Requerido | DescripciÃ³n                                                                                                        |
| --------- | -------- | --------- | ------------------------------------------------------------------------------------------------------------------ |
| `type`    | Cadena   | Y         | El tipo de conexiÃ³n del servidor MCP debe ser `"remote"`.                                                          |
| `url`     | Cadena   | Y         | URL del servidor MCP remoto.                                                                                       |
| `enabled` | booleano |           | Habilite o deshabilite el servidor MCP al inicio.                                                                  |
| `headers` | Objeto   |           | Encabezados para enviar con la solicitud.                                                                          |
| `oauth`   | Objeto   |           | OAuth configuraciÃ³n de autenticaciÃ³n. Consulte la secciÃ³n [OAuth](#oauth) a continuaciÃ³n.                          |
| `timeout` | NÃºmero   |           | Tiempo de espera en ms para recuperar herramientas del servidor MCP. El valor predeterminado es 5000 (5 segundos). |

---

## OAuth

Voracode maneja automÃ¡ticamente la autenticaciÃ³n OAuth para servidores MCP remotos. Cuando un servidor requiere autenticaciÃ³n, Voracode:

1. Detectar la respuesta 401 e iniciar el flujo OAuth
2. Utilice **Registro dinÃ¡mico de cliente (RFC 7591)** si el servidor lo admite.
3. Almacene tokens de forma segura para futuras solicitudes

---

### AutomÃ¡tico

Para la mayorÃ­a de los servidores MCP habilitados para OAuth, no se necesita ninguna configuraciÃ³n especial. Simplemente configure el servidor remoto:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-oauth-server": {
      "type": "remote",
      "url": "https://mcp.example.com/mcp"
    }
  }
}
```

Si el servidor requiere autenticaciÃ³n, Voracode le pedirÃ¡ que se autentique cuando intente usarlo por primera vez. De lo contrario, puede [activar manualmente el flujo](#authenticating) con `voracode mcp auth <server-name>`.

---

### Preinscrito

Si tiene credenciales de cliente del proveedor del servidor MCP, puede configurarlas:

```json title="voracode.json" {7-11}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-oauth-server": {
      "type": "remote",
      "url": "https://mcp.example.com/mcp",
      "oauth": {
        "clientId": "{env:MY_MCP_CLIENT_ID}",
        "clientSecret": "{env:MY_MCP_CLIENT_SECRET}",
        "scope": "tools:read tools:execute"
      }
    }
  }
}
```

---

### AutenticaciÃ³n

Puede activar manualmente la autenticaciÃ³n o administrar las credenciales.

Autenticar con un servidor MCP especÃ­fico:

```bash
voracode mcp auth my-oauth-server
```

Enumere todos los servidores MCP y su estado de autenticaciÃ³n:

```bash
voracode mcp list
```

Eliminar las credenciales almacenadas:

```bash
voracode mcp logout my-oauth-server
```

El comando `mcp auth` abrirÃ¡ su navegador para obtener autorizaciÃ³n. DespuÃ©s de su autorizaciÃ³n, Voracode almacenarÃ¡ los tokens de forma segura en `~/.local/share/voracode/mcp-auth.json`.

---

#### Deshabilitando OAuth

Si desea deshabilitar el OAuth automÃ¡tico para un servidor (por ejemplo, para servidores que usan claves API), configure `oauth` en `false`:

```json title="voracode.json" {7}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-api-key-server": {
      "type": "remote",
      "url": "https://mcp.example.com/mcp",
      "oauth": false,
      "headers": {
        "Authorization": "Bearer {env:MY_API_KEY}"
      }
    }
  }
}
```

---

#### Opciones de OAuth

| OpciÃ³n         | Tipo            | DescripciÃ³n                                                                                  |
| -------------- | --------------- | -------------------------------------------------------------------------------------------- |
| `oauth`        | Objeto \| falso | OAuth objeto de configuraciÃ³n, o `false` para deshabilitar la detecciÃ³n automÃ¡tica de OAuth. |
| `clientId`     | Cadena          | OAuth ID de cliente. Si no se proporciona, se intentarÃ¡ el registro dinÃ¡mico del cliente.    |
| `clientSecret` | Cadena          | OAuth secreto del cliente, si lo requiere el servidor de autorizaciÃ³n.                       |
| `scope`        | Cadena          | OAuth alcances para solicitar durante la autorizaciÃ³n.                                       |

#### DepuraciÃ³n

Si un servidor MCP remoto no puede autenticarse, puede diagnosticar problemas con:

```bash
# View auth status for all OAuth-capable servers
voracode mcp auth list

# Debug connection and OAuth flow for a specific server
voracode mcp debug my-oauth-server
```

El comando `mcp debug` muestra el estado de autenticaciÃ³n actual, prueba la conectividad HTTP e intenta el flujo de descubrimiento OAuth.

---

## Administrar

Sus MCP estÃ¡n disponibles como herramientas en Voracode, junto con herramientas integradas. Para que puedas administrarlos a travÃ©s de la configuraciÃ³n Voracode como cualquier otra herramienta.

---

### Global

Esto significa que puede habilitarlos o deshabilitarlos globalmente.

```json title="voracode.json" {14}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-mcp-foo": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command-foo"]
    },
    "my-mcp-bar": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command-bar"]
    }
  },
  "tools": {
    "my-mcp-foo": false
  }
}
```

TambiÃ©n podemos usar un patrÃ³n global para deshabilitar todos los MCP coincidentes.

```json title="voracode.json" {14}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-mcp-foo": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command-foo"]
    },
    "my-mcp-bar": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command-bar"]
    }
  },
  "tools": {
    "my-mcp*": false
  }
}
```

AquÃ­ estamos usando el patrÃ³n global `my-mcp*` para deshabilitar todos los MCP.

---

### Por agente

Si tiene una gran cantidad de servidores MCP, es posible que desee habilitarlos solo por agente y deshabilitarlos globalmente. Para hacer esto:

1. DesactÃ­velo como herramienta a nivel global.
2. En su [configuraciÃ³n del agente](/docs/agents#tools), habilite el servidor MCP como herramienta.

```json title="voracode.json" {11, 14-18}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "my-mcp": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command"],
      "enabled": true
    }
  },
  "tools": {
    "my-mcp*": false
  },
  "agent": {
    "my-agent": {
      "tools": {
        "my-mcp*": true
      }
    }
  }
}
```

---

#### Patrones globales

El patrÃ³n global utiliza patrones globales simples de expresiones regulares:

- `*` coincide con cero o mÃ¡s de cualquier carÃ¡cter (por ejemplo, `"my-mcp*"` coincide con `my-mcp_search`, `my-mcp_list`, etc.)
- `?` coincide exactamente con un carÃ¡cter
- Todos los demÃ¡s caracteres coinciden literalmente

:::note
Las herramientas del servidor MCP estÃ¡n registradas con el nombre del servidor como prefijo, por lo que para deshabilitar todas las herramientas para un servidor simplemente use:

```
"mymcpservername_*": false
```

:::

---

## Ejemplos

A continuaciÃ³n se muestran ejemplos de algunos servidores MCP comunes. Puede enviar un PR si desea documentar otros servidores.

---

### sentry

Agregue el [servidor Sentry MCP](https://mcp.sentry.dev) para interactuar con sus proyectos y problemas Sentry.

```json title="voracode.json" {4-8}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "sentry": {
      "type": "remote",
      "url": "https://mcp.sentry.dev/mcp",
      "oauth": {}
    }
  }
}
```

DespuÃ©s de agregar la configuraciÃ³n, autentÃ­quese con Sentry:

```bash
voracode mcp auth sentry
```

Esto abrirÃ¡ una ventana del navegador para completar el flujo OAuth y conectar Voracode a su cuenta Sentry.

Una vez autenticado, puede utilizar las herramientas Sentry en sus indicaciones para consultar problemas, proyectos y datos de errores.

```txt "use sentry"
Show me the latest unresolved issues in my project. use sentry
```

---

### Context7

Agregue el [servidor Context7 MCP](https://github.com/upstash/context7) para buscar en los documentos.

```json title="voracode.json" {4-7}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "context7": {
      "type": "remote",
      "url": "https://mcp.context7.com/mcp"
    }
  }
}
```

Si se ha registrado para obtener una cuenta gratuita, puede usar su clave API y obtener lÃ­mites de tarifas mÃ¡s altos.

```json title="voracode.json" {7-9}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "context7": {
      "type": "remote",
      "url": "https://mcp.context7.com/mcp",
      "headers": {
        "CONTEXT7_API_KEY": "{env:CONTEXT7_API_KEY}"
      }
    }
  }
}
```

AquÃ­ asumimos que tiene configurada la variable de entorno `CONTEXT7_API_KEY`.

Agregue `use context7` a sus indicaciones para usar el servidor Context7 MCP.

```txt "use context7"
Configure a Cloudflare Worker script to cache JSON API responses for five minutes. use context7
```

Alternativamente, puede agregar algo como esto a su [AGENTS.md](/docs/rules/).

```md title="AGENTS.md"
When you need to search docs, use `context7` tools.
```

---

### Grep by Vercel

Agregue el servidor [Grep by Vercel](https://grep.app) MCP para buscar fragmentos de cÃ³digo en GitHub.

```json title="voracode.json" {4-7}
{
  "$schema": "https://voracode.ai/config.json",
  "mcp": {
    "gh_grep": {
      "type": "remote",
      "url": "https://mcp.grep.app"
    }
  }
}
```

Dado que llamamos a nuestro servidor MCP `gh_grep`, puede agregar `use the gh_grep tool` a sus indicaciones para que el agente lo use.

```txt "use the gh_grep tool"
What's the right way to set a custom domain in an SST Astro component? use the gh_grep tool
```

Alternativamente, puede agregar algo como esto a su [AGENTS.md](/docs/rules/).

```md title="AGENTS.md"
If you are unsure how to do something, use `gh_grep` to search code examples from GitHub.
```
