---
title: GitHub
description: UÅ¼yj voracode w problemach z GitHubem i zastosujch Å›ciÄ…gniÄ™cia.
---

voracode integruje siÄ™ z przepÅ‚ywem pracy w GitHub. Wspomnij o `/voracode` lub `/oc` w swoim komentarzu, a voracode wykonaj zadania w ramach moduÅ‚u uruchamiajÄ…cego GitHub Actions.

---

## Cechy

- **Problem zwiÄ…zany z segregacjÄ…**: PoproÅ› voracode o szczegÃ³Å‚owe wyjaÅ›nienie problemu i wyjaÅ›nienie go.
- **Napraw i zaimplementuj**: PoproÅ› voracode o naprawienie problemu lub zaimplementowanie funkcji. BÄ™dzie dostÄ™pny w nastÄ™pnym oddziale i wysyÅ‚aÄ‡ PR ze stosowaniem dodatku.
- **Bezpieczny**: voracode dziaÅ‚a w moduÅ‚ach sprzÄ™towych GitHuba.

---

## Instalacja

Uruchomione dalsze postÄ™powanie w przypadku wystÄ…pienia w repozytorium GitHub:

```bash
voracode github install
```

Aby przeprowadziÄ‡ CiÄ™ przez proces instalacji aplikacji GitHub, utwÃ³rz dziaÅ‚anie i skonfiguruj wpisy tajnych.

---

### Konfiguracja rÄ™czna

MoÅ¼na teÅ¼ uszkodziÄ‡ to rozwiÄ…zanie.

1. **Zainstaluj aplikacjÄ™ GitHub**

   WejdÅº na [**github.com/apps/voracode-agent**](https://github.com/apps/voracode-agent). wystÄ…pienie siÄ™, Å¼e jest natychmiastowe w repozytorium usuwam.

2. **Dodaj przepÅ‚yw pracy**

   Dodaj zdalny plik pracy do `.github/workflows/voracode.yml` w swoim repozytorium. wystÄ…piÅ‚, Å¼e ustawiÅ‚eÅ› sÄ™dziego `model` i wymagany klucz API w `env`.

   ```yml title=".github/workflows/voracode.yml" {24,26}
   name: voracode

   on:
     issue_comment:
       types: [created]
     pull_request_review_comment:
       types: [created]

   jobs:
     voracode:
       if: |
         contains(github.event.comment.body, '/oc') ||
         contains(github.event.comment.body, '/voracode')
       runs-on: ubuntu-latest
       permissions:
         id-token: write
       steps:
          - name: Checkout repository
            uses: actions/checkout@v6
            with:
              fetch-depth: 1
              persist-credentials: false

          - name: Run Voracode
           uses: mysterious75/voracode/github@latest
           env:
             ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
           with:
             model: anthropic/claude-sonnet-4-20250514
             # share: true
             # github_token: xxxx
   ```

3. **Przechowuj klucze API w tajemnicy**

   W **ustawieniach** swojej organizacji lub projektu rozwiÅ„ **Sekretne i zmienne** po lewej stronie i wybierz **DziaÅ‚ania**. DodaÅ‚em wymagane klucze API.

---

## Konfiguracja

- `model`: Model uÅ¼ywany z voracode. Przyjmuje format `provider/model`. To **wymagane**.
- `agent`: Agent, ktÃ³rego naleÅ¼y uÅ¼ywaÄ‡. Musi byÄ‡ odlegÅ‚ym agentem. Wraca do `default_agent` z konfiguracjÄ… lub `"build"`, jeÅ›li nie zostaÅ‚ znaleziony.
- `share`: CzyÄ‡ funkcjÄ™ sesji voracode. DomyÅ›lnie **true** dla repozytoriÃ³w publicznych.
- `prompt`: Opcjonalny niestandardowy monit o zastÄ…pienie przestrzegania zachowania. Wykorzystanie tego, aby dostosowaÄ‡ sposÃ³b przetwarzania przez voracode.
- `token`: opcjonalny token dostÄ™pu GitHub podstawowe operacje, takie jak tworzenie komentarzy, zatwierdzanie zmian i otwieranie zastosowania Å›ciÄ…gniÄ™cia. DomyÅ›lnie voracode uÅ¼ywa tokena dostÄ™pu do instalacji z aplikacji voracode GitHub, wiÄ™c zatwierdzenia, komentarze i zasady Å›ciÄ…gniÄ™cia widoczne jako ÅºrÃ³dÅ‚o z aplikacji.

  Alternatywnie moÅ¼esz uÅ¼yÄ‡ [wbudowanego `GITHUB_TOKEN`](https://docs.github.com/en/actions/tutorials/authenticate-with-github_token) moduÅ‚u uruchamiajÄ…cego GitHub Action bez instalacji aplikacji voracode GitHub. PamiÄ™taj tylko o przyznaniu wymaganych mocy w przepÅ‚ywie pracy:

  ```yaml
  permissions:
    id-token: write
    contents: write
    pull-requests: write
    issues: write
  ```

  JeÅ›li chcesz, moÅ¼esz takÅ¼e uÅ¼yÄ‡ [osobistych tokenÃ³w dostÄ™pu](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/managing-your-personal-access-tokens)(PAT).

---

## ObsÅ‚ugiwane wydarzenia

voracode moÅ¼e zostaÄ‡ wywoÅ‚any przez zdarzenie GitHub:

| Typ zdarzenia                 | WywoÅ‚ane przez                                   | SzczegÃ³Å‚y                                                                                                                                             |
| ----------------------------- | ------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `issue_comment`               | Skomentuj problem lub PR                         | Wspomnij o `/voracode` lub `/oc` w swoim komentarzu. voracode odczytuje kontekst i moÅ¼e byÄ‡ czÄ™Å›ciÄ… skÅ‚adowÄ…, otwieranÄ… przez PR lub odpowiedzialnÄ….  |
| `pull_request_review_comment` | Komentarz okreÅ›lonym kodem w PR                  | Wspomnij o `/voracode` lub `/oc` podczas przeglÄ…dania kodu. voracode ÅºrÃ³dÅ‚o pochodzenia, numery linii i kontekst rÃ³Å¼nicowy.                           |
| `issues`                      | Wydanie otwarte lub edytowane                    | Automatycznie wyzwalaj voracode po utworzeniu lub zmodyfikowaniu problemÃ³w. Wymaga wejÅ›cia `prompt`.                                                  |
| `pull_request`                | PR otwarty lub zaktualizowany                    | Automatycznie wyzwalaj voracode, gdy PR sÄ… otwierane, synchronizowane lub ponownie otwierane. Przydatne w przypadku automatycznych znajomych.         |
| `schedule`                    | Harmonogram oparty na Cron                       | Uruchom voracode zgodnie z harmonogramem. Wymagane wejÅ›cie `prompt`. Dane wejÅ›ciowe trafiajÄ… do dziennikÃ³w i trafiaÅ„ PR (nie ma problemu z recenzjÄ…). |
| `workflow_dispatch`           | RÄ™czny wyÅ‚Ä…cznik z interfejsu uÅ¼ytkownika GitHub | Uruchom voracode na karcie Akcje. Wymagane wejÅ›cie `prompt`. Dane wejÅ›ciowe trafiajÄ… do dziennikÃ³w i odbiorcÃ³w PR.                                    |

### PrzykÅ‚ad harmonogramu

Uruchamiaj voracode zgodnie z harmonogramem, aby wykonaÄ‡ zautomatyzowane zadania:

```yaml title=".github/workflows/voracode-scheduled.yml"
name: Scheduled Voracode Task

on:
  schedule:
    - cron: "0 9 * * 1" # Every Monday at 9am UTC

jobs:
  voracode:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
      pull-requests: write
      issues: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Run Voracode
        uses: mysterious75/voracode/github@latest
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        with:
          model: anthropic/claude-sonnet-4-20250514
          prompt: |
            Review the codebase for any TODO comments and create a summary.
            If you find issues worth addressing, open an issue to track them.
```

W przypadku wystÄ…pienia danych wyjÅ›ciowych `prompt` sÄ… **wymagane**, ktÃ³re nie sÄ… dostarczane, z ktÃ³rych moÅ¼na wyodrÄ™bniÄ‡ instrukcje. Zaplanowane przepÅ‚ywomierze pracy bez kontekstu uÅ¼ytkownika, ktÃ³ry zostaÅ‚ uruchomiony, wiÄ™c przepÅ‚yw pracy musi `contents: write` i `pull-requests: write`, wystÄ…pisz, Å¼e voracode utworzyÅ‚ gÅ‚Ã³wne lub PR.

---

### PrzykÅ‚ad Å¼Ä…dania Å›ciÄ…gniÄ™cia

Automatycznie przeglÄ…daj Å¼Ä…dania Å›ciÄ…gniÄ™cia po ich otwarciu lub aktualizacji:

```yaml title=".github/workflows/voracode-review.yml"
name: voracode-review

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      pull-requests: read
      issues: read
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: mysterious75/voracode/github@latest
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          model: anthropic/claude-sonnet-4-20250514
          use_github_token: true
          prompt: |
            Review this pull request:
            - Check for code quality issues
            - Look for potential bugs
            - Suggest improvements
```

W przypadku zdarzenia `pull_request`, jeÅ›li nie podano `prompt`, voracode uÅ¼ytkownika przeglÄ…dajÄ…cego Å›ciÄ…gniÄ™cie.

---

### PrzykÅ‚ad segregacji problemÃ³w

Automatycznie segreguj nowe problemy. DziesiÄ™Ä‡ przykÅ‚adÃ³w filtruje do kont starszych niÅ¼ 30 dni w celu ograniczenia spamu:

```yaml title=".github/workflows/voracode-triage.yml"
name: Issue Triage

on:
  issues:
    types: [opened]

jobs:
  triage:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: write
      pull-requests: write
      issues: write
    steps:
      - name: Check account age
        id: check
        uses: actions/github-script@v7
        with:
          script: |
            const user = await github.rest.users.getByUsername({
              username: context.payload.issue.user.login
            });
            const created = new Date(user.data.created_at);
            const days = (Date.now() - created) / (1000 * 60 * 60 * 24);
            return days >= 30;
          result-encoding: string

      - uses: actions/checkout@v6
        if: steps.check.outputs.result == 'true'
        with:
          persist-credentials: false

      - uses: mysterious75/voracode/github@latest
        if: steps.check.outputs.result == 'true'
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        with:
          model: anthropic/claude-sonnet-4-20250514
          prompt: |
            Review this issue. If there's a clear fix or relevant docs:
            - Provide documentation links
            - Add error handling guidance for code examples
            Otherwise, do not comment.
```

W przypadku zdarzenia `issues` wprowadzenie `prompt` jest **wymagane**, poniewaÅ¼ nie ma komentarza, z ktÃ³rego moÅ¼na wyodrÄ™bniÄ‡ instrukcje.

---

## Niestandardowe monity

ZastÄ…p domyÅ›lne monit, aby zastosowaÄ‡ zachowanie voracode do twojego wÅ‚asnego pracy.

```yaml title=".github/workflows/voracode.yml"
- uses: mysterious75/voracode/github@latest
  with:
    model: anthropic/claude-sonnet-4-5
    prompt: |
      Review this pull request:
      - Check for code quality issues
      - Look for potential bugs
      - Suggest improvements
```

Jest to przepis wykonawczy, ktÃ³ry podlega przepisom kodowania lub zakresÃ³w tematycznych dla twojego projektu.

---

## PrzykÅ‚ady

Oto kilka moÅ¼liwoÅ›ci wykorzystania voracode w GitHubie.

- **WyjaÅ›nij problem**

  Dodaj dziesiÄ™Ä‡ komentarzy w numerze GitHub.

  ```
  /voracode explain this issue
  ```

  voracode przeczytaj caÅ‚oÅ›Ä‡, zawierajÄ…c dodatek z komentarzami i odpowiedziÄ… z jasnym wyjaÅ›nieniem.

- **Napraw problem**

  W numerze GitHub powiedz:

  ```
  /voracode fix this
  ```

  A voracode utworzy nowÄ… podstawÄ™, wdroÅ¼y zmiany i zastÄ…pi PR ze zmiany.

- **PrzeglÄ…d zmiany PR i wprowadzenie zmiany**

  Zostaw komentarz w PR GitHub.

  ```
  Delete the attachment from S3 when the note is removed /oc
  ```

  voracode zaimplementuje uruchomionÄ… zmianÄ™ i zatwierdzi jÄ… do tego samego PR.

- **Przejrzyj konkretne linie kodu**

  Zostaw komentarz bezpoÅ›rednio w wierszu kodu w dodatku â€žPlikiâ€ PR. voracode automatyczne wykrywanie pliku, numery linii i kontekst rÃ³Å¼nicowy, aby sprawdziÄ‡ odpowiedzi.

  ```
  [Comment on specific lines in Files tab]
  /oc add error handling here
  ```

  KomentujÄ…c okreÅ›lone linie, voracode otrzymuje:
  - DokÅ‚adny plik, ktÃ³ry jest sprawdzany
  - Konkretne linie kodu
  - OtaczajÄ…cy kontekst rÃ³Å¼nicowy
  - Informacje o numerze linii

  DostÄ™pne na bardziej szczegÃ³Å‚owe rozwiÄ…zanie bez koniecznoÅ›ci stosowania rÄ™cznego okreÅ›lania plikÃ³w lub numerÃ³w wierszy.
