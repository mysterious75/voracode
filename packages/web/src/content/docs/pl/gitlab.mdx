---
title: GitLab
description: uÅ¼yj voracode w problemach z GitLabem i zastosujch scalania.
---

voracode integruje siÄ™ z przepÅ‚ywem pracy GitLab poprzez potok GitLab CI/CD lub z GitLab Duo.

W obu przypadkach voracode bÄ™dzie moduÅ‚em GitLab.

---

## GitLab CI

voracode dziaÅ‚a w zwykÅ‚ym potoku GitLab. MoÅ¼esz wbudowaÄ‡ go w potok jako [komponent CI](https://docs.gitlab.com/ee/ci/components/)

Tutaj obowiÄ…zkowego przez spoÅ‚ecznoÅ›Ä‡ komponentu CI/CD dla voracode â€” [nagyv/gitlab-voracode](https://gitlab.com/nagyv/gitlab-voracode).

---

### Cechy

- **UÅ¼yj konfiguracji dla kaÅ¼dego zadania**: Skonfiguruj voracode z niestandardowym katalogiem konfiguracyjnym, na przykÅ‚adzie `./config/#custom-directory`, aby wÅ‚Ä…czyÄ‡ lub funkcjonalnoÅ›ci na kaÅ¼de wywoÅ‚anie voracode.
- **Konfiguracja minimalna**: Komponent CI konfiguruje voracode w tle, wystarczy skonfigurowaÄ‡ voracode i poczÄ…tkowy monit.
- **Elastyczny**: Komponent CI obsÅ‚uguje kilka danych, ktÃ³re sÄ… stosowane w celu dostosowania jego zachowania

---

### OrganizowaÄ‡ coÅ›

1. Przechowuj dane uwierzytelniajÄ…ce voracode w formacie JSON jako zmienne parametry CI typu pliku w specyfikacji **Ustawienia** > **CI/CD** > **Zmienne**. Zachowaj, aby byÄ‡ oznaczony jako â€žZamaskowane i ukryteâ€.
2. Dodaj elementy do swojego pliku `.gitlab-ci.yml`.

   ```yaml title=".gitlab-ci.yml"
   include:
     - component: $CI_SERVER_FQDN/nagyv/gitlab-voracode/voracode@2
       inputs:
         config_dir: ${CI_PROJECT_DIR}/voracode-config
         auth_json: $VORACODE_AUTH_JSON # The variable name for your Voracode authentication JSON
         command: optional-custom-command
         message: "Your prompt here"
   ```

WiÄ™cej danych dotyczÄ…cych uÅ¼ycia [sprawdÅº opis](https://gitlab.com/explore/catalog/nagyv/gitlab-voracode) tego komponentu.

---

## Duet GitLab

voracode integruje siÄ™ z przepÅ‚ywem pracy GitLab.
Wspomnij o `@voracode` w komentarzu, a voracode wykonaj zadania w ramach swojego potoku GitLab CI.

---

### Cechy

- **Problem zwiÄ…zany z segregacjÄ…**: PoproÅ› voracode o szczegÃ³Å‚owe wyjaÅ›nienie problemu i wyjaÅ›nienie go.
- **Napraw i zaimplementuj**: PoproÅ› voracode o naprawienie problemu lub zaimplementowanie funkcji.
  UtworzyÅ‚ nowy oddziaÅ‚ i zgÅ‚osi ataku ze sobÄ….
- **Bezpieczny**: voracode dziaÅ‚a na moduÅ‚ach GitLab.

---

### OrganizowaÄ‡ coÅ›

voracode dziaÅ‚a w potoku CI/CD GitLab. Oto, czego potrzebujesz, aby przejÅ›Ä‡ do:

:::tip
Aktualne instrukcje dotyczÄ…ce [**dokumentacji GitLab**](https://docs.gitlab.com/user/duo_agent_platform/agent_assistant/).
:::

1.  Skonfiguruj swoje Å›rodowisko GitLab
2.  Skonfiguruj CI/CD
3.  Uzyskaj klucz API dostawca modelu AI
4.  UtwÃ³rz konto usÅ‚ugi
5.  Skonfiguruj zmienne CI/CD
6.  UtwÃ³rz plik konfiguracyjny, oto przykÅ‚ad:

        <details>

    <summary>Flow configuration</summary>

    ```yaml
    image: node:22-slim
    commands:
      - echo "Installing voracode"
      - npm install --global voracode-ai
      - echo "Installing glab"
      - export GITLAB_TOKEN=$GITLAB_TOKEN_VORACODE
      - apt-get update --quiet && apt-get install --yes curl wget gpg git && rm --recursive --force /var/lib/apt/lists/*
      - curl --silent --show-error --location "https://raw.githubusercontent.com/upciti/wakemeops/main/assets/install_repository" | bash
      - apt-get install --yes glab
      - echo "Configuring glab"
      - echo $GITLAB_HOST
      - echo "Creating Voracode auth configuration"
      - mkdir --parents ~/.local/share/voracode
      - |
        cat > ~/.local/share/voracode/auth.json << EOF
        {
          "anthropic": {
            "type": "api",
            "key": "$ANTHROPIC_API_KEY"
          }
        }
        EOF
      - echo "Configuring git"
      - git config --global user.email "voracode@gitlab.com"
      - git config --global user.name "Voracode"
      - echo "Testing glab"
      - glab issue list
      - echo "Running Voracode"
      - |
        voracode run "
        You are an AI assistant helping with GitLab operations.

        Context: $AI_FLOW_CONTEXT
        Task: $AI_FLOW_INPUT
        Event: $AI_FLOW_EVENT

        Please execute the requested task using the available GitLab tools.
        Be thorough in your analysis and provide clear explanations.

        <important>
        Please use the glab CLI to access data from GitLab. The glab CLI has already been authenticated. You can run the corresponding commands.

        If you are asked to summarize an MR or issue or asked to provide more information then please post back a note to the MR/Issue so that the user can see it.
        You don't need to commit or push up changes, those will be done automatically based on the file changes you make.
        </important>
        "
      - git checkout --branch $CI_WORKLOAD_REF origin/$CI_WORKLOAD_REF
      - echo "Checking for git changes and pushing if any exist"
      - |
        if ! git diff --quiet || ! git diff --cached --quiet || [ --not --zero "$(git ls-files --others --exclude-standard)" ]; then
          echo "Git changes detected, adding and pushing..."
          git add .
          if git diff --cached --quiet; then
            echo "No staged changes to commit"
          else
            echo "Committing changes to branch: $CI_WORKLOAD_REF"
            git commit --message "Codex changes"
            echo "Pushing changes up to $CI_WORKLOAD_REF"
            git push https://gitlab-ci-token:$GITLAB_TOKEN@$GITLAB_HOST/gl-demo-ultimate-dev-ai-epic-17570/test-java-project.git $CI_WORKLOAD_REF
            echo "Changes successfully pushed"
          fi
        else
          echo "No git changes detected, skipping push"
        fi
    variables:
      - ANTHROPIC_API_KEY
      - GITLAB_TOKEN_VORACODE
      - GITLAB_HOST
    ```

        </details>

SzczegÃ³Å‚owe instrukcje w [dokumentacji agentÃ³w GitLab CLI](https://docs.gitlab.com/user/duo_agent_platform/agent_assistant/).

---

### PrzykÅ‚ady

Oto kilka moÅ¼liwoÅ›ci wykorzystania voracode w GitLabie.

:::tip
MoÅ¼na zastosowaÄ‡ innÄ… metodÄ™ ograniczajÄ…cÄ… niÅ¼ `@voracode`.
:::

- **WyjaÅ›nij problem**

  Dodaj dziesiÄ™Ä‡ komentarzy w numerze GitLab.

  ```
  @voracode explain this issue
  ```

  voracode przeczytaj problem i odpowiedÅº z wyjaÅ›nieniem.

- **Napraw problem**

  W numerze GitLab powiedz:

  ```
  @voracode fix this
  ```

  voracode utworzy nowÄ… podstawÄ™, zaimplementuje zmiany i spowoduje, Å¼e skalowanie bÄ™dzie moÅ¼liwe.

- **Przejrzyj proÅ›by o poÅ‚Ä…czenie**

  Zostaw komentarz w sprawie rozwiÄ…zania poÅ‚Ä…czenia GitLab.

  ```
  @voracode review this merge request
  ```

  voracode sprawdzenie rozwiÄ…zania i przepuszczenie.
