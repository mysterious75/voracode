---
title: Agenci
description: Konfiguruj i uÅ¼ywaj potÄ™Å¼nych agentÃ³w.
---

Agenci to asystenci AI, ktÃ³rych moÅ¼na przypisaÄ‡ do rÃ³l i workflow. UmoÅ¼liwiajÄ… tworzenie ukierunkowanych narzÄ™dzi z niestandardowymi promptami, modelami i dostÄ™pem do narzÄ™dzi.

:::tip
UÅ¼yj agenta Plan, aby uzyskaÄ‡ dostÄ™p do kodu i przeglÄ…daÄ‡ informacje bez wprowadzania zmian w kodzie.
:::

MoÅ¼esz przeÅ‚Ä…czaÄ‡ siÄ™ miÄ™dzy agentami podczas sesji lub wywoÅ‚ywaÄ‡ ich za pomocÄ… wzmianki `@`.

---

## Typy

W Voracode obowiÄ…zuje zasada dwÃ³ch typÃ³w agentÃ³w: Primary (GÅ‚Ã³wni) i Subagents (Subagenci).

---

### Primary Agents (Agenci gÅ‚Ã³wni)

GÅ‚Ã³wni asystenci, z ktÃ³rymi prowadzisz interakcjÄ™. MoÅ¼esz przeÅ‚Ä…czaÄ‡ siÄ™ miÄ™dzy nimi za pomocÄ… klawisza **Tab** lub skrÃ³tu klawiszowego `switch_agent`. Ci agenci obsÅ‚ugujÄ… gÅ‚Ã³wnÄ… pÄ™tlÄ™. DostÄ™p do narzÄ™dzi jest konfigurowany za pomocÄ… uprawnieÅ„ â€” na przykÅ‚ad w agencie Build wszystkie narzÄ™dzia sÄ… dostÄ™pne, a w agencie Plan sÄ… ograniczone.

:::tip
UÅ¼yj klawisza **Tab**, aby przeÅ‚Ä…czaÄ‡ siÄ™ miÄ™dzy agentami podczas sesji.
:::

Voracode zawiera dwa wbudowane agenty gÅ‚Ã³wne: **Build** i **Plan**. Przyjrzymy siÄ™ im poniÅ¼ej.

---

### Subagents (Subagenci)

Subagenci to asystenci pomocniczy, ktÃ³rych mogÄ… przywoÅ‚ywaÄ‡ agenci gÅ‚Ã³wni w celu wykonania konkretnych zadaÅ„. MoÅ¼esz takÅ¼e wywoÅ‚ywaÄ‡ ich rÄ™cznie, **wzmiankujÄ…c ich (@)** w swoich wiadomoÅ›ciach.

Voracode ma trzech wbudowanych subagentÃ³w: **General**, **Explore** i **Scout**. Przyjrzymy siÄ™ im poniÅ¼ej.

---

## Wbudowane

Voracode ma dwa wbudowane agenty gÅ‚Ã³wne i trzech wbudowanych subagentÃ³w.

---

### Build

_Mode_: `primary`

Build to **domyÅ›lny** agent ogÃ³lnego przeznaczenia z peÅ‚nym zestawem narzÄ™dzi. Jest to standardowy agent do prac programistycznych, ktÃ³ry ma dostÄ™p do operacji na plikach i poleceÅ„ systemowych.

---

### Plan

_Mode_: `primary`

Dedykowany agent do analizy. UÅ¼ywa zestawu narzÄ™dzi tylko do odczytu, aby zmniejszyÄ‡ ryzyko i zapobiec niezamierzonym zmianom.
DomyÅ›lnie uprawnienia sÄ… ustawione na `ask` dla:

- `file edits`: Wszystkie zapisy, patche i edycje
- `bash`: Wszystkie polecenia bash

Ten agent jest zalecany, gdy chcesz, aby LLM zapoznaÅ‚ siÄ™ z kodem, stworzyÅ‚ plan lub odpowiedziaÅ‚ na pytania bez ryzyka modyfikacji bazy kodu.

---

### General

_Mode_: `subagent`

RozwiÄ…zuje zÅ‚oÅ¼one zadania wieloetapowe. PeÅ‚ny dostÄ™p do narzÄ™dzi (z moÅ¼liwoÅ›ciÄ… modyfikacji plikÃ³w), dziÄ™ki czemu moÅ¼e w razie potrzeby wprowadzaÄ‡ zmiany w plikach. UÅ¼ywany przez agenta nadrzÄ™dnego do delegowania jednostek pracy.

---

### Explore

_Mode_: `subagent`

Szybki agent tylko do odczytu do eksploracji baz kodu. Nie moÅ¼e modyfikowaÄ‡ plikÃ³w. UÅ¼ywany, gdy chcesz szybko znaleÅºÄ‡ pliki wedÅ‚ug wzorcÃ³w, wyszukaÄ‡ w kodzie sÅ‚owa kluczowe lub uzyskaÄ‡ odpowiedzi na pytania dotyczÄ…ce bazy kodu.

---

### Scout

_Mode_: `subagent`

Agent tylko do odczytu do pracy z zewnÄ™trznÄ… dokumentacjÄ… i badaniem zaleÅ¼noÅ›ci. UÅ¼ywaj go, gdy chcesz sklonowaÄ‡ repozytorium zaleÅ¼noÅ›ci do zarzÄ…dzanej pamiÄ™ci podrÄ™cznej Voracode, przejrzeÄ‡ kod ÅºrÃ³dÅ‚owy biblioteki albo porÃ³wnaÄ‡ lokalny kod z implementacjami upstream bez modyfikowania swojego workspace.

---

### Compaction

_Mode_: `primary`

Ukryty agent systemowy, ktÃ³ry kompresuje kontekst w zwiÄ™zÅ‚e podsumowanie. DziaÅ‚a automatycznie i nie jest widoczny w interfejsie uÅ¼ytkownika.

---

### Title

_Mode_: `primary`

Ukryty agent systemowy, ktÃ³ry generuje krÃ³tki tytuÅ‚ sesji. DziaÅ‚a automatycznie i nie jest widoczny w interfejsie uÅ¼ytkownika.

---

### Summary

_Mode_: `primary`

Ukryty agent systemowy, ktÃ³ry generuje podsumowanie sesji. DziaÅ‚a automatycznie i nie jest widoczny w interfejsie uÅ¼ytkownika.

---

## UÅ¼ycie

1. W przypadku agentÃ³w gÅ‚Ã³wnych uÅ¼yj klawisza **Tab**, aby przeÅ‚Ä…czaÄ‡ siÄ™ miÄ™dzy nimi podczas sesji. MoÅ¼na to rÃ³wnieÅ¼ zrobiÄ‡ za pomocÄ… skrÃ³tu klawiszowego `switch_agent`.

2. SubagentÃ³w moÅ¼na wywoÅ‚ywaÄ‡:
   - **Automatycznie** przez gÅ‚Ã³wnych agentÃ³w do specjalistycznych zadaÅ„ na podstawie ich opisÃ³w.
   - RÄ™cznie, **wzmiankujÄ…c (@)** subagenta w swoich wiadomoÅ›ciach. Na przykÅ‚ad:

     ```txt frame="none"
     @explore help me search for this function
     ```

3. **Nawigacja pomiÄ™dzy sesjami**: Kiedy sesja ma sesje podrzÄ™dne, moÅ¼esz nawigowaÄ‡ pomiÄ™dzy sesjÄ… nadrzÄ™dnÄ… a sesjami podrzÄ™dnymi za pomocÄ…:
   - **\<Leader>+Prawo** (lub skonfigurowanego klawisza `session_child_cycle`), aby przeÅ‚Ä…czaÄ‡ siÄ™ w dÃ³Å‚: rodzic â†’ dziecko 1 â†’ dziecko 2 â†’ ... â†’ rodzic
   - **\<Leader>+Lewo** (lub skonfigurowanego klawisza `session_child_cycle_reverse`), aby przeÅ‚Ä…czaÄ‡ siÄ™ w gÃ³rÄ™: rodzic â† dziecko 1 â† dziecko 2 â† ... â† rodzic

   UmoÅ¼liwia to pÅ‚ynne przeÅ‚Ä…czanie siÄ™ miÄ™dzy gÅ‚Ã³wnÄ… rozmowÄ… a pracÄ… specjalistycznÄ… wykonywanÄ… przez subagentÃ³w.

---

## Konfiguracja

MoÅ¼esz tworzyÄ‡ wÅ‚asnych agentÃ³w lub dostosowywaÄ‡ istniejÄ…cych. AgentÃ³w moÅ¼na definiowaÄ‡ na dwa sposoby:

---

### JSON

Skonfiguruj agenta w pliku `voracode.json`:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "agent": {
    "build": {
      "mode": "primary",
      "model": "anthropic/claude-sonnet-4-20250514",
      "prompt": "{file:./prompts/build.txt}",
      "tools": {
        "write": true,
        "edit": true,
        "bash": true
      }
    },
    "plan": {
      "mode": "primary",
      "model": "anthropic/claude-haiku-4-20250514",
      "tools": {
        "write": false,
        "edit": false,
        "bash": false
      }
    },
    "code-reviewer": {
      "description": "Reviews code for best practices and potential issues",
      "mode": "subagent",
      "model": "anthropic/claude-sonnet-4-20250514",
      "prompt": "You are a code reviewer. Focus on security, performance, and maintainability.",
      "tools": {
        "write": false,
        "edit": false
      }
    }
  }
}
```

---

### Markdown

MoÅ¼esz takÅ¼e definiowaÄ‡ agentÃ³w za pomocÄ… plikÃ³w markdown. Przechowuj je w:

- Globalnie: `~/.config/voracode/agents/`
- W projekcie: `.voracode/agents/`

```markdown title="~/.config/voracode/agents/review.md"
---
description: Reviews code for quality and best practices
mode: subagent
model: anthropic/claude-sonnet-4-20250514
temperature: 0.1
tools:
  write: false
  edit: false
  bash: false
---

You are in code review mode. Focus on:

- Code quality and best practices
- Potential bugs and edge cases
- Performance implications
- Security considerations

Provide constructive feedback without making direct changes.
```

Nazwa pliku markdown staje siÄ™ nazwÄ… agenta. Na przykÅ‚ad `review.md` tworzy agenta `review`.

---

## Opcje

Przyjrzyjmy siÄ™ szczegÃ³Å‚owo opcjom konfiguracji.

---

### Description (Opis)

UÅ¼yj `description`, aby krÃ³tko opisaÄ‡, co robi agent i kiedy go uÅ¼ywaÄ‡.

```json title="voracode.json"
{
  "agent": {
    "review": {
      "description": "Reviews code for best practices and potential issues"
    }
  }
}
```

Jest to **wymagana** opcja konfiguracji.

---

### Temperature (Temperatura)

Kontroluj losowoÅ›Ä‡ i kreatywnoÅ›Ä‡ odpowiedzi LLM za pomocÄ… konfiguracji `temperature`.

NiÅ¼sze wartoÅ›ci sprawiajÄ…, Å¼e odpowiedzi sÄ… bardziej skupione i deterministyczne, podczas gdy wyÅ¼sze wartoÅ›ci wprowadzajÄ… wiÄ™cej rÃ³Å¼norodnoÅ›ci i kreatywnoÅ›ci.

```json title="voracode.json"
{
  "agent": {
    "plan": {
      "temperature": 0.1
    },
    "creative": {
      "temperature": 0.8
    }
  }
}
```

WartoÅ›Ä‡ temperatury zwykle waha siÄ™ od 0.0 do 1.0:

- **0.0-0.2**: Bardzo skoncentrowane i deterministyczne odpowiedzi, idealne do analizy kodu i debugowania
- **0.3-0.5**: ZrÃ³wnowaÅ¼one odpowiedzi, dobre do ogÃ³lnych zadaÅ„ programistycznych
- **0.6-1.0**: Bardziej kreatywne i zrÃ³Å¼nicowane odpowiedzi, przydatne podczas burzy mÃ³zgÃ³w i eksploracji

```json title="voracode.json"
{
  "agent": {
    "analyze": {
      "temperature": 0.1,
      "prompt": "{file:./prompts/analysis.txt}"
    },
    "build": {
      "temperature": 0.3
    },
    "brainstorm": {
      "temperature": 0.7,
      "prompt": "{file:./prompts/creative.txt}"
    }
  }
}
```

JeÅ›li nie zostanie skonfigurowana, Voracode uÅ¼ywa wartoÅ›ci domyÅ›lnej dla danego modelu; 0 dla modeli rozumowania (reasoning models), 0.55 dla modeli Qwen.

---

### Steps (Kroki)

Kontroluj maksymalnÄ… liczbÄ™ iteracji, jakÄ… agent moÅ¼e wykonaÄ‡, zanim poprosi o interwencjÄ™ uÅ¼ytkownika. Pomaga to kontrolowaÄ‡ koszty i zapobiegaÄ‡ pÄ™tlom.

JeÅ›li ta opcja nie zostanie skonfigurowana, agent bÄ™dzie kontynuowaÅ‚ iteracjÄ™, dopÃ³ki model nie zakoÅ„czy zadania lub uÅ¼ytkownik nie przerwie dziaÅ‚ania.

```json title="voracode.json"
{
  "agent": {
    "quick-thinker": {
      "description": "Fast reasoning with limited iterations",
      "prompt": "You are a quick thinker. Solve problems with minimal steps.",
      "steps": 5
    }
  }
}
```

Po osiÄ…gniÄ™ciu limitu agent otrzymuje specjalny monit systemowy nakazujÄ…cy podsumowanie swojej pracy i zarekomendowanie kolejnych krokÃ³w.

:::caution
Starsze pole `maxSteps` jest przestarzaÅ‚e. Zamiast tego uÅ¼yj `steps`.
:::

---

### Disabled (WyÅ‚Ä…czony)

Ustaw na `true`, aby wyÅ‚Ä…czyÄ‡ agenta.

```json title="voracode.json"
{
  "agent": {
    "review": {
      "disable": true
    }
  }
}
```

---

### Prompt (Monit)

Podaj niestandardowy prompt systemowy dla tego agenta za pomocÄ… konfiguracji `prompt`. Prompt powinien zawieraÄ‡ instrukcje dotyczÄ…ce celu i zachowania agenta.

```json title="voracode.json"
{
  "agent": {
    "review": {
      "prompt": "{file:./prompts/code-review.txt}"
    }
  }
}
```

ÅšcieÅ¼ka jest wzglÄ™dna do miejsca, w ktÃ³rym znajduje siÄ™ plik konfiguracyjny. DziaÅ‚a to zarÃ³wno w przypadku globalnej konfiguracji Voracode, jak i konfiguracji specyficznej dla projektu.

---

### Model

UÅ¼yj `model`, aby okreÅ›liÄ‡ model dla tego agenta. Przydatne do uÅ¼ywania rÃ³Å¼nych modeli do rÃ³Å¼nych zadaÅ„. Na przykÅ‚ad uÅ¼ycie modelu rozumowania do planowania, ale szybszego modelu do edycji.

:::tip
JeÅ›li model nie zostanie okreÅ›lony, agenci gÅ‚Ã³wni uÅ¼ywajÄ… [modelu skonfigurowanego globalnie](/docs/config#models), a agenci podrzÄ™dni uÅ¼ywajÄ… modelu agenta, ktÃ³ry ich wywoÅ‚aÅ‚.
:::

```json title="voracode.json"
{
  "agent": {
    "plan": {
      "model": "anthropic/claude-haiku-4-20250514"
    }
  }
}
```

Identyfikator modelu jest taki sam, jak w konfiguracji Voracode w `provider/model-id`. Na przykÅ‚ad, [Voracode Zen](/docs/zen), uÅ¼yj `voracode/gpt-5.1-codex` dla GPT 5.1 Codex.

---

### Tools (NarzÄ™dzia)

Kontroluj, ktÃ³re narzÄ™dzia sÄ… dostÄ™pne dla tego agenta, za pomocÄ… konfiguracji `tools`. MoÅ¼esz wÅ‚Ä…czyÄ‡ lub wyÅ‚Ä…czyÄ‡ okreÅ›lone narzÄ™dzia, ustawiajÄ…c je na `true` lub `false`.

```json title="voracode.json" {3-6,9-12}
{
  "$schema": "https://voracode.ai/config.json",
  "tools": {
    "write": true,
    "bash": true
  },
  "agent": {
    "plan": {
      "tools": {
        "write": false,
        "bash": false
      }
    }
  }
}
```

:::note
Konfiguracja specyficzna dla agenta nadpisuje konfiguracjÄ™ globalnÄ….
:::

MoÅ¼esz takÅ¼e uÅ¼ywaÄ‡ symboli wieloznacznych (wildcards), aby kontrolowaÄ‡ wiele narzÄ™dzi jednoczeÅ›nie. Na przykÅ‚ad, aby wyÅ‚Ä…czyÄ‡ wszystkie narzÄ™dzia z serwera MCP:

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "agent": {
    "readonly": {
      "tools": {
        "mymcp_*": false,
        "write": false,
        "edit": false
      }
    }
  }
}
```

[Dowiedz siÄ™ wiÄ™cej o narzÄ™dziach](/docs/tools).

---

### Permissions (Uprawnienia)

MoÅ¼esz skonfigurowaÄ‡ uprawnienia dla potencjalnie wraÅ¼liwych dziaÅ‚aÅ„ wykonywanych przez agenta. Obecnie obsÅ‚ugiwane dla narzÄ™dzi `edit`, `bash` i `webfetch`:

- `"ask"` â€” Pytaj o zatwierdzenie przed uÅ¼yciem narzÄ™dzia
- `"allow"` â€” Zezwalaj na uÅ¼ycie bez zatwierdzenia
- `"deny"` â€” ZabroÅ„ uÅ¼ycia narzÄ™dzia

```json title="voracode.json"
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "edit": "deny"
  }
}
```

MoÅ¼esz nadpisaÄ‡ to dla kaÅ¼dego agenta.

```json title="voracode.json" {3-5,8-10}
{
  "$schema": "https://voracode.ai/config.json",
  "permission": {
    "edit": "deny"
  },
  "agent": {
    "build": {
      "permission": {
        "edit": "ask"
      }
    }
  }
}
```

MoÅ¼na to rÃ³wnieÅ¼ ustawiÄ‡ w agentach Markdown.

```markdown title="~/.config/voracode/agents/review.md"
---
description: Code review without edits
mode: subagent
permission:
  edit: deny
  bash:
    "*": ask
    "git diff": allow
    "git log*": allow
    "grep *": allow
  webfetch: deny
---

Only analyze code and suggest changes.
```

MoÅ¼esz to skonfigurowaÄ‡ szczegÃ³Å‚owo dla poleceÅ„ bash.

```json title="voracode.json" {7}
{
  "$schema": "https://voracode.ai/config.json",
  "agent": {
    "build": {
      "permission": {
        "bash": {
          "git push": "ask",
          "grep *": "allow"
        }
      }
    }
  }
}
```

MoÅ¼na to poÅ‚Ä…czyÄ‡ z globalnym symbolem wieloznacznym.

```json title="voracode.json" {7}
{
  "$schema": "https://voracode.ai/config.json",
  "agent": {
    "build": {
      "permission": {
        "bash": {
          "git *": "ask"
        }
      }
    }
  }
}
```

MoÅ¼esz takÅ¼e uÅ¼yÄ‡ symbolu wieloznacznego `*`, aby zarzÄ…dzaÄ‡ wszystkimi innymi poleceniami.
Najbardziej szczegÃ³Å‚owa reguÅ‚a ma pierwszeÅ„stwo, symbol wieloznaczny `*` jest sprawdzany na koÅ„cu.

```json title="voracode.json" {8}
{
  "$schema": "https://voracode.ai/config.json",
  "agent": {
    "build": {
      "permission": {
        "bash": {
          "*": "ask",
          "git status *": "allow"
        }
      }
    }
  }
}
```

[Dowiedz siÄ™ wiÄ™cej o uprawnieniach](/docs/permissions).

---

### Mode (Tryb)

Kontroluj tryb agenta za pomocÄ… konfiguracji `mode`. Opcja `mode` okreÅ›la, w jaki sposÃ³b agent moÅ¼e byÄ‡ uÅ¼ywany.

```json title="voracode.json"
{
  "agent": {
    "review": {
      "mode": "subagent"
    }
  }
}
```

OpcjÄ™ `mode` moÅ¼na ustawiÄ‡ na `primary`, `subagent` lub `all`. JeÅ›li nie podano `mode`, domyÅ›lnie jest to `all`.

---

### Hidden (Ukryty)

Ukryj subagenta w menu autouzupeÅ‚niania `@` za pomocÄ… `hidden: true`. Przydatne w przypadku subagentÃ³w narzÄ™dziowych, ktÃ³rzy powinni byÄ‡ wywoÅ‚ywani tylko programowo przez innych agentÃ³w, a nie bezpoÅ›rednio przez uÅ¼ytkownika.

```json title="voracode.json"
{
  "agent": {
    "internal-helper": {
      "mode": "subagent",
      "hidden": true
    }
  }
}
```

WpÅ‚ywa to tylko na widocznoÅ›Ä‡ w menu autouzupeÅ‚niania. Model nadal moÅ¼e wywoÅ‚ywaÄ‡ ukrytych agentÃ³w za pomocÄ… narzÄ™dzia zadania, jeÅ›li ma do nich dostÄ™p.

:::note
Dotyczy tylko agentÃ³w z `mode: subagent`.
:::

---

### Task Permissions (Uprawnienia zadaÅ„)

Kontroluj, ktÃ³rych subagentÃ³w moÅ¼e wywoÅ‚aÄ‡ ten agent za pomocÄ… narzÄ™dzia zadania, uÅ¼ywajÄ…c `permission.task`. UÅ¼ywa wzorcÃ³w glob do elastycznego dopasowywania.

```json title="voracode.json"
{
  "agent": {
    "orchestrator": {
      "mode": "primary",
      "permission": {
        "task": {
          "*": "deny",
          "orchestrator-*": "allow",
          "code-reviewer": "ask"
        }
      }
    }
  }
}
```

Po ustawieniu wartoÅ›ci `deny` subagent zostanie usuniÄ™ty z definicji narzÄ™dzia Zadania, wiÄ™c model nie bÄ™dzie nawet wiedziaÅ‚ o jego istnieniu.

:::tip
ReguÅ‚y sÄ… oceniane w kolejnoÅ›ci i **wygrywa ostatnia pasujÄ…ca reguÅ‚a**. W powyÅ¼szym przykÅ‚adzie `orchestrator-planner` pasuje zarÃ³wno do `*` (odmowa), jak i `orchestrator-*` (zezwolenie), ale poniewaÅ¼ `orchestrator-*` wystÄ™puje po `*`, wynikiem jest `allow`.
:::

:::tip
Zawsze moÅ¼esz wywoÅ‚aÄ‡ subagenta rÄ™cznie, wspominajÄ…c go bezpoÅ›rednio (`@`), nawet jeÅ›li obecny agent ma uprawnienia ustawione na `deny`.
:::

---

### Color (Kolor)

Dostosuj wyglÄ…d agenta w interfejsie uÅ¼ytkownika za pomocÄ… opcji `color`. WpÅ‚ywa to na ikonÄ™ i obramowanie agenta w interfejsie.

UÅ¼yj kodu hex (np. `#FF5733`) lub nazwy koloru motywu: `primary`, `secondary`, `accent`, `success`, `warning`, `error`, `info`.

```json title="voracode.json"
{
  "agent": {
    "creative": {
      "color": "#ff6b6b"
    },
    "code-reviewer": {
      "color": "accent"
    }
  }
}
```

---

### Top P

Kontroluj rÃ³Å¼norodnoÅ›Ä‡ odpowiedzi za pomocÄ… opcji `top_p`. Alternatywa dla temperatury w celu kontrolowania losowoÅ›ci.

```json title="voracode.json"
{
  "agent": {
    "brainstorm": {
      "top_p": 0.9
    }
  }
}
```

WartoÅ›Ä‡ waha siÄ™ od 0.0 do 1.0. NiÅ¼sze wartoÅ›ci sÄ… bardziej skoncentrowane, wyÅ¼sze wartoÅ›ci sÄ… bardziej zrÃ³Å¼nicowane.

---

### Passthrough (Przekazywanie)

Wszelkie inne konfiguracje w agentach sÄ… **przekazywane bezpoÅ›rednio** do dostawcy modelu. Pozwala to na korzystanie z funkcji i parametrÃ³w specyficznych dla dostawcy.

Na przykÅ‚ad, uÅ¼ywajÄ…c modelu rozumowania OpenAI, moÅ¼esz kontrolowaÄ‡ wysiÅ‚ek rozumowania:

```json title="voracode.json" {6,7}
{
  "agent": {
    "deep-thinker": {
      "description": "Agent that uses high reasoning effort for complex problems",
      "model": "openai/gpt-5",
      "reasoningEffort": "high",
      "textVerbosity": "low"
    }
  }
}
```

Te dodatkowe wÅ‚aÅ›ciwoÅ›ci zaleÅ¼Ä… od modelu i dostawcy. SprawdÅº dokumentacjÄ™ swojego dostawcy pod kÄ…tem dostÄ™pnych parametrÃ³w.

:::tip
Uruchom `voracode models`, aby zobaczyÄ‡ dostÄ™pne modele.
:::

---

## Tworzenie agentÃ³w

MoÅ¼esz stworzyÄ‡ nowych agentÃ³w interaktywnie za pomocÄ… polecenia:

```bash
voracode agent create
```

Poprowadzi CiÄ™ to przez proces:

1. Pytanie o miejsce zapisu agenta (globalnie lub dla projektu).
2. Opis tego, co agent powinien robiÄ‡.
3. Wygenerowanie odpowiedniego promptu systemowego i identyfikatora.
4. WybÃ³r narzÄ™dzi, do ktÃ³rych agent powinien mieÄ‡ dostÄ™p.
5. Zapisanie pliku markdown z konfiguracjÄ… agenta.

---

## Przypadki uÅ¼ycia

Oto kilka typowych zastosowaÅ„ rÃ³Å¼nych agentÃ³w:

- **Agent programista**: PeÅ‚ne prace programistyczne z dostÄ™pem do wszystkich narzÄ™dzi.
- **Agent Plan**: Analiza i planowanie bez ryzyka modyfikacji kodu.
- **Agent recenzujÄ…cy**: PrzeglÄ…d kodu z dostÄ™pem do odczytu plikÃ³w i narzÄ™dzi dokumentacyjnych.
- **Agent debugujÄ…cy**: Koncentruje siÄ™ na rozwiÄ…zywaniu problemÃ³w z dostÄ™pem do narzÄ™dzi bash i odczytu plikÃ³w.
- **Agent Documentation**: Tworzenie i utrzymywanie dokumentacji z dostÄ™pem do operacji na plikach, ale bez poleceÅ„ systemowych.

---

## PrzykÅ‚ady

Oto kilka przykÅ‚adowych agentÃ³w, ktÃ³rzy mogÄ… byÄ‡ przydatni.

:::tip
Masz agenta, ktÃ³rym warto siÄ™ podzieliÄ‡? [PrzeÅ›lij PR](https://github.com/mysterious75/voracode).
:::

---

### Agent Documentation

```markdown title="~/.config/voracode/agents/docs-writer.md"
---
description: Writes and maintains project documentation
mode: subagent
tools:
  bash: false
---

You are a technical writer. Create clear, comprehensive documentation.

Focus on:

- Clear explanations
- Proper structure
- Code examples
- User-friendly language
```

---

### Security Auditor

```markdown title="~/.config/voracode/agents/security-auditor.md"
---
description: Performs security audits and identifies vulnerabilities
mode: subagent
tools:
  write: false
  edit: false
---

You are a security expert. Focus on identifying potential security issues.

Look for:

- Input validation vulnerabilities
- Authentication and authorization flaws
- Data exposure risks
- Dependency vulnerabilities
- Configuration security issues
```
